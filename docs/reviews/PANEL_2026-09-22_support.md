# Панель ревью · 22.09.2026 · обращения в поддержку (протокол §4.10)

- **Что ревьюили:** коммит `762e8de` ветки `day56`: `relay/node/src/routes/support.ts`,
  `relay/node/db/039_support_requests.sql`, CTE `unlinked` в `lib/identity_sweeper.ts`; контракт,
  протоколы, спека §13; экраны 14 в репозиториях `sosed.place` и `neighbro.place` (ветка `day56`).
- **Линзы:** Безопасность (чтением), Контракт и данные (чтением).
- **Перепроверка:** ведущий — тест, увиденный красным, на каждую правку кода; ворота фактов и листов.

## Находки по существу

### Безопасность
1. Мелочь — отошедший не может написать в поддержку (409 `stepped_away`); решение по спеке.
2. Мелочь — nonce проверялся до блокировки: два параллельных с одним nonce дают 503 вместо 201.
3. Мелочь — `nonces.response` NULL дал бы 201 с `null`; пути нет.
4. Мелочь — почта и текст живут бессрочно, пока нет `support.sweeper` (открытый пункт).
5–12. Без дефекта: равномерность номера, IDOR, замороженная сессия только пишет, гонка счёта,
    429 не пишется в nonce, домен в 429 публичен, уборщик не зацикливается, других путей закрытия нет.

### Контракт и данные
1. Противоречие — экран 14 обещает время в отказе, у 429 нет `Retry-After` (протокол §6 требует).
2. Противоречие — экраны: «предел не задан, 0/1000»; `open.tsv` держит `support.body.max` открытым.
3. Дефект — у `support.body.length` в местах нет экранов 14.
4. Противоречие — тег `support` «не построено» при `x-status: built`.
5. Противоречие — `SupportRequest` без `nullable` у `answer`/`answered_at`, без `required`.
6. Дефект — коды ответов GET и seen не совпадают с маршрутом; 201 без `required`.
7. Дефект — `open.tsv` у `support.sweeper` говорит «таблица не мигрирована».
8–9. Без дефекта: счёт 12 немигрированных, DDL сходится.
10. Мелочь — `[retired]` стоит после нового имени, а не после снятого.
11. Мелочь — протокол не называет 204 на номер вне алфавита.
12. НЕ ПРОВЕРЕНО линзой — условие экрана «только пока у личности нет другой живой сессии».
13. RU/EN сходятся.

## Проверка ведущим и что закрыто

| Находка | Чем |
|---|---|
| `Retry-After` и `until` на 429 (Контр. 1) | тест красный до кода |
| Условия заморозки (Контр. 12) — и второе, которого линза не назвала: писать может только сессия, замороженная **пределом ПИНа**, не переносом и не закрытием | **ПРОВЕРЕНО** по экрану 14 (`sosed.place`, строка 22); маршрут пускал любую замороженную; тест, каждая половина красная при своей поломке |
| nonce под блокировкой (Без. 2), NULL-ответ → 409 (Без. 3) | чтением; правка |
| `seen` на сбое базы молчал 204 — клиент думал, что точка погашена | нашёл ведущий; теперь 503 |
| Контракт: тег, `nullable`, `required`, коды GET и seen (Контр. 4–6) | ворота контракта; 409 у GET и seen линза велела снять — ворота вернули: отлучку (`stepped_away`) эти маршруты отдают |
| Экраны 14 обоих репозиториев витрин, макет `screen-14-15-18` (счётчик 0/2000, четыре места), места предела, `open.tsv` (Контр. 2, 3, 7) | ворота пределов, открытых пунктов, листов и текста |
| `[retired]`, 204 вне алфавита, 503 у seen в протоколе RU/EN (Контр. 10, 11) | ворота парности |

Отброшено: Без. 4 (открытый пункт, не новое), Без. 5–12 и Контр. 8, 9, 13 (без дефекта).

## Нарезка — открыто

| # | Задача | Цена |
|---|---|---|
| 1 | Отошедший и поддержка (Без. 1): пускать ли `stepped_away` на `POST /support` — решение продукта | решение + полчаса |
| 2 | ~~Чистка через год~~ — построена 22.09.2026 (`sweep_support`, тест красный при сроке в два года). Открыта суточная сводка: в `support_requests` нет бренда, чей ящик — решение владельца | решение + час |
| 3 | Перевод обращения в уведомление по ст. 16 (строка удаляется той же транзакцией) | полдня, с панелью |
