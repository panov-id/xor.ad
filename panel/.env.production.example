# Production build config for the admin panel (Vite reads VITE_* at build
# time). Copy to .env.production and set the relay backend for this environment.
# .env.production is gitignored.
VITE_RELAY_API_URL=https://api.relay.panov.id
