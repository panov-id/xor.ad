name: Deploy prod

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Release tag to deploy (e.g. v2026.7.6-gabc1234)"
        required: true

jobs:
  call:
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: production
      ref: ${{ inputs.ref }}
    secrets: inherit
