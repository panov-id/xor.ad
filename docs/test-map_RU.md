# Карта тестов: что проверять в каждом потоке

Спека говорит, **что** строим (`chat_RU.md`), схемы — **как это движется**
(`chat-flows_RU.md`). Здесь третье: **чем доказывать, что построенное совпало с
описанным**.

Собрано 26.08.2026 проходом по всем 22 потокам.

## Как читать

Каждая строка — утверждение, которое можно предъявить машине. Не «модуль
покрыт», а «вот это должно быть правдой, и вот запрос, который врёт, если это
неправда».

| Состояние | Что значит |
|---|---|
| **есть** | тест написан и его падение видели |
| **нечем** | кода нет — проверять физически нечего |
| **можно сейчас** | код есть, тест не написан; долг, а не ожидание |

Колонка **шаг** — номер шага порядка сборки (`chat_RU.md` §13), после которого
утверждение становится проверяемым. Раньше него строку не открывать: она
покраснеет от отсутствия таблицы, а не от ошибки.

Три правила, общие для всех строк, — из `CLAUDE.md` проекта:

- **Тест, который никогда не падал, не доказывает ничего.** Написал — сломай то,
  что он сторожит, посмотри на красное, восстанови.
- **Проверяется поведение, а не схема.** «В базе нет сообщений» доказывается
  `SELECT`, а не чтением миграции.
- **Открытый клиент не является местом проверки.** Всё, что клиент «не даёт
  сделать», проверяется запросом мимо клиента.

## Что уже покрыто сегодня

| Где | Кейсов | О чём |
|---|---|---|
| `relay/node/test` | 372 | лендинги, панель, тенантность, DSA, ключи, лимиты, подпись запроса, ручки личности, счётчик ПИНа, первый ПИН, восстановление бумажным кодом, заморозка сессии, уборщик личностей |
| `testing/e2e` | 10 | вейтлист и заголовки витрин |
| **Итого** | **382** | **из них о ленте — 42 (отправка, вердикт, выдача по пересечению кругов, сетка и полосы, снятие, сроки, ступень плотности, мотивировка по ст. 17 и граница видимости на опубликованном центре и предел чтения по личности и курсор в микросекундах, строка счётчиков на лету язык `und` мимо фильтра и расширение радиуса через индекс-бокс потолок подсчёта плотности, старый узел во время выката 027 бэкфилл по той же сетке и предел плотности в час, 21.09.2026), о чате — 0; о подписи запроса — 12, о шаге 1 — 61: ручки 34, перенос 8, ключ печати доли 5, заморозка 3, уборщик 10 против живого Postgres, общий тормоз 3 по часам; плюс 4 о таймаутах соединения, подметальщиках приглашений и разовых меток, счётчиках закрытия личности и датчике тормоза на скрейпе (21.09.2026) и 5 о метриках очереди (20.09.2026, ночь, после панели ревью)** |

Пять из них (`chat_stub.test.ts`) сторожат ровно одно: что заглушка чата отвечает
`501` и ничего не делает. Это верный тест — он упадёт в тот день, когда чат
включат, и напомнит, что карта пора открывать.

Числа пересчитываются `scripts/count-tests.sh --check` — он же сверяет их с этой таблицей и краснеет при расхождении. До 08.09.2026 здесь стоял `scratchpad/count-tests.sh`, которого в репозитории нет и не было: число нельзя было проверить, и оно разошлось (158 против 168 на день, когда скрипт появился); кейсы объявлены тремя разными
обёртками, глазами их считать нельзя.

---

## 1. Первый заход: личность заводится (шаг 1)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 1.1 | Узел не хранит закрытых половин ключей | `SELECT` по всем колонкам `identities`: приватного ключа нет ни в одной | нечем |
| 1.2 | Имя и возраст `NOT NULL` с первой строки | `INSERT` без них падает на уровне БД, а не приложения | нечем |
| 1.2a | **Имя длиннее 24 графем отвергает узел, а не обрезает** | запрос мимо клиента с именем в 25 графем → отказ; в базе прежнее имя | нечем |
| 1.2b | Предел считается в графемах | имя из 24 эмодзи с модификаторами → принято; 25 → отказ | нечем |
| 1.2c | Возраст меньше 13 не заводится, сверху предела нет | `INSERT` с 12 падает на `CHECK`; `INSERT` со 130 проходит | нечем |
| 1.2d | Очевидный ПИН **предупреждает, но не запирает** | регистрация с `000000` проходит, в ответе — флаг предупреждения | нечем |
| 1.3 | Регистрация без ПИНа не заводит личность | запрос мимо клиента: три шага, пропуск второго → отказ | нечем |
| 1.4 | Бумажный код показан один раз и подтверждён вводом двух групп | без подтверждения регистрация не завершается | нечем |
| 1.5 | Каждый клиент начинает отдельной личностью | два клиента подряд → два разных `identity_id` | нечем |
| 1.6 | До первого поста имя не проверяется и никому не видно | очередь модерации пуста после регистрации | нечем |

## 2. Чем подписывается каждый запрос (шаг 1)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 2.1 | Запрос без подписи не проходит | голый `curl` на защищённый маршрут → отказ | **есть** — «the profile answers a finished registration and refuses an unsigned request» (`identity_routes.test.ts`) |
| 2.2 | Подпись вне окна ±5 минут не принимается | часы клиента сдвинуты на 6 минут → отказ | нечем |
| 2.3 | **Подпись считается по методу, хосту, пути с query, sha256 тела и времени** (21.09.2026: хост и query добавлены, G18) | подмена одного байта тела при той же подписи → отказ; та же подпись на другом хосте и с другим курсором → отказ | **есть** — «a changed body breaks the signature», «a request signed for one node does not verify on another», «a cursor cannot be moved under a valid signature» (`identity_auth.test.ts`) |
| 2.4 | Замороженная сессия не принимается **нигде**, включая подписку на доставку, — кроме нового обращения в поддержку при заморозке пределом ПИНа (`frozen_reason = pin_limit`, 14.09.2026, §8.2) | `frozen_at` проставлен → и REST, и WS отвечают отказом | **частично** — REST: «the tenth miss closes entry and leaves the share intact» (`identity_routes.test.ts`); WS нечем, сокетов нет (G14, G15) |
| 2.5 | ECDSA P-256 работает в движках без Ed25519 | `scripts/check-webcrypto-support.sh` в трёх движках | **есть** |

## 3. ПИН, доля узла и ключ хранилища (шаг 1)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 3.1 | ПИН проверяет узел, а не устройство | доля не выдаётся до проверки `auth`-половины | **есть** — «the right PIN hands over the share and resets the counter» и «a wrong PIN spends one try and says how many are left» (`identity_routes.test.ts`) |
| 3.2 | **Десять неверных ПИНов закрывают вход до бумажного кода, доля цела** (§8.2, 14.09.2026; «сжигают долю, база не открывается ничем» [retired]) | десять промахов → верный ПИН отвергнут («вход закрыт»); бумажный код на том же устройстве → прежний ПИН открывает базу | нечем |
| 3.3 | Счётчик сбрасывается только верным ПИНом | девять промахов, верный, снова девять → доля цела | **частично** — «the right PIN hands over the share and resets the counter» (`identity_routes.test.ts`) доказывает сброс верным ПИНом после одной потраченной попытки; слова «только» и девяти промахов подряд в кейсе нет |
| 3.3a | **Десятая ошибка замораживает сессию с причиной `pin_limit`** (§8.2, 14.09.2026) | десять промахов → подписанный `POST /feed` той же сессии получает отказ; живые и ждущие фразы, реплики стола в очереди, мэтчи и `chat_games` сняты, места за столом свободны; одно обращение в поддержку в сутки принимается, второе — нет, список прежних — нет; сессии, замороженной переносом, — отказ; ручка восстановления принимает бумажный код и выдаёт новую долю | нечем |
| 3.3b | **Задержка ПИНа растёт после пятой попытки** (§8.2, 14.09.2026) | пять промахов → шестая попытка раньше 30 секунд отвергнута, `attempts_left` не уменьшился; девятый промах → десятая раньше 4 часов отвергнута | **частично** — «the delay starts after the fifth miss, and waiting is not a way to test a PIN» (`identity_routes.test.ts`) держит сам факт задержки и то, что ранняя попытка счётчик не тратит; **чисел кейс не проверяет** — он стирает задержку `clearDelay()` после каждого промаха, то есть ровно то, что строка измеряет (панель ревью 20.09.2026) |
| 3.3c | **Попытка ПИНа атомарна, верный ПИН снимает ожидание** (§8.2, 14.09.2026) | три параллельные попытки после срока → счётчик уменьшился один раз; верный ПИН во время ожидания отвергнут, после — счётчик 10, ожидания нет | нечем |
| 3.4 | `local`-половина не уходит с устройства | перехват трафика регистрации: в теле только `auth` | нечем |
| 3.5 | Доля принадлежит устройству, а не личности | с живой сессии той же личности чужая доля недоступна | **есть** — «two sessions of one identity hold two different shares» (`identity_routes.test.ts`). [retired] Здесь стояла ссылка на «another session's share cannot be reached from this one»: тот кейс берёт **две разные личности** и ловит отказ стража по чужому ключу, то есть доказывает строку 2.3, а не эту (панель ревью 20.09.2026, линза «Согласованность») |
| 3.6 | Предупреждение появляется при трёх оставшихся попытках | седьмой промах → в ответе флаг предупреждения | нечем |
| 3.7 | **`POST /vault/init` — только по одноразовому праву «первый ПИН»** (панель ревью 19.09.2026) | ключ подписи без права → 409 `unauthorized`; после одобренного переноса → принят, повтор → 409 | **есть** — «the first PIN needs a grant, spends it, and works only once» (`identity_routes.test.ts`); право оставляют обе тропы: `POST /recovery/claim` и одобренный перенос (`transfer_routes.test.ts`, 21.09.2026) |
| 3.8 | **Неверный ПИН отвечает `attempts_left`, десятый — `pin_locked`** (19.09.2026) | три промаха → `attempts_left` 7; десятый → код `pin_locked` | **есть** — «a wrong PIN spends one try…» и «the tenth miss closes entry…» (`identity_routes.test.ts`) |

## 4. Перенос личности на другое устройство (шаг 1)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 4.1 | **Одна живая сессия на личность держится индексом, а не кодом** | два `INSERT` живых сессий подряд → второй падает на уникальном индексе | нечем |
| 4.2 | Без «это я» на старом устройстве переноса не происходит (§14) | `claim` без подтверждения → личность не уехала | **есть** — «a refusal kills the code and moves nothing» и «a second claim cancels the transfer» (`transfer_routes.test.ts`) |
| 4.3 | Приглашение живёт 120 секунд | попытка на 121-й секунде → отказ | **есть** — «an expired invitation cannot be claimed, and says so like a wrong code» (`transfer_routes.test.ts`): срок состаривается в базе, а не выжидается |
| 4.4 | **Промахи claim ограничены, как у восстановления, по адресу и по узлу** (§8.2, 15.09.2026) | 50 claim с неверными кодами за час с разных адресов → 51-й отклонён у всех на 15 минут; опечатка получает «код не подошёл или истёк» | нечем |
| 4.5 | Узел видит `lookup_id` и два непрозрачных конверта | лог узла и содержимое таблицы: долгого ключа нет | нечем |
| 4.6 | Старое устройство замирает в тот же момент | `frozen_at` проставлен до ответа новому | **есть** — «an identity moves to another device, and the old one goes quiet» (`transfer_routes.test.ts`): одна транзакция, и порядок в ней обратный ожидаемому — прежняя сессия гаснет **до** вставки новой, иначе частичный уникальный индекс отказывает |
| 4.7 | Диск старого устройства не стирается, но доля сожжена | вернули личность обратно → своим ПИНом история старого устройства не открывается: перенос сжёг его долю (`chat_RU.md` §8.2; правка 15.09.2026: здесь стояло «открылась вся переписка» [retired]) | **есть, через восстановление** — «a device left behind cannot open its history even with the right PIN» (`identity_routes.test.ts`): переезд там бумажным кодом, добровольный перенос ждёт G15 |
| 4.8 | Перенос работает между лицами: код показан в `depth`, набран в вебе, и наоборот (§14) | пара клиентов, оба направления | нечем |
| 4.8a | **`depth` без обёртки не стартует** (`depth-client_RU.md` §2.1, 14.09.2026) | образ без `DEPTH_WRAPPED=1` → отказ с объяснением; через обёртку → старт, `docker inspect` даёт `LogConfig.Type = none` | нечем |
| 4.9 | **Ни ссылки, ни QR: отдельной страницы под привязку не существует** | маршрута под приглашение нет; узел принимает только `lookup_id` | нечем |
| 4.10 | Вторая половина кода на сервер не уходит | перехват трафика переноса: в запросах только `lookup_id` и конверты | нечем |
| 4.11 | **«Это я» и «не совпадает» — только сессией выдавшей личности, один раз** (панель ревью 19.09.2026) | `approve` чужой подписью → `not_found`; повторный `approve` → `not_found`; `reject` → код гаснет | нечем |

## 5. Восстановление по бумажному коду (шаг 1)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 5.1 | **Код поднимает личность на чистом устройстве** (§14) | браузер стёрт → код → личность та же | **есть** — «the paper code raises an identity that has no live session left» (`identity_routes.test.ts`) |
| 5.2 | **И тогда, когда живая сессия есть** — она замораживается (§14) | восстановление при живом устройстве → у прежнего `frozen_at` | **есть** — «the paper code raises the identity on a clean device» (`identity_routes.test.ts`) |
| 5.3 | **Прежний код жив до подтверждения нового** (§8.2, 10.09.2026) | повтор тем же кодом сразу после подъёма → снова 200 | **есть** — «the paper code raises the identity on a clean device» (`identity_routes.test.ts`). [retired] Здесь стояло «прежний код после восстановления мёртв → повтор тем же кодом → отказ»: момент смерти кода перенесён на подтверждение нового, иначе обрыв между разворачиванием ключа и переписыванием шестнадцати символов оставляет личность без страховки вовсе |
| 5.4 | Новый бумажный код рождается **на устройстве**, узел его не видит | после подъёма устройство шлёт производные нового кода, и прежний гаснет той же транзакцией (`POST /recovery/reissue`) | нечем — ручки перевыпуска нет. [retired] Здесь стояло «ответ содержит новый код ровно один раз»: узел кода не видит никогда (§8.2, 28.08.2026), выдать его в ответе он не может |
| 5.5 | Узел не может развернуть долгий ключ сам | в базе только завёрнутый ключ; развернуть его вторая половина кода | нечем |
| 5.6 | Попытки считает ручка, а не строка личности | промахи с одного адреса по разным кодам → общий счётчик растёт | нечем |
| 5.6a | **Общий счётчик промахов: 50 за час на узел, пауза 15 минут** (08.09.2026) | 50 неверных бумажных кодов за час с разных адресов → 51-я попытка отклонена у всех на 15 минут; через 15 минут приём открыт | **есть** — «fifty wrong codes across the node pause the route for everyone» (`identity_routes.test.ts`) держит порог и цену для честного кода; истечение пятнадцати минут, окно в час и то, что стук во время паузы её не продлевает, — по часам в `recovery_misses.test.ts` (заведён 20.09.2026: прежде «приём открыт» доказывался вызовом `reset()`, то есть тест сам себе отвечал) |
| 5.7 | Алфавит и параметры: 16 символов Crockford base32, соль `xor.ad/recovery/v1`, 80 бит | вектор: тот же код → тот же `lookup_id` | нечем |
| 5.8 | Восстановленные чаты молчат, пока ключ не перевыпущен | сообщение в старый чат → `error`, не тишина | нечем |

## 6. Публикация фразы и модерация (шаг 2)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 6.1 | Пока `visible_at` пуст, фразы нет ни в чьей выдаче, кроме авторской | выдача третьего лица не содержит её | нечем |
| 6.2 | `expires_at` = `visible_at` + 4:20, а не время отправки | долгая очередь → жизнь фразы не съедена | нечем |
| 6.3 | **Отказ очереди закрывает**: проверить нечем → фраза ждёт | модель погашена → фраза не опубликована | нечем |
| 6.3a | **Порог модерации один на все языки, а не подобран по языку** | конфигурация узла содержит одно число; смена языка фразы его не меняет | нечем |
| 6.3b | Лексика по оригиналу стоит до переводчика | брань, которую перевод «отмывает», ловится первым слоем (§8.3) | нечем |
| 6.3c | Латиница возвращается в свой алфавит до определения языка | `ty durak` опознаётся как русский, а не как словацкий | **есть** |
| 6.3d | **Рабочая точка живёт в конфиге узла, а не в коде** (28.08.2026) | смена бюджета ложных блокировок меняет порог без пересборки и без переучивания |  нечем |
| 6.3e | **Число, обещанное правилами сообщества, совпадает с конфигом** | тест читает долю из конфига и из опубликованного текста правил; расходятся — красный. Ровно это разошлось 27.08.2026 и полдня жило незамеченным | нечем |
| 6.4 | Пятый отказ **за час** даёт 15 минут, когда ничего не уходит на проверку; следующий отказ в том же часе — снова 15 минут; лента, лайки фраз и беседы работают (правка 14.09.2026: «15 минут без отправки» [retired]) | пять отказов → `POST /feed`, реплика стола, смена имени и лайк на оффер с непринятым именем — отказ, `GET /feed` 200; шестой отказ в том же часе → новая пауза; истёкшее ожидание очереди счётчик не трогает | нечем |
| 6.4a | **Фраза уходит на проверку по одной** (§8.3, 14.09.2026) | тест «while one phrase is being checked the next is not taken», feed_publish | **есть** |
| 6.4b | **Удержание за столом не переживает паузу** (§8.3, 14.09.2026) | пять отказов 50 минут назад, очередь пуста → реплика принята; четыре отказа и своя реплика на проверке → новая ждёт вердикта | нечем |
| 6.4c | **Первая публикация пишется датой в UTC, «давно» — от 24 до 48 часов** (§8.3, 14.09.2026) | публикация в 23:58 UTC → жалоба через сутки не считается, через двое — считается; публикация в день оффера не считается | нечем |
| 6.5 | **Успешная публикация счётчик отказов не обнуляет** (правка 07.09.2026) | четыре отказа, успех, ещё один → блокировка есть. Прежняя строка требовала обратного и закрепляла обход: четыре пробы, чистая фраза, снова четыре | нечем |
| 6.5b | **Строка «проверяется…» через 60 секунд становится «дольше обычного»** (08.09.2026) | фраза в очереди, прошло 60 секунд → текст сменился, отказа нет, фраза всё ещё в очереди | нечем |
| 6.5a | **Отлучка не снимает ни часовой предел, ни паузу** (07.09.2026) | четыре публикации, уход на 20 минут, возврат → `POST /feed` отказ по часовому пределу | нечем |
| 6.6 | **Фраза выходит, только когда приняты и она, и имя** (26.08.2026) | имя отклонено → фраза лежит; имя поправлено → публикуется сама | нечем |
| 6.7 | Пока фраза ждёт имени, вторую отправить нельзя | второй `POST /feed` → отказ | нечем |
| 6.8 | Пределы: ≤4 живых фраз, ≤4 за час (28.08.2026) | тесты «four an hour counts moments…» и «four live phrases is its own limit…», feed_publish | **есть** |
| 6.9 | Снятие фразы освобождает слот, но не потолок за час | тест «taking a phrase down frees the slot but not the hour», feed_publish | **есть** |
| 6.10 | **Фраза длиннее 128 символов отвергается базой, а не приложением** | `INSERT` со 129 символами падает на `CHECK` | нечем |
| 6.11 | **Зона — только одна из пяти ступеней** (переписан 31.08.2026) | тест «a radius between the steps is refused…», feed_publish | **есть** |
| 6.12 | Ссылка в тексте вырезается, и об этом сказано | фраза со ссылкой → в ленте текст без неё, автору строка с объяснением | нечем |
| 6.13 | Непустая скидка делает фразу оффером частника | лайк на ней даёт мэтч сразу, без ответного (§8.5) | нечем |

## 7. Выдача ленты (шаг 2)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 7.1 | Видимость симметрична: вижу тебя → видишь меня | пара с пересечением кругов, выдача обоим | нечем |
| 7.2 | `band(20) = [18,22]`, `band(21) = [19,∞)`: 20 и 21 встречаются, 19 и 22 — нет | тесты «the band is two worlds that touch at the edge» и «an adult's band has no upper edge…», feed_geo | **есть** |
| 7.3 | Полоса не расширяется никогда, даже при пустой ленте | тест «the band cuts the feed, and it is never widened to fill it», feed_publish | **есть** |
| 7.4 | Пользовательский фильтр зажимается в свою полосу на записи | попытка выставить шире → значение обрезано в БД | нечем |
| 7.5 | Расширение радиуса не меняет сохранённую настройку | после расширения `radius` в профиле прежний | нечем |
| 7.6 | Карточки из расширенного радиуса помечены | тест «an empty screen grows the radius, says so…», feed_publish | **есть** |
| 7.7 | Квота: не более одной коммерческой карточки на десять обычных | выдача с двадцатью офферами → в ответе два | нечем |
| 7.8 | Свои фразы, блокировки и скрытые исключены | три случая в одной выдаче | нечем |
| 7.8a | **Язык — фильтр до трёх, а не подмешивание долями** | выдача с фильтром `ru` не содержит греческих фраз; в ответе есть число «ещё N на других языках» | нечем |
| 7.8b | Язык определяет узел локально | текст фразы не уходит наружу: сеть узла при публикации молчит | нечем |
| 7.8c | **Оффер языковым фильтром не прячется** | фильтр `ru`, греческий оффер в круге → в выдаче есть | нечем |
| 7.8d | Столы и офферы приходят тем же потоком, помеченные | в одной выдаче фраза, оффер и стол, у каждого свой тип | нечем |
| 7.9 | **Узел отдаёт ступень, а не точное число** (26.08.2026) | 7 живых фраз в круге → в ответе `около десятка`, самой семёрки нет нигде | нечем |
| 7.10 | Границы ступеней ровно те, что записаны: 0 · 1–4 · 5–14 · 15–99 · 100+ | по фразе на каждой границе: 4→`несколько`, 5→`около десятка`, 14→`около десятка`, 15→`десятки` | нечем |
| 7.11 | У счётчика своё ограничение частоты | сотня запросов подряд → отказ, а лента при этом работает | нечем |

## 8. Лайк (шаг 3)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 8.1 | Свою фразу лайкнуть нельзя | запрос мимо клиента → отказ | нечем |
| 8.2 | **Лайк недоступен без своей живой фразы** (26.08.2026) | личность без фразы → отказ, а не молчаливый `liked` | нечем |
| 8.3 | Двойной тап не накручивает счётчик | два одинаковых запроса → `like_count` вырос на 1 | нечем |
| 8.4 | Счётчики двигаются в той же транзакции | падение после `INSERT likes` → счётчик не разошёлся | нечем |
| 8.5 | 64 лайка за 32 минуты — предел | 65-й → отказ | нечем |
| 8.6 | Клиенту не сообщают, кого он лайкнул | ответ содержит только `{state}` | нечем |
| 8.7 | **Автор видит `like_count` своей фразы тем же числом, что и все** | выдача автору и выдача чужому содержат одно значение | нечем |
| 8.8 | **Кто лайкнул — не отдаётся ни автору, ни кому-либо** | в ответе по своей фразе нет ни списка, ни признака конкретного лайка | нечем |

## 9. Мэтч и двойное согласие (шаг 4)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 9.0 | **«Не сейчас» гасит карточку только у отказавшегося** (28.08.2026) | `declined_at` заполнен; выдача второго участника не изменилась, срок мэтча тот же | нечем |
| 8.9 | **Принятая редакция сверяется хэшем** (29.08.2026) | правка файла документа → личности с прежним хэшем показываются документы заново; без правки не показываются | нечем |
| 8.10 | **Дата документа держится против его текста** (29.08.2026) | правка текста без даты → красное; правка даты без текста → красное; обе → «новая редакция», деплой стоит до решения | `deploy/check-legal-revisions.py`, самопроверка `--self-test` |
| 8.11 | **Отказ `legal_reacceptance_required`** (29.08.2026) | соглашение изменилось → публикация и открытие чата отказаны 409, выдача ленты не изменилась; правила изменились → отказа нет, строка в журнале появилась сама | нечем |
| 8.12 | **Стикер несёт имя во все три лица** (29.08.2026) | терминал печатает `[имя]`, диктор читает то же слово, узел не получает ни имени, ни картинки — в шифротексте только идентификатор | нечем |
| 8.13 | **У скидки свой срок, у карточки свой** (29.08.2026) | `discount_until` обязателен и ≤90 дней; карточка уходит из ленты через 4:20, сохранённая живёт и после `discount_until` гаснет в «срок вышел» | нечем |
| 8.14 | **Доска ходит с клавиатуры и говорит словами** (29.08.2026) | стрелки+Enter двигают фишку без мыши; ход собеседника приходит как «положила фишку на e4»; у свободного стола — соседством, у физики — «щёлкнула» | нечем |
| 8.15 | **Заново и отмена — по согласию всех** (29.08.2026) | один согласился из двух за столом — ничего не произошло; согласились все — шаг назад; отказались все играть заново — доска закрыта; вдвоём ушёл один — игра закончена; за столом остался один — стол жив | нечем |
| 8.16 | **Стол не тратит квоту** (30.08.2026) | четыре живые фразы + поставленный стол → пятая фраза всё ещё отказана, стол поставлен; стол снят → квота не изменилась | нечем |
| 8.17 | **Отлучка уносит и то, что в очереди** (30.08.2026) | фраза отправлена и не получила вердикта → после ухода её нет ни в ленте, ни в очереди; квота освободилась | нечем |
| 8.18 | **Координаты в выдаче округлены до клетки** (31.08.2026, ужесточён в тот же день) | две фразы одного автора с одним радиусом из одной клетки приходят с **одинаковыми** `lat`/`lon`; в базе они разные **и по широте, и по долготе** — иначе тест зелен при наивном косинусе от точной широты; пересечение считается по точным | нечем |
| 8.19 | **Радиус — только ступень** (31.08.2026) | `area_radius = 437` → отказ на публикации; 300 и 1000 проходят | нечем |
| 9.1 | Мэтч рождается только при живых **обеих** фразах | одна истекла → лайк не даёт мэтча | нечем |
| 9.2 | TTL мэтча = `least()` от обеих фраз, без страховочного минимума | фраза с 3 минутами жизни → мэтч на 3 минуты | нечем |
| 9.3 | Новый взаимный лайк не продлевает мэтч | лайк на исходе → `expires_at` тот же | нечем |
| 9.4 | Чат не создаётся, если один согласился, а второй не успел | таймер вышел → ни `chats`, ни строки | нечем |
| 9.5 | Снимок текста берётся в момент мэтча, а не открытия | фраза истекла между шагами → карточка показывает текст | нечем |
| 9.6 | Гонка на втором нажатии не создаёт два чата | два `accept` одновременно → один `chat_id` | нечем |
| 9.7 | Закрытая между согласиями личность не даёт чата | `closed_at` у одного → `INSERT chats` не происходит | нечем |
| 9.8 | Блокировка при висящем мэтче гасит его немедленно | блок → мэтч исчез, как истёкший | нечем |
| 9.9 | **Бумажный код на этом шаге не спрашивается** (26.08.2026) | первый чат личности открывается без единого доп. экрана | нечем |
| 9.10 | **Согласившемуся первым не отдаётся ничего о чужом действии** | ответ на его запрос не содержит ни `accepted_at` второго, ни признака просмотра | нечем |
| 9.11 | Карточка отдаёт имя, возраст, режим и остатки обеих фраз (у мэтча от оффера — один) — и только их (правка 14.09.2026: «таймер» [retired]) | в ответе нет ни `identity_id` собеседника, ни его других фраз | нечем |
| 9.12 | Мэтч не открывается, пока имя отклонено | `name_state = rejected` → взаимный лайк не создаёт карточки | нечем |
| 9.13 | **«Не сейчас» записывается сразу и возвращается только в секунды отмены** (19.09.2026) | `POST /matches/{id}/decline` → мэтч отклонён; `DELETE` в срок → вернулся; после срока → отказ | нечем |

## 10. Мэтч от оффера — односторонний (шаг 4)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 10.1 | Лайк оффера даёт мэтч немедленно, без ответного лайка | одна сторона → `matched` | нечем |
| 10.2 | Автор оффера может не согласиться, и чата не будет | отказ автора → чата нет | нечем |
| 10.3 | `message_id` и `text_snapshot` необязательны | мэтч от оффера без своей фразы проходит | нечем |
| 10.4 | TTL считается по единственной живой фразе — офферу | оффер истёк → мэтч погас | нечем |
| 10.5 | У офферов заведений лайка нет вовсе | попытка лайкнуть → отказ | нечем |
| 10.6 | **Лайк оффера проходит без своей живой фразы** | личность без фраз лайкает оффер → имя уходит в очередь, мэтч создан после вердикта «принято»; имя отклонено → мэтча нет (S7, 14.09.2026); она же лайкает обычную фразу → отказ | нечем |
| 10.7 | **Возрастные полосы не режут офферы заведений** | 15-летний и 40-летний видят один и тот же оффер в круге | нечем |
| 10.8 | Оффер частника исчезает при отлучке вместе с фразами | «отойти» → оффера в ленте нет | нечем |

## 11. Открытие чата и ключи (шаг 6)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 11.1 | Долгий ключ личности в шифровании не участвует | утечка долгого ключа не даёт расшифровать перехваченный шифротекст | нечем |
| 11.2 | Смерть чата стирает `K`, эфемерные ключи и свёртки | после смерти расшифровать сохранённый шифротекст нечем | нечем |
| 11.3 | Ключи живых чатов при переносе не ротируются | перенос → `chat_key_wraps` прежние | нечем |
| 11.4 | Перевыпуск требует подписи **долгим ключом**, а не членства в чате | подпись сессией → отказ второй стороны | нечем |
| 11.5 | Перевыпуск требует согласия второй стороны | отказ → чат остаётся молчащим | нечем |
| 11.6 | Код безопасности при перевыпуске не меняется | он из долгих ключей — сверка до и после | нечем |
| 11.7 | Старый `K` после перевыпуска не восстанавливается ничем | прежнее устройство не читает новые сообщения | нечем |

## 12. Сообщение (шаг 5)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 12.1 | **В базе нет открытого текста, а очередь пуста после доставки** (§14) | прямой `SELECT` по всем таблицам, `pending_deliveries` пуста | нечем |
| 12.2 | Единственное чтение БД в тракте — проверка членства | лог запросов при доставке | нечем |
| 12.3 | Не участник чата не может послать в него | подпись чужой сессии → отказ | нечем |
| 12.4 | **Предел по байтам шифротекста отвергает узел** (§14, `max_ciphertext_bytes` = 2048) | запрос мимо клиента с 2049 байтами → отказ | нечем |
| 12.5 | `max_message_length` = 256 — счётчик клиента, не правило узла | 300 символов, влезшие в 2048 байт, узел принимает | нечем |
| 12.6 | **Сообщение офлайн-собеседнику даёт `accepted` и ждёт его возвращения** (§14) | получателя нет ни на одном узле → `ack {accepted}`, строка в очереди, выдача при подключении | нечем |
| 12.7 | Повтор с тем же `local_id` не даёт дубля у получателя | две отправки → одно сообщение на экране | нечем |
| 12.8 | Пауза растёт ×3: сразу, 5 с, 15 с, 45 с, 135 с, потолок ~10 мин | замер интервалов подряд | нечем |

## 13. Жизнь и смерть чата (шаг 7)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 13.1 | **У каждого свой срок, и он считается от своего последнего сообщения** | Петя 10 мин, Коля час; Петя молчит 11 минут → у Пети `gone_at`, у Коли беседа жива | нечем |
| 13.2 | Чужой срок и чужой остаток не отдаются | ответ на открытие беседы не содержит ни значения собеседника, ни его `gone_at`-времени | нечем |
| 13.3 | Срок меняется в открытой беседе когда угодно | смена с 10 на 60 на живой беседе → принято, счёт пересчитан от того же `last_own_message_at` | нечем |
| 13.3a | Чужое сообщение **не** сбрасывает мой счёт | Коля пишет каждые 5 минут, Петя молчит 11 → беседа у Пети кончилась | нечем |
| 13.4 | **Свой ход двигает свой счёт так же, как своё сообщение** | только свои ходы, час молчания словами → беседа жива у ходившего | нечем |
| 13.4a | **Чужой ход мой счёт не двигает** | собеседник ходит, я только смотрю → у меня беседа кончается по сроку | нечем |
| 13.4b | Доска и ключ гаснут у обоих по первой смерти | у второго беседа ещё числится живой, а доски уже нет | нечем |
| 13.5 | **Беседа исчезает у того, чей срок вышел** (§14) | после первой сверки `alive` — пусто у него, история второго цела | нечем |
| 13.5a | **Ключ и доска гаснут у обоих в момент первой смерти** (§8.13) | `chat_key_wraps` пусты у обоих, а история второго при этом читается | нечем |
| 13.5b | Писать в закончившуюся беседу нельзя **ни одному из двоих** | отправка мимо клиента с обеих сторон → отказ узла | нечем |
| 13.5c | Узел вычёркивает `chats`, только когда `gone_at` у обоих | после первой смерти строка на месте; после второй — каскад | нечем |
| 13.6 | **У того, кто смотрел, остаётся надгробие до нажатия «закрыть», и в список он не возвращается** (§14) | чат открыт на экране в момент смерти | нечем |
| 13.7 | Один чат на пару держится уникальным `pair_key` | второй `INSERT` → конфликт | нечем |
| 13.8 | После смерти `pair_key` освобождается | та же пара сходится заново | нечем |
| 13.9 | Сервер знает только **когда** было движение | в `chats` нет ни текста, ни автора, ни счёта | нечем |
| 13.10 | **«Закончить» закрывает у обоих сразу, в отличие от истечения** | нажал один → у второго `gone_at` тоже проставлен | нечем |
| 13.11 | Код безопасности выводится из долгих ключей обеих личностей | у обоих совпадает; после перевыпуска ключа чата не меняется (§8.13) | нечем |
| 13.12 | Собеседник в отлучке — над живым полем метка «отошёл», отправка принимается и ждёт в очереди (правка 14.09.2026: «вместо ввода строка, и узел отвергает отправку» [retired]) | отправка отошедшему → принята в `pending_deliveries`; беседа кончилась у него до возвращения → строки очереди удалены | нечем |
| 13.12a | **Метка «отошёл» — булево на участнике** (§8.2, 14.09.2026) | уход → `away_marked` во всех живых беседах; досрочный возврат метку не снимает; реплика или ход в беседе — снимает; наружу только `peer_stepped_away` | нечем |
| 13.12b | **Срок молчавшего участника — от создания беседы** (§8.6, 14.09.2026) | ни одной своей реплики → `gone_at` наступает по `created_at + idle_ttl_minutes`; клиент получает `created_at` при открытии | нечем |

## 14. Дополнительный лайк в открытый чат (шаг 7)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 14.1 | Лайк при открытом чате не создаёт нового мэтча | `matches` не пополнились | нечем |
| 14.2 | Строка `chat_starters` переживает исчезновение пузыря | локальная история стёрта → `position` на месте | нечем |
| 14.3 | Формулировка считается под зрителя | два клиента видят разные тексты одного события | нечем |
| 14.4 | Номер карточки совпадает с номером в шапке `Liked, in order` | сверка позиций | нечем |

## 15. Игра и стол (шаг 8)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 15.1 | Доска синхронизируется зашифрованной и узлу непрозрачна | перехват: состояние доски нечитаемо | нечем |
| 15.2 | **Исключение названо: колоду и кости узел видит** (26.08.2026) | тасовка идёт на узле, и это единственное место, где §8.13 не держит | нечем |
| 15.2a | **Стикер узлу не виден: его номер лежит внутри шифротекста** | перехват реплики со стикером → идентификатора в открытом виде нет | нечем |
| 15.2b | **Пачки стикеров грузятся при старте приложения** (14.09.2026) | журнал сети: запросы каталога только в первые секунды после старта; открытие беседы и получение стикера запросов не дают | нечем |
| 15.3 | Приватная рука приходит зашифрованной на своего игрока | второй видит рубашку, а не карты | нечем |
| 15.4 | Физика сходится у обоих по общему сиду | один щелчок → одинаковое конечное положение | нечем |
| 15.5 | Загаданное слово проходит очередь модерации | запрещённое слово → «загадайте другое» | нечем |
| 15.6 | Игра описывается примитивами: новая игра добавляется описанием, а не кодом | добавить набор без правки движка | нечем |
| 15.7 | Кеш партии уходит с беседой | удалить `chats` → `SELECT` в `chat_games` пусто (каскад, не срок) | нечем |
| 15.8 | **Стол:** полосы проверяются каждый с каждым | подсесть к столу вне полосы одного из сидящих → отказ | нечем |
| 15.9 | **Стол:** подсевший не получает истории | доска в текущем виде, реплики с момента посадки | нечем |
| 15.10 | **Стол:** речь публична и идёт через очередь | реплика видна после проверки, а не до | нечем |
| 15.11 | **Стол:** высаживает большинство, владельца нет | заводивший стол не может высадить один | нечем |
| 15.12 | **Стол:** блокировка разводит на посадке (правка 14.09.2026: «скрывает стол целиком» [retired]) | стол с заблокированным не показывается; сесть к нему нельзя и ему к вам нельзя; вы уже за одним столом → выходит заблокировавший, партия у остальных идёт, его ход — пас по `table.move.window` | нечем |
| 15.13 | **Стол:** срок один на всех, от последнего хода любого | один ходит, другие молчат час → стол жив у всех | нечем |
| 15.14 | **Стол:** речь и доска идут открыто, узел их видит | реплика за столом читается узлом — иначе её нечем проверять очередью | нечем |
| 15.15 | **Стол:** полоса считается по сидящим сейчас и пересчитывается (08.09.2026) | двое взрослых за столом → подростку стола в выдаче нет; встал один, полоса другая → стол появился; стол без сидящих не показывается никому. В обоих случаях стола в выдаче **нет**, а не «есть, но серый» | нечем |
| 15.16 | **Уведомление по ст. 16 принимается на реплику за столом** (28.08.2026) | `target_kind = table_line` проходит; снимок содержит текст реплики и `table_id` | нечем |
| 15.16a | **Жалоба за столом без галочки — только скрытие у себя** (14.09.2026) | жалоба без галочки → реплика скрыта у пожаловавшегося, в реестре уведомлений новой строки нет; с галочкой → строка `table_line` есть | нечем |
| 15.17 | **Доска в снимок уведомления не попадает** | снимок реплики не содержит состояния партии: незаконен текст, а не игра | нечем |

## 16. Три разных действия против человека (шаг 7)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 16.1 | Скрытие видно только скрывшему | лента автора и счётчик лайков не изменились | нечем |
| 16.1a | **Скрытие ничего не отправляет в очередь модерации** | после скрытия очередь пуста, счётчик жалоб фразы не вырос | нечем |
| 16.5a | **Жалоба без обоснования не принимается** | `POST` уведомления с пустым текстом → отказ, записи нет | нечем |
| 16.5b | **Категории правонарушения в форме нет** | в схеме уведомления нет поля категории, и API его не принимает | нечем |
| 16.2 | Блокировка симметрична | ни один из двоих не видит фраз другого | нечем |
| 16.3 | Блокировка закрывает общий чат | чат исчез у обоих | нечем |
| 16.4 | Лайк при блокировке не порождает мэтча | взаимные лайки → мэтча нет | нечем |
| 16.5 | Жалоба ничего не закрывает сама | после жалобы фраза жива | нечем |
| 16.5c | **Жалоба и блокировка не меняют квоту автора** | до и после: потолок живых фраз тот же, потолок за час тот же | нечем |
| 16.6 | Жалоба несёт слово заявителя, а не копию переписки | клиент ничего не выгружает, `snapshot_state = not_accessible` | нечем |
| 16.7 | **Жалоба на контент через поддержку попадает в тот же реестр** | обращение с описанием незаконного → запись в реестре уведомлений, а не в таблице поддержки | нечем |
| 16.8 | **Ответ показывается в приложении при следующем входе, без почты** | обращение без адреса → ответ ждёт при личности и виден на входе | нечем |
| 16.8a | **Номер обращения ключом не служит** (§13, 14.09.2026) | `GET` ответа по `public_no` без подписи или подписью другой личности → пусто; номера двух обращений подряд не соседние | нечем |
| 16.8b | **Три обращения в сутки, четвёртое — отказ с адресом почты** (§13, 14.09.2026) | три `POST` → приняты; четвёртый → отказ, в теле адрес поддержки витрины; через сутки — принят | нечем |
| 16.8c | **Команде — суточная сводка без текста** (§13, 14.09.2026) | десять обращений за день, одно из замороженной сессии → одно письмо с тремя числами, ни строки из `body` | нечем |
| 16.8d | **Предел обращений держится под параллельными запросами** (§13, 14.09.2026) | два обращения есть, два параллельных `POST` → принят один | нечем |
| 16.8e | **Второй ответ снова зажигает точку** (§13, 14.09.2026) | ответ прочитан, записан второй → `answer_seen = false` | нечем |
| 16.9 | **Решение по уведомлению видно по квитанции без почты** (`dsa/SPEC_RU.md` §6, 14.09.2026) | уведомление без почты → на узле только `receipt_hash`; решение принято → запрос с кодом показывает его | нечем |
| 16.9a | **Квитанцию нельзя подобрать** (§6) | неизвестный код и «решения ещё нет» → статус 200 у обоих, тела совпадают байт в байт, `Cache-Control: no-store`, время ответа не меньше общего минимума у обеих веток; код передан телом `POST`; в журнале адреса нет | нечем |
| 16.9b | **Хэш квитанции уходит с уведомлением** (§9) | `prune_dsa_records` через год → нет ни уведомления, ни `receipt_hash` | нечем |

## 17. Смена имени и возраста (шаг 1, полностью — с шага 2)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 17.1 | Принятое имя заморожено при живой фразе или открытом чате | правка → отказ | нечем |
| 17.2 | Отклонённое имя правится всегда | при живой фразе правка проходит | нечем |
| 17.3 | Отвергнутое имя не сохраняется, прежнее продолжает действовать | после отказа в профиле старое | нечем |
| 17.4 | Смена имени не рассылает системных сообщений | в чатах пусто | нечем |
| 17.5 | Возраст 20 → 21 разрешён и необратим | обратно → отказ | нечем |
| 17.6 | При смене возраста фильтр пере-зажимается в новую полосу | значения в БД обрезаны | нечем |
| 17.7 | Смена возраста рассылает системное сообщение во все открытые чаты | у собеседников строка появилась | нечем |
| 17.8 | Переспрос раз в год, молчание ничего не меняет | год спустя без ответа → возраст прежний, блокировок нет | нечем |
| 17.9 | **Бумажный код перевыпускается только предъявлением текущего** | запрос без кода → отказ; с верным кодом → новый выдан, прежний мёртв | нечем |
| 17.10 | Смена ПИНа перешифровывает базу и берёт новую долю | старый ПИН после смены не открывает ничего | нечем |
| 17.11 | «Начать заново» закрывает личность, а не удаляет строку | `closed_at` проставлен, фразы ушли из выдачи, бумажный код больше не поднимает | нечем |

## 18. Инбокс (шаг 8)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 18.1 | Таблицы `notifications` нет | список таблиц | нечем |
| 18.2 | Инбокс выводится из `matches`, `chats`, `chat_starters`, `last_activity_at` | все пять поводов на одном наборе данных | нечем |
| 18.3 | Пуш не уходит никуда ни в одном лице | сеть при закрытой вкладке молчит | нечем |
| 18.4 | Пропущенное сообщение выводится косвенно, текст не восстанавливается | строка «вы пропустили» есть, текста нет | нечем |
| 18.5 | Сгоревший мэтч в инбоксе не появляется | истёк, пока клиент был закрыт → его нет | нечем |
| 18.6 | **Угасание считается от своего молчания, а не от общей активности** | собеседник пишет каждую минуту, я молчу → у меня беседа в инбоксе помечена угасающей | нечем |
| 18.7 | Счётчик отдаётся только по предложениям, ждущим моего ответа | в ответе инбокса нет числа по открытым беседам | нечем |

## 19. Три таймера и уборка (шаг 7)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 19.1 | Истечение фразы уносит лайки каскадом | `likes` пусты | нечем |
| 19.2 | `chat_starters` переживают смерть исходной фразы | текст скопирован, а не связан ссылкой | нечем |
| 19.3 | Смерть чата уносит участников, стартеры и свёртки каскадом | четыре таблицы пусты | нечем |
| 19.4 | **`POST /chats/alive` при недоступной базе отвечает 503 и клиент не удаляет ничего** (§14) | Postgres погашен → локальная история цела | нечем |
| 19.5 | Одна сверка `alive` закрывает истёкший TTL, закрытую личность, блокировку и «месяц не заходил» | четыре причины в одном ответе | нечем |

## 20. Доставка между узлами (шаг 5)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 20.1 | **Разговор переживает перезапуск узла** (§14, замена критерия про два узла) | узел убит на середине → клиенты переподключились, история та же | нечем |
| 20.2 | Сообщение доходит между участниками на разных узлах | два бокса на общей базе — **после появления пула** | нечем |
| 20.3 | Узел без сокета получателя не делает ничего | лог третьего узла пуст | нечем |
| 20.4 | Payload сообщения влезает в 8 КБ `NOTIFY` | сообщение предельной длины проходит | нечем |

## 21. Лестница раскрытия (шаг 4)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 21.1 | В ленте нет ничего об авторе | ответ выдачи: ни `identity_id`, ни имени, ни возраста | нечем |
| 21.2 | Имя и возраст появляются ровно с карточки мэтча | ответ до и после | нечем |
| 21.3 | Чужой `identity_id` не отдаётся никогда | ни в одном ответе API | нечем |
| 21.4 | Кто лайкнул — не отдаётся никогда | ответ по фразе содержит только число | нечем |
| 21.5 | Две фразы одного человека не связываются снаружи | в выдаче нет общего ключа | нечем |

## 22. Отошёл (шаг 1)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 22.1 | Фразы удаляются, а не скрываются | `SELECT` — строк нет; слоты квоты свободны сразу | нечем |
| 22.2 | Мэтчи гаснут, и второй не узнаёт причины | у второго предложение исчезло без объяснения | нечем |
| 22.3 | Чаты не замораживаются: TTL идёт | `last_activity_at` не сдвинулся | нечем |
| 22.4 | Сокеты сессии закрываются тем же путём, что при заморозке | соединение разорвано узлом | нечем |
| 22.5 | Собеседник в открытом чате видит метку `stepped_away` над живым полем | единственное исключение из «о присутствии не сообщаем»; снимается первой репликой или ходом, а не сроком (14.09.2026) | срок отлучки вышел без реплики — метка на месте; вернувшийся написал — метки нет; реплика собеседника во время отлучки доставлена при подключении |
| 22.6 | Счётчик времени в приложении на узел не попадает | ни колонки, ни запроса рядом с личностью | нечем |
| 22.7 | Досрочный выход работает, частота не ограничена | три ухода подряд → отказа нет | нечем |
| 22.8 | **Стол переживает отлучку поставившего, а сам он из-за него встаёт** | «отойти» → стол в ленте есть, в списке сидящих ушедшего нет | нечем |
| 22.9 | Оффер частника уходит вместе с фразами | «отойти» → фраза со скидкой удалена | нечем |

---

## 23. Контракт: версия, форма ошибки, пагинация, отлучка, блокировки (16.09.2026)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 23.1 | Запрос без `x-protocol-version` или с неподдерживаемой версией → 400 `protocol_version_unsupported`, в `message` команда обновления (протокол §6) | два запроса к любому подписанному маршруту | нечего проверять |
| 23.2 | Отказ подписанного маршрута — `{error: {code, message, reason?}}`; 429 несёт `Retry-After` в секундах (§6) | превысить `identities.create.hour` и прочитать тело и заголовок | нечего проверять |
| 23.3 | `GET /feed` и `GET /inbox` отвечают `{items, next}`; `?after=<next>` не повторяет и не пропускает строк при угасании между страницами (§6) | две страницы, между ними снять фразу | нечего проверять |
| 23.4 | Столы приходят только на первой странице, `kind: table`, в `items` с курсором их нет (§4.2) | вторая страница при живых столах | нечего проверять |
| 23.5 | Во время отлучки любой подписанный запрос, кроме `DELETE /away`, `GET /identities/me` и `POST /identities/close`, → 409 `stepped_away` (§4.9) | `POST /away`, затем `GET /feed` и `POST /chats/:id/ticket` | нечего проверять |
| 23.6 | `POST /blocks` → 204 всегда, повтор — 204; `DELETE /blocks/:id` → 204 и на чужой `id`; столы пересчитываются только при следующем входе в ленту (§4.8) | три запроса и `GET /feed` до и после | нечего проверять |
| 23.7 | `POST /hidden {feed}` → `{id}`, `DELETE /hidden/:id` возвращает фразу; `{line}` принимается только формой жалобы без галочки (§4.8) | скрыть, вернуть, сверить `GET /hidden` | нечего проверять |
| 23.8 | Повтор любого из семи маршрутов §2 (`/tables`, `/away`, `/support`, `/recovery/reissue`, `/identities/close`, `/vault/pin`, `/blocks`) с тем же `nonce` в десять минут отвечает первым ответом и не действует второй раз (§2) | два одинаковых запроса | нечего проверять |
| 23.9 | Ход с чужой версией доски → 409 `stale_seq`; то же тело при том же `seq` → та же доска (§4.6) | три хода подряд | нечего проверять |
| 23.10 | Фильтр ленты: любые `min <= max` внутри полосы принимаются, за полосу → `filter_out_of_band` (§4.11; ступени сняты 17.09.2026) | `PATCH /identities/me` с 21–23 у 40-летнего — 200; с 15–30 — 400 | нечего проверять |
| 23.11 | Лайкнутое уходит из выдачи: после `POST /feed/:id/like` фраза не приходит в `GET /feed` этого человека, но приходит в `GET /likes` со `state: liked`; после `DELETE` — обратно (17.09.2026) | лайк, две выдачи, снятие | нечего проверять |
| 23.12 | Лайк стола: `POST /tables/:id/like` не сажает (`GET /tables/:id` без места — 404), `like_count` +1 виден сидящим, стол в `GET /likes`; повтор — то же; `DELETE` → 204 и −1 (17.09.2026) | два человека, один сидит | нечего проверять |
| 23.13 | Имя стола: `POST /tables` с `name` — стол в выдаче сразу без `name`, после вердикта очереди `name` есть; отклонённое — кадр `name_verdict` с `table` автору, стол жив; 25 графем → 400 (17.09.2026) | три постановки | нечего проверять |

## 24. Офферы (шаг 10, `offers/SPEC_RU.md`, протокол §4.13)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 24.1 | Кабинет живёт на своём источнике, cookie `__Host-adv` не уходит на витрину (SPEC §2.1, 19.09.2026) | запрос к витрине с сессией кабинета → cookie в запросе нет | нечем |
| 24.2 | Каждый запрос кабинета проверяет владение, а не роль (SPEC §2.1) | чужой `venue_id` в `PATCH /adv/venues/{id}` → отказ | нечем |
| 24.3 | Публикует только `verified`-заведение; автопроверки отказывают с причиной (§6.1) | `unverified` → отказ; сокращатель в ссылке → 422 с причиной | нечем |
| 24.4 | Код конверта: неверный — 422 и попытка засчитана, погашенный — 409 (§2.1) | серия неверных → код погашен → 409 | нечем |
| 24.5 | «Это не мы» отвечает 204 на любой код и расходует тот же счётчик, что ввод кода (§11, 19.09.2026) | неверные коды в `not-us` гасят код для `/verify` | нечем |
| 24.6 | Жалоба на скидку засчитывается по первой публикации заявителя, значение заморожено (§3, §10) | заявитель без публикации старше суток → 202 с `counts_towards_autohide: false` | нечем |
| 24.7 | Три засчитанные жалобы разных людей скрывают оффер (§10) | три → `hidden`; две и одна незасчитанная → `active` | нечем |
| 24.8 | Две засчитанные жалобы на ссылку гасят её сразу, незасчитанная уходит модератору (§10.1) | две одновременные → `redirect_disabled_at` проставлен один раз | нечем |
| 24.9 | `/o/{code}/go`: `no-store`, `no-referrer`, превьюеры не считаются, неизвестный код — 404 (§6.2, 19.09.2026) | заголовки ответа; `HEAD` не меняет `redirect_hits` | нечем |
| 24.10 | Опубликованный оффер не редактируется; «Показать снова» — новый с `repeated_from` (§3.1, §8) | правки к опубликованному нет; повтор → новая строка | нечем |

## 25. QR места (экран 26, протокол §4.2, 19.09.2026)

| № | Что должно быть правдой | Чем доказывается | Состояние |
|---|---|---|---|
| 25.1 | Сделать QR — ни одного запроса к узлу | журнал сети браузера при «сделать QR», «поделиться», «сохранить картинкой» → пуст | нечем |
| 25.2 | Разбор фрагмента строгий; сломанный — обычная лента без листа; узел клетки совпадает с `grid_round_lat/lon` | таблица входов: `#p=17472.4457&s=2`, `s=0`, `s=6`, `p=abc`, `p=1.5e9.1`, широта за ±90°, отрицательные индексы; восстановленный узел против `grid_round_*` (`chat_RU.md` §8.3) | нечем |
| 25.2а | QR не попадает на сервер: журнал витрины и CDN без `p`/`s`; ссылка афиши `?p=&w=&l=` живёт по своим правилам | открыть QR → запрос к витрине без фрагмента; афиша → строка над лентой, а не лист | нечем |
| 25.3 | Открытый QR точку сам не двигает, радиус просмотра не меняет, дальше 25 км точку не предлагает; фрагмент убирается при любом исходе | открыть QR → точка прежняя; «поставить» → точка в центре клетки, радиус прежний; клетка в 30 км → нет «поставить»; после листа → адрес без `#` | нечем |
| 25.4 | Зона фразы по точке QR: по умолчанию большая из ступени QR и 300 м | QR 1 км → 1 км; QR 100 м → 300 м; сдвиг точки → обычные правила | нечем |
| 25.5 | Узел не отличает фразу по QR: тело `POST /feed` — те же ключи, что у любой фразы | сравнить ключи тела с фразой без QR → совпадают; поля `place`/`qr` в `openapi.yaml` нет | нечем |
| 25.6 | Одна клетка и ступень дают одну ссылку, кто бы ни делал | два профиля, одна точка и ступень → строки ссылок равны | нечем |

## Что из этого можно проверить уже сегодня

**Первая таблица появилась 20.09.2026** — миграция `relay/node/db/022_identity_and_sessions.sql`
положила в базу `identities`, `sessions`, `vault_shares`, `nonces`, `legal_acceptances`
и `identity_appearance` (замерено: `scripts/check-facts-schema.sh` видит в базе 15 таблиц
против 9 до неё). Тем самым строка **1.1** перестала быть «нечем»: `SELECT` по всем
колонкам `identities` теперь выполним. Сверх неё проверяема **2.5** (замер WebCrypto —
сделан, `scripts/check-webcrypto-support.sh`) и косвенно **весь остальной список** — тем,
что `chat_stub.test.ts` сторожит `501`. Всё прочее ждёт своего шага §13, и это не
отговорка, а состояние: ручек продукта нет, `/feed` отвечает 404, `/chat` — 501.

Карта нужна раньше кода именно поэтому. Когда появится первая таблица, спорить о
том, что считать готовым, будет не нужно — список уже написан и выведен из спеки,
а не из того, что оказалось удобно проверить.

## Читать вместе с

- [`chat_RU.md`](chat_RU.md) — спека: §13 порядок сборки, §14 критерии приёмки.
- [`chat-flows_RU.md`](chat-flows_RU.md) — те же потоки, нарисованные.
- [`retired-terms.txt`](retired-terms.txt) — формулировки, которые решение
  отменило; проверяются `scripts/check-retired-terms.sh`.
- `scripts/check-mermaid.sh` — разбирает каждую схему из `docs/*.md` в
  контейнере: сломанная картинка на GitHub молчит, а скрипт называет её по имени.
- [test-map_EN](test-map_EN.md)
