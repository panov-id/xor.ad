# Панель ревью · 22.09.2026 · очередь модерации ленты

- **Что ревьюили:** `relay/node/src/routes/feed_queue.ts`, `lib/feed_verdict.ts`
  (publish принимает имя), `access/permissions.ts`, `roles.ts`;
  `panel/src/pages/feed-queue/list.tsx`, `App.tsx`, `access/*`; `panel/tests/helpers/*`,
  `scripts/seed-local-phrase.sh`, `depth/tools/seed_phrase.ts`, `relay/local/docker-compose.yml`;
  коммиты `41456a3`, `8d340db`, `fdbb504` ветки `day56`.
- **Линзы:** Безопасность (чтением), Данные и согласованность (чтением; `count-tests.sh --check` прогнан).
- **Перепроверка:** ведущий — тест, увиденный красным, на каждую правку кода; чтение DDL и сиблинга `dsa.ts`.

## Находки по существу

### Безопасность
1. Дефект дизайна — TOCTOU на имени: вердикт принимает `name_pending` на момент коммита,
   а не то, что модератор прочёл. Сегодня `name_pending` никто не пишет — не эксплуатируемо.
2. Дефект дизайна — отклонённое имя переходило в `accepted` без `name_pending`
   (`coalesce` возвращал прежнее имя).
3. Дефект дизайна — брендовая ограда проверялась вне транзакции вердикта; сиблинг `dsa.ts`
   держит её внутри.
4. Мелочь — оракул по брендам: нет, чужой id всегда 404.
5. Мелочь — пределов частоты нет; как у `dsa.ts`.
6–8. Без дефекта: гонки вердиктов и уборщика, личность автора не отдаётся, CSRF (Bearer).
9. Мелочь — inner JOIN прячет фразу автора, закрывшего личность.
10. Мелочь — `seed-local-phrase.sh` вставляет текст в `$$…$$`; только локальный стенд.
11. Мелочь — `VAULT_SHARE_KEY` на стенде — того же класса, что `SESSION_SECRET` там же.
12. Не найдено — кнопки не скрываются по `feed_queue.decide`; сервер право проверяет.

### Данные и согласованность
1. Дефект дизайна — то же, что Без. 2; спека §8.2 говорит обратное.
2. Противоречие — «имя проходит очередь при первой публикации» недостижимо: регистрация
   заводит `name_state = accepted`, `pending` никто не ставит.
3. Транзакция: `identities` обновлялась без блокировки; дыра появится с маршрутом смены имени.
4–5. Без дефекта: уборщик против вердикта; CHECK не нарушается.
6. Противоречие — 404 в контракте описан у́же кода; 404/409 зависят от роли для одного факта.
7. Мелочь — то же, что Без. 9.
8. Мелочь — `x-total-count` равен размеру страницы, не всему хвосту (>200 врёт).
9. Индекс не нужен: `feed_one_waiting` и уборщик держат хвост ≤ 10 минут.
10. Мелочь — 10 минут лежат в трёх местах, ворота видят одно; 15 с и 480 с только в коде.
11. Противоречие — `limits.tsv` называл исполнителем `moderation.queue.wait.unbuilt`, а задача
    есть; `open.tsv` утверждал, что `publishPhrase()` зовёт подметальщик — дважды неверно.
12. Противоречие — `feed.phrase.span` в реестре «4», описание «4 часа 20 минут».
13–14. Без дефекта: страница и маршрут согласованы; RU/EN сходятся. «Три теста» в роадмапе — НЕ ПРОВЕРЕНО линзой.

## Проверка ведущим и что закрыто

| Находка | Чем |
|---|---|
| Имя сверяется с прочитанным (Без. 1) | тест «publish accepts only the name the moderator saw…», красный до правки; страница шлёт `{name}`, узел отвечает 409 |
| Только `pending` принимается (Без. 2, Данн. 1) | тот же тест, вторая половина; спека RU/EN поправлена |
| Бренд внутри транзакции (Без. 3) | чтением: `publishPhrase`/`refusePhrase` берут `scope.brand` в `FOR UPDATE`; `identities` блокируется тоже (Данн. 3) |
| LEFT JOIN, метка «author gone» (Без. 9, Данн. 7) | чтением; тестом не покрыто |
| Контракт: 404/409, тело Publish (Данн. 6) | ворота контракта |
| Исполнитель предела и открытый пункт (Данн. 11) | ворота пределов и открытых пунктов |
| `feed.phrase.span` (Данн. 12) | «4 20» ворота не принимают («20 часа» не найдено) — оставлено «4», граница записана в описании |
| «Три теста» в роадмапе | ПРОВЕРЕНО: тестов очереди в `feed_publish.test.ts` теперь пять; роадмап поправлен |

Отброшено: Без. 4, 5, 10, 11 — не дефекты либо класс уже принят; Данн. 9 — нечего делать.

## Нарезка — открыто

| # | Задача | Цена |
|---|---|---|
| 1 | Регистрация с `name_state = pending`, чтобы имя действительно проходило очередь при первой публикации (Данн. 2); решение продукта: до этого имя не видно никому, так что можно и оставить `accepted` и снять фразу спеки | решение + час |
| ~~2~~ | ~~Маршрут смены имени~~ — построен 22.09.2026 (`PATCH /identities/me`, панель `PANEL_2026-09-22_profile-patch.md`) | — |
| ~~3~~ | ~~`x-total-count`~~ — закрыто 22.09.2026: `count(*) OVER ()`; тест на 201 фразе красный до кода | — |
| 4 | 15 с и 480 с — в `limits.tsv`, если это факты, а не подробность страницы (Данн. 10) | решение |
| ~~5~~ | ~~Кнопки по `feed_queue.decide`; тест LEFT JOIN~~ — закрыто 22.09.2026: `useCan` на странице, пара в `resources.ts` (тест красный до кода); тест автора без личности, красный при `JOIN` | — |
