# Ночь 18.09.2026 — сводка для владельца

Режим: с 00:35 до 07:00 без команд, по указанию владельца от 17.09.2026 («аналитический режим до 7 утра»).
Ограничения, которые он поставил: только аналитика, графика и прощупывание UX/UI; код не пишется,
описания экранов и канон не правятся, пуша нет. Всё ниже — в ветке `day54` репозитория `xor.ad`,
коммит на каждую волну.

Приоритеты, названные владельцем (в его порядке): остаток обхода экранов и SVG-макеты; план сборки
API без кода; UX-проходы целиком от регистрации до первой беседы; маркетинговый материал —
афиши с QR-кодом, «сканишь — предлагается принять геопозицию и что-нибудь сказать», волнами по
районам, «нам же не важна реальная геопозиция».

**Все листы одной страницей:** https://claude.ai/artifact/1DumwaLADcc3hUzjVBdKFF (приватная, уменьшенные копии PNG, под каждым листом три главных вопроса).

## Состав ночи

| Волна | Что | Кто | Куда |
|---|---|---|---|
| 1 | Обход экранов 08, 18, 19, 10, 14, 15, 20, 21 письменно, с вопросами | агент | `NIGHT_2026-09-18_screens-walk.md` |
| 1 | SVG-макеты 02 и 25 | агент | `panel/design/screen-02.svg`, `screen-25.svg`, `NIGHT_2026-09-18_mockups-02-25.md` |
| 1 | SVG-макеты 03 и 23 | агент | `panel/design/screen-03.svg`, `screen-23.svg`, `NIGHT_2026-09-18_mockups-03-23.md` |
| 1 | SVG-макет 08 и широкая раскладка | агент | `panel/design/screen-08.svg`, `NIGHT_2026-09-18_mockup-08.md` |
| 1 | План сборки API без кода | агент | `NIGHT_2026-09-18_api-build-plan.md` |
| 1 | UX-панель: линзы «новичок», «доступность», «терминал depth» | три агента | сырьё в scratchpad, протокол ниже |
| 1 | Афиши с QR: механика ссылки, волна, риски, макет A3 | агент | `NIGHT_2026-09-18_marketing-posters.md`, `panel/design/poster-a3.svg` |
| 2 | Опровергающий по UX-панели, сводка ведущего | агент + ведущий | `PANEL_2026-09-18_ux-first-conversation.md` |
| 2 | Сверка макетов со словарём дизайн-системы | ведущий | здесь |

## Итоги волн

_(заполняется по мере прихода результатов; пустой раздел значит, что волна ещё идёт или не дошла)_

### Обход экранов

Агент прошёл 08, 18, 19, 10, 14, 15, 20, 21 (`NIGHT_2026-09-18_screens-walk.md`, 333 строки, 32 вопроса, числа состояний сверены с матрицей). Три главные находки, **ПРОВЕРЕНО мной по строкам**:
- Экран 19 «Стол» не имеет раскладки того, что видит севший: доска, реплики, поле, кнопки — `19-table_RU.md:9-17`; при 14 состояниях из 14 это самый большой пробел перед рисованием.
- «Кто высаживает» расходится: `19-table_RU.md:47` — большинство играющих (16.09.2026), `00-mechanics_RU.md:1150` — большинство сидящих, текст состояния `11-empty-and-edge-states_RU.md:160` — «Большинство сидящих попросило». Механика и текст отстали от решения 16.09.
- Экран 25 (`25-my-likes_RU.md:22`, написан мной вчера) говорит «отлучка снимает лайки вместе с фразами» про поставленные лайки, а механика `00-mechanics_RU.md:1166` и экран 20 — про лайки, набранные своими фразами. Мой дефект: поставленные лайки отлучка не трогает, строку на 25 надо переписать.
Остальные 29 вопросов — в протоколе, сводка из 10 в его конце.

### Макеты

**03 и 23** — `panel/design/screen-03.svg` (5 кадров), `screen-23.svg` (6 кадров), PNG в `testing/screenshots/design/`, протокол `NIGHT_2026-09-18_mockups-03-23.md` (12 вопросов, 7 нехваток дизайн-системы). Смотрел оба PNG сам: наложений и обрезанного текста нет, тёмная тема, акцент терракота. Главное:
- Агент, линза макетов: `--accent-text` витрины равен `--accent` (#bd4b2a), и на тёмном фоне это ниже 4.5:1 — **ПРОВЕРЕНО**: `sosed.place/landing/index.html:82` ставит `--accent-text:var(--accent)`, расчёт WCAG даёт 3.93:1 на #0d0b0a (фон самого лендинга не сверял — **НЕ ПРОВЕРЕНО**). Экран 22 обещает, что палитра ниже 4.5:1 останавливает выкат; в макете акцентный текст осветлён до #e0714f (6.21:1). Решить: узаконить светлый акцент для текста токеном.
- Стол в ленте нарисован без миниатюры доски; нужна ли она — вопрос владельцу (`19-table_RU.md:133` оставляет вид стола рисованию).
- В просмотре 23 текст прижат к верху, середина пустая, ряд «предыдущая/следующая» занимает 56 px над кнопками — центрировать текст, убрать ряд на сенсорном?
- Два текста в описаниях отсутствуют и стоят в макете как «[текст не задан]»: строка «полоса сдвинулась» после дня рождения (03) и что видит зритель у стола в просмотре (23).

**02 и 25** — `screen-02.svg` (7 кадров), `screen-25.svg` (4 кадра), протокол `NIGHT_2026-09-18_mockups-02-25.md` (12 вопросов, 9 компонентов без правила, 3 недостающих токена, 8 текстов «[текст не задан]»). Смотрел оба PNG: чисто. Моё замечание: на кадре 2 экрана 02 при ошибке «сюда с 13 лет» кнопка «дальше» нарисована активной, а по `02-name-screen_RU.md` невалидный возраст дальше не пускает — дефект макета. Главные вопросы агента: цитата про код и подтверждение заполняют кадр 4 до дна — один экран или два; пустая треть на шаге 1 между ссылками и «дальше»; карточка предложения на 25 отличается от фразы только приглушённым сердцем и словом — нужна ли своя рамка. Оговорка агента: вспомогательный текст нарисован 13–14 px, ниже правила «не меньше 16 px».

**08** — `screen-08.svg` (7 кадров 375 + широкий 1030), протокол `NIGHT_2026-09-18_mockup-08.md` (12 вопросов, 6 пробелов дизайн-системы). Смотрел PNG: кадры беседы чистые. В широком кадре колонка ленты нарушает канон дважды, **ПРОВЕРЕНО глазами**: чип фильтра возраста «21–45» числами (запрещено `03-feed-screen_RU.md`, границы не подписаны) и «остаток 3 ч 50 мин» на чужих фразах (срок чужих фраз наружу не отдаётся, `chat_RU.md` §8.11). Дефект макета, не решения. Главные вопросы агента: где живёт «закончить» — кнопкой в шапке или в меню «…»; различать ли «вы лайкнули» и «лайкнули вашу» в строке лайка; после «Закрыть» надгробия пункт уходит из списка (08:69) или остаётся до своего срока (07) — описания расходятся. Пробел: в `panel/design/fonts/` нет кириллического Golos Text с осью веса, полужирное синтезируется.

**19** — `screen-19.svg` (12 кадров), протокол `NIGHT_2026-09-18_mockup-19.md` (12 вопросов). Это ответ на главный пробел обхода: раскладки стола не было. Смотрел PNG: чисто. Предложено три раскладки: доска всегда сверху 208 px и реплики в 4 строки; доска свёрнута в полосу 44 px и раскрывается листом поверх реплик. Главные вопросы: какую из двух; у зрителя половина доски пуста, потому что рука приватна; видят ли отсчёт хода 5 минут все или только ходящий — всем он объясняет молчание, но это знание о том, что человек отошёл. Моё замечание: над доской имена сидящих с числом фишек на руках — канон §6.1 «что видно» этого не описывает, решать владельцу. Не нарисованы строки Н5/Н4 для 11 состояний из 14 — после выбора раскладки.

**06, 07, 10** — `screen-06-07-10.svg` (16 кадров), протокол `NIGHT_2026-09-18_mockups-06-07-10.md` (12 вопросов, 8 нехваток). Смотрел PNG: чисто, согласуется с 08 по пунктам бесед. Экран 10 нарисован с предложенным порядком в семи группах (живое → о себе → лента и беседы → приложение → помощь → личность и безопасность карточкой с зазором → документы). Главные вопросы: состав пункта «Предложения» на вкладке 07 нигде не описан (имя и возраст уже здесь? один остаток или оба?); согласен ли порядок групп и имена двух безымянных; предупреждение на карточке мэтча в 10 строк одним абзацем уводит кнопки под сгиб на 375 px.

**12, 13** — `screen-12-13.svg` (17 кадров), протокол `NIGHT_2026-09-18_mockups-12-13.md` (12 вопросов). Смотрел PNG: чисто, тексты замка и счётчика попыток дословно по §8.2. Главные вопросы: цена «начать заново» числами прямо в списке или своим экраном подтверждения, как у «отойти»; у окна «это я» второе действие описанием не названо — нужна ли кнопка отказа и гасит ли она приглашение; состав Б — одна безымянная кнопка на два поля или у каждого своя. Не нарисованы: скелет между ПИНом и вердиктом, «нет связи» на замке, перевыпуск кода с предупреждением экрана 2.

**20, 21, 22, 24** — `screen-20-21-22-24.svg` (12 кадров), протокол `NIGHT_2026-09-18_mockups-20-24.md` (12 вопросов). Смотрел PNG: чисто; в консоли три рода строк различены знаками «!» и «→», а не только цветом. Главные вопросы: снимает ли отлучка поставленные мной лайки — тогда в цене «отойти» нужна пятая строка (тот же мой дефект на 25:22, что нашёл обход); подсказка первого свайпа на 23 нарисована с затемнением, на 24 без — какой верный; «откатывается одним касанием» на 22 — где контрол отката, описание его не называет. Текст строки «во время ухода» не задан нигде.

### План сборки API

`NIGHT_2026-09-18_api-build-plan.md`, 543 строки. Каркас узла: Deno без фреймворка, два роутера, Postgres через пул, миграции файлами под advisory-lock — 18 файлов, все контур и DSA, таблиц продукта мигрировано 0 из 29 в `schema.tsv`; из протокола §2–§6 в коде нет ничего: ни подписи ECDSA, ни сокета, ни LISTEN/NOTIFY (**ПРОВЕРЕНО** grep по `relay/node/src`). План — 8 шагов с миграциями 022–028, маршрутами, тестами и часами; оценка агента 380–520 ч одного разработчика с агентом, без движков игр и модели модерации 305–400 ч (**НЕ ПРОВЕРЕНО**, прикидка). Шестнадцать недоговорённостей канона и контракта; три главные, **ПРОВЕРЕНО** мной:
- формат подписи и ключа не назван: raw или DER, как хранится публичный ключ сессии, что подписывать при пустом теле (`openapi.yaml:75-80` говорит только «ECDSA P-256 над методом, путём, sha256 тела и временем»);
- 17 операций спеки без `requestBody` (часть законно: лайк, место, билет; но `POST /identities`, `POST /feed`, `PATCH /chats/{id}`, `/matches/{id}/consent` тела требуют) и 7 без схемы ответа — ворота зелёные, а строить не по чему;
- время: протокол §1 «unix-секунды везде», а `format: date-time` в 10 местах контракта.
Дальше по цене: нет DDL под приглашения переноса, билеты сокета и незавершённую регистрацию; алгоритм хэша `auth` не назван; `tables.game` в DDL против `class` в контракте; SQL-функций `grid_round_*`, `haversine`, `band_*` нет как DDL.

### UX-панель

`PANEL_2026-09-18_ux-first-conversation.md`: три линзы, 71 находка, опровергающий: стоят 59, не так 10, опровергнуты 2; сырьё целиком в протоколе. Мной **ПРОВЕРЕНО** три: `00-mechanics_RU.md:1071` и `design-system-app_RU.md:145` несут отменённое 17.09 «лайк снимается повторным касанием»; `accessibility-and-i18n_RU.md:69` не называет слайдер возраста среди того, что дублируется касанием. Главное:
- терминал `depth-client_RU/EN.md` не знает ни одного решения 14–17.09 (нет экрана 25 и `GET /likes`, лайкнутое остаётся в ленте, нет лайка и имени стола, стрелки не назначены, один таймер мэтча, числа на упорах полосы, замок только при запуске) — при том что §13 канона ставит терминал первым лицом; задача 1, ~1 ч;
- 22 дефекта доступности: слайдер возраста и точка зоны без замены перетаскиванию, нет ролей и имён для диктора у точки «новое», сегментов, вкладок, «не сейчас» необратимо без подтверждения;
- решений владельца четыре: точка навигации на новое предложение; «не сейчас» с подтверждением или без; колонка «роль и имя для диктора» в словаре; Space в просмотре.
Нарезка на 8 задач в протоколе; задачи 1–4 и 7 закрываются пачкой без решений, ~2 ч.

`PANEL_2026-09-18_ux-tables-and-games.md`: линза «Игрок», 21 находка, опровергающий: стоят 13, не так 8, опровергнуто 0. Два тупика, оба **ПРОВЕРЕНО** мной по строкам: свой пустой стол — экран 09 обещает «сядьте, и его увидят», а `leave_table` ставит `closed_at` при последнем вставшем и уборщик удаляет; в двухместной игре ушёл единственный соперник — правила «играющих меньше минимума → конец» нет нигде. Плюс «первый круг за свежим столом не назван» и композер не спрашивает число мест, которое `POST /tables` требует. Попутно: протокол ссылается на индекс `table_seats_seat_taken`, которого в каноне нет. Семь задач, четыре из них — решения владельца.

### Афиши

`NIGHT_2026-09-18_marketing-posters.md` (5 разделов, 8 вопросов) и `panel/design/poster-a3.svg` — два A3, sosed.place RU «ХОЙ / Хорошие соседи. Живые моменты.» терракота и neighbro.place EN «HEY / Good neighbors. Real moments.» золото, QR-заглушка с подписью «ведёт только на sosed.place», домен словами и «набрать в браузере словами». Смотрел PNG: чисто, читается с расстояния. Механика ссылки, предложение агента: `https://sosed.place/?p=<клетка>&w=<волна>&l=<язык>` — клетка сетки ступени 1 км (та же, что узел отдаёт в выдаче), одно слово на волну из списка панели, язык; без `say=`, без радиуса, без номера афиши и utm. Ложится на экран 02 строкой «Точка с афиши: <район>, примерно» с «принять» / «поправить на схеме»; зарегистрированному — строка над лентой «афиша из X — переставить точку?». Три риска: клетка 300 м равна подъезду, предложено 1 км для точки с афиши; QR-фишинг и переклейка — домен словами внутри квадрата; расклейка на остановках — собственность операторов наружки, штрафы **НЕ ПРОВЕРЕНО** по городам; на листе нет оператора «by PSYTICAN» — добавить.

**17, кабинет заведения** — `screen-17-venue.svg` (10 кадров), протокол `NIGHT_2026-09-18_mockups-17-venue.md` (12 вопросов). Закрывает открытый пункт экрана 17 «экран создания оффера для заведения не нарисован». Смотрел PNG: чисто; кабинет — отдельный веб-вход по почте без нижней навигации, конверт с кодом, форма, предпросмотр карточкой ленты, живой оффер с числом переходов и жалобами «скидку не дали» текстом и датой; в приложении — кнопка после «Сохранить», список сохранённого с «уже не в ленте» и «срок вышел», экран перехода с доменом крупно, «ссылка отключена». Главные вопросы: слово на кнопке после «Сохранить» и снимается ли сохранение повторно; имя кнопки жалобы («Ссылка ведёт не туда» на экране против «Пожаловаться на ссылку» в спеке §6.3); нужен ли предпросмотр отдельным шагом перед необратимой публикацией.

### Нехватки дизайн-системы, сведённые по одиннадцати листам

Повторяются в нескольких протоколах макетов, поэтому это не вкус одного агента, а пробел системы:
1. **Значения токенов.** `design-system-app_RU.md` называет роли, но не значения; все листы брали цвета из `sosed.place/landing/index.html`. Таблица значений по темам и акцентам нужна в самой системе.
2. **Кириллического Golos Text 600 нет** в `panel/design/fonts/` — полужирное имя синтезируется браузером (08, 19, 14/15/18).
3. **Вспомогательный текст меньше 16 px** — счётчики, метки «стол»/«оффер», подписи под кнопками: правило «не меньше 16 px» о них молчит, все листы рисовали 13–14 px (02/25, 08, 03/23).
4. **Компоненты со статусом «нет — макет» в словаре**, вид которых листы задали впервые и ждут согласования: вторичная кнопка, кнопка необратимого действия, строка с отменой «· вернуть», полоса состояния Н5 с четырьмя исходами сокета, экран-замок, окно подтверждения, лист снизу, список обращений, свой пузырь против чужого, закрытое поле ввода пунктиром.
5. **Иконки:** фильтр-воронка, четыре знака навигации, класс доски — набора нет.
6. **Цвета доски** — клетки заведены макетом (#2a2018/#5a4634), в системе нет.
7. **Цвет счётчика тишины** — `--err` единственный тревожный токен, а угасание не ошибка.
8. **Акцентный текст** — `--accent-text` равен акценту и на тёмном фоне 3.93:1 (см. макеты 03/23).
9. **Тексты «[текст не задан]»** — сорок с лишним мест по десяти листам, по каждому есть список в протоколе; крупнейшие группы: экран 02 (8), 14/15/18 (12), 12/13 (5).
10. **Английские кадры и светлая тема** не рисовались нигде, кроме 22; бриф §3 требует обе.

### Контраст палитр

`NIGHT_2026-09-18_contrast-matrix.md`, скрипт в scratchpad. Палитр не 36 и 30, а 22: ступеней контраста в лендингах нет, они только в документах, «как в системе» — не третья тема. Пар 396, провалов 10 уникальных. Мной **ПРОВЕРЕНО**: `--ok #9ecb7a` задан один раз (`sosed.place/landing/index.html:71`) и в светлой теме не переопределён — на `#ece4d8` даёт 1.48:1; то же у neighbro.place и у `--err` neighbro в светлой теме; `--border` на тёмном 1.49:1 против 3:1 для рамок компонентов. Все 88 акцентных пар проходят. Существующий `landing/check-contrast.mjs` не считает `--ok`, `--err`, `--border` и печатает «every pair passes»; из выката он не вызывается (в `deploy-landing.sh` только комментарий, **ПРОВЕРЕНО** grep) — утверждение `accessibility-and-i18n_RU.md:141` «уже гасит выкат» не подтверждается. Предложены значения: `--ok` light `#4b712c`, `--err` light neighbro `#a3311f`, `--border` dark `#725a3f`; и место проверки — `scripts/check-storefront-contrast.sh` в `check-all.sh`. Это правки витрин и скрипт, вне ночных ограничений — на утро.

### Свод вопросов и черновик терминала

`NIGHT_2026-09-18_questions.md` — все вопросы ночи одной таблицей: 157 строк, из них владельцу 67, правок 30, рисованию 60; схлопнут 51 дубль; цена названных правок 5 ч 35 мин плюс 2 ч 30 мин после ответов. `NIGHT_2026-09-18_depth-client-draft.md` — готовые абзацы для 14 правок `depth-client_RU/EN.md` по решениям 14–17.09 и таблица клавиш (`→`/`←` только в просмотре, `Space`/`↓`/`j` и `↑`/`k` листают, `L` — экран `liked`, `l` на столе — закладка, `n` — «не сейчас»); три вопроса владельцу: память терминала о показанном (файл на volume или показывать заново), замок по простою в tmux, имена новых экранов.

## Что решать владельцу утром

Десять первых из `NIGHT_2026-09-18_questions.md`, по тому, что блокирует больше всего; варианты и цены там же:
1. **Пустой стол автора** — живёт час или закрывается сразу (экран 09 обещает одно, канон делает другое).
2. **Первый круг за свежим столом** — чем открывается; создаётся ли партия при постановке.
3. **Раскладка доски 18/19 на 375 px** — свёрнутая полоса с листом или доска сверху; от этого зависит лист состояний стола.
4. **Отлучка и мои лайки** — снимает или нет; сейчас 25:22 и механика расходятся.
5. **Точка «Беседы» от нового предложения** — зажигать или нет.
6. **«Не сейчас» без подтверждения** — оставить, дать «вернуть» до ухода с экрана или подтверждение.
7. **Срок хода вдвоём** — есть ли, или пять минут только за столом.
8. **Ступень точки с афиши** — 1 км или 300 м.
9. **Порог высадки при одном-двух играющих.**
10. **Ушёл единственный соперник в двухместной игре** — конец сразу или после трёх пасов.

Сверх десяти, но раньше выстрелит: контраст `--ok`/`--err`/`--border` в витринах (правка токенов и скрипт проверки, вне ночных ограничений), `--accent-text` для текста, и **мой вчерашний пропуск**: строка `PATCH /identities/me` в `protocol_RU.md:265` и `protocol_EN.md:274` всё ещё говорит «кратны 5, охват не меньше 5» — первая утренняя правка, 2 минуты.

### Согласованность листов между собой

`NIGHT_2026-09-18_mockups-consistency.md`: скрипт разобрал 12 SVG, 122 кадра. Листы рисовали три «руки» (видно по `<style>`): A — 01, 03, 17, 20, 23; B — 02, 12, 25; C — 06, 08, 14, 19; внутри семейства совпадает почти всё, между — почти ничего. 22 расхождения с одним правилом на каждое, 30 готовых строк геометрии для словаря. Три самых заметных: поле экрана 16 против 24 (карточка 343 против 327); высота первичной кнопки 40/44/48/52/56 по листам — предложено 48; три оттенка акцентного текста, в 19 акцент заливки положен как текст 15 раз (3.93:1). Что совпало везде и стоит закрепить: карточка rx 13 с рамкой, иконка-кнопка 44×44, цвет ошибки, акцентная заливка с чернилами 4.68:1. Числа агента из атрибутов SVG, мной **НЕ ПРОВЕРЕНО** построчно.

### Длина строк: EN, DE, HY

`NIGHT_2026-09-18_i18n-stress.md` и `panel/design/screen-i18n-03-08.svg` (15 кадров: 03 и 08 в трёх языках, геометрия листов 03/08 без изменений). 291 строка замерена в контейнере рендера. Смотрел верх PNG сам: у немецкого фраза карточки уходит на третью строку и ложится на подпись, «Platz nehmen» шире кнопки; армянский рисуется системным fallback без полужирного. **ПРОВЕРЕНО** мной: в `panel/design/fonts/` три файла, диапазоны глифов в `screen-08.svg` только `U+0000-00FF` и `U+0400-04FF` — армянского и грузинского начертания в системе нет при 16 заявленных языках витрины. Проблемных элементов: EN 2, DE 7, HY 3 жёстких. Правила для дизайн-системы в протоколе: карточка ленты не фиксированной высоты, кнопки по содержимому с минимумом, пилюля срока в две строки или сокращение. Оговорка: DE и HY — черновые переводы агента, не строки продукта.

### Светлая тема

`NIGHT_2026-09-18_light-theme.md` и `panel/design/screen-light-03-08-23.svg` (9 кадров, настоящие токены светлой темы витрины). Смотрел ряд беседы сам: галочка доставки на `--ok` почти не различима, с предложенным `#4b712c` читается. Числа матрицы подтверждены до сотых на кадрах. Попутно два дефекта макетов: в тёмном листе 08 галочка нарисована `--muted`, а не `--ok` (вопрос: чем ей быть); тост «лайкнуто · вернуть» на листе 03 вшил чернила литералом и в светлой теме даёт чёрное по чёрному. Три правила: токен статуса переопределяется в каждой теме или не заводится; статус-текст только на `--bg`/`--panel`; инвертированные элементы берут чернила из токена. Живой сайт в светлой теме не открывался — **НЕ ПРОВЕРЕНО**.

### Черновик правок контракта

`NIGHT_2026-09-18_contract-draft.md` (~560 строк): по 16 местам плана — 9 закрываются правкой без развилки, 7 ждут решения. Три главные развилки: второй шаг регистрации — протокол `:99` говорит «два запроса», но ни один не назван ни в §4.1, ни в контракте (`POST /identities` одним телом или два новых маршрута); время — unix-секунды везде против `date-time` в 10 местах; подпись и ключи — raw `r‖s` и SPKI против DER/JWK. Замеры в контейнерах по правилу 15: `Intl.Segmenter` в Deno 2.1.4 считает семейный эмодзи одной графемой; функции `band_*` и `haversine` создаются в postgres:16 и дают 18–22 для двадцатилетнего. Находки сверх плана, **ПРОВЕРЕНО** мной: `table_likes` (17.09) не внесена в `docs/facts/schema.tsv`, и открытый пункт `product.tables.unmigrated` говорит «29 таблиц» при 26 `CREATE TABLE` в каноне — мой пропуск, ворота схемы без стенда его не ловят; якоря `protocol_RU.md` в плане сдвинуты все (таблица исправлений в §0.1 черновика); `dsa_statements.recipient_identity` уже есть в миграции 005, новая колонка не нужна.

## Числа ночи

Коммитов в `day54` за ночь 28, листов SVG 15 (12 экранных, стресс-лист, светлый лист и афиша), кадров по отчётам агентов 147 (сумма 7+4+5+6+8+12+16+17+12+15+10+11+15+9; регулярка по прямоугольникам 375×812 даёт больше из-за служебных клипов, поэтому взята сумма отчётов), протоколов 24. Агентов 27 в семи волнах. Каждый лист смотрел сам по PNG до коммита; чужие утверждения помечены ПРОВЕРЕНО/НЕ ПРОВЕРЕНО в местах, где я их проверял.

## Предлагаемый порядок дня 18.09

1. Форма из десяти решений (`NIGHT_2026-09-18_questions.md`, «первые десять»), 15 минут.
2. Пачка «правка» без решений: `table_likes` в реестр схемы, «кратны 5» в протоколе, отменённое «снимается повторным касанием» в механике и словаре, `depth-client` по черновику, слайдер и точка зоны в правиле доступности, ложная ссылка на индекс в протоколе — одна пачка с якорями, круг доводки, коммит; ~3 ч.
3. Правки по ответам формы: столы (пустой стол, первый круг, соперник ушёл, порог высадки), лайки и отлучка, «не сейчас», срок хода вдвоём; ~2.5 ч.
4. Токены витрин: `--ok`/`--err`/`--border`/`--accent-text` и скрипт `check-storefront-contrast.sh` в `check-all.sh` — код витрин, отдельное слово; ~1.5 ч.
5. Контракт по черновику после трёх развилок (второй шаг регистрации, время, подпись); ~2 ч.
6. Дизайн-система: значения токенов, геометрия компонентов из сверки листов, армянское начертание; потом второй круг листов одной «рукой».
7. Пуш day54 → dev трёх репозиториев отдельным словом; пост в блог по пушу — тема есть: ночь без владельца и что она поймала.

## Что не успелось

- Английские кадры всех экранов (бриф §3) — только стресс-лист EN/DE/HY для 03 и 08; светлая тема только на 22.
- Лист состояний стола (11 из 14) ждёт выбора раскладки (Q-3).
- Экраны 09 блоками сверху (отклонённая, ограниченная, имя стола), 11 как каталог, 16 стикеры — не рисовались.
- Правки документов по ночным находкам не делались намеренно: пачка «правка» из `NIGHT_2026-09-18_questions.md` (30 строк, ~5.5 ч), пропуск «кратны 5» в протоколе и `table_likes` в реестре схемы — на утро, по слову.
- Скрипт проверки контраста витрин и правка токенов `--ok`/`--err`/`--border` — вне ночных ограничений, на утро.
- Пуш: ничего не пушено; xor.ad впереди dev на 4 + 18 коммитов, витрины на 3.

### Согласованность листов между собой

`NIGHT_2026-09-18_mockups-consistency.md`: скрипт разобрал 12 SVG, 122 кадра. Листы рисовали три «руки» (видно по `<style>`): A — 01, 03, 17, 20, 23; B — 02, 12, 25; C — 06, 08, 14, 19; внутри семейства совпадает почти всё, между — почти ничего. 22 расхождения с одним правилом на каждое, 30 готовых строк геометрии для словаря. Три самых заметных: поле экрана 16 против 24 (карточка 343 против 327); высота первичной кнопки 40/44/48/52/56 по листам — предложено 48; три оттенка акцентного текста, в 19 акцент заливки положен как текст 15 раз (3.93:1). Что совпало везде и стоит закрепить: карточка rx 13 с рамкой, иконка-кнопка 44×44, цвет ошибки, акцентная заливка с чернилами 4.68:1. Числа агента из атрибутов SVG, мной **НЕ ПРОВЕРЕНО** построчно.

### Длина строк: EN, DE, HY

`NIGHT_2026-09-18_i18n-stress.md` и `panel/design/screen-i18n-03-08.svg` (15 кадров: 03 и 08 в трёх языках, геометрия листов 03/08 без изменений). 291 строка замерена в контейнере рендера. Смотрел верх PNG сам: у немецкого фраза карточки уходит на третью строку и ложится на подпись, «Platz nehmen» шире кнопки; армянский рисуется системным fallback без полужирного. **ПРОВЕРЕНО** мной: в `panel/design/fonts/` три файла, диапазоны глифов в `screen-08.svg` только `U+0000-00FF` и `U+0400-04FF` — армянского и грузинского начертания в системе нет при 16 заявленных языках витрины. Проблемных элементов: EN 2, DE 7, HY 3 жёстких. Правила для дизайн-системы в протоколе: карточка ленты не фиксированной высоты, кнопки по содержимому с минимумом, пилюля срока в две строки или сокращение. Оговорка: DE и HY — черновые переводы агента, не строки продукта.

### Светлая тема

`NIGHT_2026-09-18_light-theme.md` и `panel/design/screen-light-03-08-23.svg` (9 кадров, настоящие токены светлой темы витрины). Смотрел ряд беседы сам: галочка доставки на `--ok` почти не различима, с предложенным `#4b712c` читается. Числа матрицы подтверждены до сотых на кадрах. Попутно два дефекта макетов: в тёмном листе 08 галочка нарисована `--muted`, а не `--ok` (вопрос: чем ей быть); тост «лайкнуто · вернуть» на листе 03 вшил чернила литералом и в светлой теме даёт чёрное по чёрному. Три правила: токен статуса переопределяется в каждой теме или не заводится; статус-текст только на `--bg`/`--panel`; инвертированные элементы берут чернила из токена. Живой сайт в светлой теме не открывался — **НЕ ПРОВЕРЕНО**.

### Черновик правок контракта

`NIGHT_2026-09-18_contract-draft.md` (~560 строк): по 16 местам плана — 9 закрываются правкой без развилки, 7 ждут решения. Три главные развилки: второй шаг регистрации — протокол `:99` говорит «два запроса», но ни один не назван ни в §4.1, ни в контракте (`POST /identities` одним телом или два новых маршрута); время — unix-секунды везде против `date-time` в 10 местах; подпись и ключи — raw `r‖s` и SPKI против DER/JWK. Замеры в контейнерах по правилу 15: `Intl.Segmenter` в Deno 2.1.4 считает семейный эмодзи одной графемой; функции `band_*` и `haversine` создаются в postgres:16 и дают 18–22 для двадцатилетнего. Находки сверх плана, **ПРОВЕРЕНО** мной: `table_likes` (17.09) не внесена в `docs/facts/schema.tsv`, и открытый пункт `product.tables.unmigrated` говорит «29 таблиц» при 26 `CREATE TABLE` в каноне — мой пропуск, ворота схемы без стенда его не ловят; якоря `protocol_RU.md` в плане сдвинуты все (таблица исправлений в §0.1 черновика); `dsa_statements.recipient_identity` уже есть в миграции 005, новая колонка не нужна.

## Числа ночи

Коммитов в `day54` за ночь 28, листов SVG 15 (12 экранных, стресс-лист, светлый лист и афиша), кадров по отчётам агентов 147 (сумма 7+4+5+6+8+12+16+17+12+15+10+11+15+9; регулярка по прямоугольникам 375×812 даёт больше из-за служебных клипов, поэтому взята сумма отчётов), протоколов 24. Агентов 27 в семи волнах. Каждый лист смотрел сам по PNG до коммита; чужие утверждения помечены ПРОВЕРЕНО/НЕ ПРОВЕРЕНО в местах, где я их проверял.

## Предлагаемый порядок дня 18.09

1. Форма из десяти решений (`NIGHT_2026-09-18_questions.md`, «первые десять»), 15 минут.
2. Пачка «правка» без решений: `table_likes` в реестр схемы, «кратны 5» в протоколе, отменённое «снимается повторным касанием» в механике и словаре, `depth-client` по черновику, слайдер и точка зоны в правиле доступности, ложная ссылка на индекс в протоколе — одна пачка с якорями, круг доводки, коммит; ~3 ч.
3. Правки по ответам формы: столы (пустой стол, первый круг, соперник ушёл, порог высадки), лайки и отлучка, «не сейчас», срок хода вдвоём; ~2.5 ч.
4. Токены витрин: `--ok`/`--err`/`--border`/`--accent-text` и скрипт `check-storefront-contrast.sh` в `check-all.sh` — код витрин, отдельное слово; ~1.5 ч.
5. Контракт по черновику после трёх развилок (второй шаг регистрации, время, подпись); ~2 ч.
6. Дизайн-система: значения токенов, геометрия компонентов из сверки листов, армянское начертание; потом второй круг листов одной «рукой».
7. Пуш day54 → dev трёх репозиториев отдельным словом; пост в блог по пушу — тема есть: ночь без владельца и что она поймала.

## Что не успелось

- Английские кадры всех экранов (бриф §3) — только стресс-лист EN/DE/HY для 03 и 08; светлая тема только на 22.
- Лист состояний стола (11 из 14) ждёт выбора раскладки (Q-3).
- Экраны 09 блоками сверху (отклонённая, ограниченная, имя стола), 11 как каталог, 16 стикеры — не рисовались.
- Правки документов по ночным находкам не делались намеренно: пачка «правка» из `NIGHT_2026-09-18_questions.md` (30 строк, ~5.5 ч), пропуск «кратны 5» в протоколе и `table_likes` в реестре схемы — на утро, по слову.
- Скрипт проверки контраста витрин и правка токенов `--ok`/`--err`/`--border` — вне ночных ограничений, на утро.
- Пуш: ничего не пушено; xor.ad впереди dev на 4 + 18 коммитов, витрины на 3.
