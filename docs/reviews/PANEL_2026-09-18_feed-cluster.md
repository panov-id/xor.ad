# Панель 18.09.2026 · кластер «Лента и просмотр» · шаг 5 плана

**Дата:** 18.09.2026, 11:25–11:45, под стоячим словом владельца (11:07: «действуй сам до 13:00,
вопросы — кворумом линз»).
**Что ревьюили:** листы `panel/design/screen-03.svg`, `screen-23.svg`, `screen-25.svg`,
`screen-20-21-22-24.svg` (кадры 24), рендеры в `testing/screenshots/design/`, после
коммита 5a17bb7 (шаги 0–4 плана); описания экранов репозитория `sosed.place`
(`docs/03-feed-screen_RU.md`, `23-phrase-viewer_RU.md`, `24-hints_RU.md`, `25-my-likes_RU.md`,
`09-my-messages_RU.md`); отчёт ночи `NIGHT_2026-09-18_i18n-stress.md`.
**Состав линз (4):** визуал · UX · доступность · типографика и локализация. Безопасность не
собиралась — предмет тот же, что у панели утра (SVG и документы).
**Кворум по текстам:** пять развилок, три голосующих (UX — автор развилок, визуал, доступность).

---

## 1. Сырые находки линз (как вернули агенты)

### 1.1 Линза «Доступность»

Линза «Доступность» · кластер «Лента и просмотр» · листы 03, 23, 25, 20-21-22-24. Источники: PNG + grep по SVG (`panel/design/*.svg`), правило `docs/accessibility-and-i18n_RU.md` (п.3 = 44 px, строка 68; п.69 — у свайпа 23 есть кнопки 2.5.1).

**1. Цели касания < 44 (WCAG 2.5.8 / правило 3 — вижу дефект, числа из SVG)**

- **03**, кадр 1 (лента), каждая карточка: сердце — path 32×21 (x 238–270, y 191–212), меню «…» — три circle r=2, 14×4 (x 314–328). Хит-зоны (rect/`<a>`) в SVG нет — «не нашёл, где описано». Центры 254 и 321 → 67 px, промах в допуске, если хит-зона 44. Что сделать: записать в спеку карточки хит-зону 44×44 у обоих; на листе — нарисовать невидимый rect 44.
- **03**, кадр 2 (фильтры): поле «[текст не задан] — полоса сдвинулась» rect 343×40 (y 196), поле «район или город» 343×40 (y 294), сегмент «компания» 108×38 (y 627). Поля — до 44; сегмент-контрол — до 44.
- **23**, все кадры: стрелки «предыдущая/следующая» — path 6×12 (x 33–39 / 336–342, y 700–712), подпись рядом, хит-зона не описана. Сердце-счётчик в теле (x 21–47, y 394–411 ≈ 26×17) — если это кнопка, мала; если только счётчик — нужен текст «3 лайка», не число. Промокод rect 210×40 (y 304) — если копируется по касанию, 40 < 44.
- **25**, кадры 1/4: «повторить» 96×34 (x 255, y 62) — самая маленькая кнопка кластера; «[к предложению]» 295×40 (y 344, 440, 720, 816); «сесть» 120×40 (y 508, 604); сердца path scale 0.83 ≈ 20 px (x 303) — хит-зоны нет.
- **20-21-22-24**: «×» на полосе «вы здесь час, отойти?» (кадр 20-D) — path без rect, хит-зона не описана. Кнопки 20/22/24 ≥ 44 — ок.

**2. Состояние только цветом (1.4.1 / 4.1.2)**

- **25**, кадр 1, 2-я карточка: сердце с `opacity="0.45"` (y 265) — состояние (снимается? истекает?) передано только прозрачностью, слова нет. Дефект: подписать («снятие через…» / «истекает»).
- **03**, кадр 5 «лайкнуто · вернуть»: тост есть, а сердце на карточке остаётся контурным muted — после ухода тоста лайк на карточке не виден вообще (ни цвет, ни форма). Вижу дефект: заливка + `aria-pressed=true` + имя «лайк, поставлен».
- **23**, кадр 4 (стол): кнопка-сердце залита accent без слова, тогда как на кадрах 1/6 — «♡ лайк». Имя кнопки не описано (правило 5, 2.5.3): дать имя «лайк».
- Сегмент «компания» инверсией fg/bg — **не дефект**: разница 13:1 и форма (плашка) — 1.4.1 держит; для диктора нужен `role=radiogroup` + `aria-checked` — в листе не описано.
- **21** консоль: ошибки — красный текст + глиф «!», сделанное за человека — «→». Ок.

**3. muted #9a8d7c на panel-2 #302720 (4.51:1 — проходит, но на пределе)**

- **23**, кадр 3: «промокод» class t14 (x 36, y 330) на rect 24,304 panel-2 — единственное место в четырёх листах. Остальные muted-тексты стоят на panel #262019 или bg. «[текст не задан] — полоса сдвинулась» (03, кадр 2) — уже fg. Что сделать: ничего обязательного; если поле промокода станет интерактивным — перевести подпись в fg, чтобы не сидеть на 4.51.
- **24**, кадр 24-B: «До «понятно» ни жест, ни клавиша…» muted 14 px на карточке подсказки — фон panel, ок.

**4. Порядок чтения и диктор (1.3.2 / 4.1.2 — «не нашёл, где описано»)**

- Карточка 03/25: визуально текст → «компания» → ♡ 3 → «…». Диктор должен прочитать: «фраза, компания, 3 лайка, кнопка лайк, кнопка ещё». Число «3» без единицы и без связи с сердцем — читается голым числом. Роль «один/компания/туса» стоит после текста — порядок допустим, но должен быть в разметке тем же. Срок: полоса aria-hidden — для своей фразы в 03 срок словом не показан нигде (в 23 кадр 2 — «исчезнет через 2:41», в 03 кадр 1 «проверяется…» это статус, не срок). Дефект: карточка своей фразы в ленте должна нести срок текстом (хотя бы `aria-label` полосы).
- Оффер 03/23: «−20 %» стоит справа от «оффер» — в DOM должен идти после названия, не первым.
- 23, кадр 1: «компания» в шапке справа — читается до текста; нормально, если это подпись экрана.

**5. Клавиатура и свайп на 23 (2.1.1 / 2.5.1)**

- Кнопки «предыдущая/следующая», «скрыть», «лайк», «сохранить», «сесть», «закрыть» — путь есть, 2.5.1 закрыт (правило, строка 69). Не описано: Escape = «закрыть», ←/→ = листание, порядок фокуса (текст → ссылка «перейти на сайт» → навигация → кнопки).
- Кадр 5 (подсказка 24 поверх 23): «ни жест, ни клавиша не срабатывают до понятно» — модальность заявлена, фокус на «понятно» и его возврат после закрытия не описаны (2.4.3). Для клавиатурного человека подсказка про свайп бесполезна — её текст должен упоминать «← → на клавиатуре» или не показываться вне coarse pointer.

**6. Подсказки 24**

- Карточки-подсказки в ленте (первый лайк, первый стол) — не модальные, кнопка «понятно» 44 — ок. Не описано: `role=note`/`aria-live`, чтобы диктор объявил появление под карточкой.
- Полоса «вы здесь час, отойти?» (20-D): закрытие «×» без имени (правило 5) и без размера; для 2.2.1 — полоса не по таймеру исчезает, ок.

Итого дефекты: 25 «повторить» 34, сердца ≈ 20 без хит-зон (03/23/25), поля/сегмент 40/38 на 03, opacity-состояние на 25, невидимый лайк после тоста на 03, безымянная сердце-кнопка 23-4, срок своей фразы без текста в 03. Остальное — «не нашёл, где описано» (хит-зоны, ARIA, фокус, клавиши).

### 1.2 Линза «UX»

Отчёт UX-линзы, кластер «Лента и просмотр». Источники: PNG в xor.ad/testing/screenshots/design (03, 23, 25, 20-21-22-24) и описания в репозитории sosed.place (`docs/*_RU.md`, ветка day55). Всё ниже — по чтению листов и описаний; в живом приложении не проверено.

## 1. Заглушки «[текст не задан…]»

### Лист 03, кадр 2 (фильтры) — «[текст не задан] — полоса сдвинулась»
Не решён. `sosed.place/docs/03-feed-screen_RU.md:27` требует «одной строкой при следующем заходе», текста нет; в таблице `11-empty-and-edge-states_RU.md` строки нет тоже. Уровень: дефект дизайна (полоса стоит на листе с пустым текстом).
Развилка:
- А (мой голос): «Вам стало больше лет — фильтр возраста сузился до вашей полосы.» Причина названа, чисел нет (правило 03:25 — границы числами не подписаны).
- Б: «Фильтр возраста обновлён.» Короче, но молчит о причине — противоречит духу 03:27 («молча менять ленту нельзя»).
Точечно: строку внести в таблицу экрана 11 (состояние «Полоса возраста сдвинулась», действие «понятно»), на листе заменить.

### Лист 23, кадр 4 (стол) — «[текст не задан] — что видит зритель до посадки»
Не решён. `19-table_RU.md:136` — «как стол выглядит в ленте… не определено», решается рисованием; описание 23:21 перечисляет только имя, игру, «играют N, смотрят M», лайки, кнопки. Что кроме этого показывать до посадки, нигде не сказано; 19:54 говорит о зрителе уже сидящем.
Уровень: противоречие (лист рисует ряд полей виселицы «к _ _ о _», т.е. доску, а 19:130/138 запрещают выдавать содержимое стола тем, кто не сидит; кроме того, рисовать доску до посадки — это «серая карточка» наоборот).
Развилка:
- А (мой голос): до посадки доски нет — строка «сядьте, чтобы увидеть доску» под «играют 2, смотрят 3»; ряд букв убрать. Согласуется с 19:54 (доску видит зритель, т.е. сидящий).
- Б: оставить миниатюру доски без букв (пустые черты) как знак игры; требует правки 19-table (доска видна снаружи), и это утечка хода партии.

### Лист 20-21-22-24 — «[текст не задан] / до 15:02 / осталось 41 мин»
Заглушка стоит на кадре экрана 20 (третий кадр первого ряда, «во время ухода»), а не 24: на четырёх кадрах 24 заглушек нет. Тексты 24 сверены с `24-hints_RU.md:19,23,24` — совпадают по смыслу, дословных строк описание не задаёт (строка 42: вид решается рисованием).
Текст экрана 20 не решён: `20-step-away_RU.md:35` — «одна строка и время до конца», без слов; в таблице экрана 11 состояния нет (там только «без связи» :171 и «у нас не получилось» :186).
Развилка:
- А (мой голос): «Вы отошли. Фразы сняты, беседы ждут.» — называет то, что человек сам выбрал на предыдущем кадре, без устройства системы.
- Б: «Перерыв.» — одно слово; экономно, но «до 15:02» под ним читается как срок чего угодно.

### Лист 25 (14 заглушек, кадры 1 и 4 повторяют одно и то же)
- «[текст не задан: фраза соседа] / [вторая строка]» ×4 — это не текст продукта, а образец контента. Мелочь. Решение: взять фразы с листа 03 («Кто-нибудь идёт на набережную вечером? Возьму термос.» / «Ищу, с кем поиграть в го.»), как на 23 и 24 — тогда сценарий лента→лайки видно одной фразой.
- «[текст не задан: к предложению]» ×4 — не решён. `25-my-likes_RU.md:11` — «кнопка ведёт на экран 6», подписи нет; :36 — вид карточки предложения решается рисованием. На экране 7 вкладка зовётся «Предложения» (07:9).
  Развилка: А (мой голос) — «к предложению» (в тон «к ленте» 25:30, «в ленту» 11:179); Б — «поговорить?» — глагол, но это слово занято кнопкой согласия на экране 6 (06:11) и обещало бы лишнее.
- «[текст не задан: имя стола]» и «[название игры]» ×2 — образец контента. Мелочь: «Пятничный» · «виселица», как на 03/23; описание 25:9 форму задаёт.
- «[текст не задан: встанете из-за нынешнего]» ×2 — не решён: `25-my-likes_RU.md:13` — «кнопка предупреждает, что поднимет из-за нынешнего», `04-post-composer_RU.md:35` — «экран говорит об этом до нажатия», слов нет ни там, ни в 19.
  Развилка: А (мой голос) — «сядете сюда — встанете из-за нынешнего стола»; Б — «поднимет вас из-за нынешнего стола» (форма 09:23), безличнее.
  Дефект дизайна: подпись стоит под кнопкой мелкой строкой, а условие должно читаться до нажатия — поставить над кнопкой или показывать только когда человек за столом (у 09:22-23 это условие).
- «[текст не задан: текст оффера]», «скидка [не задана] · до ДД.ММ ЧЧ:ММ» ×2 — образец контента. Мелочь: «Отдаю два табурета, забирать сегодня.» / «−10 % · до 20 сентября, 12:00» с листа 03, кадр 5.

Итого решённых дословно в описаниях — 0 из 14+3; описания дают форму, а не слова.

## 2. Сценарий лента → просмотр → лайк → мои лайки → вернуть

- Лист 03, кадр 1 → 23, кадр 1: касание по тексту (03:20). Разрыв — мелочь: на 03 чужая фраза «Кто-нибудь идёт…» с 3 лайками без «скоро исчезнет», на 23 та же фраза уже с полосой срока и «скоро исчезнет». Полоса срока решена сегодня только у своих — на 23 кадр 1 (чужая) полосу надо снять, оставить слово + угасание (23:16). Уровень: противоречие с сегодняшним решением.
- 23, кадр 1 → лайк → «следующая» (23:25). На листе нет кадра «лайкнуто · вернуть» в просмотре — есть только «скрыто · вернуть» (кадр 6). 03:15 и 05:18 требуют строку и для лайка. Дефект дизайна: добавить кадр или пометить, что строка одна и та же.
- Лист 03, кадр 5 показывает «лайкнуто · вернуть» в ленте — но лента при этом всё ещё содержит карточку оффера, а лайкнутая карточка не показана уходящей. Мелочь.
- 25, кадр 1: «снято · вернуть» стоит в верху списка, «лайкнуто · вернуть» на 03 — внизу. Противоречие в положении одной и той же строки; 25:10 говорит «как „скрыто · вернуть“». Решение: одно место (низ, над навигацией) на всех трёх экранах.
- 25: карточка фразы с «скоро исчезнет» без полосы — верно по сегодняшнему решению. Но у карточки нет «…» (блок/жалоба) — на 03 и 23 есть; 25:9 форму зовёт «та же, что в ленте». Дефект дизайна: либо «…» вернуть, либо в 25 записать, что его нет.
- 25: закрашенное сердце → снять → карточка «возвращается в ленту» (25:10); на листе нет кадра, как выглядит карточка после снятия (уходит целиком или гаснет). Мелочь, один кадр.
- 25, кадр 1: нет нижней навигации, «<» ведёт в «Я» (25:5). Вернуться в ленту — два шага («Я» → «Лента»). 25:30 обещает «к ленте» только на широком экране. Дефект дизайна для сценария «вернуть»: сложить «показать ещё» и «В ленту» так же, как на пустом состоянии (кадр 2), или оставить таббар.
- 25: стол «Пятничный» с «сесть» — при нажатии человек уже за столом (03, кадр 1 «вы за столом») — предупреждение под кнопкой, см. выше.
- 23, кадр 5 (первый свайп): кнопки внизу затемнены, а 24-hints кадр 3 и подпись листа говорят «без затемнения, кнопки ждут „понятно“». Противоречие между двумя листами одного экрана. Решение: одна версия — по 24 (24:9 «подсказка не закрывает то, что объясняет»).

## 3. Лист 09 — минимум по `09-my-messages_RU.md`
Кадры: (1) список — блок сверху «проверяется…» (09:9), пункты трёх типов с пометкой «фраза / оффер / стол» (09:16), у каждого таймер числом (09:17) и — по сегодняшнему решению — полоса срока (только здесь и в беседах), кнопка «снять» у фразы/оффера, «вернуться»+«встать» у стола, где сидишь, «сесть» у своего стола без вас с предупреждением (09:22-23), число лайков (09:18-19), «сказать снова» у истекающей (09:25), строка квоты «свободно N из 4» (09:26); (2) блок отказа с причиной и текстом под рукой (09:10) и отдельно ограниченная фраза с мотивировкой (09:11); (3) пусто — «Своих фраз нет / Свободных мест: N из четырёх / Написать» (11:152); (4) скелет (09:31). Расположение — верх экрана «Я» (09:44), т.е. лист 10 сверху должен совпадать.

### 1.3 Линза «Визуал»

Линза «Визуал», кластер «Лента и просмотр». Смотрел PNG 03, 23, 25, 20-21-22-24 (нижний ряд — 24) и разделы дизайн-системы (Типографика/Поверхности/Знаки/Движение). Всё ниже — по картинке, не по SVG-коду; размеры оценены по кадру 375 px (2.16× на листе 03).

## Счёт акцента (залитых --accent поверхностей / акцентных текстов; норма ≤1 / ≤3)

- 03-1 лента: 0 / 3 («вернуться», «оффер», «стол»+иконка) — на пределе; плюс точка на «Я» и подчёркивание таббара.
- 03-2 фильтры: 0 / 0, но три акцентных штриха (полоса возраста, обод кольца, подложка) — кольцо доминирует кадр.
- 03-3 пусто: 1 / 0 — единственный кадр, где акцент = действие. Лучший кадр кластера.
- 03-5 конец выдачи: 0 / 3 («оффер», «показать», «вернуть» на кремовом тосте).
- 23-1 чужая: 1 («лайк») / 0 + полоса срока. Норма.
- 23-3 оффер: 1 («сохранить») / 2 («оффер», «перейти на сайт →»). Норма.
- 23-4 стол: 1 (лайк-квадрат) / 1. Норма.
- 25-1 список: 1 («сесть») / 1 («показать ещё») — плюс 4 залитых сердца (2 полных, 2 приглушённых). Формально проходит, визуально 6 пятен терракоты в столбик — самый «красный» кадр кластера.
- 24 «первый свайп» (нижний ряд, кадр 3): 2 залитых — «понятно» и «лайк» → нарушение ≤1. 24 «первый лайк»: 1 залитая + залитое сердце + «лайкнуто · вернуть» акцентом.

## Находки

**Критично**
1. 24, кадр «первый свайп»: две залитые кнопки на кадре («понятно» 44 и «лайк» 44). Сделать: пока подсказка открыта, нижний ряд кнопок — в состоянии disabled: «лайк» на --panel-2 c текстом --muted (как «предыдущая/следующая» уже приглушены). Тот же приём на 23-5.
2. 23, все кадры: фраза 26/34 с полями 24 занимает верхние 30 % кадра, ниже ~430 px пустоты до «предыдущая». «Во весь экран» читается как карточка, у которой сняли рамку. Сделать: две ступени по длине — до 90 знаков 32/40, длиннее 26/34; блок «фраза + подпись + лайк» центрировать по вертикали между шапкой (56) и строкой навигации; поля 24 оставить.

**Дефект дизайна**
3. 03-1, метки «оффер» 16 и «стол» 16 акцентом: тот же кегль, что имя «Пекарня «Колос»» 16 и фраза 16 — метка отличается от тела только цветом и съедает 2 из 3 акцентных текстов кадра, при том что «компания»/«один» на соседних карточках — 14 --muted внизу. Две системы меток на одном экране. Сделать: все метки категории 14/600 --muted, letter-spacing 0.04 em, без uppercase (кириллический капс шире на ~15 %), вверху карточки; акцент оставить иконке стола 14 и числу «−20 %». Тогда 03-1 — 0 / 1, и акцент возвращается действию.
4. 03-1 ритм: карточки 112 / 88 / 180 / 136 при зазоре 12; тяжёлая карточка оффера (6 строк + кнопка) стоит между двумя лёгкими. Нижний ряд карточек неровный: у фразы ♡+«…», у оффера только кнопка+«…», у стола кнопка+♡+«…». Сделать: нижний ряд всегда одна сетка — слева кнопка-или-пусто, справа ♡ N и «…» на фиксированных x; у оффера условия сжать в одну строку 14 («от двух · до 30.09 18:00»).
5. 25-1 сердце: залитое 24 --accent со счётчиком под ним — в «Моих лайках» каждая карточка лайкнута по определению, сердце не несёт информации, а даёт 4 акцентных пятна. Сделать: сердце убрать, счётчик «♡ 3» 14 --muted в строку меты, как на 03; приглушённая (снятая) версия — только текст «снято · вернуть».
6. 23-3: «−20 %» ~26/600 белым — второй display на кадре, спорит с фразой. Сделать 20/600 --fg в строке метки.
7. 03-2 кольцо: подложка + обод + ручка + полоса слайдера — четыре акцентных элемента, кольцо 240 px перевешивает всё. Сделать: полосу возраста — --border-control с акцентными только ручками (2 × 20); подложку кольца оставить по решению кворума.

**Противоречие**
8. 25-1 «сесть» — залитая 44 с тенью; 03-1 «сесть» — контурная 44 без тени. Один компонент, два рендера. Взять контурную (03), тень снять (в системе тени на кнопках нет).
9. 25-1 «показать ещё» — акцентный текст 16 внизу; 03-5 — контурная кнопка 44 во всю ширину. Взять кнопку.
10. Отмена действия дана тремя способами: кремовый тост «лайкнуто · вернуть» (03-5, 23-6), тёмная плашка «снято · вернуть» без выделения действия (25-1), строка под карточкой «лайкнуто · вернуть» акцентом (24-1). Взять тост, плашку на 25 — в тост.
11. 25-2 пусто: без иконки 48 и без display-заголовка — против решения второго круга сегодня (иконка + заголовок display + одно действие). Сделать по 03-3: сердце-иконка 48 --muted (или «дом»), заголовок 26 «Пока ничего», текст 16, кнопка «В ленту» — одна залитая (сейчас контурная, а акцент на кадре 0).
12. 25-3 скелет: круг 32 в правом верхнем углу карточки обещает аватар — аватаров нет (система: --r-round в роли аватара не применяется). Убрать круг, оставить три полосы; скелет 03-4 без круга — правильный.
13. 24, кадры «первый лайк» и «первый свайп»: подсказка с левым ребром 3 px --accent — ровно тот приём, что сегодня отклонён для полосы срока («сливается с чертой цитаты и рамкой»). На кадре «первый стол» ребра, похоже, нет — ещё и внутри листа неровно. Сделать: подсказка = --panel-2 без ребра, «понятно» единственная заливка.

**Мелочь**
14. 25-1: «предложение  поговорить», «скоро  исчезнет» — двойные пробелы в мете.
15. 23-1: «♡ 3» стоит 60 px под полосой срока, «скоро исчезнет» 14 — 8 px над полосой; вертикаль блока не по 8-сетке (полоса, потом 24, потом лайк).
16. 03-1 «вы за столом · вернуться» плашка 40 на --panel-2 сразу под шапкой без воздуха — с шапкой без линии она стала «второй шапкой». Отступ 8 сверху либо втянуть в первую карточку.
17. 25 шапка «Мои лайки» 20/600 с шевроном 24 — ок; на 03 логотип-дом 24 + «sosed» 20 — ок; но чипы «ru ×»/«en ×» 44 при кнопке-фильтре 24 — чипы весят больше действия. Мелочь, не трогать без 04.

## Что держит листы на «структуре» (и мешает завлекать)

Всё в кадре одного кегля 16 и одного тона --panel: фраза соседа — герой продукта — ничем не крупнее условий оффера. Акцент потрачен на метки и сердца, а не на действие. Единственный кадр с воздухом и одним акцентом — пустое состояние 03-3 — и он же единственный завлекательный.

## Три главных вывода

1. Акцент вернуть действию: метки категорий → 14/600 --muted (п. 3), сердца на 25 убрать (п. 5), подсказки без левого ребра (п. 13), лайк приглушён под подсказкой (п. 1). После этого на каждом кадре кластера ≤1 заливка и ≤1 акцентный текст.
2. Лист 23 — дать масштаб: 32/40 для коротких фраз, центрирование блока, «−20 %» подчинить (п. 2, 6). Сейчас 60 % экрана пусто, а фраза меньше, чем ей позволено.
3. Лист 25 живёт по своим правилам: «сесть» залитая с тенью, «показать ещё» текстом, пустое без иконки, скелет с аватаром, undo тёмной плашкой (п. 8–12). Собрать из компонентов 03 — тогда кластер станет одним экраном в двух видах, а не двумя экранами.

Не проверено: точные кегли/px на SVG — оценка по PNG; наличие ребра на 24 «первый стол».

### 1.4 Линза «Типографика и локализация»

```diff
+ кегли: на 4 листах все в шкале, кроме двух inline 13px Golos на листе 25 и пары «−/+» 26px в кнопках фильтра
! интерлиньяж: фраза 26px на листе 23 набрана через 36, шкала — 34 (7 пар строк); мета-пары 14px на 03 через 22 вместо 20
@@ i18n: переход 15→16 / 17→20 задел только одну из 17 строк — «строку лайка» (EN запас 20→4, DE +29→+48); остальные 16 сидят на 14/16 и не изменились
```

**1. Кегли и интерлиньяж (ПРОВЕРЕНО скриптом по `<style>` и Δy текстов одного класса)**

Кегли: `screen-03.svg:9-19`, `screen-23.svg:9-19`, `screen-i18n-03-08.svg:7-12` — только 26/20/16/14 и mono 13. Вне шкалы:
- `screen-25.svg:26` и `:29` — `class="m" style="font-size:13px"` для «[текст не задан: встанете из-за нынешнего]» (Golos 13 — такой ступени нет, это mono-кегль). Должно быть `.capm`/14.
- `screen-25.svg:12` — `.m` 16px muted и `.mono` 13 используются для «[название игры] · играют 2, смотрят 4» и «один · скоро исчезнет»; на листе 03 те же строки — `t14fg`/`t14` 14px (`screen-03.svg:22`, y=638/208). Один смысл, две ступени и два шрифта.
- `screen-03.svg:23` кадр 2: «−» и «+» в кнопках 44×44 набраны `.t22b` 26px — глиф вместо иконки; шкала не запрещает, но это единственный «текст» 26 в контроле.

Интерлиньяж:
- `screen-03.svg` кадры 1, 3, 5: `t16` пары y=142/166, 378/402, 318/342, 50/74 — Δ24 ✓. `t14` пары «условия/скидка» y=460/482 и 370/392 — Δ22, ступень 14/20 требует Δ20 ✗ (то же на `screen-i18n-03-08.svg:26,51`, скопировано).
- `screen-23.svg` кадры 1, 2, 3, 5, 6: `.big` 26px y=140/176/212/248/284 — Δ36 везде, ступень 26/34 ✗. Семь пар. Либо править на 34 (текст на 5 строк сожмётся на 8 px), либо записать в шкалу, что display в просмотре идёт через 36 — на 26 px 34 даёт 1.31, 36 — 1.38; для строки в 20 символов на 375 px 36 читается свободнее, я бы записал исключение, а не ужимал.
- `screen-25.svg:29` — `.m` inline 14 y=112/132 Δ20 ✓.

**2. Плотность карточки ленты, лист 03 кадр 1 (`screen-03.svg:22`)**

Сейчас: карточки y=108/244/356/564/720, зазоры ровно 12; плашка «вы за столом» кончается на 96, до карточки 12. Внутренние поля 16 (x=32 при x-карточки 16; верх: базовая 142 при 16/24 даёт линию с 124.5 — 16.5). Меньше внешнего зазора — внутреннего, читается как один сплошной столбик.

Сдвиги (карточка k → +4·k, начиная с первой, потому что зазор от плашки тоже 12→16). Всё, что перечислено, — в одной строке `screen-03.svg:22`, tab-bar (y≥748 фиксированный блок, rect 762/768/774/804 и текст 796) не трогать:

| Карточка | Сдвиг | Элементы (y → новый y) |
|---|---|---|
| 1 фраза | +4 | rect 108→112; text 142→146, 166→170, 208→212 (×2: «компания», «3»); rect 180→184 (×2); circle cy 202→206 (×3); path сердца `M254 212…` — все ординаты +4 |
| 2 своя | +8 | rect 244→252; text 278→286, 320→328 (×2) |
| 3 оффер | +12 | rect 356→368; text 386→398 (×2), 408→420, 432→444, 460→472, **482→492** (Δ22→20); rect 496→**506**, text 524→**534**; rect 496→506 (⋯), circle cy 518→530 (×3); высота карточки 196→194 |
| 4 стол | +14 (после −2 выше) | rect 564→578; rect 582→596; circle 586→600 (×2), 592→606 (×2); text 594→608 (×2), 616→630, 638→652; rect 652→666 (×3), text 680→694 (×2), circle 674→688 (×3), path сердца +14 |
| 5 дальше | +18 | rect 720→738; text 748→766, 776→794, 818→836 (×2); rect 790→808 (×2); circle 812→830 (×3); path сердца +18 |

Итог: зазоры 16 (`--s-4`), внутри карточки блоки идут через 12 (строка условий → кнопка: линия 14/20 кончается на 497, кнопка с 506 — было 9, стало ~9; чтобы стало 12 — кнопку на 509 и высоту карточки 196, тогда сдвиг 4-й и 5-й +16/+20 как в исходной схеме). Пятая карточка уходит под tab-bar на 4 px глубже, чем сейчас, — она и так обрезана намеренно.

**3. i18n после 15→16 и 17→20 (оценка, НЕ замер: ночные ширины × коэффициент ступени; `git show 3ae43d2~1` даёт старые кегли: `body15` 15→16 = ×1.067, `name/h` 17→20 = ×1.176, `acc` 14→16 = ×1.143, `t22b` 22→26; `t14/t16/meta` без изменений)**

Из 17 строк отчёта `NIGHT_2026-09-18_i18n-stress.md:58-102` на изменённых классах сидит только одна:
- **строка лайка «набережная, к девяти, компания»** (`screen-i18n-03-08.svg:65`, `class="body15"`, место 261): EN 241→**257/261**, запас 20→4 — впритык, ещё 1–2 символа и обрезано; DE 290→**309** (+48 вместо +29); HY 217→232 влезает. Делать: перенос на вторую строку внутри карточки цитаты (высота +24) — это цитата, её нельзя сокращать; либо усечение с «…» после 261 px, если карточка фиксированной высоты.

Остальные 16 — на `t14`/`t16`/`t14b`/`t16b`/`meta`, кегль не менялся, статусы ночи в силе без изменения: карточка DE в 3 строки (`:26` y=142), «условия» DE 295/311, «Platz nehmen» 105/104 (`:26` y=680, кнопка 104), «Conversations» 97/93.75 (`:22,34,46`), «по году / без ограничения» HY зазор 23, «полоса сдвинулась» DE +72 / HY +44 (`:38,42` y=221, плашка 343), сегмент «Gesellschaft» 87/100 (`:38`), «зарядка type-c» EN 315/311 (`:46` y=186), строка языков DE +11 / HY 332 (`:51`), пилюля срока EN 111/111, DE +18, HY +8 (`:61,65,73,79` `meta`), пузырь HY 311.

Косвенно от перехода `acc` 14→16 (оценка): HY «вернуться» 86→98 в шапке «вы за столом» — остаток 247 держит; «показать» HY 63→72 — свободно. `name` 17→20: «Anja, 24» 67→79, «Անի, 24» 60→71 — в шапке 08 запас есть (значок сверки уже сдвинут на 14, `design-system-app_RU.md:121`). Ничего из «влезло» в «впритык» не перешло.

Что делать с тремя худшими (не зависят от перехода): «полоса сдвинулась» — перенос на две строки в плашке 40→60 px, а не сокращение (это системное сообщение с плейсхолдером); «Platz nehmen» — кнопка по содержимому с min 104, max 160, либо DE-строка «Setzen»; таб «Conversations» — на 13 символов ячейка 93.75 не рассчитана, EN-строка «Chats» (5) — единственный таб, где перевод короче исходника допустим.

**4. Метки категорий: 16/600 против 14/600 — голос за 14/600**

Сейчас метка («оффер», «стол») — `.acc` 16/600 `--accent-text` (`screen-03.svg:17`, шкала `design-system-app_RU.md:112` говорит «метка категории — цвет, а не кегль»). Но метка режима под фразой («компания», «один», «туса») — `t14` 14/400 muted (`screen-03.svg:22` y=208). Одна сущность «категория» на одной карточке живёт на двух ступенях. На листе 25 та же метка — `.mono` 13. Довод за 14/600: (а) метка — служебный ярлык над контентом, шкала отдаёт 14 подписям, и акцентный цвет уже даёт ей вес; на 16/600 она равна заголовку «Пекарня «Колос»» (тоже 16/600, y=408) — две строки одного веса и кегля подряд, отличие только цветом; (б) i18n: «առաջարկ» на 16/600 — 59→68 px, «Angebot» 56→64, при 14 они на 12 % уже, и строка «стол · виселица» (acc + t14 в одной строке, y=594) перестаёт прыгать по базовой между 16 и 14; (в) шкала 14/600 в ней уже есть — `t14b` для табов и чипов языка, то есть это не новая ступень. Цена: `--accent-text` на 14/600 в тёмной теме — проверить контраст, ночью он мерился на 16; и правило «цвет, а не кегль» в `design-system-app_RU.md:112` придётся переписать на «14/600, цвет `--accent-text`».

---

## 2. Кворум по пяти текстам заглушек (UX — автор развилок, визуал, доступность)

| # | Место | A | B | UX | Визуал | A11y | Итог |
|---|---|---|---|---|---|---|---|
| 1 | 03, плашка после сдвига полосы возраста | «Вам стало больше лет — фильтр возраста сузился до вашей полосы.» | «Фильтр возраста обновлён.» | A | A | A | **A** |
| 2 | 23, стол до посадки | убрать доску, строка «сядьте, чтобы увидеть доску» | пустые черты доски как знак | A | B | A | **A** |
| 3 | 20, во время отлучки | «Вы отошли. Фразы сняты, беседы ждут.» | «Перерыв.» | A | A | A | **A** |
| 4 | 25, кнопка к предложению | «к предложению» | «поговорить?» | A | A | A | **A** |
| 5 | 25, предупреждение у «сесть» | «сядете сюда — встанете из-за нынешнего стола» | «поднимет вас из-за нынешнего стола» | A | B | A | **A** |

Тексты применены к листам 03, 20-21-22-24, 23, 25 (11:35). В описания экранов
репозитория `sosed.place` (03:27, 20:35, 25:11, 25:13, 11 — таблица состояний) и в их
зеркала в `neighbro.place` **не перенесены** — задача 1 нарезки ниже.

## 3. Проверки ведущего и опровержения

- ПРОВЕРЕНО: метки `.acc` 16/600 на 9 листах (grep по `<style>`); переведены на 14/600 —
  голоса «Визуал» (muted) и «Типографика» (accent-text) сходятся в кегле, расходятся в
  цвете; оставлен `--accent-text` по записанному правилу «цвет, а не кегль», в шкалу
  добавлена ступень `--fs-meta-strong` 14/20/600.
- ПРОВЕРЕНО: поля фильтров 03 h 40 (y 196, 294) — переведены на 44; «повторить» на 25
  96×34 — 44; сегмент «компания» 108×38 — в SVG не найден по указанной форме, не тронут.
- ПРОВЕРЕНО: доска виселицы на 23 кадр 4 — 7 черт `rect 22×3` и две буквы `t18b`
  (screen-23.svg:33) — сняты по кворуму.
- ПРОВЕРЕНО: «сесть» на 25 — две залитые 120×40 с тенью-прямоугольником `#3a2e20` со
  сдвигом 3 px (`git diff` листа показал оба rect) — линза «Визуал» права; переведены на
  контурную 44 без тени, как на 03.
- **Опровергнуто частично (UX, «полоса срока на 23 кадр 1 противоречит решению»):** решение
  кворума 11:20 явно включает «в просмотре 23 под словом „скоро исчезнет“» — полоса на
  чужой фразе в просмотре была выбрана сознательно; возражение UX записано, не применено.
- **Не проверено:** оценки ширин i18n линзы «Типографика» (сама называет их оценкой);
  счёт акцента линзы «Визуал» по PNG; «двойные пробелы» на 25 — не искал.
- **Отброшено как не новое:** «Conversations» 97/93.75, «Platz nehmen» 105/104 — уже в
  ночном отчёте i18n; «Своих фраз нет» без иконки — сознательно пропущено при вставке
  иконок (заголовок на y=140 упирается в шапку), записано в скрипте шага 4.

## 4. Сводка ведущего

Сделано в тот же цикл (коммит следом): метки 14/600 на 9 листах; поля и кнопка до 44;
пять текстов кворума; доска до посадки снята; «сесть» на 25 как на 03; образцы
контента вместо 8 заглушек-образцов на 25. Заглушек на листах стало 94 (было 111).

## 5. Нарезка на задачи

| # | Задача | Уровень | Цена |
|---|---|---|---|
| 1 | Перенести пять текстов кворума в описания `sosed.place/docs` (03:27, 20:35, 25:11, 25:13, таблица 11) и зеркала `neighbro.place`, RU+EN | согласованность | 0.5 ч |
| 2 | Лист 25 собрать из компонентов 03: сердца → «♡ N» в мете, «показать ещё» кнопкой, undo тостом внизу, пустое состояние с иконкой и display, скелет без круга-аватара, «…» на карточках | противоречие ×5 | 1.5 ч |
| 3 | Лист 23: масштаб фразы (две ступени 32/40 и 26/34 по длине — развилка на кворум), центрирование блока, «−20 %» 20/600, интерлиньяж 36 → записать исключением или ужать до 34 | дефект дизайна | 1 ч |
| 4 | 24/23-5: подсказка без левого ребра, второй залитой кнопки нет (лайк приглушён под подсказкой) | критично по акценту | 0.5 ч |
| 5 | Хит-зоны 44 у сердца и «…» на 03/23/25, срок своей фразы текстом на 03, имя кнопки-сердца на 23-4, opacity-состояние на 25 словом | доступность | 1 ч |
| 6 | Ритм ленты 03: сдвиги по таблице линзы «Типографика» (зазор 16, Δ22→20) | дефект дизайна | 0.5 ч |
| 7 | i18n: строка лайка EN/DE на второй строке; «полоса сдвинулась» DE/HY в плашке 60; «Chats» вместо «Conversations» — развилка | i18n | 0.5 ч |
| 8 | Лист 09 нарисовать по минимуму из 1.2 §3 (4 кадра) | нет листа | 1.5 ч |
