# Роадмап — состояние на 22.09.2026

Снимок того, где продукт стоит целиком. Подробный трекер работ —
[`open-work_RU.md`](open-work_RU.md), порядок сборки продукта — §13
[`chat_RU.md`](chat_RU.md). Здесь только уровень выше: какие слои есть, какие
нет и чем это проверено. Снимок сверен панелью ревью из пяти линз —
[`reviews/PANEL_2026-09-21_roadmap.md`](reviews/PANEL_2026-09-21_roadmap.md); сводка 22.09.2026 сверена линзой «Согласованность», построенное за день — панелями `reviews/PANEL_2026-09-22_*.md` (очередь ленты, профиль, шифрование, перевыпуск, поддержка).

`[x]` — сделано и проверено, `[~]` — частично, `[ ]` — впереди. У каждого
утверждения о живом контуре — дата и способ замера. Снимок от 7 июля 2026
(Supabase, «prod позже») заменён целиком: ни одно из его инфраструктурных
утверждений больше не верно. Номер юридического раздела (§2) сохранён: на него
ссылаются `17-offer_*.md` обеих витрин и `legal-review-brief_*.md`.

## Сводка

| Слой | Готовность | Проверено |
|---|---|---|
| Витрины neighbro.place и sosed.place | ~90% | `curl` 22.09.2026: обе 200 |
| Панель xor.panov.id | ~80% | 11 страниц в `panel/src/pages` (22.09.2026: очередь ленты, поддержка); e2e 5, юнит 32 |
| Узел relay: платформа (ключи, бренды, DSA, почта) | ~70% | `/health` 22.09.2026: `p1-prod`, `database: ok`, `mail: resend`; открытое — §1 |
| Эксплуатация: копии, восстановление, тревоги, откат | ~50% | копии без ключа и без листа ожидания; учение восстановления — 07.08.2026 |
| Юридическое и DSA | ~60% | открыта передача Bunny за ЕЭЗ — §2 |
| Узел relay: продукт, шаги 1–4 §13 | ~60% | шаг 1 и правка профиля; шаг 2 — вердикт даёт человек в панели (модели нет); шаг 3 — лайк, снятие, отказ имени; шаг 4 — согласие с эфемерной половиной; контракт: 84 операции построены, 53 — спека (замер 22.09.2026, `check-openapi.sh`, разбором YAML — `grep` недосчитывает операции с якорями); поддержка вне шагов §13: `POST/GET /support`, сторона команды, `db/039`–`040` |
| Узел relay: продукт, шаги 5–8 §13 | ~40% | шаг 5 — транспорт, сокет, доставка, коды закрытия; шаг 6 — ключи направления и перевыпуск; шаг 7 — блоки, скрытое, уборщики; шаг 8 — не начат (игры), вместо уведомлений — инбокс с курсором и суточная сводка поддержки |
| Клиент `depth` (терминал, идёт первым) | ~55% | ядро `depth/core/`: подпись, регистрация, профиль, лента, лайк, согласие, сквозное шифрование, перевыпуск ключа и код безопасности против живого узла (36 тестов ядра, 39 во всём прогоне `depth/` вместе со строками экранов); отрисовка на Ink с 22.09.2026 — `depth/ink/`: регистрация, локация, лента, фраза, входящие, чат; только стрелки и enter; 17 языков витрин (`scripts/check-depth-i18n.sh`), 4 теста экранов (`scripts/run-depth-ui-tests.sh`) и живой прогон терминала против узла до общего кода безопасности (`scripts/run-depth-live-ui.sh`); образ и команда `scripts/depth.sh` (22.09.2026); ПИН и бумажный код — заглушки под `testOnly`, личность живёт до выхода: ключ неизвлекаемый, поэтому на диск не ложится, а `depth join` ждёт параметров Argon2id |
| Веб-приложение (шаг 9) | ~5% | есть только `neighbro.place/prototype/neighbro-app-proto.html` |

Оценки в процентах экспертные: они не взвешены по часам, потому что нарезки
с ценой для шагов 3–9 ещё нет.

## 1. Живой контур и эксплуатация

- [x] Прод публичен: `api.relay.panov.id/health` 200, узел `p1-prod`, образ
  `v2026.9.11-g8f89e7a`, бренды `sosed` и `neighbro`. Замер 21.09.2026.
- [x] Витрины `sosed.place` и `neighbro.place`, панель `xor.panov.id` — 200.
  Замер 21.09.2026. Прод выкачен 27.07.2026–28.07.2026.
- [x] Supabase выведен из тракта 22.07.2026: состояние в своём Postgres рядом
  с узлом, раздача через Bunny.
- [ ] Контур dev и staging (бокс n1) — не замерен: закрыт белым списком IP.
- [~] Приём уведомлений по ст. 16 — хост `report.relay.panov.id` жив
  (`/health` 200, `p1-prod`, замер 21.09.2026), маршрут `POST /report`; в прод
  витрин не выкачен. Причина переезда: WAF всех зон режет тела, цитирующие
  `<script>` или `../`, а собственных правил у зоны ноль
  (`open-work_RU.md`, G12/G13).
- [ ] **Выкат `day56`.** 76 коммитов впереди `origin/dev` (`cd21688`,
  19.09.2026); миграции `025`–`029` на контурах не применялись (по коду и
  ветке; `schema_migrations` на боксах не опрашивался). Начинать с dev (n1),
  не с прода.
- [ ] `NOT NULL` для опубликованного центра (P5) — только следующим выкатом
  после `027`, никогда в одном с ней.
- [~] Шифрование ночных копий (P4): механизм сделан 21.09.2026 (`4a4da9b`),
  ключа на боксах нет — действие владельца: открытую половину в `backup.env`
  на p1 и n1, приватную из файла в хранилище паролей. Пока так, копии
  открытым текстом.
- [ ] L1: лист ожидания в резервную копию не входит — он в хранилище Bunny,
  а копия снимает только Postgres.
- [~] Учение восстановления — последнее 07.08.2026
  (`scripts/verify-backup-restore.sh`).
- [~] Тревоги: `relay/local/observability/alerts.yml` и ворота
  `scripts/check-metrics-exist.sh`, руководство `runbook-node-down_*.md`;
  работа тревог на проде не проверена.
- [~] Откат — регламент есть (`relay/RELEASE_RU.md`, предыдущий `:vX.Y.Z`),
  на живом контуре не отрабатывался.
- [ ] Пределы частоты по адресу живут в памяти узла: не переживают
  пересоздание контейнера и не делятся между узлами.

## 2. Юридическое и DSA

- [x] Terms, Privacy, Community Guidelines; оператор PSYTICAN & PEJEDED.
  Адреса `support@` обеих витрин — действующие по реестру ст. 30.
- [x] Реестр обработки по ст. 30 GDPR (`article-30-register_RU.md`); строка
  GA4 в нём помечена «живой прод не проверен».
- [x] Процедура утечки, 72 часа по ст. 33 GDPR (`breach-procedure_RU.md`).
- [x] Почта: Resend на staging и prod, Mailpit на dev.
- [~] Реестр уведомлений и решений по DSA в панели, `GET /statements` —
  до выката `day56` и приёма ст. 16 на проде не работает целиком. P3 закрыт
  21.09.2026: у выдачи курсор `after`, мотивировки дальше сотой доступны.
- [ ] **Передача Bunny за ЕЭЗ: SCC нет** (гл. V GDPR, ст. 44–46) —
  `article-30-register_RU.md`. SCC или замена.
- [x] LAW-7: уборщик брошенных личностей — построен 20.09.2026
  (`lib/identity_sweeper.ts`, год без сессии закрывает, через 30 дней удаляет;
  тесты `identity_sweeper.test.ts`). До 21.09.2026 здесь и в реестре ст. 30
  стояло «не построен» — кворум агентов выбрал его следующим куском, и проверка
  кода показала, что он уже есть.
- [ ] J9: перепроверить статус микропредприятия **до 05.08.2027**. От него
  зависит освобождение по ст. 19(1) DSA от ст. 20–28. Отчётов о прозрачности
  нет по этому же основанию (ст. 15(3), 19(1)); на запрос — ст. 24(3).
- [ ] Юр-ревью Terms (13+ вместе с офлайн-встречами).
- [ ] Юридический разбор сохранения оффера (заведено 29.08.2026), три
  вопроса: чем связано заведение, когда `discount_until` на сохранённой
  карточке истёк; что делать с копией оффера, снятого по жалобе; оферта это
  или приглашение делать оферты. Подробно — `legal-review-brief_RU.md`.
- [~] Экран мотивировки по ст. 17 для автора без почты — нарисован 21.09.2026
  (кадры U, V, W листа `panel/design/sheets/screen-06-07-10.svg`, решения
  владельца в экранах 9 и 14 витрин); клиента, который его покажет, нет.

## 3. Продукт: порядок сборки (§13 спеки чата)

Терминал идёт первым; веб — последним шагом по уже проверенному протоколу.
Сервер строится впереди клиента; клиент — ядро `depth/core/` без отрисовки, против живого узла (с 21.09.2026).

| Шаг | Сервер (`xor.ad/relay/node`) | Клиент `depth` |
|---|---|---|
| 1. Личность и сессия | [x] миграция `db/022`; `POST /identities`, `/vault/*`, `/sessions/*`, `/recovery/*`, `PATCH /identities/me` (22.09.2026: имя в очередь, заморозка при живой фразе или чате, возраст через 20/21 только вверх, фильтр в полосе; кадра `name_verdict` и `filter_modes` нет); P2 закрыт 21.09.2026; не выкачено | [~] ядро: регистрация и бумажный код против живого узла; ПИН, код и доля — заглушки |
| 2. Лента и гео | [~] миграции `db/025`–`029`; `POST/GET/DELETE /feed`, `/feed/density`, выдача по кругам; **модель-модератор не подключена**; вердикт даёт человек: `GET /admin/feed-queue` и `POST …/publish`, `…/refuse` построены на узле (22.09.2026, `routes/feed_queue.ts`, пять тестов), страница очереди в панели построена (`panel/src/pages/feed-queue`, макет `panel/design/feed-queue-mockup.svg`, e2e-тест вердикта против стенда); отказ имени отдельно от фразы построен 22.09.2026 (`refuse-name`, фраза ждёт нового имени, мэтч требует `accepted`) — `publish()` зовут руками со страницы, без присмотра работает лишь чистильщик (`lib/feed_verdict.ts`); проверка имени при первой публикации зависит от него же; возраст автора вычислим — принятая цена P1; не выкачено | [~] ядро: фраза и лента с курсором |
| 3. Лайки | [~] `likes` — `db/030`; `POST /feed/:id/like` (21.09.2026): полоса, блокировка, самолайк без оракула, мэтч при встречном; `DELETE` — снятие до мэтча, иначе `spent`; предел 300 в час; уборка истёкших мэтчей раз в минуту; нет — мэтча от оффера (блокер снят 22.09.2026: `publish` в очереди принимает имя тем же вердиктом, `name_state = accepted` — признак «имя проверено»; сам мэтч от оффера ещё не построен), теста гонок на двух соединениях | [~] ядро: лайк и снятие |
| 4. Мэтч и двойное согласие | [~] `db/030`; `POST /matches/:id/consent` (`waiting`/`agreed`), «не сейчас» и его отмена (21.09.2026); чат при `agreed` не открывается до шага 5, эфемерный ключ — шаг 6 | [~] ядро: согласие и «не сейчас» |
| 5. Чат: транспорт | [~] `db/031`: `chats`, `chat_participants`, `chat_starters`; согласие обоих открывает чат и отдаёт `chat_id`; `db/032` `pending_deliveries`, отправка шифротекста (202 без сигнала присутствия) и подтверждение получения (21.09.2026); потолок очереди 200 с молчаливым вытеснением и уборка строк старше 260 минут; `db/033` билеты и сокет `GET /chat`: накопленное при подключении, новое через `NOTIFY`, два терминала `depth` обмениваются шифротекстом; конец беседы — `DELETE /chats/:id`, свой срок, уборщик `sweep_chats`, комната закрывается 4003 (21.09.2026); `PATCH` срока, `POST /chats/alive`, `GET /inbox` (мэтчи и беседы; без `offer_interest` и второй страницы); нет — 1001 | [ ] |
| 6. Шифрование | [~] эфемерная половина с подписью долгим ключом едет на согласии (`db/035`, `routes/matches.ts`), инбокс отдаёт чужую половину, долгий ключ и `me` (22.09.2026); перевыпуск после потери пары — `POST /chats/:id/rekey`, эпоха в `db/038` (22.09.2026); нет — `chat_key_wraps` (нужны веб-лицу, не терминалу без диска) | [~] `depth/core/seal.ts`: ECDH P-256 → HKDF (соль `chat_id`) → два ключа направления, AES-GCM с 96-битным nonce; `Client.consent` публикует половину, `openConversation`/`sayInChat`/`read`; сквозной тест двух терминалов против узла, отражение не открывается (22.09.2026); нет — свёрток под ключ сессии и Ink |
| 7. Блокировки, скрытие, чистка | [~] `POST`/`GET`/`DELETE /blocks` (21.09.2026): по фразе или беседе, 204 без оракула, `nonce` против повтора, гасит мэтч и кончает беседу у обоих с закрытием комнаты 4003; уборщики мэтчей, очереди и бесед; `db/034` и `/hidden` — скрыть фразу только у себя и вернуть; нет — блокировки и скрытия реплики за столом (столов нет) | [x] `depth/ink` (23.09.2026): в ленте «скрыть» и «заблокировать», экран скрытого с возвратом, в чате «закончить беседу» и «заблокировать»; блокировка спрашивает дважды и называет последствие; ядро — `hide`, `hidden`, `unhide` |
| 8. Уведомления и игры | [ ] | [ ] |
| 9. Веб-лицо | — | [ ] прототип есть, приложения нет |

Все одиннадцать таблиц первого среза (§13) есть: шесть до 21.09.2026, `likes`,
`matches`, `match_participants`, `blocks` миграцией `db/030`, `support_requests` —
`db/039` (22.09.2026). Миграций в `relay/node/db` — 37 (замер 22.09.2026).

Код чата разрешён словом владельца 21.09.2026.

## 4. Витрины и панель

- [x] Лендинги обеих витрин: темы, акценты, i18n, лист ожидания, `legal.html`.
- [x] Макеты экранов приложения собираются из кита (`panel/design/sheets`,
  `panel/design/kit`), галерея и ворота текста.
- [x] Панель: вход по ссылке, ключи API и секретные ключи с квотами, бренды,
  пользователи панели, лист ожидания, журналы, реестр уведомлений DSA.
- [ ] Перевод строк приложения: сейчас есть только селектор языка в прототипе.

## 5. Хвосты панелей ревью 21.09.2026

Первая панель (`PANEL_2026-09-21_steps1-2.md`) закрыта целиком. Вторая
(`PANEL_2026-09-21_day-fixes.md`) закрыта целиком 21.09.2026: задача 7 —
`6dd62de`, 8 — `ae65510`, 5 — `2bde94b`.

## Открытые вопросы

- Минимальный возраст: 13+ сейчас, юрист может настоять на 16+. Порог
  согласия ст. 8 GDPR касается только обработки на основании согласия.
- Набор акцентов и шрифт-эталон бренда — были открыты в снимке от 7 июля;
  решены ли, не сверено.
