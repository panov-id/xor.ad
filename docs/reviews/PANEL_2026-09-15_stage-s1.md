# Панель ревью: Э1 маршрута до отрисовки — 15.09.2026

**Что ревьюили.** Ветка `day52`:

- `git -C xor.ad diff 3b7d480..3878fe8`
- `git -C sosed.place diff 93c65a9..cf6cea0`
- `git -C neighbro.place diff 12b85a1..b99118b`

Страница «что изменилось» и её проверка в `check-legal-bar.mjs`; квитанция без почты (`dsa/SPEC_*` §6, §8,
экраны 5 и 11, тесты 16.9); журнал сообщений по ст. 18; спецификация сторожей `docs/watchdogs_*`;
инструменты `harden-cycle.sh` и `batch-edit.py`.

**Линзы.** Безопасность и право, Данные и автономность, Согласованность; опровергающий по всем находкам.

## Сырьё линз и опровержения (дословно, `panel-extract.py`)

### Безопасность и право

**Итог:** уязвимостей уровня «сломано» нет. Выжили 3 юридических дефекта средней тяжести, 3 низких (включая одну проверенную запись за пределы корня в batch-edit.py) и 2 замечания.

```diff
! LAW-1, LAW-2, SEC-1 — средние: квитанция описана без места хранения, без политики и без уведомления
+ SEC-3 проверено в контейнере: "../" в "file" пишет за корень группы
@@ страница изменений в sosed.place и neighbro.place сверена с git diff юр-текстов — пропуск один (LAW-4)
```

Репозитории: xor.ad, sosed.place, neighbro.place (в последнем страница изменений та же, отличается только бренд; **ПРОВЕРЕНО** `diff`). Везде ниже **ПРОВЕРЕНО** чтением файла или командой, если не сказано иначе.

**LAW-1 — средняя.** Файл `sosed.place/landing/legal/privacy_EN.md:46` (и то же в neighbro.place).
- **Дефект:** в политике нет ни кода квитанции на устройстве, ни `receipt_hash` на узле. `grep -i receipt` по `landing/legal/` находит только terms:89 («We confirm receipt»).
- **Сценарий:** функцию строят по SPEC §6, а политика молчит о новых данных. Это нарушение GDPR 13, при том что хэш хранится год (`xor.ad/docs/dsa/SPEC_EN.md:419`).
- **Исправление:** завести пункт в `open.tsv`, чтобы политика обновилась вместе с кодом, и описать в §3/§5: что хранится на устройстве, что на узле, срок 1 год.

**LAW-2 — средняя (толкование, не юрист).** Файл `xor.ad/docs/dsa/SPEC_EN.md:416`: «The decision and its reason are shown on that device by the code».
- **Дефект:** это модель «зайди и посмотри», а ст. 16(5) требует «notify … without undue delay» (`eurlex/DSA-art16.txt`, п. 5).
- **Сценарий:** человек без почты не открывает приложение месяц. Решение формально «доступно», но уведомления не было.
- **Исправление:** хотя бы локальная отметка (точка или бейдж) при следующем открытии, по образцу `answer_seen` (test-map 16.8e). Вопрос о достаточности pull-модели записать юристу, а не закрывать `dsa.article16.no-email` насовсем.

**SEC-1 — средняя.** Файл `xor.ad/docs/dsa/SPEC_EN.md:414–421`.
- **Дефект:** не сказано, где устройство хранит код (localStorage, IndexedDB, ключ по бренду) и что с ним при «начать заново» и на общем устройстве. `grep -i "localStorage|start over"` по SPEC ничего не находит.
- **Сценарий:** семейный планшет. Следующий человек открывает приложение и видит, что с устройства подавали уведомление, а также решение и причину (§6:401–404: «what was decided … why»). Причина может описывать сам контент. «Начать заново» уничтожает личность, а квитанцию оставляет. Или наоборот, стирает её молча, и решение пропадает.
- **Исправление:** в §6 назвать хранилище и ключ. Код не привязывать к личности и не удалять при «начать заново» либо удалять с явным предупреждением. Решение показывать только после действия пользователя (открыть «мои уведомления»), в причине не цитировать контент. Добавить тест-кейсы 16.9c и 16.9d.

**SEC-2 — низкая.** Файл `xor.ad/docs/dsa/SPEC_EN.md:417–418`.
- **Дефект:** равенство ответов задано только как «same answer after the same delay». Не зафиксированы статус-код, `Cache-Control`, `ETag`, `Content-Length`, а лимит частоты на поиск не задан вообще. Лимит 10/час в `:46` относится к `POST /report`.
- **Сценарий:** «нет квитанции» отвечает 404, «не решено» отвечает 200 с тем же текстом, либо CDN (Bunny) кэширует один из вариантов. Подобрать код это не помогает (128 бит), но эндпоинт без лимита можно заливать запросами. Есть и противоречие: «address is not logged» против любого лимита по адресу.
- **Исправление:** один статус (200), `Cache-Control: no-store`, одинаковое тело байт в байт. Код передавать в теле `POST`, а не в URL (иначе он попадёт в логи CDN и в Referer). Лимит держать в памяти без записи адреса. Тест 16.9a расширить: сравнивать статус и заголовки, не только тело.

**SEC-3 — низкая, ПРОВЕРЕНО в контейнере python:3.12-slim.** Файл `xor.ad/scripts/batch-edit.py:73–76`: `p = root / rel`, проверки, что путь внутри корня, нет.
- **Дефект:** `"file": "../outside/t_EN.md"` плюс `unpaired` переписали файл вне корня: `SECRET` стал `PWNED`, вывод «применено: замен 1». Абсолютный путь падает на `relative_to` (`:86`) ещё до записи, так что пишет только `..`.
- **Сценарий:** пачку JSON составил агент или скопировали из ревью, и правка уходит в `~/.claude/...` или в соседний проект.
- **Исправление:** `p = (root / rel).resolve()` и отклонять пачку, если `root.resolve()` не входит в `p.parents`. Добавить пробу в `test_batch-edit.sh`. Дефект был и до этого диффа: новый код `:97–101` только проверяет glob на существование и ничего не пишет.

**LAW-3 — низкая.** Файл `sosed.place/landing/check-legal-bar.mjs:305`: `const edition = ["terms_EN.md", "privacy_EN.md"]`.
- **Дефект:** редакцию определяют только terms и privacy, community-guidelines не участвует.
- **Сценарий:** правила сообщества получают дату 20 сентября без правки terms и privacy. Ворота зелёные, а на странице изменений нет раздела этой редакции (ст. 14(2)).
- **Исправление:** добавить `community-guidelines_EN.md` в массив.

**LAW-4 — низкая.** Файл `sosed.place/landing/legal/changes_EN.md` (и neighbro.place).
- **Сверка:** пословный diff редакций 12 → 14 сентября (terms от `e4191bb`, privacy и guidelines от `10ddafe`, до HEAD). Названы §15, §11, §10, Art. 18, §19; в privacy §3, §5, §7, §12, §13.
- **Пропуск:** в Terms появилась квота «(four messages per sliding hour)», а на странице её нет. Это новое конкретное ограничение для пользователя.
- **Исправление:** добавить строку в раздел Terms.

**Замечания, дефектами не считаю:**
- Журнал ст. 18 (`SPEC_EN.md:279–283`): канал, номер у адресата и список переданных полей без копии контента, срок равен сроку уведомления. С GDPR 5(1)(c) это соразмерно.
- Письма наблюдателей (`watchdogs_EN.md:28–29`, `:37–38`): номер, тип цели, возраст, без текста и данных заявителя, утечки нет. Мелочь: при напоминании на общий `support@` тип цели слегка раскрывает характер жалобы. Приемлемо.

**Не проверено:**
- Всё про квитанцию — это спецификация, кода в relay нет. В форме `sosed.place/landing/report.html:259–260` пока старый текст про почту, про решение ни слова.
- Поведение CDN с кэшем ответа поиска.

Перечитывать тексты EUR-Lex по ст. 14 и 18 не понадобилось. Цитировалась только ст. 16 из `eurlex/DSA-art16.txt`.

### Данные и автономность

```diff
- DATA-1: в §9 обещан «обезличенный счётчик», но prune_dsa_records его не ведёт: уведомления удаляются без следа
! Входит ли хэш квитанции в счётчик, спрашивать не о чем: самого счётчика нет
! AUT-1/AUT-2: у сторожей С1 и С3 не описано, как часто повторять письмо, и через год без присмотра почту завалит
+ Сигналы сторожей в relay/node действительно не построены, и документ честно так и пишет
@@ Проверено: grep по relay/node, два прогона на временных копиях (batch-edit, check-legal-bar в node:22-alpine)
```

Всё ниже я проверил сам. Репозитории: xor.ad, sosed.place, neighbro.place, ветка day52. В самих репозиториях ничего не менял.

**DATA-1 · высокая · `xor.ad/docs/dsa/SPEC_EN.md:491` и `xor.ad/relay/node/tools/prune_dsa_records.ts:10-12`**
- **Дефект.** По §9 после года должен оставаться обезличенный счётчик. Шапка скрипта говорит «What survives is a count», но в коде есть только `DELETE`.
- **Сценарий.** Через год на вопрос «растёт ли число уведомлений» ответить нечем. Сравнивать хэш с этим счётчиком бессмысленно.
- **Чем проверено.** `grep -rni "counter|count|anonym"` по `db/*dsa*.sql`, `src`, `tools` ничего не нашёл. Скрипт я прочитал целиком.
- **Исправление.** Либо таблица помесячных счётов, которую пополняет сам скрипт до удаления, с явной строкой «хэш в неё не входит». Либо убрать обещание из §9 и из шапки скрипта.

**DATA-2 · средняя · `SPEC_EN.md:469` (в RU строка 449)**
- **Дефект.** У `receipt_hash` не указаны тип, уникальность и индекс, хотя поиск идёт именно по хэшу.
- **Сценарий.** Без индекса запрос по коду читает весь `dsa_notices`. Время ответа растёт с числом строк, и поиск становится медленнее. Без уникальности хэш может найти две строки, и ответ не определён.
- **Исправление.** Записать: `bytea`, 32 байта, `CREATE UNIQUE INDEX … (receipt_hash) WHERE receipt_hash IS NOT NULL`, `NULL` — только для уведомлений без квитанции.
- **Удаление вместе с уведомлением.** Сработает само: хэш лежит в той же строке, а скрипт делает `DELETE FROM dsa_notices WHERE id = ANY($1)`. Это видно из кода, но не прогнано: колонки пока нет.

**DATA-3 · средняя · `SPEC_EN.md:417-419`**
- **Дефект.** Фраза «equal answer and equal delay» не проверяется. Не сказано, что именно совпадает (код ответа, тело, заголовки), какая задержка и нужен ли лимит частоты.
- **Сценарий.** При 128 битах перебор кода и так невозможен. Но если реализовать «одинаковую задержку» как `sleep` только на промахе, появится утечка, которой без этого не было.
- **Исправление.** Записать так: у «нет такой квитанции» и «ещё не решено» одинаковые статус и тело байт в байт. Минимальное время ответа одно на обе ветки. Проверка для карты тестов: два запроса дают одинаковые байты.

**AUT-1 · средняя · `xor.ad/docs/watchdogs_EN.md:141-151` (С1)**
- **Дефект.** Задача запускается раз в час, но не сказано, одно ли напоминание на уведомление или новое каждый час. Непонятно, где отмечается, что оно уже ушло.
- **Сценарий.** Если никто не смотрит, уведомление 25 часов назад даёт по письму в час на `support@`, а с 48 часов ещё и на личные адреса. Такой поток перестают читать. Кроме того, письмо уходит через ту же почту узла, чей отказ описывает С2. Резервная почта у С1 не упомянута.
- **Исправление.** Отметка на уведомлении (`reminded_at`/`escalated_at`) или одно письмо на порог. Эскалацию слать через `MAIL_FALLBACK_TRANSPORT`.

**AUT-2 · средняя · `watchdogs_EN.md:162-169` (С3)**
- **Как устроено сейчас.** Дублей перевзвод не создаст: индекс `jobs_standing` (`db/020`) исключает надгробия, и `enqueueOnce` видит пустое место. Зато вечная петля возможна. Если `prune_dsa_records` падает всегда, перевзвод раз в час ставит новую строку на завтра. Через 8 попыток (`db/001:69`, пауза между ними около 70 минут) получается новое надгробие, и так каждые сутки. `prune_job_tombstones` удаляет их только через 30 дней.
- **Дефект.** Не описано, как не слать повторно «срочное письмо на каждое надгробие». Проверка в таблице ждёт `relay_jobs_tombstones{kind="prune_dsa_records"} 1`, а через сутки там будет 2.
- **Исправление.** Письмо только на новое надгробие (по `id`). В проверке считать `≥1`. Надгробие самого уборщика надгробий уходит лишь в дайджест. Дайджеста в коде нет: `grep digest` находит только `hash.ts` и `waitlist.ts`.
- **Остальные сигналы.** `relay_dsa_*`, `relay_jobs_tombstones`, `DSA_ESCALATION_EMAILS`, `MAIL_FALLBACK_TRANSPORT`, `dsa_notice_age`/`dsa_notice_notify` не найдены. Пометка «не построено» соответствует правде. Статусы `received`/`in_review` и индекс по ним есть (`db/005:39-55`). Пороги в `limits.tsv` (24 и 48) совпадают с текстом.

**AUT-3 · низкая · `watchdogs_EN.md:171-177` (С4) и `xor.ad/docs/facts/limits.tsv` (`watchdog.notice.*`)**
- **Дефект.** Ворота ловят только суффиксы `*.ttl`, `*.retention`, `*.delay`. Строки `watchdog.notice.remind` и `.escalate` помечены `узел` и остаются без исполнителя, но ворота их не заметят.
- **Исправление.** Либо `enforced_by = —` до постройки, либо ловить все строки с исполнителем `узел`.

**DATA-4 · средняя · `xor.ad/scripts/batch-edit.py:135-146` · атомарность**
- **Дефект.** Файлы пишутся по очереди без временного файла и переименования. Сбой посередине оставляет часть правок.
- **Проба.** На копии сделал реестр доступным только для чтения, в пачке были `a_RU`/`a_EN` и одна снятая формулировка. Результат: `PermissionError`, оба файла уже переписаны (`y`/`y`), реестр не тронут. Обещание «всё или ничего» в шапке не выполняется.
- **Исправление.** Сначала записать всё во временные файлы, затем сделать `os.replace` для каждого, запись в реестр тоже включить.

**DATA-5 · низкая · `batch-edit.py:107-110` · дубли в реестре**
- **Дефект.** Уже записанные формулировки отсеиваются, но повторы внутри одной пачки — нет.
- **Проба.** Две одинаковые `phrase` в пачке дали 2 строки в реестре.
- **Исправление.** Сверять ещё и с уже накопленным `retired_lines`.

**AUT-4 · низкая · `xor.ad/scripts/harden-cycle.sh:30`**
- **Дефект.** `FACES="sosed.place"` молча убирает все ворота второй витрины, и итог зелёный с меньшим «пройдено».
- **Что в порядке.** Коды выхода считаются верно: `local code=$?` берётся отдельно от присваивания. `pipefail` ничего не маскирует, потому что конвейеры стоят только в выводе. Сверка через grep MISMATCH верна: `check-docs-pairing.sh` на успехе даёт код 0 и не печатает MISMATCH. Упавший `apk add` делает ворота заголовков безопасности красными, пустая сверка в `brand_modulo` даёт код 3.
- **Исправление.** Печатать ожидаемое число ворот и краснеть, если `FACES` отличается от двух витрин по умолчанию.

**DATA-6 · низкая · `sosed.place/landing/check-legal-bar.mjs:69-81` (в neighbro.place тот же файл, blob `40a566e`)**
- **Проба** на копии в `node:22-alpine`:
  - основной случай зелёный;
  - `4 September` разбирается нормально (`\d{1,2}`);
  - `04 September` ложно краснеет: ищется заголовок `## Edition of 04 September 2026`;
  - `Sept` ложно краснеет, а `MONTHS.indexOf` возвращает −1 и сортировка ломается.
- **Дефект.** Если в одном из двух документов нет «Last updated», это молча игнорируется: я удалил дату в privacy, а проверка взяла дату terms. Community Guidelines, которые есть в `changes_EN.md`, в расчёт не входят.
- **Смена редакции.** Работает как задумано: новая дата в terms без раздела в `changes_EN.md` даёт понятный красный.
- **Исправление.** Требовать дату в каждом из трёх документов. Число сравнивать без ведущего нуля, неизвестный месяц считать ошибкой.

Дальше очевидно ровно одно: разработчику выбрать, что делать с DATA-1 — завести счётчик или снять обещание из §9. От этого зависит, что писать о хэше в §9.

### Согласованность

```diff
- CON-1: экраны 8 и 14 обеих витрин по-прежнему говорят, что решение приходит «только на почту»
! CON-2: форма report.html расходится со спекой §6: квитанции нет, строки у поля почты нет
! CON-3: receipt_hash не назван ни в политике конфиденциальности, ни в реестре ст. 30
@@ Экраны 5 и 11, спека §6/§8/§9, карта тестов и реестры open/limits между собой согласованы
```

Ниже каждое утверждение проверено мной: `grep`/`sed`/`diff`/`cmp` по файлам репозиториев `xor.ad`, `sosed.place` и `neighbro.place` (ветка day52). Экраны двух витрин (`05`, `11`, `15`) `cmp` не различает. Юридические тексты, `changes_EN.md` и `check-legal-bar.mjs` совпадают с точностью до бренда (`diff`).

**CON-1 · критично (формулировка противоречит канону)**
- **Где:** `sosed.place/docs/08-chat-screen_EN.md:31` и `neighbro.place/docs/08-chat-screen_EN.md:31` — «the decision still arrives only by email, if you left one». В RU то же на строке `:30` («всё равно придёт только на почту»). Кроме того, `{sosed,neighbro}.place/docs/14-support_EN.md:30` — «the decision on it reaches them only by email, if they leave one», и `14-support_RU.md:30` — «решение по ней придёт только на почту, если её оставить».
- **Дефект:** противоречит `xor.ad/docs/dsa/SPEC_EN.md:413` («reaches you without email too, by a receipt on the device») и экранам 5 и 11. Маршрут Э1 перечислял только экраны 5 и 11 — экраны 8 и 14 выпали. В `retired-terms.txt` добавлены только фразы экрана 11, поэтому `check-retired-terms` эти места не ловит.
- **Последствие:** в витринах живут две несовместимые версии, и финальная панель или отрисовщик возьмёт устаревшую.
- **Как чинить:** переписать четыре пары строк под квитанцию и занести обе формулировки в `retired-terms.txt`. На экране 8 оговорить, что квитанция лежит на том же устройстве.

**CON-2 · внимание**
- **Где:** `{sosed,neighbro}.place/landing/report.html:137-142` и `:259-260`.
- **Дефект:** у поля почты нет строки «сотрёте данные браузера — решение не увидите». Без почты форма отвечает «there is nowhere to send a confirmation — keep the reference above». Код квитанции не создаётся. Спека `SPEC_EN.md:419-421` и экран 5 описывают другую форму. Пункта в `open.tsv` о том, что форма не построена, нет (grep `receipt` в `open.tsv` пуст). Закрыт только `dsa.article16.no-email`, а тесты 16.9–16.9b помечены «nothing to check».
- **Последствие:** на живой форме по-прежнему действует прежнее решение «без почты решения нет». Разрыв между спекой и витриной нигде не записан.
- **Как чинить:** завести пункт `dsa.receipt.unbuilt` в `open.tsv` со ссылкой на `report.html` и `SPEC §6`. Код формы — вне стоячего разрешения.

**CON-3 · внимание**
- **Где:** `sosed.place/landing/legal/privacy_EN.md:28` и `:45`, `xor.ad/docs/article-30-register_EN.md:147` (RU `:141`), DPIA (grep `receipt` пуст).
- **Дефект:** в состав данных уведомления входят reasons, name, email и копия, а `receipt_hash` (`SPEC_EN.md:469`) нигде не назван. При этом политика в §5 перечисляет даже метку «stepped away».
- **Последствие:** в реестре обработки и в политике не хватает поля, которое хранится год рядом с уведомлением. Считать ли хэш персональными данными — не вывод юриста, но несогласованность налицо.
- **Как чинить:** одна строка в §7 реестра ст. 30 (RU/EN), в privacy §3 и §5, в DPIA — либо записанное решение «не персональные данные» с доводом.

**CON-4 · мелочь**
- **Где:** `{sosed,neighbro}.place/docs/15-legal-documents_EN.md:9` — «Links to three documents»; сразу под ним добавлена страница changes. RU `:9` — то же.
- **Дефект:** страница изменений — четвёртая вкладка `legal.html` («Changes», `legal.html:94`), но в перечень она не внесена. `SPEC_EN.md:51` лендинга перечислений не держит, манифест берёт `legal/*.md`, так что выкладку это не ломает.
- **Как чинить:** «три документа и страница изменений».

**CON-5 · мелочь**
- **Где:** `sosed.place/landing/check-legal-bar.mjs:69`.
- **Дефект:** номер редакции берётся только из `terms_EN.md` и `privacy_EN.md`. `changes_EN.md:5` при этом обещает перечислять и редакции Community Guidelines.
- **Последствие:** правка одних правил сообщества без раздела на странице изменений не покраснеет.
- **Как чинить:** добавить файлы `community-guidelines_*` в список `edition` или сузить обещание в `changes_EN.md:5`.

**CON-6 · мелочь**
- **Где:** `xor.ad/docs/watchdogs_EN.md:56` и RU `:52`.
- **Дефект:** «can be built before product code (S4 of the route)», но `ROADMAP_…_EN.md:93-97` (Э4 «Факты») гейт `check-facts-limits.sh` не упоминает.
- **Как чинить:** дописать гейт С4 в Э4 маршрута (RU/EN) или убрать ссылку.

**CON-7 · мелочь**
- **Где:** `xor.ad/docs/runbook-node-down_EN.md:137-138` и RU `:134-135`.
- **Дефект:** «There is no alerting… open work». Ссылки на `watchdogs_*` §С5 и пункт `node.external.pinger` нет, хотя `watchdogs_EN.md:18` ссылается на ранбук.
- **Как чинить:** одна строка с отсылкой в обе версии.

**CON-8 · мелочь**
- **Где:** `xor.ad/docs/facts/open.tsv`, новая строка `watchdogs.unbuilt`.
- **Дефект:** категория `legal`, но в описании перечислены С3 (надгробия задач чистки) и С5 (пингер) — это эксплуатация, а не право. `node.external.pinger` при этом помечен `product`.
- **Последствие:** при разметке Э4 пункт попадёт не в ту кучу.
- **Как чинить:** разделить на С1–С2 (`legal`) и С3/С5 (`product`) или сменить категорию.

**CON-9 · мелочь**
- **Где:** `xor.ad/docs/facts/decisions.tsv`, строки `sosed-legal/neighbro-legal.2026-08-29.12`.
- **Дефект:** RU-строку сдвинули на `:22`, а EN осталась `:12`. Фактически «Who the operator is» стоит на `15-legal-documents_EN.md:23` в обеих витринах. Расхождение было и раньше, правка его не заметила.
- **Как чинить:** EN → `:23`.

**Что проверено и дефектов не дало**
- Экраны 5 и 11 в RU и EN говорят одно, их retired-пометки на месте.
- Спека §6, §8 (`receipt_hash`) и §9 (`prune_dsa_records`) согласованы с тестами 16.9–16.9b в RU и EN.
- Пороги сторожей 24/48 ч в `limits.tsv` совпадают с `watchdogs_*` и планом v1 (1.1). 72 ч — цель из `SPEC_EN.md:217`, не норма.
- Разделы С1–С5 в RU и EN одинаковы по содержанию.
- Страница изменений датирована «14 September 2026», как `Last updated` в terms и privacy. Кнопка полосы в `index.html:518` ведёт на `legal.html?doc=changes`.
- В `dsa/README_*`, `CHECKLIST_*`, `legal-review-brief_*`, `open-work_*`, terms §19, privacy §13 и в строках i18n устаревших формулировок про «только почту» нет. Упоминания «statement of reasons by email» в README касаются автора (ст. 17), а не заявителя.
- Выход Э1 в маршруте — панель (`ROADMAP_EN.md:67`). Это ревью и есть её часть, так что противоречия с проделанным нет.
- Процесс, пункты 5–6 (RU и EN): «только спецификация». Сторожа так и сделаны — кода в `relay/` в диффе нет.

### Опровергающий

Итог: из 26 находок подтверждено 17, частично верны 6, опровергнуто 3 (AUT-3, AUT-4, DATA-2). Все статусы ниже я проверил сам по файлам, отзывы агентов на веру не брал. Во всех трёх репозиториях (`xor.ad`, `sosed.place`, `neighbro.place`) только читал, ничего не менял.

```diff
+ подтверждено 17: CON-1, SEC-3, LAW-3=CON-5, LAW-1, LAW-4, SEC-1, DATA-1, DATA-3, DATA-5, AUT-2, CON-2, CON-3, CON-4, CON-6, CON-7, CON-9, AUT-1
! частично 6: LAW-2, SEC-2, DATA-4, DATA-6, CON-8, AUT-1(эскалация)
- опровергнуто 3: AUT-3, AUT-4, DATA-2
```

**CON-1 — ПОДТВЕРЖДЕНО.** `sosed.place/docs/08-chat-screen_EN.md:31` и `14-support_EN.md:30` по-прежнему говорят «only by email».

**SEC-3 — ПОДТВЕРЖДЕНО.** Путь из `file` склеивается как `root / rel` без проверки, что он остался внутри корня (`xor.ad/scripts/batch-edit.py`, цикл по ops).

**LAW-3=CON-5 — ПОДТВЕРЖДЕНО.** Редакцию `sosed.place/landing/check-legal-bar.mjs` берёт только из `["terms_EN.md", "privacy_EN.md"]`, `community-guidelines` не читает.

**LAW-1 — ПОДТВЕРЖДЕНО.** В `sosed.place/landing/legal/privacy_EN.md` нет ни «receipt», ни кода на устройстве. В разделе local storage (:97) названы только идентичность и настройки.

**LAW-2 — ЧАСТИЧНО.** `SPEC_EN.md` §6 (~:413) действительно устроен на опросе: «shown on that device by the code». Сигнала при следующем открытии нет. Нарушение ли это ст. 16(5) — вопрос толкования, а не факт. Замечание о пробеле в спеке верное.

**SEC-1 — ПОДТВЕРЖДЕНО.** В §6 нет места хранения кода, нет поведения при «начать заново» и на общем устройстве. Решение и причина показываются любому, у кого есть код.

**SEC-2 — ЧАСТИЧНО.** «Same answer after the same delay» в §6 записано, так что суть есть. Но статус, заголовки и байты тела не зафиксированы. Лимит на поиск и способ передачи кода (POST или URL) в §6 не названы. Лимит 10 в час (:46) относится к подаче уведомлений, не к поиску.

**LAW-4 — ПОДТВЕРЖДЕНО.** «four messages per sliding hour» впервые появилось в c6936de от 14.09 (прежний текст: «posting quotas,» без числа). Это та же редакция от 14 сентября, но в `sosed.place/landing/legal/changes_EN.md` строки о квоте нет.

**DATA-1 — ПОДТВЕРЖДЕНО (дефект был и до диффа).** `SPEC_EN.md:491` и `privacy_EN.md:45` обещают анонимный счётчик. Шапка `xor.ad/relay/node/tools/prune_dsa_records.ts` тоже его обещает, но в коде только `DELETE` и никакой записи счётчика.

**DATA-2 — ОПРОВЕРГНУТО.** Сущности в §8 (`SPEC_EN.md:455+`) описаны вообще без типов и индексов, это логическая модель, а не DDL. Уникальность при 128 битах случайности практически гарантирована. Индекс — забота миграции, которой ещё нет.

**DATA-3 — ПОДТВЕРЖДЕНО (дубль SEC-2).** Тест 16.9a в `test-map_EN.md` проверяет «the same body and the same delay» без допуска на задержку и без статуса. Как написано, его не измерить.

**AUT-1 — ЧАСТИЧНО.** В `watchdogs_EN.md:24-29` задача ежечасная, поля `reminded_at` нет. Значит, напоминание придёт либо каждый час, либо один раз — не определено. Резервный транспорт назван только в W2 (:37). Для эскалации W1 он не указан, но это спецификация несобранного, а не дефект кода.

**AUT-2 — ПОДТВЕРЖДЕНО.** `enqueueOnce` пропускает надгробия (`jobs.ts:102`), поэтому ежечасный повторный взвод (:43) после каждого исчерпания попыток создаёт новую задачу. Надгробия копятся до 30 дней (`scheduled.ts:45`), и на каждое уходит срочное письмо. Ожидание `tombstones 1` в :73 верно только для первого цикла.

**AUT-3 — ОПРОВЕРГНУТО.** `watchdog.notice.remind/escalate` — пороги оповещения, а не срок хранения. W4 (:52-54) сознательно ловит только сроки, которым нужен исполнитель. К тому же оба порога уже закрыты пунктом `watchdogs.unbuilt`.

**DATA-4 — ЧАСТИЧНО.** Все проверки идут до первой записи. Запись полупроходит только при ошибке ввода-вывода (диск, права), а в докстринге обещано «всё или ничего». Дефект реальный, но узкий.

**DATA-5 — ПОДТВЕРЖДЕНО.** Проверка на дубль сверяет фразу только с `current`, то есть с реестром на диске. Две одинаковые фразы в одной пачке обе попадут в `retired_lines`.

**AUT-4 — ОПРОВЕРГНУТО.** `faces="${FACES:-sosed.place neighbro.place}"` (`harden-cycle.sh:29`) — осознанное переопределение через окружение. По умолчанию проверяются обе витрины. Укороченный список задаёт тот, кто сам выставил переменную.

**DATA-6 — ЧАСТИЧНО.** Про пропуск верно: документ без «Last updated» молча выпадает через `.filter(Boolean)`. Остальное — нет:
- «04 September» дало бы ложный красный, но сейчас все даты без ведущего нуля.
- Неизвестный месяц не ломает сортировку: `indexOf` возвращает −1, и такая дата просто уходит вниз.

**CON-2 — ПОДТВЕРЖДЕНО.** В `sosed.place/landing/report.html:259-260` по-прежнему «no email… nowhere to send». Кода-квитанции и строки у поля почты нет. В `open.tsv` нет пункта про форму.

**CON-3 — ПОДТВЕРЖДЕНО.** `receipt_hash` ищется только в SPEC, test-map и reviews. В `article-30-register_EN.md` и `dpia_EN.md` его нет.

**CON-4 — ПОДТВЕРЖДЕНО.** В `sosed.place/docs/15-legal-documents_EN.md:9` «Links to three documents», а в :10 рядом добавлена четвёртая страница — changes.

**CON-6 — ПОДТВЕРЖДЕНО.** `watchdogs_EN.md:56` отсылает к S4, но в `ROADMAP_2026-09-15_to-drawing_EN.md:93-97` о W4 и `check-facts-limits` ничего нет.

**CON-7 — ПОДТВЕРЖДЕНО.** В `runbook-node-down_EN.md:137` «There is no alerting» без ссылки на W5 и `node.external.pinger`.

**CON-8 — ЧАСТИЧНО.** В `open.tsv` пункт `watchdogs.unbuilt` имеет вес legal. Для W1–W2 (ст. 16) это обосновано, для W3 спорно: сроки обещаны в privacy. W5 — чистая эксплуатация, но он и так вынесен отдельным пунктом (product). Разделить пункт стоит, ошибкой это не является.

**CON-9 — ПОДТВЕРЖДЕНО.** `decisions.tsv` указывает `15-legal-documents_EN.md:12`, а «Who the operator is» стоит в :23 в обеих витринах. Правка обновила только RU-якорь (21→22).

Дальше очевидно ровно одно: этот вердикт нужен ведущему панели, решать, что брать в работу, будет он.

`2026-09-15 · day52 · в xor.ad не закоммичен docs/reviews/PANEL_2026-09-15_stage-s1.md · фоновых задач нет`

## Сводка ведущего

**Опровергнуты:** AUT-3 (пороги сторожей — не сроки хранения, ворота С4 их ловить не должны), AUT-4
(`FACES` — осознанное переопределение), DATA-2 (§8 — логическая модель без типов и индексов).

**Закрыто пачкой исправлений (xor.ad `9e3045c`, sosed.place `589ba06`, neighbro.place `52d9c39`):**

| Находка | Уровень | Что сделано |
|---|---|---|
| CON-1 | дефект | экраны 8 и 14 обеих витрин: решение по квитанции на устройстве и на почту, если оставлена; снятые фразы в реестре |
| SEC-3 | дефект | `batch-edit.py` отклоняет путь за корень группы; проба 5a |
| DATA-4 | мелочь | `batch-edit.py` пишет всё во временные файлы и затем `os.replace` |
| DATA-5 | мелочь | повтор фразы внутри пачки — одна строка реестра; проба 5b |
| LAW-3 = CON-5, DATA-6 | мелочь | `check-legal-bar.mjs`: редакция из трёх документов, документ без даты или с неизвестным месяцем — красное; проба на копии |
| SEC-1, SEC-2, DATA-3 | дефект | §6: хранилище `ss-receipts`/`nb-receipts` отдельно от личности, `POST` с кодом в теле, один статус и тело, `no-store`, общее минимальное время, лимит в памяти; точка вместо текста на общем устройстве; тест 16.9a точнее |
| LAW-2 | мелочь | устройство само спрашивает и ставит точку; достаточность для ст. 16(5) — вопрос 31 юристу |
| LAW-1, CON-2, CON-3 | дефект | пункт `dsa.receipt.unbuilt`: форма, политика, реестр ст. 30 и DPIA — вместе с кодом квитанции |
| DATA-1 | дефект | §9: обещанный помесячный счётчик описан; пункт `dsa.notice.counter` — сейчас только `DELETE` |
| LAW-4 | мелочь | страница изменений называет квоту «четыре за скользящий час» |
| AUT-1, AUT-2 | дефект | С1 — одно письмо на порог, эскалация через резервный транспорт; С3 — письмо на новое надгробие, проверка `≥ 1` |
| CON-4 | мелочь | экран 15: три документа и страница изменений |
| CON-6 | мелочь | маршрут Э4 называет ворота С4 |
| CON-7 | мелочь | runbook ссылается на С5 и `node.external.pinger` |
| CON-8 | мелочь | `watchdogs.unbuilt` (С1–С2, legal) и `watchdogs.jobs.unbuilt` (С3, product) |
| CON-9 | мелочь | EN-якорь решения экрана 15 — строка 23 в обеих витринах |

**Машинные проверки:** `harden-cycle.sh` — 18 из 18; пробы `test_batch-edit.sh` — 10 из 10; проба
`check-legal-bar.mjs` на копии — зелёная на настоящих файлах, красная без даты и при более поздней дате правил.

**Поймано процессом, а не панелью:** `harden-cycle.sh` дважды остановил коммит — на непереадресованном
якоре и на снятой фразе экрана 11, оставшейся в соседнем абзаце; первый раз его вывод скрыл, какие ворота
красные, и был исправлен.

## Нарезка на задачи

Открытых находок этой панели нет. Этап Э1 закрыт; дальше — Э2 маршрута: экраны и механика.
