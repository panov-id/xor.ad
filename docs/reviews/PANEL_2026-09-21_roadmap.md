# Панель ревью · 21.09.2026 · роадмап

- **Что ревьюили:** `docs/roadmap_RU.md`, `docs/roadmap_EN.md` — переписаны
  21.09.2026 поверх снимка от 7 июля; ветка `day56`, не закоммичено.
- **Линзы:** Безопасность, Данные и СУБД, Протоколы и стандарты, Эксплуатация,
  Согласованность. Плюс один состязательный опровергатель по 16 заявкам.
- **Воспроизвести:** `git diff HEAD -- docs/roadmap_RU.md docs/roadmap_EN.md`.

## Сырые находки

Отчёты агентов — по существу как вернули: ссылки, уровни и формулировки
находок сохранены, сокращены только связки.

### Линза «Данные и СУБД» — сырьё

**Подтверждено (ПРОВЕРЕНО)**
- «76 коммитов впереди origin/dev» (`docs/roadmap_RU.md:39`): `git rev-list --count origin/dev..HEAD` → 76.
- 6 из 11 таблиц (`roadmap_RU.md:64-67`): в миграциях есть `identities`, `sessions`, `vault_shares`, `legal_acceptances`, `feed_messages`, `identity_stats`; нет `likes`, `matches`, `match_participants`, `blocks`, `support_requests` — ни CREATE, ни под другим именем (ALTER только по `api_keys`, `dsa_statements`, `jobs`). Дополнительно есть `identity_appearance`, `nonces`, `session_invites` — вне списка §13, это не противоречие.
- Миграции 025–029 существуют в `relay/node/db/` (номера 011, 018, 019 пропущены — не дефект).

1. **Дефект дизайна — `roadmap_RU.md:55` «вердикт в `lib/feed_verdict.ts`» подаёт вердикт как сделанный.** `relay/node/src/lib/feed_verdict.ts:10-18`: «There is no moderator here yet… nothing calls `publish()` on its own» — модель не подключена, очередь разбирается руками, без присмотра работает только чистильщик, удаляющий просроченное. «Модерация до публикации через очередь» (`chat_RU.md` §13 п.2) на сервере не закрыта. Правка: в строку шага 2 — «модератор не подключён: `publish()` зовут только руками, без присмотра — лишь чистильщик».
2. **Противоречие / мелочь — шаг 1 `[x]` (`roadmap_RU.md:54`)**, а проверка имени по §13 (`chat_RU.md:2783-2790`) перенесена на первую публикацию, т.е. живёт в шаге 2 и зависит от того же неподключённого модератора. Сам шаг 1 от этого не ломается, но стоит упомянуть проверку имени в долге шага 2.
3. **Мелочь — `~30%` для шагов 1–4 (`roadmap_RU.md:20`)** защитимо по порядку (шаг 1 готов, шаг 2 частичный, 3–4 ноль таблиц), но выкладки в документе нет. Совет: заменить на «1 из 4 шагов готов, 1 частично» или привязать к счёту таблиц (6/11).
4. **Мелочь — шаг 3 (`roadmap_RU.md:56`) «таблицы `likes` нет»,** но `identity_stats` уже несёт `likes_received`/`likes_given` (`db/025_feed_and_stats.sql:75-76`) и `like_count` (`:42`). Точнее: «схема счётчиков есть, таблицы `likes` и маршрутов нет» — иначе строка расходится с перечнем шести таблиц на `:64`.

**Проверено, расхождений нет:** гео-округление (`feed.ts:20`, `quantise`/`band`) и выдача по пересечению кругов (`feed.ts:192`, `:354`) в коде есть; `delivered` считается (`feed.ts:409`). Экран ст. 17 (`roadmap_RU.md:87`) верно открыт: в `src/` его нет, только упоминание в `statements.ts:75`.

**Не проверено:** «025–029 не применялись ни на одном контуре» (`roadmap_RU.md:40`) — контуры (`schema_migrations` на dev/staging/prod) не опрашивал, только код; остаётся заявкой. `roadmap_EN.md` построчно с RU не сверял (grep делал по RU).

### Линза «Безопасность» — сырьё

Вывод: роадмап не приписывает закрытых дыр напрямую, но умалчивает — пять открытых пунктов безопасности из трекера в нём не названы ни разу, а слой «платформа ~80%» и шаг 1 `[x]` их не учитывают.

**Вижу дефект**

1. **Дефект дизайна — шаг 1 отмечен `[x]` «сделано и проверено» (`roadmap_RU.md:54`, легенда `:8`), хотя у маршрутов этого шага открыты хвосты безопасности:** `POST /sessions/invite` без предела по адресу (P2, `open-work_RU.md:2152-2159`; пункт `transfer.invite.address.limit`, `reviews/PANEL_2026-09-21_steps1-2.md:904`); окно ±5 мин у `/vault/init` и `/sessions/invite` при неиспользуемой таблице `nonces` (`PANEL_2026-09-21_steps1-2.md:365-367`); параметры Argon2id для `auth_hash` не записаны (там же `:370-371`). Плюс миграция `022` не проверена на проде — `day56` не выкачена (`roadmap_RU.md:39-40`). Правка: `[~]` и перечислить P2/nonce/Argon2 в строке шага 1.
2. **Противоречие — приём по ст. 16 `[~]` (`roadmap_RU.md:37-38`) без причины переезда:** WAF всех зон режет тела с цитатами атак, `customWafRulesLimit` = 0, исключений нет (`open-work_RU.md:1066-1069`, G12/G13). То есть на проде уведомление, цитирующее `<script>`, сейчас не принимается — это не видно из снимка, а слой «Юридическое и DSA ~80%» (`roadmap_RU.md:24`) это игнорирует. Правка: одной строкой назвать, что режется на `api.relay`, и не считать ст. 16 закрытой до выката.
3. **Дефект дизайна — шифрование резервных копий (P4, `open-work_RU.md:2171-2207`) в роадмапе отсутствует,** есть только перенос приватной половины ключа (`roadmap_RU.md:44-45`), а EN `roadmap_EN.md:19` включает backups в слой на ~80%. Трекер при этом противоречит себе: `:2199-2205` «ключа на проде ещё нет, действие владельца» — если так, дамп всей базы (доли ключей хранилища, почты заявителей) лежит у третьей стороны открытым текстом, а это ст. 32 GDPR, которую Hetzner переложил на нас (`:2173-2176`). Правка: отдельный `[ ]` «P4: ключ копий на p1/n1 + учение по восстановлению» с замером наличия `BACKUP_PUBLIC_KEY`; согласовать строку `:44` с `open-work_RU.md:2199`.
4. **Противоречие — лимиты по адресу в памяти узла, обнуляются пересозданием контейнера (`open-work_RU.md:894`, `:971-972`);** пункт про N-кратный порог на пуле из N узлов в трекере не найден (`PANEL_2026-09-21_steps1-2.md:369-370`). На этих лимитах держится принятая цена P1 (`open-work_RU.md:2141-2149`). В роадмапе ни слова. Правка: строка в разделе 1 «лимиты не переживают рестарт и не делятся между узлами».
5. **Мелочь — раздел 5 перечисляет только хвосты `day-fixes` (`roadmap_RU.md:89-97`),** а хвосты второго отчёта `PANEL_2026-09-21_steps1-2.md` (пункты 5–19 нарезки «не трогались», `:910`) не упомянуты. Правка: добавить второй отчёт с числом открытых.
6. **Мелочь — принятая цена P1 (возраст подростка вычислим, лишь дороже, `open-work_RU.md:2146-2148`) не названа;** шаг 2 `[~]` (`roadmap_RU.md:55`) без неё. Правка: дописать к шагу 2 «возраст автора вычислим — принятая цена, §4.2».

**Не нашёл, где описано:** G6 (проверка длины шифротекста, `open-work_RU.md:1169`) и G9 (ключ нативного клиента, `:1261`) — открыты, это отложенное, а не дыры, но в роадмапе нет. Статус находки «оракул бумажного кода» (`PANEL_2026-09-21_steps1-2.md:289`, `:870`) — в отчёте закрыта коммитом (`:900`), в роадмапе не отражено; без ссылки на коммит читателю не видно, что критичное закрыто. EN-версия длиннее RU на 6 строк (107 vs 101) — построчно пары не сверял.

**Проверено:** чтением указанных файлов. Живые URL не дёргал — замеры доступности `roadmap_RU.md:31-38` не перепроверены. Наличие ключа копий на боксах не проверено.

### Линза «Протоколы и стандарты» — сырьё

Итог: «~80%» для раздела 4 не обосновано — раздел из 6 пунктов, 3 из них `[x]`, и в нём отсутствует несколько открытых обязательств с последствиями по GDPR и DSA.

1. **Критично — незакрытая передача данных Bunny за ЕЭЗ (гл. V GDPR) не упомянута, а раздел `docs/roadmap_RU.md:82-84` ставит `[x]`.** `docs/article-30-register_RU.md:276`: «SCC нет, п. 4.6 разрешает обработку по миру… передача открыта». Открытая правовая основа передачи — самый весомый юр-пункт, и его нет в дорожной карте. Правка: добавить `[ ] Передача Bunny за ЕЭЗ — SCC или замена (ст. 44–46 GDPR)`.
2. **Критично — резервные копии не шифруются (`docs/open-work_RU.md:2171`, P4) — нарушение ст. 32 GDPR**, а по DPA Hetzner (приложение TOM) шифрование — наша обязанность. В roadmap раздел 1 (`:44`) упоминает только ключ копий, не само шифрование. Правка: пункт `[ ] P4 шифрование копий/диска (ст. 32 GDPR)` в раздел 4.
3. **Дефект дизайна — `roadmap_RU.md:83` «Реестр уведомлений… `GET /statements`» `[x]` при том, что `:37` `[~]` (приём ст. 16 не выкачен в прод)** — вместе читается как работающий на проде механизм ст. 16/17, а это не так. Плюс `GET /statements` отдаёт максимум 100 строк без курсора (`relay/node/src/routes/statements.ts:53-60`, P3 `open-work_RU.md:2161`) — со 101-й мотивировки автор её не получит, это ст. 17(1) DSA. Правка: `[~]` и дописать «P3 без курсора; ст. 16 на прод не выкачен».
4. **Дефект дизайна — срок хранения брошенной личности: «уборщик не построен» (LAW-7), `docs/article-30-register_RU.md:58`.** Реестр обещает срок, которого в коде нет — ст. 5(1)(e) GDPR (ограничение хранения). В roadmap отсутствует. Правка: `[ ] Уборщик брошенных личностей (LAW-7)`.
5. **Противоречие — реестр ст. 30 GDPR и процедура утечки (`docs/breach-procedure_RU.md`, 72 ч, ст. 33 GDPR) в roadmap не упомянуты ни как `[x]`, ни как `[ ]`**, хотя «~80%» на них, видимо, опирается. Правка: две строки `[x]` со ссылками — плюс оговорка, что в реестре ст. 30 строка GA4 помечена «живой прод НЕ ПРОВЕРЕН» (`article-30-register_RU.md:279`).
6. **Пропущенный срок — J9: перепроверка статуса микропредприятия до 05.08.2027 (`open-work_RU.md:612`).** От статуса зависит освобождение по ст. 19(1) DSA от ст. 20–28 (кроме ст. 24(3)); потеря статуса сразу включает внутреннее обжалование ст. 20, отчётность ст. 24, защиту несовершеннолетних ст. 28. Единственное обязательство с идущим сроком — и его нет в roadmap. Правка: `[ ] J9 — до 05.08.2027`.
7. **Мелочь — формулировка `docs/dsa/README_RU.md:84` сверена:** исключение из освобождения — именно ст. 24(3) (сведения о среднем числе активных получателей по запросу), верно. Регулярные отчёты о прозрачности не нужны (ст. 15 DSA освобождена для микропредприятий по ст. 15(3); ст. 24(2) — по ст. 19(1)). Но roadmap стоит явно сказать «отчётности нет — ст. 15(3), 19(1)», иначе отсутствие выглядит пропуском.
8. **Мелочь — минимальный возраст `roadmap_RU.md:85/:101` «13+, юрист может 16+»** без привязки к ст. 8 GDPR (согласие ребёнка на онлайн-услуги: 16 по умолчанию, государства вправе снизить до 13). При этом основание обработки по LAW-9 — не согласие; если так, ст. 8 не применяется — это надо сказать, иначе вопрос выглядит открытее, чем есть.

**Не нашёл, где описано:** LAW-* как отдельного списка в `open-work_RU.md` нет — встречаются только внутри текста реестра и SPEC; проверить, все ли LAW закрыты, не смог (LAW-7 — открытое, нашёл). Возрастной порог в юр-документах репозитория neighbro.place (`landing/legal`) grep по `.html` не дал — сверить 13+ с Terms не смог, НЕ ПРОВЕРЕНО. Юр-документы репозитория `sosed.place/legal` не открывал.

**Проверено:** только чтением файлов по указанным `file:line`; живых эндпоинтов не вызывал.

### Линза «Эксплуатация» — сырьё

**Подтверждено живым замером**
- `curl https://api.relay.panov.id/health` → 200, `node:p1-prod`, `image:v2026.9.11-g8f89e7a`, `database:ok`, `mail:resend`, `brands:[sosed,neighbro]` — совпадает с roadmap_RU.md:19, :31–32.
- sosed.place, neighbro.place, xor.panov.id → 200 — совпадает с :33.
- report.relay.panov.id/ → 404 — совпадает с :37–38.

**Находки**

1. **Дефект дизайна — «404 на корень» как признак приёма (roadmap_RU.md:37–38) ничего не доказывает.** 404 отдаст и мёртвый за Caddy узел, и любой чужой хост. Реальный путь приёма — `POST /report` (relay/node/src/main.ts:44). При этом `curl https://report.relay.panov.id/health` → 200, `node:p1-prod`: хост жив и это прод-узел. Правка: «`/health` на report-хосте 200, `p1-prod`; приём — `POST /report`» (тело на проде не слать: сквозные проверки только на staging по open-work_RU.md:745–750).
2. **Противоречие — «копии» внутри слоя ~80% (roadmap_RU.md:19) подтверждены только `/health`,** который о копиях ничего не говорит. Кроме того, open-work_RU.md:728 — **«L1. Лист ожидания в резервную копию не входит — открыто»**: waitlist живёт в хранилище Bunny (relay/node/src/routes/waitlist.ts), а relay/wizard/backup-postgres.sh копирует только Postgres. Единственные накопленные пользовательские данные не копируются, а в роадмапе L1 нет. Там же открыта отдельная зона `relay-backups` (копии сейчас в `sosed-waitlist-dev` рядом с файлами витрины). Правка: пункт `[ ]` L1 в §1 и «Проверено» — дата последнего учения восстановления (`scripts/verify-backup-restore.sh`, последнее найденное — 07.08.2026, open-work_RU.md:722), а не `/health`.
3. **Мелочь/неполнота — контур dev/staging (бокс n1) в §1 не показан**, упомянут только в «начинать с dev (n1)» (:41) и в строке почты (:84). Нет ни образа, ни доступности. Staging заведён 27.07.2026 (open-work_RU.md:26); в памяти проекта dev на `sha-c9fc20b`, staging себя не называет. Живьём не мерил — контур за белым списком IP. Правка: одна строка «n1 dev/staging: образ X, замер Y / не замерено — закрыт белым списком».
4. **Дефект дизайна — слоя эксплуатации нет: мониторинг/алертинг, откат, наблюдаемость.** Откат описан только в relay/RELEASE_RU.md:62 («задеплоить предыдущий `:vX.Y.Z`»); отработан ли на живом контуре — не нашёл. Мониторинга/оповещений (uptime, алерты) в open-work_RU.md и relay/*.md не нашёл (grep «монитор|alert|uptime» по делу пусто). Правка: отдельная строка сводки «Эксплуатация: копии/восстановление/откат/мониторинг» с честными `[~]`/`[ ]`.
5. **Мелочь — пункт :39–40 (76 коммитов впереди `origin/dev`)** я не перепроверял (git не запускал — не входило в задачу).

**Где я не смог найти описание (не дефекты, а пробелы поиска):** мониторинг/оповещения — описания нигде; отработанный откат — только регламент (RELEASE_RU.md:62); учение восстановления после 07.08.2026 — в open-work не найдено новее, живьём не проверял.

EN-версию (roadmap_EN.md) не сверял; правки п. 1–4 нужно внести в обе.

### Линза «Согласованность» — сырьё

**Вывод:** RU и EN совпадают построчно; главный дефект — битые ссылки «§2» из трёх репозиториев на раздел, который сменил тему.

**Противоречие — битые ссылки на §2 (дефект вижу)**
- `sosed.place/docs/17-offer_RU.md:49`, `sosed.place/docs/17-offer_EN.md:49`, `neighbro.place/docs/17-offer_RU.md:49`, `neighbro.place/docs/17-offer_EN.md:49` отсылают к `roadmap_*.md §2` за юридическим вопросом оффера. Теперь §2 — «Продукт: порядок сборки»; оффер лежит в §4 (`roadmap_RU.md:86`, `roadmap_EN.md:91`).
- То же — `xor.ad/docs/legal-review-brief_RU.md:134` и `legal-review-brief_EN.md:135`.
- Правка: заменить «§2» на «§4 (Юридическое и DSA)» во всех шести местах, либо ссылаться на заголовок раздела, а не номер (номер уже раз уехал).

**Дефект — выпавшие ещё-верные факты (дефект вижу)**
1. Три юр-вопроса по сохранённому офферу: старый `HEAD:docs/roadmap_RU.md:42` их перечислял (чем связано заведение при истёкшем `discount_until`; что с копией снятого оффера; оферта или приглашение к оферте). Новый `roadmap_RU.md:86` / `EN:91` говорит только «три вопроса», а 17-offer и legal-review-brief отсылают сюда за деталями (третий вопрос — «оферта или нет» — в 17-offer:49 не назван). Правка: вернуть три вопроса одной строкой или сослаться на `legal-review-brief_*.md`.
2. `support@neighbro.place` — старый `[ ]` пункт (строка 40) исчез без следа. `article-30-register_RU.md:17-18` называет `support@sosed.place` / `support@neighbro.place` действующими адресами; где сказано, что ящики заведены, не нашёл — поэтому не знаю, выпал открытый или закрытый пункт. Правка: `[x]` с датой в §4 или сохранить `[ ]`.
3. Открытые вопросы: из трёх остался один; пропали «набор акцентов» и «шрифт-эталон» (старые 78–79). Где решены — не нашёл. Если решены — так и сказать, а не удалять молча.
4. Решение по sosed («деплоить витрину или нет», старая 72) и «зеркалирование на sosed» (57) — первое закрыто фактом (витрина 200, `roadmap_RU.md:33`), по зеркалированию статуса нет нигде.
5. «Экран показа принятых документов» (старая 43) — пропал; где закрыт, не нашёл.

**Мелочь**
- `roadmap_RU.md:55` «миграция `025`–`029`» — единственное число; EN (`:58`) «migrations». RU → «миграции».
- `roadmap_RU.md:20` и `:52` «миграции и маршруты в `relay/node`» — файлы миграций в `relay/node/db/`; точнее указать.
- `roadmap_RU.md:35` дата вывода Supabase 22.07.2026 совпадает с `open-work_RU.md:468`; но старая шапка давала «prod выкачен 27–28.07.2026» — в новом тексте дата выката прода пропала. Не ошибка, но потеря факта.
- `roadmap_RU.md:96` / `EN:101` — `migrate_control_state.ts` без пути `tools/`, соседний файл — с путём; выровнять.

**Проверено, расхождений нет:** RU↔EN — числа (76, 7/10, 9 страниц, 11/6/5), статусы, даты, имена файлов совпадают по всем разделам. 76 коммитов: `git rev-list --count origin/dev..day56` = 76; `origin/dev` = `cd21688` 2026-09-19. 9 страниц в `xor.ad/panel/src/pages` — подтверждено `ls`. Шесть таблиц есть в `relay/node/db`, пяти нет — совпадает с `chat_RU.md:2817-2829` (одиннадцать с 14.09.2026). Файлы существуют: `relay/node/db/025`–`029`, `relay/node/src/lib/feed_verdict.ts`, `relay/node/src/chat/relay.ts` (501 на строке 39), `relay/node/tools/prune_dsa_records.ts`, `relay/node/tools/migrate_control_state.ts`, `neighbro.place/prototype/neighbro-app-proto.html`, `docs/reviews/PANEL_2026-09-21_day-fixes.md`. 🟡 `sheets/`, `kit/` (`roadmap_RU.md:74`/`EN:78`) в корне не существуют — это `xor.ad/panel/design/sheets` и `xor.ad/panel/design/kit`; указать полный путь.

**Не проверял:** README трёх репо на прочие факты роадмапа (grep «roadmap» в README пуст — построчно не сверял). Живые замеры (200, `/health`, образ, 404 report) не повторял. «7 из 10» по панели не сверял с `PANEL_2026-09-21_day-fixes.md`.

## Опровержения и проверка ведущим

Опровергатель — отдельный агент, по умолчанию «опровергнута». Вердикты:

| Заявка | Вердикт | Чем |
|---|---|---|
| D1 модератор не подключён | подтверждена | `relay/node/src/lib/feed_verdict.ts:11-17`; ведущий перечитал сам |
| D4 счётчики лайков уже в схеме | подтверждена | `relay/node/db/025_feed_and_stats.sql:42`, `:75-76` |
| S1 хвосты шага 1 | частично | P2 открыт (`open-work_RU.md:2152-2159`); окно ±5 мин, `nonces`, Argon2 панель сама пометила «это НЕ находки» (`PANEL_2026-09-21_steps1-2.md:363`), подметальщик `nonces` — `10e1ebc` |
| S2 WAF режет цитаты | подтверждена | `open-work_RU.md:1064-1069` |
| S3/P2 шифрование копий | частично | механизм сделан 21.09 (`7f5365b`, `4a4da9b`, `open-work_RU.md:2194`); открыто только действие владельца — ключ на боксах (`:2204-2207`); ведущий перечитал сам |
| S4 лимиты в памяти узла | подтверждена | `open-work_RU.md:894`, `:970-972` |
| S5 пункты 5–19 первой панели | **опровергнута** | закрыты `38087bb`…`d176249`, поправка `PANEL_2026-09-21_steps1-2.md:962-983` |
| O1 report-хост жив, приём `POST /report` | подтверждена | `curl …/health` 200 `p1-prod` (опровергатель и ведущий), `relay/node/src/main.ts:44` |
| O2 L1 лист ожидания без копии | подтверждена | `open-work_RU.md:728`, учение `:722` |
| O4 мониторинга нет | **опровергнута** | `relay/local/observability/alerts.yml:123`, `scripts/check-metrics-exist.sh`, `docs/runbook-node-down_*.md`, `6d82b22`; работа на проде не проверена |
| C1 ссылки на §2 | подтверждена | шесть строк; ведущий перечитал `grep` |
| C2 `support@` не заведён | **опровергнута** как открытый пункт | `article-30-register_RU.md:17-18` — действующий адрес; существование ящика не проверено |
| C3 `sheets/`, `kit/` | подтверждена | `panel/design/sheets`, `panel/design/kit` |
| L1 Bunny без SCC | подтверждена | `article-30-register_RU.md:276`; ведущий перечитал |
| L3 `/statements` без курсора | подтверждена | `relay/node/src/routes/statements.ts:57`, P3 `open-work_RU.md:2161-2169` |
| L4 LAW-7 уборщика нет | подтверждена | `article-30-register_RU.md:58` |
| L6 J9 до 05.08.2027 | подтверждена | `open-work_RU.md:612` |

Не отданы опровергателю как несущественные: мелочи линз (число, пути,
выпавшие открытые вопросы) — закрыты правкой напрямую.

## Сводка ведущего

Закрыто тем же заходом правкой роадмапа (RU и EN):

- юридический раздел возвращён на номер §2 — шесть ссылок из `sosed.place`,
  `neighbro.place` и `xor.ad` снова верны, чужие репозитории не тронуты;
- шаг 1 → `[~]` (P2); шаг 2 — модератор не подключён, проверка имени, цена P1;
  шаг 3 — схема счётчиков есть;
- добавлены: Bunny без SCC, LAW-7, J9, P3, реестр ст. 30 и процедура утечки,
  отсутствие отчётности по ст. 15(3)/19(1), WAF, лимиты в памяти, L1, ключ
  копий как действие владельца, n1 без замера, эксплуатация отдельным слоем;
- report-хост мерится `/health`, а не 404 на корень;
- три вопроса по офферу возвращены; пути `panel/design/…`, `tools/…`;
- проценты пересмотрены вниз: платформа 80→70, юридическое 80→60,
  шаги 1–4 30→25.

Отброшено: S5, O4 (как «мониторинга нет»), C2 — опровергнуты. Окно ±5 мин и
Argon2 из S1 — сама первая панель назвала не находками.

## Нарезка на задачи

| # | Уровень | Задача | Цена |
|---|---|---|---|
| 1 | критично | Передача Bunny за ЕЭЗ: SCC или замена хранилища | решение владельца + юрист |
| 2 | критично | Ключ копий на p1 и n1, приватная — в хранилище паролей, учение восстановления | полчаса владельца + прогон визарда |
| 3 | дефект | L1: лист ожидания в копию | час |
| 4 | дефект | LAW-7: уборщик брошенных личностей | два часа |
| 5 | дефект | P3: курсор у `GET /statements` | час, контракт |
| 6 | дефект | P2: предел по адресу на `/sessions/invite` | час |
| 7 | дефект | Модератор ленты: подключить модель по §8.14 | крупно, отдельная нарезка |
| 8 | срок | J9: статус микропредприятия до 05.08.2027 | напоминание |
| 9 | неполнота | Замер n1 (dev/staging) — образ и доступность | четверть часа с адреса из белого списка |

## Поправка, 21.09.2026 вечер

Задача 4 нарезки (LAW-7, уборщик брошенных личностей) была ложной: уборщик
построен 20.09.2026 (`relay/node/src/lib/identity_sweeper.ts`, тесты
`identity_sweeper.test.ts`: год без сессии закрывает, через 30 дней удаляет).
Линза «Протоколы» взяла находку из реестра ст. 30, где стояло «уборщик не
построен», а ведущий перенёс её в роадмап, проверив строку реестра, но не код.
Нашлось вечером, когда кворум выбрал LAW-7 следующим куском и перед работой был
прочитан уборщик. Реестр ст. 30 RU/EN и роадмап исправлены.
