# Панель ревью · 21.09.2026 · шаг 5: транспорт чата

- **Что ревьюили:** `relay/node/src/chat/relay.ts`, `routes/chats.ts`,
  `routes/matches.ts` (открытие чата), `db/031`–`033`, `main.ts` (ответ 101);
  коммиты `8b9572b`, `df1781f`, `e617e97`, `4c492cd` ветки `day56`.
- **Линзы:** Безопасность, Данные и СУБД, Протоколы и контракт. Все три
  работали чтением, без прогона.
- **Перепроверка:** ведущий — тест, увиденный красным, на каждую правку.

## Находки по существу

### Безопасность
1. Критично — сокет переживает заморозку сессии: `session_frozen` никто не слушал.
2. Критично — блокировка не действует на чат: ни один путь не читал `blocks`.
3. Дефект — ушедший участник (`gone_at`) остаётся в комнате; кода 4003 нет.
4. Дефект — нет потолка комнат и предела на билеты.
5. Дефект — нет проверки Origin на апгрейде (не CSWSH: билет без подписи не добыть).
6. Дефект — сессия, замороженная в 30 секунд билета, открывает комнату.
7. Мелочь — билет отражается в заголовке ответа; сбой базы выдаётся за 4001.
   Не нашёл: утечки между чатами, повтора и фиксации билета, оракула присутствия.

### Данные и СУБД
1. Живой чат + истёкший мэтч: взаимный лайк рождает новый мэтч, согласие падает
   на `chats.pair_key` — 503 на каждое нажатие.
2. Уборщик очереди сканирует таблицу: нет индекса по `created_at`.
3. `UPDATE identity_stats … IN (subquery)` без упорядоченной блокировки.
4. Повтор `local_id` после подтверждения доставит дубль — дедупликация на клиенте.
5. Вытеснение при равных метках времени решает uuid — безвредно.

### Протоколы и контракт
1. Субпротокол: протокол требует `xor.p1, ticket.<t>`, код брал голый билет; 4004 нет.
2. 4002 не сделан. 3. 4003 не сделан. 4. 1001 и 1011 не сделаны.
5. `GET /chat` в контракте — «слот, 501». 6. Таблица §4.3 говорит «спека».
7. `/received` 400 не в контракте; `local_id` без `format: uuid`.
8. Устаревшая шапка `chats.ts`. 9. 429 раньше 404 у `/messages` (SEC-22).

## Проверка ведущим и что закрыто

| Находка | Чем |
|---|---|
| Блокировка в чате (Без. 2) | тест «a block closes the chat to both» красный до правки |
| Второй мэтч при живом чате (Данн. 1) | тест красный до правки; первая версия теста прошла зря — проверяла не тот лайк |
| `xor.p1` и 4004 (Прот. 1, Без. 7) | тест depth красный до правки; узел отвечает `xor.p1`, билет не отражает |
| 4002 при заморозке (Без. 1, Прот. 2) | тест depth; контроль — без закрытия красный |
| Заморозка в окне билета (Без. 6) | `DELETE … USING sessions … frozen_at IS NULL` — чтением |
| 1011 на сбой базы (Без. 7, Прот. 4) | чтением |
| Одна комната на (чат, сессия), предел на билеты (Без. 4) | чтением |
| 429 после 404 (Прот. 9) | чтением |
| Упорядоченная блокировка и `ON CONFLICT` при согласии (Данн. 1, 3) | тесты согласия зелёные |
| Контракт `GET /chat`, протокол §4.3 и §6 (Прот. 5–7) | ворота контракта и парности |

Попутно в тестах depth: предел «10 регистраций в час с адреса» валил прогон —
стенд теперь даёт клиентам разные адреса через токен происхождения; ожидание
закрытия сокета получило предел 5 с (прогон висел 10 минут); тест погашенного
билета падал раз из трёх — закрывал сокет до рукопожатия, билет не гасился;
после правки 8 прогонов из 8 зелёные.

## Нарезка — открыто

| # | Задача | Цена |
|---|---|---|
| 1 | 4003: закрытие комнаты при конце беседы и `gone_at`; `DELETE /chats/:id` | два часа |
| ~~2~~ | ~~1001 при остановке узла~~ — закрыто 22.09.2026: `closeAllRooms()` на SIGTERM/SIGINT в `main.ts`; тест | — |
| ~~3~~ | ~~Индекс `pending_deliveries (created_at)`~~ — закрыто 22.09.2026: `db/037`; тест по `pg_indexes` | — |
| ~~4~~ | ~~Белый список Origin на апгрейде~~ — закрыто 22.09.2026: 403 чужому origin, терминал без origin проходит; тест красный при инверсии | — |
| 5 | Блокировка закрывает живую комнату сразу, а не на следующем запросе | час, вместе с 1 |

## Дозакрыто

- Задача 1: конец беседы. `DELETE /chats/:id` кончает её у обоих и чистит
  очередь; свой срок пишется при отправке; `lib/chat_sweeper.ts` (задача
  `sweep_chats`, раз в минуту) ставит `gone_at` по сроку и удаляет беседу,
  кончившуюся у обоих; `NOTIFY chat_closed` закрывает комнаты 4003. Три теста,
  красные до кода. Открытый пункт `chat.janitor.missing` закрыт; исполнители
  сроков `chat.idle.ttl` и `nonce.ttl` в реестре — настоящие задачи.
- Задача 5 (мгновенное закрытие комнаты при блокировке) ждёт маршрута
  блокировки: `POST /blocks` ещё не построен, он и пошлёт `chat_closed`.
- Задача 5 закрыта: `POST /blocks` построен (`routes/blocks.ts`), кончает общую
  беседу у обоих и шлёт `chat_closed`; сквозной тест depth — комната
  заблокированного закрывается 4003 сразу; без оповещения тест красный.
