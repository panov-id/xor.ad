# Роадмап — чеклист

Статус на 7 июля 2026. `[x]` — сделано, `[~]` — частично, `[ ]` — впереди.

## 0. Сделано (база)
- [x] Бизнес-логика и описания экранов (neighbro + sosed).
- [x] Лендинг neighbro (dark/gold, i18n 6 языков, вейтлист, PWA, иконки, config.js).
- [x] Интерактивный **прототип приложения** (single-file): сплэш `by PSYTICAN`, эйдж-гейт с согласиями, трёхколоночный рабочий стол со сворачиванием в рейлы, живая лента (refresh/auto), плюс/минус свайп, чат с лайкнутыми фразами, профиль-попап (имя/возраст/фильтр возраста/язык/тема/документы), 11 акцентов, свет/тьма.
- [x] Спека прототипа `docs/app-prototype-spec_RU/EN.md`.
- [x] **Terms & Conditions** `neighbro.place/legal/terms_RU/EN.md` (оператор PSYTICAN & PEJEDED, Кипр, 13+).
- [x] Деплой-инфра (3 окружения dev/UAT/prod), локальный Supabase+nginx, тесты (E2E/визуальные).

## 1. Обновление лендинга neighbro (ближайшее)
- [x] Решить: кнопка **свет/тьма** — рядом с переключателем языка.
- [x] Решить: логотип = цикл **акцентов**, подмножество из 5 (gold/crimson/teal/azure/violet).
- [x] Система тем на лендинге: токенизация цветов, `[data-mode="light"]`, `[data-theme]`, theme-color.
- [x] Автоопределение (до отрисовки, без мигания сплэша): **язык** из браузера; **свет/тьма** из `prefers-color-scheme`; **акцент случайный каждую сессию** (другой, чем в прошлый раз) — пока пользователь сам не выберет цвет/язык/режим (тогда его выбор фиксируется).
- [x] Лента-превью под новую механику: **люди + плюс, без дистанции**.
- [x] Кредит **by PSYTICAN** в футере.
- [x] Встроить **живые мокапы приложения** в рамках устройств (телефон + широкая рамка), шрифтами лендинга; перекрашиваются вместе с темой.
- [~] Перерисовать по **брутал-канону** (токены/акценты/свет-тьма — есть; полный редизайн — впереди).
- [x] **Тексты фишек**: секция «Почему иначе» (по району / плюс-пропуск / почему совпали / игры без правил), локализована на 6 языков. Story Say/Match/Fade оставлен.
- [x] **HTML-страница `legal.html`** (вкладки Terms/Privacy, EN/RU, рендер markdown, брутал-стиль, наследует тему) + ссылки в футере (локализованы).
- [x] Проверки: `overflow=0` (десктоп/мобайл), i18n новых строк, **E2E 6/6 прошли** (вейтлист sosed+neighbro).
- [x] Сплэш-лоадер на лендинге (как в прототипе): круг основного цвета, домик-лого, `NEIGHBRO` / `by PSYTICAN`.
- [x] Выравнивание секции мокапов (сбалансированная центрированная композиция: широкая рамка слева, телефон справа).

## 2. Юридическое / комплаенс
- [x] **Privacy Policy** `neighbro.place/legal/privacy_RU/EN.md` (GDPR-стиль; served-копии в `landing/legal/`).
- [x] **Убран ARC-номер** из всех документов, прототипа и памяти.
- [x] **Правила поведения**: запрет харассмента/буллинга/шейминга/анти-соц; запрет продаж товаров/услуг — реклама только через платный блок. В **Terms §8** и **Community Guidelines**; вкладка **Rules** в legal.html + ссылка в футере (6 языков).
- [x] Разобраны дубли `legal/`: оставлены `terms_/privacy_/community-guidelines_`; старые `terms-of-service_/privacy-policy_` удалены.
- [ ] Завести почту `support@neighbro.place`.
- [ ] Юр-ревью Terms (13+ вместе с офлайн-встречами — чувствительно).
- [ ] Экран/страница показа принятых документов (в аппе уже есть в профиле).

## 3. Экраны приложения (следующие)
- [ ] Экран **Say** (публикация): поле ≤128 симв., «сколько нас», радиус размытия локации.
- [ ] **Момент мэтча** (взаимный плюс → анимация «совпало» → чат открыт).
- [ ] Экран **Set location** (карта/поиск района вместо заглушки).
- [ ] **Игры без правил** (домино/шашки/шахматы, запуск по запросу, realtime).
- [ ] **Заморозка сессии** (self-lockout, серверный дедлайн).
- [ ] Поддержка/саппорт-кнопка, инвайты и поощрения.

## 4. i18n приложения
- [ ] Полный перевод строк приложения (как i18n на лендинге), 6+ языков; сейчас в прототипе только селектор языка.
- [ ] Языки push-уведомлений (см. `docs/pwa-push`).

## 5. Sosed (зеркалирование)
- [ ] Перенести обновления на **sosed.place** (красный акцент, советская эстетика; без ЛГБТ-фич — по решению).
- [ ] Синхронизировать READMEs всех трёх репозиториев (правило проекта).

## 6. Бэкенд / данные / деплой
- [ ] Реальные таблицы/RLS под сообщения, лайки, чаты, профиль, фильтр возраста, эфемерность (TTL/угасание).
- [ ] Realtime (вебсокеты через api-прокси) для чата и игр.
- [ ] Web Push (VAPID на витрину, анонимные подписки).
- [ ] Платежи PayPal → внутренний баланс (стикеры/реклама).
- [x] **dev + UAT задеплоены** (лендинг neighbro + панель; Supabase `vrkqnfonmaixuvfqsfzt`; Bunny зоны + api-прокси с Origin Host Header/WebSockets/кэш=0; DNS panov.id; GitHub-секреты). prod — позже. sosed — только провижн, файлы не деплоили.

## Следующая сессия (deploy follow-ups)
- [x] **Панель — вход:** админ забутстраплен, allow-list/site_url настроены, вход по magic-ссылке (`generate_link`) работает.
- [ ] **Почта/SMTP (вернуться):** Resend, домен `panov.id` — DKIM verified, MX добавлен на `send.panov.id` (форварды целы), ждём пропагацию/верификацию. Когда `status=verified`: прописать **Custom SMTP в Supabase Auth** (Management API `/config/auth`: `smtp.resend.com`, user `resend`, pass = sending-ключ, отправитель `no-reply@panov.id`) и послать тестовое письмо. Детали — память `email-smtp.md`.
- [ ] Удалить тестовую строку вейтлиста `deploy-check-1783441254@example.com` (или добавить `SUPABASE_SERVICE_ROLE_KEY` в `.env.deploy` — снесу сам).
- [ ] Проверить uat api-прокси (insert) и realtime по WebSocket (как на dev).
- [ ] Решить по **sosed**: деплоить витрину или пока нет.
- [ ] **prod** (когда uat принят): `run-wizard.sh prod` + DNS prod — это уже реальные домены `neighbro.place`/`sosed.place`/`xor.panov.id` (DNS у их регистратора, не только panov.id) + Bunny SSL/прокси.
- [ ] Определиться с флоу веток (day-ветки vs `dev`/`main`) — сейчас всё на `day4`, а деплой идёт из `dev`/`main`.
- [ ] Дальше по продукту: экраны приложения (Say/мэтч/Set location/игры), таблицы/RLS, Web Push, PayPal (разделы 3–6).

## Открытые вопросы
- Набор акцентов на лендинге (все 11 или подмножество).
- Реальный шрифт-эталон бренда (Unbounded на лендинге vs системный в прототипе).
- Минимальный возраст (13+ сейчас; юрист может настоять на 16+).
