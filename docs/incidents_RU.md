# Журнал инцидентов

Ведётся по ст. 33(5) GDPR: учёту подлежат **все** нарушения безопасности данных,
а не только те, о которых уведомили ведомство. Порядок действий — в
[`breach-procedure_RU.md`](./breach-procedure_RU.md).

Пустой журнал — нормальное состояние; отсутствующий журнал — нет.

Первая запись — ниже, и она про нас самих: утечку нашла панель ревью,
а не человек, и закрывать её пришлось в два приёма — узел и кеш.

| Обнаружено | Что произошло | Чьи данные | Ведомству | Людям | Что сделано |
|---|---|---|---|---|---|
| 10.09.2026, ~14:25 UTC | Прод отдавал `/metrics` без токена на трёх именах одного узла; в ярлыках рядов лежали **три настоящих идентификатора уведомлений по ст. 16**, а также счётчики по брендам, маршрутам и админке | заявителей и авторов по трём уведомлениям о незаконном содержании | **не сообщали** — идентификаторы без содержимого и без имён, риск для прав людей не высокий; решение записано здесь, как требует ст. 33(1) | нет (ст. 34 требует высокого риска) | Токен `METRICS_TOKEN` был написан 08.09.2026 и не выкачен; ярлык рядов строился из сырого пути. 11.09.2026 выкачен прод на `v2026.9.11-g8f89e7a`: `/metrics` отвечает 404 без токена, ярлык стал шаблоном маршрута. Отдельно сброшен кеш Bunny на зонах `xorad-report-prod` и `xorad-api-prod` — он раздавал копию ещё сутки после починки узла, с `max-age` в 30 дней. Проверено живьём на трёх хостах: 404, идентификаторов нет |

этот записывается сразу: с него идут 72 часа.
