# Система оформления приложения

**Решено 31.08.2026, первое решение этапа 1.** Ни один из двадцати экранов не
ссылался на источник визуальных решений, а рисовать двадцать макетов из трёх
разных источников — значит потом сводить их дороже, чем нарисовать заново.

**Структура берётся у панели, палитра — у витрин.** Дорогое здесь не палитра, а
структура: законы, роли токенов, тёмная тема, способ проверки — четыреста строк,
уже написанных для панели (`design-system_RU.md`). Палитра переносится за час.

Этот документ описывает **приложение**. У панели своя система, у витрин — свои
лендинги; расхождения между ними названы ниже поимённо, а не оставлены на глаз.

## Что измерено, прежде чем решать

Три источника были описаны как «частичные». Замер 31.08.2026 показал резче:

| источник | токенов | покрывает | не покрывает |
|---|---|---|---|
| лендинги | 13 и 12, общих 11 | цвет, шрифты | отступы, радиусы, тени, контролы, тёмную тему |
| июльский прототип | 14 | цвет, шрифты, форму приложения | системы за ним нет; с лендингом совпадают **три** имени |
| система панели | 9 ролей, законы, 400 строк | законы, роли, тёмную тему, контролы, раскладку, проверки | сделана под админку |

**Два факта, которых в маршруте не было.**

Первый: **лендинги разошлись между собой** — у одной витрины был `--ok`, которого
не было у второй, у второй `--mono` и `--sans`, которых не было у первой, а
шрифтовые стеки лежали вписанными в восемнадцать правил. Сведено 31.08.2026 к
общему набору из **15 имён**, и закреплено проверкой
(`scripts/check-landing-tokens.sh`): значения расходятся по замыслу — акцент это
бренд, — а имена расходиться не должны.

Второй: **утверждение «острые углы витрин» из системы панели неверно**. На живых
лендингах **37 объявлений радиуса и 17 разных значений**; острых углов там нет.
Значит шкалу радиусов приложение не наследует, а **заводит** — и это сказано
прямо, чтобы никто не искал источник, которого нет.

## Токены

Роли — по образцу панели, имена — витринные там, где они уже есть.

| Токен | Роль | Откуда |
|---|---|---|
| `--bg` | за всем | витрины |
| `--panel`, `--panel-2` | карточка; вложенная поверхность | витрины |
| `--fg`, `--muted`, `--muted-2` | текст, второй план, третий | витрины |
| `--border` | линия | витрины |
| `--accent`, `--accent-ink`, `--accent-text` | бренд; чернила поверх него; акцентный текст | витрины |
| `--ok`, `--err` | исход | витрины |
| `--sans`, `--mono`, `--disp` | три начертания | витрины |
| `--r-1`, `--r-2`, `--r-pill`, `--r-round` | шкала радиусов | **заводится здесь** |
| `--s-1` … `--s-12` | шкала отступов | **заводится здесь** |

**Шкала радиусов выведена из замера, а не выбрана на вкус.** Семнадцать значений
двух витрин распадаются на три кучи: мелкие 7–9 (пять применений), основная
11–20 (семнадцать) и «таблетки» 30–46 (шесть). Отсюда:

- `--r-1: 8px` — мелкое: значок, бейдж, поле ввода;
- `--r-2: 13px` — основное: карточка, кнопка, диалог;
- `--r-pill: 999px` — то, что задумано таблеткой;
- `--r-round: 50%` — круг: аватар, точка, кружок счётчика.

Четыре токена вместо семнадцати значений. Цена названа: часть лендинговых
скруглений при переносе сдвинется на 1–3 пикселя, и это правильный размен —
шкала из четырёх ступеней читается как решение, семнадцать значений читаются как
их отсутствие.

## Чем приложение отличается от панели

Дельта ведётся здесь и только здесь. Каждое отличие — с причиной.

- **Нажатие срабатывает по наведению.** Панель это отвергла для себя и прямо
  записала, что для витрины оно верно (`design-system_RU.md` §8): там указатель
  пересекает тулбар по пути, здесь — нет.
- **Радиусы свои** (выше). У панели 6px под её контролы; у приложения шкала из
  четырёх ступеней под карточки ленты и пузыри беседы.
- **Дисплейное начертание — по бренду.** Одна витрина применяет его девять раз,
  вторая не применяет вовсе и указывает `--disp` на тот же стек, что `--sans`.
  Приложение наследует это как есть: `--disp` существует всегда, а совпадает ли
  он с `--sans` — решает бренд.
- **Набор акцентов — бренд, выбор внутри набора — настройка** (решено 15.09.2026,
  экран 22 витрин). У панели одиннадцать акцентов на выбор; у приложения набор
  приходит с витриной — шесть у sosed.place, пять у neighbro.place, — и человек
  выбирает в нём. [retired] Здесь стояло «у приложения он один».

## Что берётся у панели без изменений

- **Четыре закона** и запрет декора ради декора.
- **Тёмная тема как полноправная**, а не как инверсия: обе витрины тёмные по
  умолчанию, и приложение начинается с той же стороны.
- **Тень как расстояние**, а не как размытие; нажатие уменьшает расстояние.
- **Ничего не едет**: ни одного `transition` по умолчанию.
- **Способ проверки** — контраст и вид страницы целиком, а не покомпонентно.

## Как проверять

- `xor.ad/scripts/check-landing-tokens.sh` — наборы имён у витрин совпадают
  (сломано подсадкой в трёх видах: убранный токен, переименованный, разъехавшийся).
- Макет каждого экрана — SVG в истинной геометрии, нумерованные вопросы в конце,
  согласование, и только потом CSS.

## Словарь компонентов

Собран 16.09.2026 из описаний экранов 01–24 витрин (агент-исследователь; каждая строка сверена ведущим по экранам при подготовке брифа на отрисовку). Экран 16 «Стикеры» за пределами альфы и в словарь не входит. Пятьдесят компонентов; «нет — макет» значит, что правила в дизайн-системе нет и его задаёт макет экрана.

| Компонент | Экраны | Состояния, которые макет обязан показать | Правило или токен |
|---|---|---|---|
| Кнопка первичная | 01 02 04 06 08 13 14 19 20 | недоступна без согласия, до ответа узла, без мест, без текста отказа; фокус — кольцо 2px | `--r-2`; тень как расстояние; неактивное — правило Н1 |
| Кнопка вторичная | 01 06 08 20 | равный вес против умолчания | роль не задана — макет |
| Кнопка-иконка с именем | 01 02 03 08 | имя для диктора обязательно | `--r-1`; имя — доступность §5 |
| Поле ввода с подписью | 02 04 08 13 14 | закрыто, метка «отошёл», набранное сохраняется, текст остаётся при отказе | `--r-1`; 16px — доступность |
| Счётчик остатка символов | 02 04 08 | подсказка, предел держит узел | нет — макет |
| Счётчик остатка квоты | 04 09 11 | вид не нарисован; слот при потолке | нет — макет |
| Поле ПИН | 02 12 13 | «легко угадать», ожидание, «осталось попыток», заперто, скелет | как поле ввода |
| Поле кода группами | 02 13 | не подошёл / истёк, перенос отменён, приём закрыт | нет — макет |
| Чекбокс принятия | 02 05 11 15 | не отмечен; блокирует «дальше», публикацию и имя | нет — макет |
| Переключатель вкладок | 04 07 | счётчик на первой, точка на второй; вид на 04 не решён | `--r-pill` — не привязан |
| Сегменты режима | 03 04 23 | значение у каждой фразы | нет — макет |
| Нижняя навигация, 4 пункта | 03 07 10 | точка на «Я»; пятого не бывает | нет — макет |
| Карточка фразы | 03 04 05 06 23 25 | угасание в последнюю четверть, «дальше, чем просили», приглушённая своя, отклонённая блоком | `--r-2`; угасание — правило Н2, приглушённость — Н6 |
| Карточка стола | 03 09 19 23 25 | невидима без сидящих и вне полосы; имя после вердикта, число лайков; лайк не сажает (17.09.2026); вид не определён | наследует `--r-2` |
| Карточка оффера | 03 17 24 | скидка и срок обязательны; «не в ленте», «срок вышел», «ссылка отключена»; словом, не рамкой | нет; словом, не цветом — доступность §1 |
| Карточка мэтча | 06 | «ответа пока нет», «исчезло», «ждёт вашего имени», одна фраза при оффере | нет — макет |
| Скелет загрузки | 02 07 08 09 12 19 | без подписи; кнопка недоступна до ответа | правило Н3 |
| Пустое состояние | 07 09 14 21 · экран 11 | заголовок + строка + действие; семь строк без заголовка | нет — макет |
| Надгробие беседы | 07 08 | до касания; пометка по своей попытке | правило Н4 |
| Полоса «документы изменились» | 11 15 | перечень и одна галочка; лента читается; без связи не ставится | нет — макет |
| Полоса состояния связи | 08 11 19 21 | четыре исхода сокета; «нет сети» против «у нас не получилось»; «приложение устарело» | правило Н5; `--ok`, `--err` |
| Строка «вы за столом — вернуться» | 03 07 19 23 | ровно одна; остаётся, когда стол ушёл из ленты | нет — макет |
| Строка с отменой | 06 07 23 | несколько секунд; отказ записан сразу | нет — макет |
| Лист действий «…» | 05 08 17 19 23 | от тихого к громкому; нет на ходах; наборы разные по экранам | нет — макет |
| Подтверждение с перечнем исчезающего | 05 08 12 13 20 | цена числами до нажатия | нет — макет |
| Обратный отсчёт / остаток срока | 06 08 09 13 19 20 23 | у чужой фразы словом; вдвоём без счётчика | доступность §86 |
| Доска игры по классам | 18 19 | подсветка фишки, ход клавишами, «так нельзя», «просрочен — пас», «три паса», «стола нет» | клетка ≥ 24 px — доступность |
| Ручка радиуса просмотра | 03 | без чисел; плотность на отпускании; касанием тоже | нет — макет |
| Ручка зоны фразы, ступени | 04 10 | пять ступеней | нет — макет |
| Слайдер возраста | 03 | по году внутри полосы, за полосу нельзя, край «без ограничения» | нет — макет |
| Ползунок срока молчания | 08 10 | когда угодно; чужой не виден | нет — макет |
| Список с возвратом / снятием | 10 12 | скрытое, блокировки, квитанции; недолог | нет — макет |
| Список обращений | 10 14 | «обращений нет»; замороженной сессии нет | нет — макет |
| Форма жалобы / ст. 16 | 05 11 14 17 | первая строка отговаривает; обоснование обязательно; галочка не отмечена | нет — макет |
| Подсказка при первой встрече | 10 23 24 | одна за раз, один раз; «понятно» ≥ 44 px | нет — макет |
| Образцы акцента и контраста | 22 11 | подписан словом; сразу; «применено, сохраним» | `--accent*`; набор витрины |
| Знак-домик в шапке | 10 22 | перебор акцента; не расходиться с 22 | `--accent` |
| Иконка лайка | 05 17 23 | приглушена, но видна; снимается повторно; у оффера нельзя | 44 px — доступность |
| Число лайков | 09 | уменьшается; кто — никогда | нет — макет |
| Системная строка беседы | 08 18 19 | не шифруется; без меню | нет — макет |
| Значок сверки ключей | 08 | касание открывает код; не меняется при смене устройства | ≥ 3:1 — доступность |
| Строка «предлагаю скидку» | 04 17 | раскрывается в три поля; один оффер за раз | нет — макет |
| «Показать ещё» | 03 23 | N ступенью; конец ведёт в ленту; автообновления нет | нет — макет |
| Языковой фильтр в шапке | 03 10 | одним касанием; оффер не прячется | нет — макет |
| Счётчик и точка на вкладке | 07 10 14 | счётчик гаснет по ответам, точка — от захода | `--r-round`, `--r-1` |
| Предупредительный текст | 02 06 12 | везде, где код; не второе согласие | нет — макет |
| Модальный вопрос, две кнопки | 08 | без умолчания; строка о риске | `--r-2` |
| Экран-замок | 12 | только ПИН; «за столом ваш ход»; «забыли ПИН?» | нет — макет |
| Консоль | 21 10 | пустая без заголовка; «мы не ответили»; ≤ 200 строк | `--mono` |
| Экран выбора отлучки | 20 | три срока и цена; без связи не уходит | нет — макет |

### Правила, которых у дизайн-системы не было

Шесть паттернов экраны требуют, а правила для них не было. Их вид решает макет; здесь — что макет обязан показать.

- **Н1. Неактивное состояние** (02, 05, 11, 19): контрол виден, причина неактивности — рядом словом, не только цветом; фокус на него ставится.
- **Н2. Угасание в последнюю четверть срока** (07, 08, 23): срок виден словом или числом, угасание — второй канал, не единственный (доступность §86).
- **Н3. Скелет** (11:30): без подписи и текста; та же геометрия, что у карточки; запустившая кнопка недоступна до ответа.
- **Н4. Надгробие беседы** (08, 11:47): остаётся до касания; не ошибка и не пустое состояние — третий вид.
- **Н5. Полоса состояния в шапке** (08:79): один слот, четыре исхода сокета, «нет сети» и «у нас не получилось» разной формулировкой, «приложение устарело» на всех экранах.
- **Н6. Приглушённость своей непроверенной фразы** (03:10): своя фраза до вердикта отличается от чужих и от отклонённой; отличие — не только цвет.

### Расхождения экранов, снимаемые макетом

Десять мест, где один контрол описан по-разному; направление снятия названо, макет его закрепляет.

1. Ручка радиуса плавная (03) и ручка зоны ступенчатая (04) — два разных контрола, дать им разные имена.
2. Лист «…» с пятью наборами (05, 08, 17, 19, 23) — один компонент, наборы по экрану; порядок «от тихого к громкому» общий.
3. «Тап по кнопке-логотипу» (заметки) и «иконка лайка» (05) — одно и то же, имя — «иконка лайка».
4. Переключатель вкладок решён на 07, не решён на 04 — один компонент, 04 берёт вид 07.
5. «Отклонено · вернуть» (06, 07) и «скрыто · вернуть» (23) — один паттерн «строка с отменой».
6. Три контрола со словом «язык» (01, 02, 10) — три разных: переключатель интерфейса, список при регистрации, языки ленты.
7. «Дальше» иконкой снята с 01, оставлена на 02 — на 02 тоже словом.
8. Счётчик подтверждения состава есть за столом (19), нет вдвоём — по канону, оба верны.
9. «Удалить / снять / встать / сесть» у своих записей (09, 23) — глаголы по сущности, компонент один.
10. Точка «ответ поддержки» на «Я» (10, 14) — оставить; иконка поддержки не возвращается.

### Что есть в дизайн-системе, но экранам не нужно

`--r-round` в роли аватара (аватаров нет), `--r-pill` (ни один экран не просит таблетку), `--panel-2` (вложенной поверхности нет), `--disp` (заголовочный шрифт не назван), `--ok` как отдельный исход (нужен только ✓ и ошибка), «нажатие по наведению» (взаимодействия касанием и клавиатурой). Токены остаются, но макет их не обязан применять.

## Открытого нет

Открытых пунктов у дизайн-системы приложения нет: ниже — записанное решение
о шкале отступов и два закрытых вопроса, оставленные как история.

**Шкала отступов заведена 31.08.2026 — и её не наследуют.** С радиусами замер дал
три чистые кучи; с отступами он дал обратный ответ. Замер — по тем же двум файлам,
что читает `scripts/check-landing-tokens.sh`, то есть `landing/index.html` обеих
витрин: **201 объявление** `margin`/`padding`/`gap` и **62 различных значения** в
них. Шкалы там нет. В семь ступеней четвёрочной шкалы точно попадают 64 значения
из 201 (32%), кратны четырём хоть как-то — половина; перевод сдвинул бы 137
объявлений. Поэтому приложение берёт обычную четвёрочную —
`--s-1: 4px`, `--s-2: 8px`, `--s-3: 12px`, `--s-4: 16px`, `--s-6: 24px`,
`--s-8: 32px`, `--s-12: 48px`, — а **витрины не переводятся**: это живые
страницы, которые работают, и перекладывать в них 137 объявлений никто не просил.
Плохое совпадение шкалы с лендингами — не довод против шкалы, потому что
конвертировать нечего. (Числа пересчитаны 31.08.2026: прежняя редакция говорила
«30 значений на 164 применения» и не называла, что именно меряла, — воспроизвести
её не удалось ни одним из четырёх охватов.)
- ~~Светлая тема приложения~~ — **есть: светлая, тёмная и как в системе, с тремя
  ступенями контраста и акцентом из набора витрины** (экран 22 витрин, 15.09.2026).
  Тема стояла на экране 10 с 26.08.2026, а эта строка её не видела.
- ~~Куда девать июльский прототип~~ — **остаётся образцом раскладки на глаз,
  токены из него не берутся** (решено 15.09.2026: это следовало из этой же строки;
  светлый режим из его §2 заменён экраном 22 витрин).
