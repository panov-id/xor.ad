# Панель ревью · day51 · 14.09.2026

## Шапка

- **Дата:** 14.09.2026, прогон 12:58–13:30.
- **Что ревьюили:** ветку `day51` против `origin/dev`, только документы (кода продукта нет).
  - `xor.ad` `2a7f820..b531554`, 9 коммитов, 23 файла (+373/−215): канон `docs/chat_RU/EN.md`, реестры `docs/facts/*.tsv`, `depth-client`, `offers/SPEC`, `refusal-wordings`, `chat-flows`, `dpia`, `test-map`, `migrations-step1`.
  - `sosed.place` `853e239..648230c`, 12 коммитов, 38 файлов (+293/−159): экраны `docs/NN-*_RU/EN.md`, `00-mechanics`.
  - `neighbro.place` `786dabe..317d511`, 12 коммитов: те же 38 файлов. Отдельно не ревьюилась — побайтно равна `sosed.place` во всех 38, кроме `01-splash-screen_RU/EN`, где отличается одна строка с именем домена (ПРОВЕРЕНО `cmp` ведущим и линзой «Согласованность»).
- **Линзы (параллельно, независимо):** Безопасность (обязательна), Данные и СУБД, Согласованность, Протоколы и регулирование (DSA, GDPR). «Эксплуатация» не собиралась: из её предмета в day51 только `--log-driver none`, подтверждённый замером.
- **Опровержение:** два агента — A (безопасность и данные), B (согласованность и регулирование); по умолчанию «опровергнута».
- **Машинные проверки до панели:** `check-facts-{decisions,limits,schema,open}.sh` и `ontology.py --check` — все с кодом 0 на `b531554`.
- **Воспроизвести диапазон:** `git -C <repo> diff origin/dev..day51`.

## Сырые находки линз

Отчёты вставлены скриптом из транскриптов агентов, без правки. Статусы ПРОВЕРЕНО/НЕ ПРОВЕРЕНО внутри — заявки самих линз, не ведущего.

### Линза «Безопасность»

# Ревью безопасности: day51 против `origin/dev`

```diff
- критично: «забыли ПИН?» и «десятая ошибка → задайте новый ПИН» (sosed.place, экраны 11 и 12) открывают дыру, которую канон xor.ad закрыл бумажным кодом
! дефект дизайна: 8-часовая отлучка несовместима с TTL очереди недоставленного и с максимальным сроком беседы, обещание «написанное дождётся» ложно
! дефект дизайна: S11 и S14 закрыты на бумаге, но обходятся (пауза не останавливает реплики стола; «была публикация» стоит одной фразы)
@@ проверено: 13 находок, ссылки сверены чтением файлов на ветке day51; код не запускался, продукта нет
```

Что прочитано: `git diff origin/dev..day51` по двум репозиториям (xor.ad, 23 файла; sosed.place, 38 файлов) и контекст в полных файлах. Пути ниже относительно `xor.ad/docs/` и `sosed.place/docs/`. Всё помеченное **ПРОВЕРЕНО** сверено по указанным строкам. Что относится к реализации, а не к тексту, отмечено отдельно.

---

## 1. Критично. «Забыли ПИН?» и «задайте новый ПИН» после десятой ошибки отдают личность тому, кто держит устройство

**Где:**
- `sosed.place/docs/12-identity-and-security_RU.md:53-56` — новый пункт.
- Он спорит с тем же файлом, `:49-52`: «отпереть можно только ПИНом… обойти её с живой сессии той же личности нельзя ниоткуда».
- `sosed.place/docs/11-empty-and-edge-states_RU.md:32` и `:75` (кнопка «Задать ПИН»).
- Канон против: `xor.ad/docs/chat_RU.md:1171` («смена ПИНа требует **старый** ПИН») и `:1177` («Доля сожжена — все три действия отказывают… Выход один — бумажный код… иначе посторонний с отпертой вкладкой… увёл бы личность»).

**Сценарий.** Телефон лежит на столе, вкладка заперлась по 5 минутам простоя. Посторонний жмёт «забыли ПИН?» (или десять раз вводит что попало) и задаёт свой ПИН, узел выдаёт новую долю. Он получает живую сессию личности и может:
- публиковать и лайкать от её имени;
- открыть «Я» → «Поддержка»: список обращений с ответами и почтой (`chat_RU.md:2599-2605`);
- открыть сохранённые офферы;
- переносить личность и нажать «начать заново»: по `:1171` для этого нужно лишь доказательство знания ПИНа, а ПИН теперь его.

Владелец отрезан, пока не восстановится бумажным кодом. Канон сам называет эту атаку в `:1177`, а экраны day51 возвращают её.

**Проверка.** ПРОВЕРЕНО по строкам выше. Не проверено: что после стирания истории собеседники получат вопрос о перевыпуске ключей (`chat_RU.md:2357-2366`) и вор пройдёт проверку подписи. Это вывод из `:1177` («в вебе ключ подписи лежит вне доли»), дословно не сверял.

**Минимальная правка.** «Забыли ПИН?» (`12:53`) и кнопка на `11:75` ведут на ввод бумажного кода, а не на задание ПИНа. В `11:32` и `12:53-56` вместо «задайте новый ПИН» написать «восстановите бумажным кодом: он заведёт новую долю и новый ПИН» (канон `:1177`).

---

## 2. Дефект дизайна (противоречие). На 8 часов «написанное дождётся вас» — неправда, и беседа такой срок не переживает

**Где:**
- `chat_RU.md:829` («слова не пропадают, они ждут доставки») и `:827` («уход на восемь часов переживают только беседы с долгим сроком»).
- Против них: `chat_RU.md:54` и `:60` (самый длинный срок беседы — 4:20), `:2092-2093` (`chat.pending.ttl` 260 минут), `:2088` (строка очереди умирает, когда сессия получателя заморожена), `:828` (при отлучке сокеты закрываются «тем же путём, что при заморозке»).
- Экраны: `sosed.place/docs/00-mechanics_RU.md:1077` и `:1080`, `20-step-away_RU.md:9-10` и `:40-41`.

**Сценарий.**
- Срок беседы отсчитывается от своей последней реплики, и даже «пока говорим» (260 минут) короче 480. Значит, **ни одна** беседа восьмичасовой отлучки не переживёт, и «цена на месте» (`20:10`) всегда будет «N из N».
- Собеседник видит метку над живым полем и пишет. Его строки в `pending_deliveries` живут не дольше 260 минут, а если узел по `:828` считает отлучку заморозкой — удаляются сразу по `:2088`. Потеря молчаливая: переполнение и истечение ничего не сообщают (`:2094`).
- Человека прямо зовут писать в пустоту. Именно от этого защищал снятый довод.

**Проверка.** ПРОВЕРЕНО по строкам выше, арифметика 260 < 480.

**Правка.**
1. В `chat_RU.md:2088` явно сказать, что отлучка — не заморозка для `pending_deliveries`.
2. В `:827` и `00-mechanics:1077` написать «8 часов не переживёт ни одна беседа».
3. В `:829`, `00-mechanics:1080` и `20:40` заменить «дождётся» на «дойдёт, если беседа будет жива к возвращению».
4. Либо третью ступень сделать не длиннее 4:20.

---

## 3. Дефект дизайна. Правило `last_own_message_at < stepped_away_at`: не определён NULL, метка может появиться в новой беседе, и на личности навсегда остаётся отметка времени

**Где:** `chat_RU.md:750` (новая колонка `identities.stepped_away_at`), `:829`, `:1926` (`last_own_message_at` может быть NULL), `test-map_RU.md:378`, `dpia_RU.md:67`.

**Сценарий.**
- **(а) Новая беседа.** A отходил вчера, сегодня сматчился с B и открыл беседу, но ещё не написал: у A `last_own_message_at = NULL`. Если реализовать через `COALESCE` или `NOT (own >= away)`, B видит «отошёл» у человека, который сейчас здесь. Заодно B узнаёт о прошлой отлучке, которую ему никто не объявлял.
- **(б) Буквальный SQL.** С NULL сравнение не истинно, и в беседе, где A не писал до ухода, метки нет, хотя A отсутствует. Спека не выбирает ни один из вариантов.
- **(в) Хранение.** `stepped_away_at` никогда не обнуляется. Узел бессрочно держит на личности время последней отлучки — новая поведенческая метаданная, которая попадает в дампы. В DPIA (`dpia_RU.md:67`) её нет.

**Проверка.** ПРОВЕРЕНО: колонки и правило по строкам выше. Как это будет реализовано — не проверено: кода нет.

**Правка.** Вместо отметки на `identities` завести `chat_participants.away_marked boolean`:
- ставится в транзакции ухода для бесед, открытых в этот момент;
- снимается своей репликой;
- `stepped_away_at` убрать.

В `test-map_RU.md:378` добавить два случая: «беседа открыта после возвращения — метки нет» и «своих реплик не было (NULL) — метка есть».

---

## 4. Не нашёл описания: как метка доходит до собеседника

**Где:**
- `chat_RU.md:1944` — «Открытие чата отдаёт: … **И всё**». Метки в списке нет.
- `:829` — метку показывает узел.
- `:54` — «чужая настройка и чужой остаток — нет».
- `sosed.place/docs/08-chat-screen_RU.md:63` — «без времени ухода».

**Сценарий.** Самая простая реализация отдаёт клиенту собеседника его `stepped_away_at` и `last_own_message_at`, чтобы сравнить на месте. Тогда модифицированный клиент получает точное время ухода и время последней реплики собеседника. Из второго вместе с разрешённым списком сроков (10/30/60/260) восстанавливается чужой остаток, закрытый `:54`.

**Правка.** В список `:1944` добавить одно булево поле `peer_stepped_away`, вычисляемое на узле, и прямо написать, что чужие отметки времени не отдаются.

---

## 5. Дефект дизайна. S11: реплики стола теперь питают счётчик отказов, но пауза их не останавливает

**Где:**
- `chat_RU.md:1469` — источники счётчика: фразы, реплики и заявки за столом.
- `:1461` — «заблокирована именно отправка», без уточнения, чего именно.
- `sosed.place/docs/11-empty-and-edge-states_RU.md:36` и `:86` — «не отправляются **только новые фразы**».
- `refusal-wordings_RU.md:84-86`.
- `11-empty:43` — отказ за столом называет причину класса.

**Сценарий.** За своим пустым столом подбирается формулировка. Пятый отказ включает пятнадцатиминутную паузу, но по тексту экрана блокируются только фразы ленты. Реплики стола идут дальше и получают причину класса, то есть подбор без предела остаётся. Правка S11 даёт рост счётчика без последствий.

**Проверка.** ПРОВЕРЕНО по строкам.

**Правка.** В `:1461` написать: «блокируется отправка всего, что идёт в очередь модерации: фраз, реплик и заявок за столом». Тексты `11:86` и `refusal-wordings §4` привести к этому.

---

## 6. Дефект дизайна. Имя не питает счётчик отказов, а S7 даёт проверять имя без публикации

**Где:** `chat_RU.md:1469` (имени в списке источников нет), `:1726`, `sosed.place/docs/17-offer_RU.md:17`, `refusal-wordings_RU.md:70-77`.

**Сценарий.** Лайк на любой оффер частника отправляет имя в очередь. Дальше отказ, «поправьте имя», новая проверка — и так без счётчика, без слота квоты и без фразы. Получается бинарный оракул фильтра на поле имени. Каждое прошедшее имя вдобавок рождает мэтч у автора чужого оффера: побочный спам карточками «интересуется».

**Правка.**
- В `:1469` добавить «имена».
- В `:1726` написать: лайк на оффер с отклонённым именем не запускает проверку повторно, пока имя не изменено, и отказ считается в том же окне.

---

## 7. Дефект дизайна. S14: «была принятая публикация» стоит одну безобидную фразу, и хранить этот признак негде

**Где:**
- `offers/SPEC_RU.md:478` и `:670-677` (довод «если двое — это два человека»).
- `sosed.place/docs/17-offer_RU.md:23`.
- `chat_RU.md:1738-1745`: в `identity_stats` нет признака «публиковал»; фразы удаляются по истечении и при отлучке. Шаблон `EXISTS … expires_at > now()` в `:1720` проверяет только **живую** фразу.

**Сценарий.** Конкурент открывает два приватных окна и заводит две личности (десять секунд каждая). Каждая публикует «привет, соседи» рядом с зоной оффера: имя и фраза проходят за секунды. Затем две жалобы «ссылка ведёт не туда», и переход гаснет автоматически. Цена атаки выросла примерно на минуту, так что довод в `:672` не держится. Слово «была» при текущей схеме реализовать нечем.

**Правка.**
- Завести `identity_stats.first_published_at`.
- Считать жалобу, только если первая принятая публикация старше N часов (например 24).
- До разбора модератором — не больше одного автоматического гашения на оффер.

---

## 8. Противоречие. `support_requests.seen_at` — отметка прочтения на узле, запрещённая в том же day51

**Где:** `chat_RU.md:2605` против `:37` (добавлено в day51: «**Непрочитанного нет** — оно означало бы, что узел знает, кто когда открывал»). Также `sosed.place/docs/14-support_RU.md:13` и `:30`, `chat_RU.md:2609-2611` (год хранения, исполнителя чистки нет).

**Сценарий.** Тот, кто читает базу или копию, получает время, когда личность открыла «Я → Поддержка». Это отметка присутствия, связанная с личностью и необязательной почтой из той же строки (`:2601`), и хранится она год без уборщика.

**Правка.** Сделать `seen boolean` без времени или держать факт «видел ответ» на устройстве по номеру обращения.

---

## 9. Противоречие. Поддержка связывает жалобу с личностью, а решение DSA от 11.09 это запрещает

**Где:**
- `sosed.place/docs/14-support_RU.md:27` (жалоба через поддержку переводится в реестр уведомлений) и `:30`.
- `chat_RU.md:2599-2600` (`identity` FK и `body`).
- `dsa/SPEC_RU.md:393-397` («личности заявителя нет… заводить её значит связать „кто пожаловался“ с личностью на год»).

**Сценарий.** Жалоба прошла WAF через поддержку и переведена в реестр. Уведомление по замыслу без личности, но строка `support_requests` год держит `identity`, текст и почту. Связь восстанавливается совпадением текста. Day51 зафиксировал это в схеме, не предусмотрев состояния «перенесено».

**Правка.** В транзакции переноса в реестр обнулить `support_requests.identity` и `body`. Статус «перенесено» человеку показывать из локального состояния клиента. Правило записать под `chat_RU.md:2597`.

---

## 10. Дефект дизайна. `--log-driver none` назван обязательным, но ничем не обеспечен, а §6 обещает больше, чем даёт флаг

**Где:** `depth-client_RU.md:72`, `:77-82`, `:292-293` (альтернативный буфер описан только для экрана переноса), `:743-745` («ничего из этого на диск не попадает»).

**Сценарий.**
- Человек запускает команду из старой инструкции или истории shell, без флага. Расшифрованная переписка уходит в `json-file`, а на хосте с драйвером `journald` по умолчанию остаётся в журнале и после выхода. Какой драйвер стоит по умолчанию в дистрибутивах — **НЕ ПРОВЕРЕНО**.
- Даже с флагом основной интерфейс не объявлен в альтернативном буфере. Переписка остаётся в прокрутке терминала и истории tmux, а сохранение сессий терминала пишет её на диск.

**Правка.**
- Единственный документированный способ запуска — скрипт `depth` с зашитым флагом.
- Клиент не стартует без переменной окружения, которую ставит этот скрипт. Это защита от старых скопированных команд, а не от намеренного обхода.
- Весь интерфейс рисовать в альтернативном буфере, как в `:292`.
- В `:743-745` оговорить прокрутку терминала.

---

## 11. Дефект дизайна. Консоль пишет «открыта форма уведомления о незаконном», а скриншот консоли теперь основной путь в поддержку

**Где:** `sosed.place/docs/21-console_RU.md:32-38` (строка маршрута) и `:39` (отметка времени), `:47-49` («всё, что попало в консоль, надо считать опубликованным»), `:71` (строка уже продублирована состоянием экрана 11), `:72-75` («поддержке хватает скриншота»), `10-settings-screen_RU.md:41`.

**Сценарий.** Человек пожаловался на фразу соседа, а позже присылает скриншот консоли в поддержку или выкладывает публично. На нём время подачи уведомления по ст. 16. Автор снятой фразы сопоставляет это время с моментом снятия и узнаёт заявителя.

**Правка.** Убрать строку смены маршрута из консоли: её покрывает `11-empty:47`. Либо писать нейтрально, без слова «незаконном» и без времени.

---

## 12. Мелочь. Загрузка пачек стикеров при открытии поля ввода выдаёт время захода в беседу

**Где:** `sosed.place/docs/16-stickers_RU.md:26` против `chat_RU.md:829` («о заходах узел не знает и узнавать не должен»).

**Сценарий.** Если каталог не закэширован, запрос пачек при каждом открытии беседы оставляет в логах CDN или узла IP и время каждого захода.

**Правка.** Грузить все пачки при старте приложения, вместе с первой страницей ленты, с заголовками неизменяемого кэша, и не привязывать загрузку к событиям интерфейса беседы.

---

## 13. Мелочь, не нашёл описания. У `support_requests` нет предела частоты, а номер обращения последовательный

**Где:** `chat_RU.md:2598` (`bigserial` — «номер, который видит человек»), `sosed.place/docs/14-support_RU.md:19` (уведомление команде на каждое обращение). В `facts/limits.tsv` строки про поддержку нет (ПРОВЕРЕНО grep).

**Сценарий.**
- Личности бесплатны: скрипт заваливает поддержку, и каждое обращение шлёт письмо или вебхук команде.
- Разница номеров за время показывает объём обращений.
- Если ответ будут отдавать по номеру без проверки владельца, это прямой IDOR: ответы и почта чужих обращений.

**Правка.** Добавить в `limits.tsv` предел обращений на личность и на адрес. Видимый номер сделать случайным. Прямо написать, что выдача идёт только по `identity = :me`.

---

**Что сознательно не вынесено.** Выход заблокировавшего из-за стола с пасом по 5 минутам: задержка у ушедшего игрока существовала и до day51 (`19-table_RU.md:34-36`). Время слота в отказах по квоте: считается по своим фразам, чужого не выдаёт.

### Линза «Данные и СУБД»

```diff
- противоречие: отлучку на 8 часов не переживает ни одна беседа, а в тексте написано «переживают беседы с долгим сроком»
- дефект дизайна: обещание «написанное дождётся вас» ломается о чистку pending_deliveries, а правило метки при NULL не срабатывает
! не нашёл описания: где хранятся данные для часового предела, для скользящего окна отказов и для условия «была принятая публикация»
+ ПРОВЕРЕНО в Docker (postgres:16): поведение NULL в правиле метки; индексов по identity в chat_participants и support_requests нет, запросы идут через Seq Scan
@@ реестр: 35 таблиц всего и 26 продуктовых сходится (23 в спеке чата + 3 в спеке офферов), но open.tsv пишет «22 в спеке чата»
```

Линза «Данные и БД». Проверял ветку `day51` против `origin/dev` в двух репозиториях: `xor.ad` и `sosed.place`. Файлы не менял. Всё ниже я проверил сам: чтением `файл:строка`, пересчётом через `awk` и одним прогоном в Docker (команда в конце).

---

### 1. Противоречие: отлучку на 8 часов не переживает ни одна беседа
- **Где:** `xor.ad/docs/chat_RU.md:827`, `xor.ad/docs/chat-flows_RU.md:966`, `sosed.place/docs/00-mechanics_RU.md:1077`, `sosed.place/docs/20-step-away_RU.md:15`.
- **Сценарий:** срок беседы бывает 10, 30, 60 или 260 минут (`chat_RU.md:1925`), самый долгий 4:20. Отсчёт идёт от `last_own_message_at`, а пока человек отошёл, он писать не может. 8 часов больше 260 минут, поэтому у ушедшего `gone_at` встанет во всех беседах без исключения. Экран 20 говорит «не переживут те, где десять минут», то есть занижает цену. Для отлучки на час то же самое касается сроков 10, 30 и 60 минут.
- **Правка:** написать прямо: «уход на 8 часов не переживает ни одна беседа; на час — только беседы со сроком 260». Цитату на экране 20 переписать под это. Если такой результат не устраивает, это продуктовая развилка, а не правка текста.

### 2. Дефект дизайна: «написанное дождётся» не выполняется
- **Где:** `sosed.place/docs/20-step-away_RU.md:40`, `sosed.place/docs/00-mechanics_RU.md:1080`, `xor.ad/docs/chat_RU.md:829` против `chat_RU.md:2087-2093`.
- **Сценарий:** строка `pending_deliveries` умирает, как только беседа кончилась у любого из двоих. Потолок её жизни `chat.pending.ttl` = 260 минут. С учётом п. 1 при отлучке на 8 часов всё написанное собеседником удаляется. При отлучке на час оно удаляется во всех беседах, где срок ушедшего 60 минут или меньше.
- **Второй риск:** отлучка закрывает сокеты «тем же путём, что при заморозке» (`chat_RU.md:828`). А заморозка сессии по `:2088` сама удаляет её очередь. Если под «тем же путём» имеется в виду и заморозка сессии, очередь пропадает сразу при уходе.
- **Правка:** заменить «дождётся» на «дождётся, если беседа доживёт до вашего возвращения». Отдельно записать, что отлучка закрывает только сокеты и `sessions.frozen_at` не ставит.

### 3. Дефект дизайна: правило метки не определено для NULL и для старой отлучки
- **Где:** `xor.ad/docs/chat_RU.md:750`, `:829`, `:1926`.
- **Сценарий А.** Человек открыл беседу, ни разу в ней не написал и отошёл. `last_own_message_at` пуст, сравнение `NULL < stepped_away_at` даёт NULL, и метка не показывается. Собеседник пишет в пустоту без метки.
- **Сценарий Б.** Если «починить» случай А через `coalesce(..., '-infinity')`, получится обратная ошибка. Человек отходил три дня назад, вернулся, открыл новую беседу и в ней молчит. Метка висит, хотя он никуда не уходил.
- **ПРОВЕРЕНО в Docker:** для участника 1 (отошёл, не писал) буквальное правило даёт NULL, вариант с coalesce даёт `t`. Для участника 3 (отошёл до создания беседы) coalesce тоже даёт `t`.
- **Правка:** зафиксировать выражение
  `stepped_away_at IS NOT NULL AND stepped_away_at > chats.created_at AND (last_own_message_at IS NULL OR last_own_message_at < stepped_away_at)`.
  Отдельно записать, двигает ли `last_own_message_at` свой ход в игре: по `:2002` ход приравнен к реплике, значит он снимает метку.

### 4. Противоречие: скользящий час отказов записан одним целым числом
- **Где:** `xor.ad/docs/chat_RU.md:1458` (`ALTER TABLE identity_stats ADD COLUMN rejected_count integer`), `:1461`, `:1465`, `xor.ad/docs/chat-flows_RU.md:233` («отказ записан с отметкой времени»).
- **Сценарий:** одно целое число не может «выпасть» через час. Скользящее окно требует отметку времени у каждого отказа. Остатку 15-минутной паузы, который по `:1463` переживает отлучку, в схеме тоже негде лежать. Под S11 счётчик теперь кормят ещё и реплики с заявками за столом, то есть отказов станет больше.
- **Правка:** заменить колонку на `rejections timestamptz[]` (хранить не больше 5) или на таблицу `(identity, rejected_at)`. Добавить `identities.posting_muted_until timestamptz`. Фразу «помнит число, а не текст» переписать на «помнит моменты, а не текст».

### 5. Не нашёл описания: из чего считается предел «4 публикации за час»
- **Где:** `xor.ad/docs/chat_RU.md:1550`, `:1564`, `xor.ad/docs/refusal-wordings_RU.md:105` («Следующую можно в ЧЧ:ММ»), `sosed.place/docs/00-mechanics_RU.md:1075`.
- **Сценарий:** снятая фраза удаляется (`DELETE`), отлучка удаляет все фразы. После этого считать по `feed_messages` нечего. Человек публикует четыре, снимает их или отходит на 20 минут, и потолок обнулён. Между тем механика обещает, что предел переживает уход. Чтобы назвать «ЧЧ:ММ», нужен момент самой ранней публикации за час, а колонки для него нет ни в `identities`, ни в `identity_stats`.
- **Правка:** завести `identity_publications (identity, published_at)` с каскадом от личности и чисткой строк старше часа, либо массив в `identity_stats`. Указать, где объявлено.

### 6. Не нашёл описания: как узнать, что «у жалующегося была принятая публикация»
- **Где:** `xor.ad/docs/offers/SPEC_RU.md:478`, `:670`; `identity_stats` в `xor.ad/docs/chat_RU.md:1738-1745`.
- **Сценарий:** фразы удаляются через 4:20, при снятии и при отлучке. В `identity_stats` есть лайки, мэтчи и открытые чаты, но нет счётчика публикаций. Через сутки условие проверить не по чему. Кроме того, жалоба на ссылку подаётся с экрана перехода 6.2, и таблицы с личностью жалующегося нет ни в спеке офферов, ни в спеке чата. Без неё невозможно проверить и «от разных людей».
- **Правка:** добавить `identity_stats.published_count integer` или `first_published_at timestamptz`, увеличивать при `visible_at = now()`. Объявить таблицу жалоб на ссылку с колонками `(offer_id, complainant_identity, created_at)` и `UNIQUE (offer_id, complainant_identity)`.

### 7. Противоречие: «начать заново рвёт связь», но `SET NULL` срабатывает только через 30 дней
- **Где:** `xor.ad/docs/chat_RU.md:2599` (комментарий в DDL), `sosed.place/docs/14-support_RU.md:30`, против `chat_RU.md:818-819`.
- **Сценарий:** «начать заново» только ставит `closed_at`. `DELETE` делает уборщик через 30 дней, а уборщика нет (`open.tsv` `identity.sweeper`). Пока его нет, обращение с `body` и `email` навсегда остаётся привязано к закрытой личности. Список закрывающей транзакции в `:818` (хэш, ключ, сессии, доли, очередь) `support_requests` не упоминает. Срок хранения «год» (`:2609`) не говорит, от чего он считается (`created_at` или `answered_at`), и исполнителя у него нет.
- **Попутно (мелочь):** `bigserial`, «номер, который видит человек», по двум своим обращениям выдаёт общий поток обращений на площадке.
- **Правка:** обнулять `support_requests.identity` в той же закрывающей транзакции, либо поправить комментарий и экран 14 на «через 30 дней». Записать срок как `created_at + 1 год` и завести открытый пункт на уборщика.

### 8. Не нашёл описания: переход обращения в ст. 16 и привязка к личности
- **Где:** `sosed.place/docs/14-support_RU.md:27`, `xor.ad/docs/facts/open.tsv:140`, `xor.ad/docs/dsa/SPEC_RU.md:393`.
- **Сценарий:** обращение, которое прошло WAF и оказалось жалобой на незаконное, «переводится в тот же реестр». При этом строка `support_requests` с `identity` остаётся, а тело совпадает с `dsa_notices`. Связка «уведомитель ↔ личность» восстанавливается простым сравнением текста, хотя решение от 11.09.2026 её запрещает. Колонки связи и правила «что остаётся в support_requests после перевода» нет.
- **Правка:** описать перевод: создаётся `dsa_notices` без личности, а у обращения `identity` обнуляется либо `body` заменяется ссылкой на номер уведомления.

### 9. Дефект дизайна: нет индексов по `identity`
- **Где:** `xor.ad/docs/chat_RU.md:2597-2606` (`support_requests`), `:1922-1929` (`chat_participants`, первичный ключ `(chat_id, identity)`).
- **Сценарий:** точка «ответ ждёт» в «Я» и список своих обращений — это `WHERE identity = :me` на каждом показе. Каскады `SET NULL` и `CASCADE` при удалении личности уборщиком, выборка «все мои беседы» при отлучке (удаление `chat_games`, пересчёт метки) и холодный инбокс идут по `identity` без индекса.
- **ПРОВЕРЕНО в Docker:** у обеих таблиц есть только `_pkey`. `EXPLAIN` на 20 000 строк показывает `Seq Scan on support_requests` и `Seq Scan on chat_participants`.
- **Правка:** `CREATE INDEX ON support_requests (identity) WHERE identity IS NOT NULL;` и `CREATE INDEX ON chat_participants (identity);`.

### 10. Не нашёл описания: где живут таймеры стола на узле
- **Где:** `xor.ad/docs/facts/limits.tsv:69`, `:72`, `:73` (исполнитель теперь `узел`), `xor.ad/docs/chat_RU.md:403`, `:612-619`.
- **Сценарий:** срок хода 5 минут, счётчик подряд пропущенных ходов (3) и окно подтверждения 30 секунд должны пережить выкат. Этот довод приведён для `chat_games` в `:413`. Но в `table_games` колонок срока нет, а про `state jsonb` не сказано, что там лежат `turn_deadline` и `passes`. В пуле два бокса, и не сказано, какой узел держит таймер.
- **Правка:** записать, что `state` хранит `turn_started_at` и `consecutive_passes` по местам, а пас выставляет задача планировщика по `now() > turn_started_at + table.move.window`, а не таймер в памяти.

### 11. Мелочь: счёт в open.tsv и порядок в schema.tsv
- **Где:** `xor.ad/docs/facts/open.tsv:127`, `xor.ad/docs/facts/schema.tsv:75`.
- **Счёт:** написано «26 … (22 в спеке чата, 3 в спеке офферов)», а 22 + 3 = 25. **ПРОВЕРЕНО** через `awk` по `schema.tsv`: всего 35, продуктовых 26, в спеке чата 23, в спеке офферов 3. **Правка:** «23 в спеке чата».
- **Порядок:** `support_requests` дописана после `venues` и нарушает алфавит. **Правка:** переставить.

### 12. Мелочь: досрочный возврат и хвосты migrations-step1
- **Досрочный возврат, `xor.ad/docs/chat_RU.md:751`, `:836`.** Не описано, что происходит при досрочном выходе. Колонка сказана как «до него продукта нет». Если `stepped_away_until` не переписать на `now()`, узел продолжит отказывать. **Правка:** «досрочный выход: `stepped_away_until = now()`, `stepped_away_at` не трогается».
- **migrations-step1, `xor.ad/docs/migrations-step1_RU.md:4` против `:171`.** В одном файле два разных «одиннадцать»: срез первой миграции и «таблицы шагов 2–7». `vault_shares` входит в срез, но в документе её нет. Живые ссылки как на текущую схему: `sosed.place/docs/02-name-screen_RU.md:54` и `xor.ad/docs/legal-review-brief_RU.md:218`, обе про `legal_acceptances`. По этой таблице план ещё верен, но пометки о пересмотре у ссылок нет. **Правка:** в `:171` написать «шагов 2–7 по плану до 11.09»; к двум ссылкам добавить «(план до 11.09.2026, состав среза — `chat_RU.md` §13)».

---

**Команда проверки в Docker** (временный контейнер, остановлен после прогона):
`docker run --rm -d postgres:16`, затем `psql`. В урезанных `identities`, `chats`, `chat_participants` и `support_requests` вычислялись `last_own_message_at < stepped_away_at` и вариант с `coalesce(...,'-infinity')` для трёх участников, запрашивались `pg_indexes` и `EXPLAIN` по `identity`.

**Не проверял:** английские пары построчно, кроме наличия `stepped_away_at`, «8 hours» и DDL `support_requests` в `chat_EN.md`. neighbro.place по заданию пропущен.

### Линза «Согласованность»

```diff
- 1 дефект дизайна (число): после отлучки на 8 часов не выживает ни одна беседа, а документы пишут, что выживают «долгие»
! 2 остатка отменённого без [retired]: «блокировка скрывает стол целиком» в flows и в карте тестов
! S7 (имя в очередь до мэтча) не дошло до потока 10 и карты тестов; текст отказа указывает на неподходящий раздел
+ neighbro.place совпадает с sosed.place побайтно (cmp) во всех 38 изменённых файлах, кроме 01-splash; в 01-splash отличается одна строка, имя домена
@@ всё ниже проверено мной через grep/sed по day51 в xor.ad, sosed.place и neighbro.place
```

Линза «Согласованность». Все пункты **ПРОВЕРЕНО** чтением строк на диске (grep, sed) в репозиториях `xor.ad`, `sosed.place` и `neighbro.place`, ветка `day51`. Порядок: от самого серьёзного.

**1. Дефект дизайна: 8 часов больше самого долгого срока беседы.**
- Первая сторона: `xor.ad/docs/chat_RU.md:827` «уход на восемь часов переживают только беседы с долгим сроком». То же в `chat_EN.md:832`, `sosed.place/docs/00-mechanics_RU.md:1077` / `_EN.md:1142`, `chat-flows_RU.md:966` / `_EN.md:987`.
- Вторая сторона: `chat_RU.md:1925` `idle_ttl_minutes … 10 | 30 | 60 | 260`. Срок считается от `last_own_message_at`, а 260 минут это 4:20, меньше 8 часов. Значит, после 8 часов молчания не выживает ни одна беседа. После часа выживают только беседы на 260 минут.
- Отсюда же ложная строка `20-step-away_RU.md:15` / `_EN.md:15`: там сказано только про беседы на десять минут. И довод S18 «написанное дождётся вас» (`00-mechanics_RU.md` §13, `20-step-away` «слова не пропадают») при 8 часах не работает: беседа и очередь (`chat.pending.ttl` 260) гаснут раньше возвращения.
- Правка: заменить на «уход на 8 часов не переживает ни одна беседа, на час — только беседы с 260 минутами», переписать цитату экрана 20. Либо пересмотреть ступень 8 часов.

**2. Противоречие: остаток «блокировка скрывает стол целиком» без [retired].**
- Первая сторона: `xor.ad/docs/chat-flows_RU.md:754` «Блокировка скрывает стол целиком… один человек может спрятать чужую игру» и `chat-flows_EN.md:771` «A block hides the whole table».
- Вторая сторона: `sosed.place/docs/00-mechanics_RU.md` §12 «разводит на посадке… [retired]», `19-table_RU.md:121`, `depth-client_RU.md` («цена… [retired] снята 10.09.2026»).
- Правка: переписать пункт в flows по образцу `00-mechanics` и пометить старое [retired].

**3. Противоречие: строка 15.12 в карте тестов проверяет отменённое поведение.**
- Первая сторона: `xor.ad/docs/test-map_RU.md:289` / `_EN.md:288` «заблокированный за столом скрывает стол целиком».
- Вторая сторона: `19-table_RU.md:121` («выходит заблокировавший, партия у остальных идёт»).
- Для решения «сесть рядом нельзя в обе стороны, игра продолжается» строк в карте тестов нет.
- Правка: переписать 15.12 и добавить строку «блокировка за столом → выходит блокирующий, партия продолжается».

**4. Противоречие: S7 не дошло до потока 10 и карты тестов.**
- Первая сторона: `chat_RU.md:1726` / `chat_EN.md:1758` и `17-offer_RU.md:17` / `_EN.md:17`: «лайк сначала отправляет непроверенное имя в очередь, мэтч только после вердикта».
- Вторая сторона: `chat-flows_RU.md:418` «я лайкнул его → мэтч», без шага очереди. И `test-map_RU.md:212` / `_EN.md:211` (10.6): «личность без фраз лайкает оффер → мэтч создан».
- Правка: добавить узел «имя → очередь» в поток 10. В 10.6 записать «мэтч появляется после вердикта по имени» и добавить отрицательный случай «имя отклонено → мэтча нет».

**5. Противоречие: текст отказа для S7 взят из неподходящего раздела.**
- Первая сторона: `chat_RU.md:1726` «лайкнувший видит тот же текст, что при ожидании фразы (`refusal-wordings_RU.md` §3)».
- Вторая сторона: `refusal-wordings_RU.md:70-77`, §3 «Имя не прошло»: «Имя не прошло проверку — фраза подождёт. Поправьте имя, и она опубликуется сама». У лайкнувшего оффер фразы нет, поэтому текст про фразу неверен. Кроме того, ожидание очереди описано в §2, а не в §3.
- **Не нашёл описания:** что происходит с лайком, пока имя в очереди, и после того, как имя исправили. Появится ли мэтч сам?
- Правка: добавить в §3 отдельную строку для лайка на оффер и описать судьбу лайка.

**6. Противоречие: на карточке мэтча от оффера одна фраза, а канон требует остатки обеих.**
- Первая сторона: `chat_RU.md:20`, `:35`, `:1859` / `chat_EN.md:20`, `:35`, `:1891` «остатки обеих фраз, по одному на фразу» без исключения. Также `06-match-and-chat_RU.md:10`.
- Вторая сторона: `06-match-and-chat_RU.md:30` «Фраза на такой карточке одна — сам оффер» и `chat-flows_RU.md:429` «TTL по единственной живой фразе».
- Правка: в `chat_RU.md:1859` и в `06:10` дописать «у мэтча от оффера один остаток, у оффера».

**7. Противоречие: заголовок в каноне всё ещё говорит «десять таблиц».**
- Первая сторона: `chat_RU.md:2582` / `chat_EN.md:2616` «Первая миграция несёт десять таблиц» без пометки.
- Вторая сторона: `chat_RU.md:2591` «Одиннадцатая — `support_requests`», `migrations-step1_RU.md:3` «одиннадцать таблиц», `14-support_RU.md` «одиннадцатой таблицей».
- Правка: сделать заголовок «десять… (с 14.09.2026 — одиннадцать, ниже)».

**8. Противоречие: «таймер» у мэтча остался в двух местах.**
- Первая сторона: `chat_RU.md:2211` / `chat_EN.md:2231` в §8.11 «Мэтч | … имя, возраст, таймер» и `test-map_RU.md:200` / `_EN.md:199` (9.11) «имя, возраст, режим и таймер».
- Вторая сторона: `chat_RU.md:1859` «остатки обеих фраз» (с [retired] у `match expires`).
- Правка: заменить «таймер» на «остатки обеих фраз (своя и чужая)». Заодно проверить, не нарушает ли остаток чужой фразы строку «Никогда: сроки истечения чужих фраз» в той же таблице `:2211`. Это похоже на прямое противоречие внутри §8.11.

**9. Противоречие: «Никогда: чужие сроки» против остатка чужой фразы на карточке.**
- Первая сторона: `chat_RU.md` §8.11, строка «Никогда | … **сроки истечения чужих фраз**».
- Вторая сторона: `chat_RU.md:1859` и `06-match-and-chat_RU.md:10`: на карточке показан остаток **чужой** фразы.
- Правка: оговорить исключение для мэтча (срок раскрывается собеседнику после взаимного лайка) либо показывать один `least()`.

**10. Дефект дизайна: блокирующий «выходит из-за стола», но продолжает пасовать.**
- Первая сторона: `chat_RU.md:655` / `chat_EN.md:655` «из-за стола выходит тот, кто блокирует» и та же строка дальше: «Его ход становится пасом по `table.move.window`, как у любого отсутствующего». Также `19-table_RU.md:121` / `_EN.md:136`.
- Суть: ушедший не сидит за столом, значит пасовать за него нечем. А если он остаётся в составе, через 3 паса он станет зрителем за тем же столом, что нарушает «сесть рядом нельзя в обе стороны» (`00-mechanics_RU.md` §12).
- Правка: выбрать одно. Либо его место снимается сразу, либо в составе остаются пасы до конца партии, но без присутствия за столом.

**11. Противоречие: «очерёдность во всех классах», а у «Текста» в таблице её нет.**
- Первая сторона: `18-game-in-chat_RU.md` (Логика: «очерёдность держит движок — во всех классах… домино и точкам очередь добавлена»), `00-mechanics_RU.md` §12, `chat-flows_RU.md:716`.
- Вторая сторона: `chat_RU.md:93` / `chat_EN.md:93` «**Текст** (виселица) | 2 | буква не повторяется, слово угадано…», без «чей ход».
- Правка: добавить «чей ход (с 14.09.2026)» в строку «Текст» либо заменить «во всех классах» перечислением.

**12. Противоречие: flows обещает проверку законности хода, канон оставляет её людям.**
- Первая сторона: `chat-flows_RU.md:720` / `_EN.md:736` «движок проверяет очерёдность и **допустимость хода** по классу доски».
- Вторая сторона: `chat_RU.md:90` / `chat_EN.md:90`, «Доска-сетка (шашки, шахматы)… Что остаётся людям: **законность самого хода**».
- Абзац стоит вплотную к правке day51 и не был согласован.
- Правка: «проверяет то, что указано для класса в таблице §6».

**13. Не нашёл описания (S14): узлу нечем проверить «была принятая публикация».**
- Условие записано в `offers/SPEC_RU.md:670` / `_EN.md:703`, `SPEC_RU.md:478`, `17-offer_RU.md`.
- Фразы удаляются через 4:20 и при отлучке. В `identity_stats` (`chat_RU.md:1738-1745`) есть только `likes_received`, `likes_given`, `matches`, `chats_opened`.
- В модели оффера (`SPEC_RU.md:262-280`) нет ни колонки, ни признака. В карте тестов нет строки про условие.
- Правка: назвать признак (например, `identity_stats.accepted_publications` или отметку времени) и добавить строку в карту тестов.

**14. Не нашёл описания (S11): что именно блокирует пауза.**
- Счётчик теперь питается и репликами стола (`chat_RU.md` §8.3, «всем, что проходит очередь модерации»).
- Но пауза по-прежнему описана только для фраз: `sosed.place/docs/00-mechanics_RU.md:226-228`, `11-empty-and-edge-states_RU.md:86` «не отправляются только новые фразы», `refusal-wordings_RU.md` §4 «Отправка недоступна… лента, лайки и беседы работают».
- Неясно, блокирует ли пауза реплики и заявки за столом.
- Правка: назвать это в каждом из трёх мест одинаково.

**15. Мелочи, одна строка каждая.**
- `11-empty-and-edge-states_RU.md:71` / `_EN.md:73`: в строке «Квота исчерпана» времени нет («Освободится место — можно снова»), а `refusal-wordings_RU.md` §5 теперь пишет «освободится в ЧЧ:ММ». В `00-mechanics_RU.md` (таблица отказов §10, около строки 952) нет строки «четыре за час». Правка: добавить «в ЧЧ:ММ» и строку в таблицу.
- `chat_RU.md:274` помечено «Сыграть ещё (до 09.09.2026 — «сыграть заново»)», а в `chat_EN.md:274` пометки нет. RU и EN расходятся.
- `xor.ad/docs/chat_RU.md:2214-2216`: абзац 08.09 про «разбор, почему экран квоты не показывает время» оставлен без [retired]. Абзац-уточнение ниже его опровергает, но старое утверждение читается как действующее.

@@ Все изменённые экраны `neighbro.place` совпадают с `sosed.place` побайтно (38 файлов, cmp). В `01-splash-screen_RU/EN` отличается одна строка: «открыв sosed.place» / «открыв neighbro.place». Списки изменённых файлов в двух репозиториях совпадают.

### Линза «Протоколы и регулирование (DSA, GDPR)»

```diff
- 1 находка уровня «критично»: жалоба, перенесённая из поддержки в форму ст. 16 без почты, не получит ни подтверждения, ни решения, хотя экран 14 обещает ответ в приложении
! 3 противоречия с опубликованным соглашением (ст. 14): гашение ссылки, «один оффер за раз», пятнадцатиминутная пауза
! Ст. 20 и 23 к проекту не применяются: они сняты ст. 19 как для микропредприятия (xor.ad/docs/dsa/SPEC_RU.md:481, CHECKLIST_RU.md:14)
@@ Цитаты DSA и GDPR взяты с eu-digital-services-act.com и gdpr-info.eu: EUR-Lex через curl отдал пустой ответ, с официальным текстом не сверено
```

# Ревью day51, линза «Протоколы и регулирование»

**Что смотрел.** Диапазон `origin/dev..day51` в двух репозиториях: `xor.ad` и `sosed.place` (путь `/home/eugene-panov/Projects/panov-id/sosed.place`, ветка day51). Для сверки читал `xor.ad/docs/dsa/SPEC_RU.md`, `CHECKLIST_RU.md` и опубликованные юр-тексты витрины `sosed.place/landing/legal/terms_EN.md` и `privacy_EN.md`. Каталог `sosed.place/legal/*_RU.md` — это заглушки по 19 строк, в них ничего нет.

**Статус.** Каждая находка ниже сверена по `файл:строка`. Цитаты норм получены через WebFetch со вторичных сайтов, с EUR-Lex не сверены. Файлы не менял.

---

### 1. Критично — перенесённая жалоба без почты не получит ни подтверждения, ни решения

**Где:**
- `sosed.place/docs/14-support_RU.md:29`: форма ст. 16 открывается с текстом, «имя и почта пустые».
- `sosed.place/docs/14-support_RU.md:12` и `:23`: «ответ придёт сюда, в это приложение».
- `xor.ad/docs/dsa/SPEC_RU.md:393`: решение по уведомлению уходит «только на почту».
- `sosed.place/docs/11-empty-and-edge-states_RU.md:47` и `:98`.

**Норма:**
- ст. 16(5): «notify that individual or entity of» its decision, «without undue delay»;
- ст. 16(4): «Where the notice contains the electronic contact information…».

**Разрыв.** Человек пришёл в поддержку, потому что ему обещали ответ в приложении. Его увели в форму, где почта по умолчанию пустая, и строка над формой (`11:98`) об этом молчит. В итоге нет ни подтверждения по 16(4), ни решения по 16(5). Строки в `support_requests` тоже нет, потому что тело отрезал WAF, так что и ответ в «Я» → «Поддержка» некуда положить. Экран 14 сам себе противоречит, и это прямо попадает в открытый пункт `dsa.article16.no-email`.

**Минимальная правка:**
- В строку над формой (`11:98`) дописать: «без почты решение не придёт — оставьте адрес».
- Если почта была введена в поддержке, подставлять её в форму.
- Или честно сузить обещание в `14:12` до случая, когда поддержка обращение приняла.

### 2. Противоречие — один и тот же текст письмом принимается без галочки, а через перенос требует её

**Где:**
- `xor.ad/docs/dsa/SPEC_RU.md:27`: «Письмо без полей формы принимается».
- `sosed.place/docs/14-support_RU.md:27`: «отправлять человека переписывать текст в другую форму нельзя».
- `sosed.place/docs/14-support_RU.md:29`: теперь нужна галочка `bona_fide` и одно нажатие.
- `sosed.place/docs/21-console_RU.md` (строки о смене маршрута).

**Норма:** ст. 16(2): «shall be such as to facilitate the submission of notices containing…», а в перечне (d) «a statement confirming the bona fide belief». Механизм обязан облегчать подачу этого заявления, а не делать его условием приёма.

**Разрыв:**
- Галочка у нас не условие приёма: письмо без неё принимается (SPEC §2 п.2). Значит, для перенесённого текста она лишь добавляет шаг, на котором часть людей уйдёт, и это ровно тот довод, которым экран 14 обосновал автоперенос.
- Не нашёл описания, что будет с перенесённым текстом, если человек закрыл форму, не отправив: остаётся ли черновик и напоминает ли что-то о неотправленном.

**Минимальная правка** — выбрать одно из двух:
- принимать перенесённое как «письмо без полей» (SPEC §2 п.2), а галочку показывать необязательной;
- оставить галочку, но держать черновик на устройстве и показывать состояние «уведомление не отправлено» в экране 11.

### 3. Противоречие (ст. 14) — условие гашения ссылки есть в спеке, но не в соглашении

**Где:**
- `xor.ad/docs/offers/SPEC_RU.md:670–672`: считаются только жалобы личностей с принятой публикацией.
- `sosed.place/docs/17-offer_RU.md` (строка «Ссылка ведёт не туда»).
- Опубликовано: `sosed.place/landing/legal/terms_EN.md:77`: «report it from the screen… and it is switched off».

**Норма:** ст. 14(1): «include information on any restrictions that they impose… in their terms and conditions».

**Разрыв.** Соглашение обещает гашение по жалобе любого человека. Новое условие существует только в спеке. Сосед, ни разу не писавший в ленту, увидит, что ссылка после его жалобы работает, хотя соглашение обещало иное.

**Про ст. 23.** Это не мера по ст. 23: там речь о приостановке приёма от конкретного злоупотребляющего, «after having issued a prior warning» и «on a case-by-case basis». К тому же ст. 23 снята ст. 19, а жалоба не теряется — она уходит модератору. Юридического нарушения я здесь не вижу, только расхождение с опубликованным текстом.

**Минимальная правка.** В `terms_EN.md:77` (и в RU/переводы): «a link is switched off automatically after reports from two people who have posted before; other reports go to a moderator».

### 4. Дефект дизайна (ст. 14 и ст. 17(3)(e)) — «один оффер за раз» и дубль: запрет без основания в соглашении

**Где:**
- `xor.ad/docs/refusal-wordings_RU.md:49–50` — новые строки отказа;
- `sosed.place/docs/04-post-composer_RU.md` — строка «один оффер за раз»;
- `sosed.place/docs/17-offer_RU.md` — пункт про `PRIVATE_ACTIVE_OFFERS=1`;
- опубликовано: `terms_EN.md:76` перечисляет, какие офферы отклоняются, и не называет ни лимит одного живого оффера у частника, ни запрет дубля.

**Норма:**
- ст. 14(1), цитата выше;
- ст. 17(3)(e): «where the decision is based on the alleged incompatibility of the» information with the terms and conditions.

**Разрыв.** Появилось новое ограничение на публикацию, а договорного пункта, на который мог бы сослаться отказ, нет.

**Минимальная правка.** В `terms_EN.md` §10 добавить одну фразу: «a private person may have one live offer at a time; an offer identical to an earlier one is refused». Этот пункт указывать как основание отказа.

### 5. Дефект дизайна (ст. 14, ст. 17(3)(c) и (f)) — часовой потолок и пятнадцатиминутная пауза не описаны в соглашении

**Где:**
- `xor.ad/docs/refusal-wordings_RU.md:105` — «четыре за час»;
- `sosed.place/docs/11-empty-and-edge-states_RU.md:36` и `:86` — «Пятый отказ за час → пауза 15 минут»;
- `sosed.place/docs/20-step-away_RU.md` — пауза переживает отлучку;
- опубликовано: `terms_EN.md:64` — только «posting quotas… help enforce the rules»; о паузе за отказы нет ни слова.

**Норма:**
- ст. 14(1);
- ст. 17(3)(c): «where applicable, information on the use made of automated means»;
- ст. 17(3)(f): «clear and user-friendly information on the possibilities for redress».

**Разрыв:**
- Пауза — это ограничение на публикацию, и возникает оно из автоматических решений.
- Правило в day51 изменилось («подряд» → «за час»), а в соглашении нет ни числа, ни самого механизма.
- Текст паузы в `11:86` не говорит, что к паузе привели автоматические отказы, и не упоминает возможность пожаловаться координатору. Для отказов до публикации (`terms_EN.md:93`) это сказано.

**Минимальная правка:**
- В `terms_EN.md` §8 одной строкой: «4 per rolling hour; five automated refusals within an hour pause new posts for 15 minutes».
- В текст `11:86` добавить хвост: «решение автоматическое; жалоба — координатору цифровых услуг (соглашение §…)».

### 6. Противоречие (GDPR ст. 5(1)(c)) — `seen_at` в `support_requests` фиксирует момент, когда человек был в приложении

**Где:**
- `xor.ad/docs/chat_RU.md:2604` — `seen_at timestamptz`: «точка горит, пока ответ есть, а seen_at пуст».
- Против: `xor.ad/docs/chat_RU.md:829` — «о заходах узел не знает и узнавать не должен».
- Против: `xor.ad/docs/dpia_RU.md:67` — риск «Узнать, когда человек у экрана».

**Норма:** ст. 5(1)(c): «adequate, relevant and limited to what is necessary».

**Разрыв.** Чтобы погасить точку, достаточно знать, что ответ прочитан. Метка времени сверх этого записывает момент захода на год вперёд, и в DPIA этого нет.

**Минимальная правка** — одно из двух:
- хранить флаг прочитанного на устройстве;
- заменить колонку на `seen boolean` без времени.

В DPIA, строка 67, указать `support_requests` среди мер.

### 7. Дефект дизайна (GDPR ст. 5(1)(e)) — обращение в поддержку хранится год после удаления личности, а чистить его некому

**Где:**
- `xor.ad/docs/chat_RU.md:2599` — `identity … ON DELETE SET NULL`, `body`, `email`;
- `xor.ad/docs/chat_RU.md:2609` — «исполнителя у него по-прежнему нет»;
- `sosed.place/docs/00-mechanics_RU.md:774`;
- `sosed.place/docs/14-support_RU.md:25` и `:30`.

**Норма:** ст. 5(1)(e): «for no longer than is necessary for the purposes».

**Разрыв:**
- После «начать заново» цель «показать ответ» исчезает: экран 14:25 сам признаёт, что показывать некому.
- При этом `body` и `email` живут до года от `created_at`. Цель «запись о разборе» в политике не названа: там только «A support message: 1 year» (`privacy_EN.md:44`).
- Таблица входит в первую миграцию, а её уборщик отложен. Получается срок из политики без исполнителя с первого же дня.

**Минимальная правка:**
- Запрет выкатывать миграцию `support_requests` без задачи в `prune_*` (гейт в `migrations-step1`).
- При `SET NULL` обнулять `email`, а `body` сократить до срока разбора.
- Иначе назвать цель «запись о разборе» в `privacy_EN.md:44`.

### 8. Мелочь (GDPR ст. 13(2)(a)) — `stepped_away_at` хранится без срока и не упомянут в политике

**Где:** `xor.ad/docs/chat_RU.md:750–751` и `:829`. В опубликованном: `privacy_EN.md:41` («identity, year of birth, name, your settings») — состояние отлучки и её время не названы.

**Норма:** ст. 13(2)(a): «the period for which the personal data will be stored».

**Разрыв.** Колонка нужна, пока метка «отошёл» висит хоть в одной живой беседе. Правила обнуления нет, поэтому время последнего ухода остаётся в строке личности бессрочно.

**Минимальная правка.** Обнулять `stepped_away_at`, когда истёк `stepped_away_until` и не осталось живой беседы, где своя последняя реплика старше него. В политике добавить строку «when you last stepped away — until no conversation shows the label».

### 9. Мелочь — обещание «на диск не попадает» у клиента `depth` держится на флаге, который пользователь должен помнить

**Где:** `xor.ad/docs/depth-client_RU.md:72`, `:77`, `:744`.

**Норма.** GDPR здесь напрямую не применяется: запись идёт на хосте самого пользователя, у нас нет роли контролёра. Это вопрос точности нашего обещания, а не нормы, поэтому цитаты нет.

**Разрыв:**
- Гарантия теперь условная: «при запуске с `--log-driver none`». Стоит забыть флаг — и расшифрованная переписка лежит в `json-file`, а с драйверами `journald` и `syslog` остаётся и после `--rm`.
- Не нашёл описания, как это оговорено в местах, где пользователь реально копирует команду (README образа, страница установки).

**Минимальная правка.** Поставлять запуск обёрткой, в которой флаг вшит, и повторить оговорку в разделе про образ. Замер «с флагом файла лога нет» в документе заявлен, но сам я его не повторял — НЕ ПРОВЕРЕНО.

---

### Отдельно: «не нашёл описания»

- Что происходит с перенесённым в форму ст. 16 текстом, если форму закрыли без отправки (к находке 2).
- Мотивировка по ст. 17 автору-заведению при автоматическом гашении ссылки: в `offers/SPEC_RU.md` §10.1 стоит «автору не показывается». Это было и до day51, в этой ветке не ухудшено, поэтому находкой не заношу.

### Проверено и замечаний нет

- **Отказ «квота» и «четыре за час» с временем «в ЧЧ:ММ»** (`refusal-wordings_RU.md:104–105`). Это не решение о содержании, ст. 17(3) не требуется. Показывается только своё время, чужих сроков не выдаёт.
- **Ст. 20 (внутреннее обжалование).** Снята ст. 19 (`dsa/SPEC_RU.md:481`). В опубликованном `terms_EN.md:94` прямо сказано «We do not operate a formal internal appeals body». day51 этого не ухудшает.
- **8-часовая отлучка, очерёдность хода, пас и перевод в зрители** — ограничения игры и сессии, а не решения о содержании. Ст. 17 к ним не применяется. Для ст. 14 стоит упомянуть пас и перевод в зрители в правилах сообщества (`landing/legal/community-guidelines_RU.md`), но это мелочь.

## Опровержения

Отчёты опровергающих вставлены скриптом без правки. По умолчанию — «опровергнута».

### A — опровержение, безопасность и данные

Одна заявка подтверждена с высокой серьёзностью (S1). Ещё четыре подтверждаются как расхождения схемы средней или низкой серьёзности. Остальные частичные или опровергнуты.

```diff
- S1 подтверждена: «забыли ПИН?» противоречит канону, серьёзность высокая
! S2, S5, S7, S9: частично, у каждой есть подтверждённое ядро
+ S8, S11, D10 опровергнуты; у S13 ядро снято
@@ Подтверждены D4, D5, D11; частично S3, S4, S6, D7, D9
```

Все утверждения ниже ПРОВЕРЕНО чтением указанных строк. Базу в контейнере не поднимал: ни одна заявка не зависела от поведения СУБД. Код не открывал, его нет. Источники: репозитории `xor.ad` и `sosed.place`, ветка day51.

---

**S1 — подтверждена.** Серьёзность: высокая.
- `sosed.place/docs/12-identity-and-security_RU.md:53-56`: «отпереть без прежнего ПИНа можно только новым ПИНом с новой долей». Бумажный код не требуется. Ссылка стоит на замке, то есть отпертое устройство даже не нужно.
- `sosed.place/docs/11-empty-and-edge-states_RU.md:32` и `:75`: после десятой ошибки «человек задаёт новый ПИН, узел выдаёт новую долю», кнопка «Задать ПИН».
- Канон `xor.ad/docs/chat_RU.md:1177`: при сожжённой доле смена ПИНа запрещена, «выход один — бумажный код». Там же названа дыра, которую открывает обход: посторонний с вкладкой уводит личность.
- `chat_RU.md:1171`: смена ПИНа требует старый ПИН. `:1217-1220`: новая доля и новый ПИН выдаются только при восстановлении по бумажному коду.
- В `chat_RU.md` нет ни одного «забыли ПИН» (grep). Экран опирается на решение, которого в каноне нет.

**S2/D1/D2 — частично.** Серьёзность: средняя (противоречие в документах).
- Подтверждено: срок беседы не больше 260 минут (`chat_RU.md:1925`, `:54` — «пока говорим» это те же 4:20). Отлучка на 8 часов — 480 минут, TTL не останавливается (`:827`), значит `gone_at` у ушедшего наступает всегда (`:2168`). Фразы «переживут только беседы с долгим сроком» (`:827`, `sosed.place/docs/00-mechanics_RU.md:1077`) и расчёт «не переживут 2 из 7» (`20-step-away_RU.md:10`) для 8 часов ложны: не переживает ни одна беседа.
- Подтверждено: строки `pending_deliveries` умирают, как только беседа кончилась у любого из двоих (`chat_RU.md:2087-2091`). Обещание «написанное дождётся» (`:829`, `20-step-away_RU.md:40`, `00-mechanics_RU.md:1080`) дано без условия, а для 8 часов не выполняется. Для 20 минут и часа при достаточном сроке беседы оно держится.
- Опровергнуто про заморозку: «тем же путём, что при заморозке» (`:828`) означает механизм `NOTIFY` и закрытие сокетов (`:664`), а не `frozen_at`. Сессия при отлучке не замораживается, и правило `:2088` («сессия заморожена») не срабатывает.
- `chat.pending.ttl` 260 (`:2093`) не короче самой длинной беседы, так что сам по себе он не ломает ничего.

**S3/D3 — частично.** Серьёзность: низкая.
- `chat_RU.md:829`: условие `last_own_message_at < stepped_away_at`. Колонка `last_own_message_at` может быть NULL (`:1926`), а `stepped_away_at` не очищается (`:750`, «начало последней отлучки»). Поведение при NULL не описано.
- В SQL `NULL < x` не даёт true, так что при буквальной реализации метка в беседе, где человек ещё не писал, просто не появится, в том числе после свежей отлучки. Ложной метки не будет, но потеряется настоящая.
- Беседа, открытая после давней отлучки, покажет метку только при реализации через `COALESCE(-∞)`. Это ошибка реализации, а не спеки. Пробел — в том, что правило для NULL не определено.

**S4 — частично.** Серьёзность: низкая.
- `chat_RU.md:1944`: в ответе на открытие чата метки нет, стоит «И всё». `:829` говорит «узел показывает её», но способ доставки (поле при открытии, событие в сокете) не описан. Пробел подтверждён.
- Утечка чужих времён — предположение о наивной реализации. `:54` прямо запрещает показывать чужой остаток, так что реализация, отдающая `last_own_message_at` собеседника, нарушила бы уже записанное правило.

**S5 — частично.** Серьёзность: низкая/средняя.
- `chat_RU.md:1469`: реплики и заявки за столом питают счётчик отказов. `:1461`: блокируется «отправка», остаются «лента, лайки и чтение чатов».
- `sosed.place/docs/11-empty-and-edge-states_RU.md:86`: «не отправляются только новые фразы», `:36` и `refusal-wordings_RU.md:85-86`: «беседы работают». На что распространяется пауза, три места говорят по-разному. Стол не назван нигде. Расхождение подтверждено.
- Реплики стола как обход паузы — вывод из этой неясности. В каноне отказ сформулирован шире («отправка»), так что обход не записан явно.

**S6 — частично.** Серьёзность: низкая.
- `chat_RU.md:1469`: источники счётчика — фразы, реплики и заявки. Имени в списке нет. `:1726`: лайк на оффер без своей фразы отправляет имя в очередь.
- «Неограниченным» оракул не будет: лайки ограничены 64 за 32 минуты (`:1548`), рядом работает лимит частоты по адресу (`:879`, `:1245`), имя не длиннее 24 символов (`:743`). Подбирать всё равно дешевле, чем фразу: для отклонённого имени заморозки нет (`:812`), смена имени тоже идёт через очередь, и отдельный предел на неё не описан.

**S7/D6 — частично.** Серьёзность: средняя.
- `xor.ad/docs/offers/SPEC_RU.md:670-672`: считаются только жалобы личностей, «у которых была принятая публикация».
- Хранить этот признак негде: `identity_stats` (`chat_RU.md:1738-1745`) и все три `ALTER TABLE` (`:1326`, `:1458`, `:1875`) такой колонки не содержат, а `feed_messages` удаляются по сроку (`:2166`). Условие «была» после истечения фразы не вычисляется.
- «Одна безвредная фраза на личность» — верно: публикация стоит одной прошедшей фразы и принятого имени. Условие поднимает цену с двух приватных окон до двух опубликованных фраз, это замедлитель. Сама спека (`:675-677`) закладывает его ровно так.

**S8 — опровергнута.** Серьёзность: не дефект.
- `chat_RU.md:37` говорит о бесед между людьми: узел не должен знать, кто когда открывал беседу.
- `sosed.place/docs/14-support_RU.md:9` намеренно выводит ответ поддержки из инбокса: точка стоит на «Я». Это отношения человека с площадкой, и `seen_at` (`chat_RU.md:2605`) записывает прочтение нашего же ответа, а не чужой беседы. Правило `:37` на это не распространяется.

**S9/D8 — частично.** Серьёзность: средняя (правовая согласованность).
- `14-support_RU.md:27`: обращение «переводится в тот же реестр». `:30` и `chat_RU.md:2599`: `support_requests` хранит `identity` и `body` год.
- В `xor.ad/docs/dsa/SPEC_RU.md:432-446` у `notice` нет ни `identity`, ни ссылки на обращение. `:395-397` запрещает связывать «кто пожаловался» с личностью. Путь «поддержка → уведомление» в DSA-спеке не описан вовсе (grep: «поддержк» только на `:27` и `:497`, как почтовый канал). Связь уведомителя с личностью живёт в соседней таблице.
- Смягчение: тело, которое режет WAF, в `support_requests` не попадает (`14-support_RU.md:28-29`), клиент сам переносит его в форму. Утечка остаётся только для жалоб, которые WAF пропустил.

**S11 — опровергнута.** Серьёзность: нет.
- `sosed.place/docs/21-console_RU.md:60-63`: консоль живёт только в памяти, на узел ничего не уходит. Скриншот делает и пересылает сам уведомитель о себе.
- Для поддержки это не деанонимизация: обращение и так привязано к личности. Публичная выкладка — собственный выбор человека. Спека уже считает содержимое консоли опубликованным (`:47-52`) и поэтому не пишет туда идентификаторов. Строка не содержит ни цели, ни текста.

**S13 — частично, ядро опровергнуто.** Серьёзность: низкая.
- Лимит частоты по адресу стоит на всех публичных ручках узла (`chat_RU.md:879`, `:1245`, `lib/rate_limit.ts`). «Лимита нет вовсе» неверно. Своего лимита на личность у поддержки действительно нет, и в `limits.tsv` строки о поддержке нет (grep пуст).
- Подтверждено: `bigserial` показывается человеку как номер (`:2598`). Два своих обращения дают число всех обращений между ними — утечка объёма, небольшая.

**D4 — подтверждена.** Серьёзность: средняя.
- `chat_RU.md:1458`: одно `rejected_count integer`. `:1461` и `:1465`: «скользящий час», «отказ старше часа выпадает сам». Одним числом без меток времени этого не сделать.
- Остатку 15-минутной паузы, который «переживает отлучку» (`:1463`), колонки тоже нет.

**D5 — подтверждена.** Серьёзность: средняя.
- `chat_RU.md:1550` и `:1564`: «потолок за час — нет», при этом снятая фраза удаляется (`:1563`), и отлучка удаляет (`:825`, `DELETE`).
- `sosed.place/docs/00-mechanics_RU.md:196-199` и `20-step-away_RU.md:50` требуют, чтобы счётчик переживал уход. Хранения нет: единственные `ALTER TABLE` — `:1326`, `:1458`, `:1875`, в `identity_stats` счётчика публикаций нет (`:1738-1745`). Если считать по живым `feed_messages`, потолок сбрасывается ровно тем обходом, который он закрывает.

**D7 — частично.** Серьёзность: низкая.
- Отсутствие уборщика записано открытым пунктом (`chat_RU.md:818`), это не скрытый дефект.
- Настоящее расхождение: `14-support_RU.md:30` обещает, что «при "начать заново" связь рвётся», а `ON DELETE SET NULL` сработает только на `DELETE` через 30 дней. В списке действий закрывающей транзакции (`:818`) обнуления `support_requests.identity` нет.

**D9 — частично.** Серьёзность: низкая.
- ПРОВЕРЕНО grep по `CREATE INDEX`: на `support_requests(identity)` индекса нет. `chat_participants` имеет PK `(chat_id, identity)` (`:1928`), и поиск по одной `identity` его не использует.
- Каскад и `SET NULL` при удалении личности, как и выборка «мои обращения», будут идти полным просмотром. Таблицы пока только в спеке (`open.tsv:127`), так что это замечание к будущей миграции.

**D10 — опровергнута.** Серьёзность: нет.
- `chat_RU.md:400-403`: `table_games.state jsonb` хранит «позицию, запас, чей ход и руки», и jsonb выбран как раз ради разной формы состояния. Срок хода и счёт пасов ложатся туда без новой колонки.
- Отдельного поля действительно нет, но «хранения нет» не следует.

**D11 — подтверждена.** Серьёзность: низкая.
- ПРОВЕРЕНО grep `^CREATE TABLE`: в `chat_RU.md` их 23 (последняя — `support_requests`, `:2597`), в `offers/SPEC_RU.md` — 3. Итого 26, как в `docs/facts/schema.tsv:41-75`.
- `open.tsv:127` пишет «22 в спеке чата»: при добавлении `support_requests` итог подняли, а слагаемое не поправили.

Попутно: `chat_RU.md:1461` («чтение чатов остаются») читается так, будто пауза запрещает писать в беседу, а экраны `sosed.place` говорят «беседы работают». Это та же неясность, что в S5, только с беседами вместо стола.

### B — опровержение, согласованность и регулирование

Итог по панели: из 23 заявок подтверждены 12, частично 6, опровергнуты 5. Всё подтверждённое касается согласованности документов, правовых дыр я не нашёл: жёстче «желтого» ничего нет. Файлы не менял.

```diff
+ подтверждены 12: C2 C3 C4 C5 C6 C8 C9 C11 C15a C15b C15c P7
! частично 6: C7 C10 P1 P3 P5 P8
@@ опровергнуты 5: C12 C14 P2 P4 P6
```

Источник заявок — панель ревью. Каждую я перепроверил сам по строкам, указанным ниже (открывал через `sed`/`grep`). Пути `docs/NN-*` и `landing/legal/*` лежат в репозитории `sosed.place` (ветка day51), `docs/chat_*`, `dsa/*` и `offers/*` — в `xor.ad`.

## Согласованность

**C2 — подтверждена, 🟡 средняя.** ПРОВЕРЕНО.
- `xor.ad/docs/chat-flows_RU.md:754` и `_EN.md:771`: «Блокировка скрывает стол целиком», пометки [retired] нет.
- `sosed.place/docs/19-table_RU.md:121` и `00-mechanics_RU.md:1060` (§12): выходит заблокировавший, партия идёт дальше. В механике старая формулировка уже помечена [retired], в потоках нет.

**C3 — подтверждена, 🟡 средняя.** ПРОВЕРЕНО.
- `test-map_RU.md:289` и `_EN.md:288` (15.12): «стол не показывается вовсе». По этому тесту проверялось бы отменённое поведение.

**C4 — подтверждена, 🟡 средняя.** ПРОВЕРЕНО.
- `chat_RU.md:1726` и `17-offer_RU.md:17`: имя лайкнувшего проходит очередь до мэтча.
- В потоке 10 (`chat-flows_RU.md:410-431`) этого шага нет: на схеме сразу «я лайкнул → мэтч».
- Тест 10.6 (`test-map_RU.md:210`) ждёт «мэтч создан» без вердикта по имени. Тест 9.12 (`:201`) покрывает только взаимный лайк.

**C5 — подтверждена, 🟡 низкая.** ПРОВЕРЕНО.
- `refusal-wordings_RU.md:70-80`: «Имя не прошло проверку — фраза подождёт… опубликуется сама».
- У того, кто лайкает оффер без своей фразы, публиковаться нечему, и после исправления имени мэтч сам не появится. Экран `17-offer_RU.md:17` повторяет ту же ссылку.

**C6 — подтверждена, 🟡 низкая.** ПРОВЕРЕНО.
- `chat_RU.md:20, :35, :1859` и `06-match-and-chat_RU.md:10`: «остатки обеих фраз», оговорки для оффера нет.
- `06-match-and-chat_RU.md:30` и `chat-flows_RU.md:429`: фраза одна, срок жизни считается по офферу. Сам экран 06 оговорку содержит, в каноне её нет.

**C7 — частично, 🟡 косметика.** ПРОВЕРЕНО.
- `chat_RU.md:2582` («десять таблиц») стоит без [retired].
- Но через девять строк, в `:2591`, прямо сказано: «Одиннадцатая — `support_requests`, добавлена 14.09.2026». То же в `chat_EN.md:2616/:2625`. Это дополнение, а не противоречие: не исправлен только заголовок.

**C8 — подтверждена, 🟡 средняя.** ПРОВЕРЕНО.
- `chat_RU.md:2211` и `chat_EN.md:2231`: в строке «Мэтч» стоит «таймер».
- `test-map_RU.md:200` (9.11): «имя, возраст, режим и таймер — и только их». На карточке теперь два остатка (`:20`), значит тест проверяет не тот состав полей.

**C9 — подтверждена, 🟡 средняя.** ПРОВЕРЕНО.
- `chat_RU.md:2213`: в «Никогда» стоят «сроки истечения чужих фраз».
- `:2224-2228` уточняет только свои сроки квоты. А карточка мэтча (`:20`, `06-match-and-chat_RU.md:10`) отдаёт остаток фразы собеседника, то есть чужой срок.
- 🟠 Если исключение для уровня «Мэтч» задумано, в §8.11 оно не записано.

**C10 — частично, 🟡 низкая.** ПРОВЕРЕНО.
- `chat_RU.md:655` и `19-table_RU.md:121`: «выходит» и тут же «его ход становится пасом по `table.move.window`, как у любого отсутствующего».
- Чтение «место пустует до срока хода» возможно, так что это не прямое противоречие. Но непонятно, остаётся ли он в очереди ходов и почему не снимается сразу; тот же стол обычно ведёт к «трём пасам» (`11-empty…:96`).

**C11 — подтверждена, 🟡 низкая.** ПРОВЕРЕНО.
- `chat_RU.md:93`, класс «Текст»: «буква не повторяется, слово угадано…», «чей ход» нет. В строках домино и точек (`:90-91`) очередь добавили 14.09.
- `chat-flows_RU.md:716` и `18-game-in-chat_RU.md:44` говорят «во всех классах».

**C12 — опровергнута.** ПРОВЕРЕНО.
- `chat-flows_RU.md:720`: «допустимость хода **по классу доски**» — это ссылка на таблицу классов (`chat_RU.md:88-94`), где для каждого класса перечислено, что проверяет движок.
- `chat_EN.md:275` говорит так же: «checks what the class table lists». Противоречия нет.

**C14 — опровергнута.** ПРОВЕРЕНО.
- `chat_RU.md:1461`: счётчик растёт на каждом `rejected` в разделе публикации ленты.
- Ни в `19-table_RU.md`, ни в `00-mechanics_RU.md`, ни в `chat_RU.md` не нашлось, что отклонённая реплика за столом тоже его наращивает. Строка `11-empty…:94` про реплику за столом о счётчике молчит.
- Утверждение «счётчик питается репликами стола» не доказано.

**C15a — подтверждена, 🟡 низкая.** ПРОВЕРЕНО.
- `11-empty-and-edge-states_RU.md:71`: «Освободится место — можно снова», без времени.
- `refusal-wordings_RU.md` §5: «Ближайшая освободится в ЧЧ:ММ». Соседняя строка «Четыре за час» (`11-empty…:72`) время уже называет.

**C15b — подтверждена, ⚪ косметика; номер строки в заявке неверный.** ПРОВЕРЕНО.
- `chat_RU.md:274`: «(до 09.09.2026 — «сыграть заново»)».
- Английский аналог стоит на `chat_EN.md:245`, а не на `:274`, и пометки там нет.

**C15c — подтверждена, 🟡 низкая.** ПРОВЕРЕНО.
- `chat_RU.md:2215-2221` («экран квоты не показывает время») стоит без [retired]. Уточнение ниже (`:2223-2228`) его отменяет, но исходный абзац не помечен.
- Попутно: то же в `refusal-wordings_RU.md`, раздел «Открыто» — «Время освобождения слота не показывается… «через несколько минут»» без [retired], хотя §5 того же файла показывает ЧЧ:ММ.

## Регуляторика

**P1 — частично: правовой дыры нет, есть 🟡 дыра в интерфейсе.** ПРОВЕРЕНО.
- Ст. 16(4) требует подтверждения только при указанном электронном контакте. `dsa/SPEC_RU.md:393-401` записывает это решение сознательно, с открытым пунктом `dsa.article16.no-email`. Пункт 16(5) там же назван выводом не юриста. Закрытым я его не считаю.
- Интерфейсная часть подтверждена. `14-support_RU.md:12, :23` обещает ответ в приложении. Жалоба, перенесённая в форму ст. 16 (`:29`, почта пустая), решения без почты не получит. Строки `11-empty…:47` и `:98` об этом молчат.
- Смягчает то, что по `SPEC:396` сама форма говорит «без почты решение не придёт». Сам экран формы я не открывал: НЕ ПРОВЕРЕНО.

**P2 — опровергнута.** ПРОВЕРЕНО.
- `dsa/SPEC_RU.md:27`: «письмо без полей формы принимается» относится к каналу «почта поддержки», а не к обращению из приложения.
- `14-support_RU.md:28-29`: такое тело режет WAF до узла. Фактического знания у площадки нет, и неотправленная форма ничего не порождает. Требование галочки добросовестности следует из §4 спеки.

**P3 — частично, 🟡 низкая.** ПРОВЕРЕНО.
- `terms_EN.md:77`: «report it… and it is switched off» — упрощение.
- `offers/SPEC_RU.md:666-672`: ссылка гаснет автоматически после двух жалоб от людей с принятой публикацией, остальные жалобы уходят модератору.
- Ст. 14 не требует такой детали, но опубликованный текст обещает больше, чем делает продукт. Нужна правка формулировки, не дыра.

**P4 — опровергнута.** ПРОВЕРЕНО.
- `terms_EN.md:64`: «posting quotas» и право отклонять контент. `:78`: «We may reject or remove any offer».
- Ст. 14 требует описать меры модерации в общем, а не каждый отказ. Лимит «один живой оффер» — механика квоты, прямо она не названа: ⚪.

**P5 — частично, 🟡 низкая.** ПРОВЕРЕНО.
- `terms_EN.md:64` называет квоты и автоматическую предмодерацию, но не часовой предел и не паузу.
- Текст паузы (`refusal-wordings_RU.md` §4) не говорит об автоматике и о возражении. Для скрытия по порогу жалоб это сделано в §6, для паузы нет.
- Ст. 14 и 17 не снимаются ст. 19. Но 15-минутная пауза отправки — спорный кандидат на «ограничение» по ст. 17(1).

**P6 — опровергнута.** ПРОВЕРЕНО.
- `chat_RU.md:2605`: `seen_at` нужен для точки «ответ ждёт».
- `dpia_RU.md:67` говорит о риске, что момент у экрана узнает другой человек. `seen_at` видит только оператор, и значение то же, что у «ответ показан». Максимум — можно заменить на boolean: ⚪.

**P7 — подтверждена, 🟡 средняя.** ПРОВЕРЕНО.
- `chat_RU.md:2609-2610`: «исполнителя у него по-прежнему нет: чистка появится вместе с таблицей».
- `chat_RU.md:2599` и `14-support_RU.md:31`: при удалении личности `ON DELETE SET NULL`, запись с `body`/`email` остаётся.
- `privacy_EN.md:44` называет срок (1 год), но не цель хранения после разрыва связи («запись о разборе»). Раздела о целях по поддержке в политике я не нашёл.

**P8 — частично, ⚪/🟡 низкая.** ПРОВЕРЕНО.
- `chat_RU.md:750`: `stepped_away_at` лежит в `identities` и живёт вместе с личностью. Её срок хранения «пока пользуетесь» дан в `privacy_EN.md:43`, так что без срока поле не остаётся.
- В перечне «Profile data» это поле не названо. Это неполнота списка, а не отсутствие срока.

## Попутно
- 🟡 `refusal-wordings_RU.md`, «Открыто», последний пункт: противоречит §5 того же файла («через несколько минут» против «в ЧЧ:ММ»), пометки [retired] нет.
- 🟠 Ст. 19 снимает только ст. 20–28. Опора на неё корректна, запись о статусе микропредприятия есть (`dsa/SPEC_RU.md:481-483`, `CHECKLIST_RU.md:14`). На P1, P3 и P5 это не влияет: там ст. 14, 16 и 17.

Список короткий: дальше очевидно ровно одно — передать вердикты по заявкам агенту-сводчику панели. Мой запуск ничего не менял.

## Сводка ведущего

Сверено ведущим инструментами (grep, awk, чтение строк); у каждой строки — кто нашёл и чем подтверждено.

### Ошибка опровергающего

- **C14 / S5 снята опровергающим B неверно.** B: «не нашлось, что отклонённая реплика за столом наращивает счётчик». ПРОВЕРЕНО ведущим: `xor.ad/docs/chat_RU.md:1469` — «Счётчик питается **всем, что проходит очередь модерации**: фразами ленты, репликами и заявками за столом». Находка восстановлена (см. ниже), опровергающий A её же подтвердил частично.

### 🔴 Критично

| ID | Находка | Источник | Статус |
|---|---|---|---|
| S1 | «Забыли ПИН?» и «задайте новый ПИН» после десятой ошибки отдают личность тому, у кого запертая вкладка: новый ПИН и новая доля без бумажного кода | линза «Безопасность», подтверждена A | ПРОВЕРЕНО ведущим: `sosed.place/docs/12-identity-and-security_RU.md:53-56`, `11-empty-and-edge-states_RU.md:32,:75` против `xor.ad/docs/chat_RU.md:1171,:1177`. Внесено в day51 (решение 14.09 «Два состояния и забыли ПИН? с ценой») |

### 🟡 Дефекты и противоречия — требуют решения

| ID | Находка | Источник | Статус |
|---|---|---|---|
| S2/D1/D2/C1 | Отлучка 8 ч длиннее самого долгого срока беседы (260 мин): её не переживает ни одна беседа, а тексты пишут «переживут долгие»; обещание «написанное дождётся» (правка S18 этого дня) для 8 ч ложно — `pending_deliveries` умирают с беседой | три линзы, подтверждена A (про заморозку опровергнуто) | ПРОВЕРЕНО ведущим: `chat_RU.md:1925` (10/30/60/260), `:827`, `:2093` |
| S7/D6/C13 | S14 «была принятая публикация»: признак хранить негде, фразы удаляются; цена атаки — одна фраза на личность | «Безопасность», «Данные», «Согласованность»; A частично | НЕ ПРОВЕРЕНО ведущим; A: `chat_RU.md:1738-1745` без колонки |

### 🟡 Дефекты — закрываются правкой

| ID | Находка | Источник | Статус |
|---|---|---|---|
| S5/C14 | Пауза после 5 отказов по экранам блокирует «только новые фразы», а счётчик кормят реплики и заявки стола — и канон говорит «отправка» | «Безопасность», «Согласованность» | ПРОВЕРЕНО ведущим: `chat_RU.md:1469`, `11-empty-and-edge-states_RU.md:86` |
| D4 | Скользящий час отказов записан одним `rejected_count integer`; остатку паузы колонки нет | «Данные», подтверждена A | НЕ ПРОВЕРЕНО ведущим |
| D5 | «4 публикации в час» нечем считать после `DELETE` фраз (снятие, отлучка) — потолок сбрасывается обходом, который закрывает | «Данные», подтверждена A | НЕ ПРОВЕРЕНО ведущим |
| S3/S4 | Метка «отошёл»: правило для `last_own_message_at IS NULL` не определено; способ доставки метки собеседнику не описан (`:1944` «И всё») | «Безопасность», «Данные»; A частично | НЕ ПРОВЕРЕНО ведущим |
| S9/D8 | Жалоба, переведённая из поддержки в реестр ст. 16, оставляет `identity`+`body` в `support_requests` — связь уведомителя с личностью, запрещённая `dsa/SPEC_RU.md:393-397` | «Безопасность», «Данные»; A частично | НЕ ПРОВЕРЕНО ведущим |
| D7 | `14-support_RU.md:30` «при начать заново связь рвётся», а `SET NULL` сработает только на `DELETE` через 30 дней | «Данные»; A частично | НЕ ПРОВЕРЕНО ведущим |
| P1 | Жалоба, перенесённая в форму ст. 16 с пустой почтой, не получит решения, а экран 14 обещает ответ в приложении | «Протоколы»; B частично (правовой дыры нет, есть интерфейсная) | НЕ ПРОВЕРЕНО ведущим |
| P7 | Обращение с `body`/`email` живёт год после разрыва связи, уборщика нет, цель в политике не названа | «Протоколы», подтверждена B | НЕ ПРОВЕРЕНО ведущим |
| C2, C3 | «Блокировка скрывает стол целиком» без `[retired]` в `chat-flows` и тесте 15.12 | «Согласованность», подтверждена B | ПРОВЕРЕНО ведущим: `chat-flows_RU.md:754`, `test-map_RU.md:289` |
| C4, C5 | S7 не дошло до потока 10 и теста 10.6; текст отказа §3 про фразу не подходит лайкнувшему оффер | «Согласованность», подтверждена B | НЕ ПРОВЕРЕНО ведущим |
| C6, C8, C9 | «Остатки обеих фраз» без оговорки для мэтча от оффера; «таймер» в §8.11 и тесте 9.11; «Никогда: чужие сроки» против остатка чужой фразы | «Согласованность», подтверждена B | НЕ ПРОВЕРЕНО ведущим |
| C10, C11 | Ушедший из-за стола «пасует»; у класса «Текст» нет «чей ход» при «во всех классах» | «Согласованность»; B подтвердил C11, C10 частично | НЕ ПРОВЕРЕНО ведущим |

### ⚪ Мелочи

- **C7:** заголовок «десять таблиц» при одиннадцатой ниже. ПРОВЕРЕНО ведущим: `chat_RU.md:2582`, `chat_EN.md:2616`.
- **D11:** `open.tsv:127` «22 в спеке чата», должно быть 23. ПРОВЕРЕНО ведущим: `awk` по `schema.tsv` — 23 + 3 = 26.
- **C15a–c:** нет «в ЧЧ:ММ» в строке квоты экрана 11; RU/EN пометка «сыграть заново» (EN на `:245`); абзац 08.09 про экран квоты без `[retired]` (и то же в «Открыто» `refusal-wordings`). Подтверждены B, НЕ ПРОВЕРЕНО ведущим.
- **D9:** нет индексов `support_requests(identity)`, `chat_participants(identity)` — к будущей миграции. «Данные» (Docker EXPLAIN), A частично.
- **S6:** имя не питает счётчик отказов; подбор имени через лайк оффера ограничен лайками 64/32 мин. A частично.
- **S13:** `bigserial` как видимый номер выдаёт объём обращений; своего предела на личность у поддержки нет. A частично.
- **P3, P5, P8:** опубликованные `terms_EN.md`/`privacy_EN.md` проще спеки (гашение ссылки, пауза, `stepped_away_at`). B частично.

### Отброшено (опровергнуто)

S8 (`seen_at` — прочтение ответа площадки, не чужой беседы), S11 (консоль в памяти, скриншот — выбор человека), D10 (`table_games.state jsonb` держит «чей ход»), C12 («по классу доски» = таблица классов), P2 (письмо без полей — почтовый канал, не форма), P4 (квоты названы в соглашении), P6 (`dpia` о другом человеке, не об операторе). Заморозка очереди при отлучке (часть S2) — опровергнута A: «тем же путём» = NOTIFY, не `frozen_at`.

## Нарезка на задачи

1. **🔴 S1 — вернуть «забыли ПИН?» и десятую ошибку на бумажный код.** `sosed.place` и `neighbro.place`: `12-identity-and-security_RU/EN`, `11-empty-and-edge-states_RU/EN` (8 файлов); переоткрывает решение 14.09. ~20 мин.
2. **🟡 Развилка «8 часов».** Либо честная цена («8 ч не переживёт ни одна беседа, написанное пропадёт») в каноне §8.2, `00-mechanics` §13, экране 20, либо третья ступень не длиннее 4:20. Решение пользователя. ~20–30 мин.
3. **🟡 Хранение для правил day51.** D4 (моменты отказов), D5 (моменты публикаций), S7/D6 (`first_published_at`), S5/C14 (что блокирует пауза — везде одинаково), S3/S4 (NULL и доставка метки). Канон + реестр схемы + экраны 11. ~45 мин.
4. **🟡 Поддержка и ст. 16.** S9/D8, D7, P1, P7: обнуление связи при переводе и при «начать заново», строка о почте над формой, цель хранения. ~30 мин.
5. **🟡/⚪ Уборка согласованности.** C2–C11, C15, C7, D11 — механические правки с `[retired]`. ~40 мин.
6. **⚪ Юр-тексты и мелочи.** P3, P5, P8 в `landing/legal`; D9, S6, S13 — к миграции. Отложено.
