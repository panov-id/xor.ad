# Панель 18.09.2026 · «свежий глаз» по 27 дорисованным кадрам (коммит d299315)

**Дата:** 18.09.2026, 18:31–19:40, на автоматике до 20:00 по слову владельца («работай над UI + UX, делай тесты; если затык — решай кворумом»).
**Что ревьюили:** 27 кадров, добавленных пятью параллельными рисовальщиками (коммит d299315), на десяти листах `panel/design/screen-{02,06-07-10,08,12-13,14-15-18,17-venue,19,20-21-22-24,23,25}.svg`; рендеры после f00c589; описания репозитория `sosed.place`, спека офферов, канон `chat_RU.md`.
**Состав линз (3):** эвристики Нильсена · доступность + визуал · согласованность с описаниями. Метод взят из найденных в сети чеклистов heuristic evaluation (Nielsen 10) и практик visual testing на Playwright — последняя легла в ворота `check-design-text.sh` того же дня.

---

## 1. Сырые находки линз (как вернули агенты, без правки)

### 1.1 Линза «Согласованность с описаниями»

Источники: `/home/eugene-panov/Projects/panov-id/xor.ad` (листы, `docs/chat_RU.md`, `docs/offers/SPEC_RU.md`, `docs/facts/limits.tsv`) и репозиторий **sosed.place** (`git@panov-id.github.com:panov-id/sosed.place.git`, ветка day55) — файлы `docs/NN-*_RU.md`. Разбор — чтением; кадры в браузере не рендерил.

```diff
- 1 кадр противоречит решению владельца того же дня (лист 06-07-10, кадр R)
- 1 кадр обещает то, чего описание запрещает (лист 12-13, кадр 20)
! 7 выдуманных текстов без пометки «текст мой»
@@ 4 ложные ссылки в подписях + 4 числа листа 17 отсутствуют в limits.tsv
+ 5 подсказок листа 24 и тексты стола 19:35 — дословны описанию
```

### (б) Кадры, противоречащие описанию

🔴 **screen-06-07-10.svg : кадр R, translate(463,3958)**
Подпись: «срок беседы пошёл со второго нажатия (06:11, 06:39)»; кадр показывает «остаток 60 мин» в момент, когда второй согласился.
Описание: «**Когда нажал первый** (решение владельца 18.09.2026; **отменяет решение кворума линз того же дня**). Беседа открывается у него сразу в состоянии «ждём ответа»» — `sosed.place/docs/06-match-and-chat_RU.md:33`.
Вердикт: **расходится**. Кадр нарисован по отменённому решению кворума. Уровень: критично — правило суток, отменённое владельцем в тот же день.

🔴 **screen-12-13.svg : кадр 20, translate(1740,2950)**
Цитата с кадра: «Прежний бумажный код использован. **Новый выдаёт экран личности.**»
Описание: «**Перевыпустить бумажный код — только предъявлением текущего.**» — `sosed.place/docs/12-identity-and-security_RU.md:89`.
Вердикт: **расходится**. После восстановления прежний код погашен, предъявить нечего — обещание на кадре невыполнимо. Уровень: критично (кадр обещает страховку, которой нет).

🟡 **screen-17-venue.svg : кадр жалоб, translate(1740,2222)**
Цитата: «три — и заведение приостанавливается».
Описание: «`SUSPEND_RESOLVED_COUNT` решений `resolved` … → `verification_status: suspended` у заведения, **ставится модератором руками**» — `xor.ad/docs/offers/SPEC_RU.md:656`.
Вердикт: **расходится** в механике (кадр читается как автомат). Уровень: дефект.

🟡 **screen-19.svg : кадр A0, translate(48,2916)** — заливная кнопка «заявка на следующую **игру**».
Описание говорит «заявка **на следующий круг**» (`19-table_RU.md:52`), «заявкой на следующую **партию**» (`24-hints_RU.md`, строка «первый стол в ленте»).
Вердикт: **расходится** словарно. Оговорка: строка не нова — в листе она была до этого коммита (3 вхождения в `d299315^`), кадр A0 её переиспользовал. Уровень: дефект, унаследованный.

### (а) Тексты, выдуманные без пометки «текст мой»

Пометку «текст мой» в этом коммите несут ровно два кадра (06-07-10 S и T). Остальное придумано молча:

🟠 **screen-12-13 : кадр 18, translate(894,2950)** — заголовок «**Перенос отклонён**» и тело «Переноса не было: личность осталась на этом устройстве. Код погас — начните заново.» В описаниях такого заголовка нет: `13-devices_RU.md:56` знает только «**Перенос отменён**» (второй ввод кода). Подпись ссылается на `chat_RU.md:1147` — там «переноса не было» есть, но это про молчание/закрытую вкладку, не про «отклонить». Вердикт: **не описано**, без пометки.

🟠 **screen-12-13 : кадр 19, translate(1317,2950)** — заголовок «**Личность переехала сюда**». Ближайшее в каноне — `chat_RU.md:1176` «Личность переехала на другое устройство», и это текст **прежнего** устройства, а кадр — новое. Тело кадра — пересказ, не цитата: «На новом устройстве беседы будут те же, но пустые» против «**На новом устройстве беседы те же, а история пуста**» (`13-devices_RU.md:64`); «Прежнее устройство замерло: новых сообщений оно больше не получает» против «прежнее устройство **замирает немедленно**: его подпись больше не принимается…» (`13-devices_RU.md:61`). Вердикт: **не описано дословно**, без пометки.

🟠 **screen-12-13 : кадр 20** — заголовок «**Новый ПИН**» не задан ни в одном описании.

🟠 **screen-17 : кадр translate(48,2222)** — заголовок «**Ссылка истекла**». Существо совпадает (`SPEC_RU.md:142` «15 минут, гасится при первом использовании»), заголовок выдуман.

🟠 **screen-17 : кадр translate(471,2222)** — заголовок «**Код неверный**». Тело «После нескольких неверных код гасится, нужен новый конверт» ≈ `SPEC_RU.md:155–156` «после нескольких неверных код гасится и … нужен новый конверт» — **совпадает**; заголовок выдуман.

🟠 **screen-17 : кадры translate(894,2222) и (1317,2222)** — заголовки «**Опубликовано**» и «**Публикация отклонена**». Второй берётся из колонки таблицы `SPEC_RU.md:452–455» («Публикация отклонена» как исход), первого в спеке нет вовсе.

🟢 Дословны и проверены: пять карточек подсказок на screen-20-21-22-24 (все пять совпадают с таблицей `24-hints_RU.md`, строки «первое предложение поговорить», «первая открытая беседа», «первое скрытие», «первый оффер в ленте», «первый лайк стола»); «вы за столом с этого момента» и «за столом шифрования нет: узел видит речь, доску и стикеры» на screen-19 (`19-table_RU.md:35`); «собеседник изменил возраст: 39» (`10-settings-screen_RU.md:22`); «Обращение принято», «к списку», «читаем всё, отвечаем не сразу — нас мало», номер `7K3M9Q2XZ4` (`14-support_RU.md:25,26,42`); «Не совпадает с кодом — сверьте с бумагой.» (`02-name-screen_RU.md:72`); «это я»/«не совпадает» (`chat_RU.md:1138`, `13-devices_RU.md:87`).

### (в) Имена в косвенном падеже

🟢 **Не найдено.** Все вхождения — именительный: «Аня согласилась · беседа открыта», «ход: Аня», «Аня · 3», «Аня · остаток 1 ч 10 мин», «Олег, 39», «Миша, 31», «Ира, 35». Правило `names-not-declined` выдержано на всех 27 кадрах.

### (г) Кнопки и заголовки

🟡 **«Отмена» — существительное с заглавной** рядом с глаголами строчными: screen-08 кадр J («закончить» / «Отмена», translate(48,2916)) и screen-06-07-10 кадр S («сохранить» / «Отмена»). На кадре S это **санкционировано описанием**: `10-settings-screen_RU.md:19` прямо пишет «кнопками «сохранить» / «Отмена»». На кадре J описание кнопки отказа не задаёт — расхождение стиля не покрыто ничем. На screen-17 — «Продолжить» / «Отмена», и это **дословно** `SPEC_RU.md:504` `[ Продолжить ] [ Отмена ]`. Уровень: дефект стиля, частично узаконенный описаниями.

🟠 **screen-08, кадр J**: цитата с кадра «**Точно? История исчезнет у обоих**» против описания «точно? история исчезнет у обоих» (`08-chat-screen_RU.md:38`) — регистр изменён; и «Собеседник **увидит** то же надгробие» против «собеседник **видит** то же надгробие» (`08-chat-screen_RU.md:115`) — время глагола изменено. Цитата в кавычках подписи заявлена как дословная, а текст на кадре ей не равен.

🟢 Заголовки состояний с заглавной — выдержаны везде («Перенос отклонён», «Ссылка истекла», «Публикация отклонена», «Перейти в полосу 21+?», «Опубликовано», «Новый ПИН», «Обращение принято»).

### (д) Числа

🟠 **Четыре числа листа screen-17 в `docs/facts/limits.tsv` отсутствуют** (реестр их не знает; они живут только в таблице констант `SPEC_RU.md:399–407`):
| Число на кадре | Константа спеки | В limits.tsv |
|---|---|---|
| «Ссылка … живёт 15 минут» (translate 48,2222) | `ACTIVATION_LINK` / `SPEC_RU.md:142` | **нет** (есть чужие 15: `recovery.miss.pause`, `claim.miss.pause`, `posting.silence.mute`) |
| «жалоб 3 от разных людей» (1740,2222) | `AUTOHIDE_COMPLAINTS` = 3 | **нет** |
| «разбор до 19.09.2026, 14:00» (сутки) | `COMPLAINT_EXAMINATION_HOURS` = 24 | **нет** |
| «обоснованных за 90 дней 1 из 3» | `SUSPEND_RESOLVED_COUNT` = 3, `SUSPEND_WINDOW_DAYS` = 90 | **нет**; при этом в реестре есть другая 90 — `offer.card.device.retention`, и её легко перепутать |

🟢 Сошлись с реестром: «0/256» → `chat.message.length` 256 (limits.tsv:32); «0/128» на столе → `phrase.length` 128 (limits.tsv:31) и `19-table_RU.md:94` «те же 128 символов»; «остаток 60 мин» → `chat.idle.ttl` 10 30 60 260 (limits.tsv:52); «ход: Аня 4 мин» → `table.move.window` 5 минут, отсчёт (и дословно `19-table_RU.md:35` «ваш ход · 4 мин»); «2:00» → `invite.lifetime` 120 секунд (limits.tsv:54) и `13-devices_RU.md:55»; «9 символов» → `transfer.code.length` (limits.tsv:69); «16 символов» / `8K3P Q7ZW XN2M 5AY9` → `recovery.code.length` (limits.tsv:58); «Карточка живёт в ленте 4:20» → `feed.phrase.span` (limits.tsv:63) и `17-offer_RU.md:13`.

### Ложные ссылки подписей (строки нет либо она о другом)

1. **screen-02, кадр 8 (48,1022)** — «(chat_RU.md:1304)» под цитатой «не пропускает, пока не введены две группы». Строка 1304 — про вытеснение IndexedDB браузером. Настоящее место — **chat_RU.md:1302**: «дальше не пропускает, пока не введены две группы из четырёх». Промах на две строки.
2. **screen-02, кадр 9 (471,1022)** — «(02-name-screen_RU.md:83)» под «ошибка стоит личности». **Строка 83 пустая** (между абзацем про закрытую вкладку и заголовком «## Что должно быть сказано человеку»). Смысл лежит на строке 82 и в 72.
3. **screen-02, кадр 9** — «**(PANEL:261)**». Файла/реестра с таким именем нет; строка встречается ровно один раз во всём `panel/design` — в этой самой подписи. Непроверяемая ссылка. (Сам текст «Не совпадает с кодом — сверьте с бумагой.» дословен `02-name-screen_RU.md:72`.)
4. **screen-06-07-10, кадр R** — «(06:11, **06:39**)». `06-match-and-chat_RU.md:39` — навигационная строка «→ [Экран 7 — Беседы]: мэтч ждёт ответа на вкладке «Предложения»». О начале срока беседы не говорит ничего; нужное — строка **33**, и она кадру противоречит (см. выше).
5. **screen-12-13, кадр 19 (1317,2950)** — «(13-devices_RU.md:60, 66, **68**)». Строка 68 — заголовок «## Что должно быть сказано человеку»; строка 66 — про отсутствие отсрочки заморозки и бумажный код, к «доле хранилища» отношения не имеет. Верны только 60 (свой ПИН, доля устройства) и не названные 61, 64.
6. **screen-20-21-22-24, кадр ленты (471,3120)** — «лента наполняется заново (**20:46**)». `20-step-away_RU.md:46` **пустая**; текст «Чистая лента, которая наполняется заново» — на строке **47**. Промах на строку. (Соседняя ссылка «20:47-48» на «ждёт вашего ответа» — верна, это строка 48.)

Проверены и **верны**: `02-name-screen_RU.md:72`; `10-settings-screen_RU.md:17–19, 21–23, 25, 44`; `24-hints_RU.md:10, 13`; `08-chat-screen_RU.md:38, 115`; `chat_RU.md:1138, 1147, 1149, 1254`; `13-devices_RU.md:55, 87`; `14-support_RU.md:9, 25, 26`; `19-table_RU.md:35, 36`; `20-step-away_RU.md:48`; `03-feed-screen_RU.md:15`; `05-message-actions_RU.md:18`; `23-phrase-viewer_RU.md:25`; `25-my-likes_RU.md:10`; `SPEC_RU.md:142, 440, 754–756`, §6.1 (таблица 452–455), §10.1–10.2.

### Находка на соседнем, **не новом** кадре (для сведения)

🟡 **screen-14-15-18.svg, кадр 14H «Ответ прочитан», translate(48,4676)** — кадр существовал до d299315, коммит лишь переверстал его подпись. Подпись обещает «строку «**ответ прочитан · дата**»» со ссылкой просто «(chat_RU.md)», без строки. Канон: «`seen_at` заменён на `answer_seen` [retired]: **момент прочтения никому не нужен**, а точке в «Я» хватает признака» — `xor.ad/docs/chat_RU.md:2824`. Даты для этой строки в базе нет. Строки «ответ прочитан» нет ни в одном описании. Вердикт: **расходится с каноном**, но это не один из 27 кадров коммита.

### Чего НЕ проверял

- **Ничего не рендерил.** Кадры разобраны как XML — текст, координаты групп; как они выглядят, помещается ли текст, не перекрывают ли слои друг друга, я не смотрел (для этого есть `scripts/check-design-text.sh` из коммита f00c589).
- **Английские пары** `NN-*_EN.md` не сверял — задача была про русские описания.
- **Геометрию и цвета** (44 px, `--panel-2`, 26/600, 199×44 и прочие числа вёрстки в подписях) не мерил: это линза вёрстки, не согласованности.
- **Кадры листов 17 на y=1180** (лента с оффером и окно «Вы покидаете sosed») — они не новые, коммит правил в них только длину тостов; их подписи и тексты я разбирал лишь настолько, насколько попали в диф.
- **Трёхрепозиторную парность** (sosed.place / neighbro.place / xor.ad) не проверял: кадры листов — только под sosed.place.
- **Кадр «Что происходило» (screen-20-21-22-24, translate 48,1120)** — консольные строки сверял выборочно; подписи с ссылкой у него нет вовсе, а `21-console_RU.md` построчно я не прошёл.

### 1.2 Линза «Эвристики Нильсена»

Метод: вырезал по `translate(x,y)×2` из `testing/screenshots/design/*.png` все 26 групп, которых не было в `d299315^`, и смотрел их как изображения; спорное перепроверял в исходном SVG и в описаниях репозитория **sosed.place** (`/home/eugene-panov/Projects/panov-id/sosed.place`, ветка day55) и **xor.ad** (`docs/offers/SPEC_RU.md`, `docs/dsa/SPEC_RU.md`).

Сводка:
```diff
- 8 дефектов: состояние лайка не отрисовано, канон проверок выдуман, жаргон спеки в UI
! 11 противоречий между руками: одна строка отмены нарисована четырьмя разными способами
@@ 26 кадров осмотрено глазами, 7 находок опёрты цитатой из описаний
```

### Дефекты

**1. `screen-20-21-22-24.svg:frame(1317,4120)` · H1 · дефект**
Карточка помечена «лайкнуто · вернуть», а сердце осталось контурным и счётчик не вырос: в исходнике `<path d="M254 198 C238 188 …" style="fill:none;stroke:#9a8d7c">` и `<text>6</text>` — те же самые, что у двух нелайкнутых карточек ниже. Описание: `sosed.place/docs/00-mechanics_RU.md:1076` («число на фразе уменьшается» при снятии ⇒ при лайке растёт).
Правка: залить сердце `#bd4b2a`, счётчик 6 → 7.

**2. `screen-23.svg:frame(48,1022)` · H1 · дефект**
То же в просмотре: тост говорит «лайкнуто», но счётчик сверху — контурное сердце с «1» (`M34 247.2 C21.2 239.2 … stroke:#9a8d7c`), и сердце на оранжевой кнопке «лайк» тоже контурное (`stroke:#fff6f0`). Единственное закрашенное сердце на кадре — внутри тоста. После действия экран выглядит так же, как до него.
Правка: залить счётчик и его число (1 → 2), сердце кнопки — закрашенным.

**3. `screen-06-07-10.svg:frame(1293,3958)` · H4 · дефект**
Новый кадр T «Я» нарисован без нижней навигации и с шевроном «‹ Я» в шапке, тогда как соседние кадры K, L, M, N на **этом же листе** все кончаются таббаром (`class="navb"` «Я» с точкой `#bd4b2a`). Мало того, подпись 14I на `screen-14-15-18.svg` прямо ссылается сюда: «геометрия таббара — как на листе screen-06-07-10.svg». Описание: `sosed.place/docs/10-settings-screen_RU.md:41` — «точка … на „Я“ в нижней навигации», то есть «Я» — вкладка, а не подэкран с возвратом.
Правка: снять шеврон, дорисовать таббар кадра M.

**4. `screen-19.svg:frame(48,2916)` · H1 · дефект**
Шапка: «играют 2 · смотрят 4», а в ростере три имени **с числом костей** — «Аня · 3», «Олег · 5», «Миша · 2» — и «ход: Аня». Кости есть у троих, значит играют трое. Плюс строка «вы за столом с этого момента» — а меня в ростере нет и «смотрят 4» не изменилось. Описание: `sosed.place/docs/19-table_RU.md:17` (два числа), `:31` («сесть и играть — разные вещи»).
Правка: либо «играют 3», либо у третьего имени убрать число костей и пометить его зрителем.

**5. `screen-17-venue.svg:frame(1317,2222)` · H4 · дефект**
Список «Что проверяется при публикации» расходится с каноном: строки «точка подтверждена» в таблице `xor.ad/docs/offers/SPEC_RU.md:446-456` **нет**, а канонической строки «Живых фраз со скидкой у личности уже `PRIVATE_ACTIVE_OFFERS`» на кадре нет. Экран обещает заведению перечень, которого спека не описывает.
Правка: либо заменить пятую строку на лимит из §6.1, либо завести проверку «точка подтверждена» в §6.1 до рисования.

**6. `screen-17-venue.svg:frame(1740,2222)` · H2 · дефект**
Подпись под счётчиком: «три — и заведение приостанавливается» — читается как автомат. `xor.ad/docs/offers/SPEC_RU.md:599`: «систематические обоснованные жалобы → `verification_status: suspended` **вручную модератором**». И сам счётчик «обоснованных за 90 дней · 1 из 3» теряет условие из §4: `SUSPEND_RESOLVED_COUNT` — «Обоснованных жалоб **по разным офферам**».
Правка: «обоснованных по разным офферам за 90 дней» и «три — и заведение приостанавливают».

**7. `screen-08.svg:frame(48,2916)` · H2 · дефект**
В тексте подтверждения: «Собеседник увидит **то же надгробие**, что и при истечении срока». «Надгробие» — слово спеки (`sosed.place/docs/08-chat-screen_RU.md:115`, `:90`, `docs/00-mechanics_RU.md:152`), нигде в описаниях оно не приведено как текст, показываемый человеку. Фраза взята из `08-chat-screen_RU.md:115` дословно вместе с внутренним термином.
Правка: «Собеседник увидит то же, что при истечении срока».

**8. `screen-25.svg:frame(48,1012)` · H1 · дефект**
`sosed.place/docs/25-my-likes_RU.md:10`: «**У каждой карточки лайк закрашен**». На кадре закрашено одно сердце из трёх (`transform="translate(301,263)" fill:#bd4b2a`); у карточки оффера частника (`translate(301,451)`) сердце контурное `#9a8d7c`, хотя по `docs/05-message-actions_RU.md:18` лайк на оффер частника снять нельзя вовсе — то есть нарисовано состояние, которого у этой карточки не бывает. (У первой карточки контур законен: её только что сняли.)
Правка: залить сердце карточки оффера, снять с него зону касания.

### Противоречия между руками

**9. Шапка беседы — две разные. H4 · противоречие**
`screen-06-07-10(463,3958)` и `screen-08(48,2916)`, `(463,2916)`: «‹ Имя, возраст 🔑» + строка «молчание · час ⌄ | остаток 52 мин | закончить».
`screen-20-21-22-24(48,4120)`: «‹ Аня, 24» и справа «3 ч 12 мин» — ни ключа, ни строки молчания, ни «закончить». Один и тот же экран 8.
Правка: привести кадр 20-21-22-24 к шапке листа 08.

**10. Остаток времени — четыре записи. H6 · противоречие**
«остаток 3 ч 50 мин» (`20-21-22-24(1317,3120)`), «остаток 52 мин» (08), голое «52 мин» и «3 ч 12 мин» в «Беседах» (`894,3120`) и в шапке беседы (`48,4120`). Голое число заставляет вспоминать, что оно означает.
Правка: везде «остаток N».

**11. Два разных сердца. H4 · противоречие**
`screen-25` рисует сердце путём `M12 21s-7-4.5-9.5-9C.5 8 3 4 6.5 4…` (24×24), `screen-23` и `screen-20-21-22-24` — самодельной кривой `M254 198 C238 188 243 177…`. Одна иконка, две геометрии.

**12. Подсказка громче экрана. `screen-20-21-22-24(1317,3120)` · H8 · противоречие**
На «Мэтче» обе настоящие кнопки — «поговорить» и «не сейчас» — контурные (`fill:none;stroke:#80705a`), а единственная залитая оранжевым кнопка на экране — «понятно» у подсказки. Проверил по всем листам: `поговорить` встречается только здесь и в `screen-25`, залитым не нарисовано нигде.
Правка: «поговорить» — залитой.

**13. Причина неактивной кнопки — три места. H5 · противоречие**
`screen-12-13(1740,2950)`: «дальше — когда ПИН повторён» **над** кнопкой. `screen-12-13(894,2950)`: «спросит ПИН» **под** кнопкой. `screen-12-13(1317,2950)` и `screen-02(471,1022)`: подписи нет вовсе, хотя `sosed.place/docs/02-name-screen_RU.md:108` требует называть причину словом («дальше — когда код записан»).

**14. Две ошибки на одном кадре, две подачи. `screen-02(471,1022)` · H9 · противоречие**
«этот ПИН легко угадать» — красным без иконки и **не** блокирует (`docs/02-name-screen_RU.md:34`: «не запирает: кнопка остаётся живой»); «Не совпадает с кодом — сверьте с бумагой.» — красным с круглой иконкой и блокирует (`:72`). По виду человек не отличит блокирующую ошибку от предупреждения.

**15. `screen-12-13(894,2950)` · H1/H8 · противоречие**
Самый яркий элемент кадра «Перенос отклонён» — код сверки «7HXK» крупным mono, а текст рядом говорит «Код погас — начните заново». Иерархия спорит с сообщением.

**16. `screen-17-venue(1317,2222)` · H4 · мелочь**
Два одинаковых действия разными словами на одном экране: ссылка в шапке «назад к форме» и кнопка «к форме».

**17. `screen-17-venue(1740,2222)` · H4 · мелочь**
Два формата даты на одном экране: «разбор до 19.09.2026, 14:00» в карточке и «разбор до 19.09, 14:00» в жалобе.

**18. `screen-17-venue(1740,2222)` · H1 · противоречие**
Три статуса жалоб — три подачи: «ждёт вашего ответа» оранжевым слева, «признана обоснованной: условия скрыты» белым отдельной строкой, «отклонена» серым справа. Список не читается одним движением глаза.

**19. Пояснение внутри телефона. `screen-14-15-18(463,3838)` · H8 · противоречие**
Строки «точка стоит, пока ответ не прочитан» и «инбокс её не показывает: там разговоры соседей» лежат **внутри** кадра классом `meta` — тем же, каким набрана настоящая подпись строки выше («20 мин · час · 4 часа»). На `screen-06-07-10(1293,3958)` аналогичное пояснение сделано классом `capm3` подзаголовком пункта. Здесь это читается как текст продукта.
Правка: унести в подпись под кадром, как остальные заметки листа.

### Мелочи

**20. `screen-17-venue(894,2222)`** — «Карточка живёт в ленте **4:20**» в кабинете рекламодателя читается как время суток. В описаниях это длительность (`sosed.place/docs/17-offer_RU.md:13`: «уходит из ленты через 4:20»), но заведению этот жаргон нигде не объясняли. Правка: «4 часа 20 минут».

**21. `screen-17-venue(471,2222)`** — «После нескольких неверных код гасится» · H1: счётчика попыток нет, числа тоже.

**22. `screen-17-venue(471,2222)`** — «это не мы» · H5: по `xor.ad/docs/offers/SPEC_RU.md:754` это «немедленный `suspended` этого заведения», а на кадре — обычная контурная кнопка без подтверждения, и последствие написано **под** ней, то есть читается после нажатия. Правка: последствие над кнопкой, подтверждение листом, как на `screen-08(48,2916)`.

**23. Дрейф фикстуры** — один и тот же стол «Пятничный · виселица» показан как «играют 2, смотрят 4» на `screen-25(48,1012)` и «играют 2, смотрят 3» на `screen-20-21-22-24(1317,4120)`. Разделитель тоже плавает: «играют 2 · смотрят 4» на `screen-19(48,2916)` против запятой на остальных — но здесь двоятся сами описания: `docs/19-table_RU.md:17` даёт запятую, `:137` — точку.

**24. `screen-25(48,1012)`, шапка** — шеврон кончается на `x=34`, подпись «Я» начинается на `x=36`: зазор 2 px, и обводка `#80705a` вместо `#f0e7dc`, отчего кнопка выглядит неактивной. На других листах подпись шапки стоит на `x=52…56`. Оговорка: так нарисованы все пять кадров листа, новый кадр это унаследовал, а не завёл.

**25. `screen-08(463,2916)`** — системная строка «собеседник изменил возраст: 39» через двоеточие, а её соседка на `screen-06-07-10(463,3958)` — «Аня согласилась · беседа открыта» через точку. Одна грамматика системных строк.

### Не нашёл, где описано

- **«заявка на следующую игру»** (`screen-19(48,2916)`, оранжевая кнопка) — в описаниях эта кнопка зовётся «сыграть ещё» (`sosed.place/docs/19-table_RU.md:41`) и «начать игру» (`:49`). Третьего названия в документах нет.
- **Сколько неверных кодов гасят конверт** — в `xor.ad/docs/offers/SPEC_RU.md` §4 такой константы нет (есть `ACTIVATION_CODE_TTL` = 30 дней).
- **Заголовки групп в «Я»** — `sosed.place/docs/10-settings-screen_RU.md:39` требует группу «Лента и беседы»; ни один из двух новых кадров «Я» заголовков не показывает.
- **Состав и порядок пунктов «Я»** — на `screen-06-07-10(1293,3958)` подряд идут «Что происходило / Отойти / Показать подсказки снова / Мои лайки», а в `docs/10-settings-screen_RU.md:43-46` между последними двумя стоит «Зона и режим по умолчанию».
- **Какая именно SPEC** имеется в виду в подвалах `screen-17-venue` («SPEC §11», «SPEC §6.1», «SPEC §10.2», «SPEC §6»). Проверил: §11 и §6.1 сходятся с `xor.ad/docs/offers/SPEC_RU.md:740` и `:446`, но описания sosed.place коротким словом «SPEC» зовут и `xor.ad/docs/dsa/SPEC_RU.md`, где §11 — «Открытые параметры».
- **Остаётся ли подпись причины у неактивной «дальше», когда показана блокирующая ошибка** — `docs/02-name-screen_RU.md:108` перечисляет подписи, но про соседство с ошибкой молчит.

### Что НЕ проверял

- Светлую тему и i18n: новых кадров нет на `screen-light-03-08-23.png` и `screen-i18n-03-08.png`.
- Кадры этого коммита, которые были **правкой существующих** групп, а не новыми: квитанция на `screen-14-15-18`, тосты на `screen-17-venue`, кадр A на `screen-19`. Я мерил только 26 добавленных `translate`-групп.
- Контраст, размеры зон касания (`data-hit`), вылет текста за телефон — это ворота `scripts/check-design-text.sh` из f00c589, я их не перезапускал.
- Английские документы sosed.place — читал только RU-комплект.
- Поведение: прототип не запускал, все утверждения — из исходных SVG, рендеров 2× и описаний.

Оговорка о свежести: рендеры `testing/screenshots/design/*.png` датированы 18:21–18:32, коммит — 18:01, так что часть кадров я видел уже с более поздними правками; координаты всех 26 групп в HEAD (f00c589) совпадают с теми, по которым резал.

### 1.3 Линза «Доступность + визуал»

Линза «Доступность + визуал», 27 кадров коммита d299315. Всё измерено скриптами по исходнику (blame → новые строки → парсинг rect/text + формула WCAG), кадры смотрел как изображения (кропы из `testing/screenshots/design/*.png` по translate×2).

```diff
- 2 действия на светлой плашке дают 2.58:1 — «вернуть» и «в «Я»» (нужен 4.5)
- 13 строк тела 14px дают 3.15:1 на листе 17
! 6 целей касания вообще без зоны; ворота их не видят ни одной
+ Н7 держится: ровно одна залитая акцентная кнопка в каждом из 17 кадров, где она есть
@@ ворота считают только рамку из RIMS и data-hit — 3 листа из 10 отдают им ноль контролов
```

## 1. Цели касания < 44 и то, что ворота пропускают

Ворота `scripts/check-design-sheets.sh` засчитывают контролом только `<rect>` со `stroke` из набора RIMS либо с `data-hit`. Всё остальное для них не существует. Найденное вне их поля зрения:

| лист:строка | что | размер | почему ворота молчат |
|---|---|---|---|
| `panel/design/screen-17-venue.svg:47` | крестик «закрыть» | глиф 12×12 (`path d="M22 22 L34 34…"`), рамки нет вовсе | нет rect |
| `panel/design/screen-17-venue.svg:47` | «следующая» | только текст 14px | нет rect — притом «предыдущая» в том же кадре имеет `rect x=16 y=684 160×44` |
| `panel/design/screen-17-venue.svg:47` | «перейти на сайт →» (акцентная ссылка) | текст 14px, ~20px высоты | нет rect |
| `panel/design/screen-17-venue.svg:47` | поле «промокод КОЛОС20» | 210×40 | залито `#302720` без stroke |
| `panel/design/screen-20-21-22-24.svg:59` | вкладки «Предложения» / «Беседы» | текст 14px на y=81, полоса вкладок ~27px | нет rect; подчёркивание 47×3 нарисовано, зона — нет |
| `panel/design/screen-25.svg:34` | «показать ещё» | текст 14px на y=644 | нет rect |

Ещё две категории, которые ворота пропускают по устройству проверки:
- **Невидимые зоны без метки.** Правильные 44×44 часто нарисованы как `<rect fill="none">` без `data-hit` — `screen-19.svg:1571` («встать» 56×44, дом 44×44, эмодзи 44×44), `screen-23.svg:42` (закрыть 44×44, пейджер 2×160×44). Ворота их не засчитывают, то есть и не хвалят, и не ловят.
- **Кнопки-иконки без имени** (правило 5 доступности): «…» 48×56 (`screen-17-venue.svg:47`, `screen-23.svg:42`), стрелка отправки 44×44 (`screen-06-07-10.svg:756`, `screen-19.svg:1697`), эмодзи 44×44 (`screen-19.svg:1693`), три сердца с `data-hit="44"` (`screen-25.svg:34`). Видимой подписи нет ни у одной.

🟠 **Разнобой самой разметки зон.** `data-hit` в файлах: 06-07-10 — 30, 25 — 11, 20-21-22-24 — 8, 14-15-18 — 4, 08 — 2, 23 — 1, а 02, 12-13, 17-venue, 19 — **ноль**. То есть на трёх листах из новых кадров ворота не видят ни одного контрола.

🟡 **Доска.** `screen-19.svg:1591` и далее — костяшки 22×44 и 44×22: короткая сторона 22 < 24 px, минимума даже для исключения «клетки доски». На листе таких 27 — размер доcтался из прежних кадров, новый кадр его повторил.

## 2. Контрасты (пересчитано формулой WCAG 2.x на python, по каждой новой паре)

🔴 **Акцент тёмной темы положен на светлую плашку — 2.58:1.**
- `screen-23.svg:42`: «вернуть» `#e0714f` на `#f0e7dc` = **2.58** (16px/600). Это единственный путь отменить лайк.
- `screen-17-venue.svg:47`: «в «Я»» `#e0714f` на `#f0e7dc` = **2.58**.
- Светлый токен `--accent-text` `#983c22` на том же фоне даёт **5.71** — замена лечит оба.

🟡 **Третий план текста на чёрном — 3.15:1, 13 строк 14px.** `#6b5f4c` на `#0d0b0a` = **3.15**: `screen-17-venue.svg:63` (×2: «Число запросов на один адрес ограничено.», «Письмо приходит на тот же адрес.»), `:66` (×5), `:69` (×3), `:72` (×2), `:75` (×1). На карточке хуже: `#6b5f4c` на `#262019` = **2.58**, на `#302720` = **2.34**.

🟡 **Подписи-аннотации внутри телефона.** Класс `.cap` (11px, `#6b5f4c`) стоит на y=780 внутри рамки в пяти новых кадрах листа 17 (`:63`, `:66`, `:69`, `:72`, `:75`) — 3.15:1 и 11px; читаются как продуктовый текст, а не как поле листа.

🟡 **Акцентные подписи ниже 4.5 на поверхностях:** `#d56343` на `#302720` = **3.96** («вернуть», `screen-20-21-22-24.svg:68`); `#d56343` на `#262019` = **4.37** («оффер» `:71`, «стол» и «лайкнуто · вернуть» `:74`).

🟡 **Графика ниже 3:1.** Ребро нейтральной строки консоли `#3a2e20` на `#0d0b0a` = **1.49** (`screen-20-21-22-24.svg:31`) — а именно ребро там несёт уровень события (см. п.3).

🟢 Проверено и проходит: `#bd4b2a` точка непрочитанного на `#0d0b0a` = 3.93 (графика, порог 3); рамка контрола `#80705a` на `#262019` = 3.36, на `#0d0b0a` = 4.10; `#fff6f0` на залитой `#bd4b2a` = 4.68; `#9a8d7c` на `#0d0b0a` = 6.05, на `#302720` = 4.51; `#e0714f` на `#0d0b0a` = 6.21.

## 3. Состояние только цветом, порядок, lang, движение

🟡 **1.4.1 в консоли, `screen-20-21-22-24.svg:31`.** Уровней три, а слово есть у одного. Отказы: ребро `#ef7a6a` + глиф «!» + текст `#ef7a6a` — два канала, честно. Средний уровень — ребро `#d56343`, текст обычный `#f0e7dc`, глифа нет: строки 14:19 «ход просрочен», 14:30 «три паса подряд», 14:31 «открыта форма уведомления», 14:38 «состав не подтверждён вовремя» отличаются от нейтральных 14:02/14:05/14:33 **только цветом ребра**, причём нейтральное ребро при 1.49:1 практически невидимо. Заодно классификация спорна: 14:12 «заявка отклонена» получила нейтральное ребро.

🟢 Где двух каналов хватило: точка непрочитанного + слова «ждёт вашего ответа» (`screen-20-21-22-24.svg:59`); «скрыто · вернуть», «снято · вернуть», «лайкнуто» — словом; сердце лайкнутого залито против контура (форма, не цвет); «не прошло» против «пройдено» на `screen-17-venue.svg:72` — разными словами.

🟢 **Н1 держится** на `screen-12-13.svg:185–187`: неактивная «дальше» — пунктирная рамка `stroke-dasharray="4 4"`, причина словом рядом («дальше — когда ПИН повторён»), не `disabled`-заливка.

🟣 **Порядок рисования.** Совпадает с порядком чтения везде, где смотрел: затемнение `#000000 opacity=0.55` и лист рисуются последними поверх содержимого (`screen-06-07-10.svg:769`, `screen-08.svg:608`), нижняя навигация — последней (`screen-14-15-18.svg:1325`). Наложений текста на текст не нашёл — их отдельно держат ворота `check-design-text.sh` из коммита f00c589.

🟣 **`lang`.** Единственный лист с атрибутом — `screen-14-15-18.svg`: 17 строк `lang="en"` на английском юридическом тексте. В новых кадрах нерусского содержимого нет, так что ничего не пропущено; но конвенция живёт на одном листе из десяти, и следующий английский экран её, скорее всего, не получит.

🟠 **`prefers-reduced-motion` не назван нигде в новых кадрах**, хотя движение нарисовано: скелет ленты (`screen-20-21-22-24.svg:56`), отсчёт «2:00» (`screen-12-13.svg:121`), полоса остатка беседы (`screen-06-07-10.svg:753`). Единственное упоминание на всех листах — `screen-01-04-05.svg`, кадров которого коммит не касался.

## 4. Визуал

🟢 **Н7 выдержан, и это померено, а не на глаз.** Во всех 17 новых кадрах, где есть акцентная кнопка, залитый акцент ровно один: 343×44 или 327×48/56 `#bd4b2a`. Стрелка отправки при пустом поле контурная везде, где нарисована (`screen-06-07-10.svg:756`, `screen-08.svg`, `screen-19.svg:1697`). Подсказки 20E–20H одинаковы: «понятно» 140×44.

🟢 **Кегли.** Вне шкалы 26/20/16/14/13/11 в новых строках — только `screen-12-13.svg:121`: 44px и 34px на кодах переноса («7HXK», «K7Q-M3F-2X9») и 22px на таймере «2:00». 🟠 Строка переписана этим коммитом целиком, поэтому blame приписывает размеры ему; решить, дисплейные это размеры или дефект, должен владелец шкалы.

🟡 **Разные руки — разная шапка просмотрщика.** `screen-23.svg:42` и `screen-17-venue.svg:47` рисуют одну и ту же шапку и один и тот же пейджер, но 23-й даёт крестику `rect x=6 y=6 44×44` и обеим стрелкам пейджера по `160×44`, а 17-й не даёт крестику ничего и даёт зону только «предыдущей». Это не стиль, это разница в том, куда попадёт палец.

🟡 **Разная высота нижнего ряда действий.** 56px на `screen-17-venue.svg:47` и `screen-23.svg:42` (96×56, 121×56, 48×56) против 44px на `screen-25.svg:34` (120×44, 295×44) и в листах 06/08 (343×44). Ряд один и тот же по роли — «действия над карточкой», — а высота у двух рук разная.

🟠 **Исходник.** Листы 17, 20-21-22-24, 23, 25 держат кадр одной строкой (до 6058 символов — `screen-20-21-22-24.svg:31`), листы 06-07-10, 08, 14-15-18, 19 — по элементу на строку. Diff и blame по кадру на первых четырёх нечитаемы, и ссылка «лист:строка» там указывает на весь кадр целиком.

🟢 Рамок на карточках не завелось: новые карточки — заливка `#262019`/`#302720` без stroke; рамку носят только поля ввода (`#80705a`, 1px) и контурные кнопки (`#9a8d7c`/`#80705a`).

## Чего я НЕ проверял

- **Светлую тему и три ступени контраста** — мерил только тёмные кадры; `screen-light-03-08-23.svg` коммит не трогал.
- **200% увеличения, ширину 320px, межстрочный 1.5** — геометрия в SVG зафиксирована, макет этого не переживает и не ломает: испытать нечем.
- **Длину немецких и армянских строк** в новых кадрах — переводы живут только на `screen-i18n-03-08.svg`.
- **Обход клавиатурой и порядок табуляции** — у SVG нет DOM-семантики приложения; смотрел только порядок отрисовки.
- **Кольцо фокуса** — не нарисовано ни в одном новом кадре, но и ни в одном старом; как отсутствие в новых кадрах не считаю.
- **Перерисовку PNG после коммита** — кропы резал из `testing/screenshots/design/*.png` как они лежат в дереве; расхождение рендера с исходником не исключено.
- **Контраст под затемнением листа** (шапка под `opacity 0.55` на 08_A, 06_B) — содержимое под модальным листом инертно, порог к нему не применяю.

---

## 2. Опровержения и проверки ведущего

| Находка | Вердикт | Чем |
|---|---|---|
| Согласованность 🔴 кадр R: «срок со второго нажатия» против 06:33 | **опровергнута** | канон `chat_RU.md:2055` (решение владельца 18.09.2026): «срок беседы идёт со второго нажатия» — кадр верен, линза читала не ту строку; в подписи ссылка исправлена на 06:33 и chat_RU.md:2055 |
| Согласованность 🔴 кадр 20 на 12-13: «Новый выдаёт экран личности» против 12:89 | **подтверждена** | `12-identity-and-security_RU.md:89` — перевыпуск только предъявлением текущего; строка заменена на «Перевыпуск — по 12:89, вопрос открыт», противоречие описания и восстановления — владельцу |
| Нильсен 1, 2: сердца после «лайкнуто» контурные, счётчики не выросли | **подтверждена** | исходник `fill:none;stroke:#9a8d7c` на обоих кадрах; залиты, счётчики 6→7 и 1→2 |
| Нильсен 3: кадр T «Я» без таббара и с шевроном | **подтверждена** | вырез рендера; таббар кадра M скопирован, шеврон снят, тост поднят над таббаром |
| Нильсен 4: ростер троих с костями при «играют 2» | **подтверждена** | «Миша · 2» → «Миша · смотрит» |
| Нильсен 5, 6: список проверок и текст жалоб против offers/SPEC | **подтверждена** | пятая строка → «живых офферов — не больше предела», «приостанавливают», «по разным офферам» |
| Нильсен 7: «надгробие» — слово спеки; Согласованность: «увидит» против «видит» | **подтверждена** | «Собеседник видит то же,» |
| Нильсен 8: сердце оффера частника контурное с зоной | **подтверждена** | залито, зона снята |
| Доступность 🔴 `#e0714f` на светлой плашке 2.58:1 | **подтверждена** пересчётом линзы | `#983c22` на трёх тостах (23 ×2, 17 ×1) |
| Согласованность: семь заголовков без пометки «текст мой», шесть ложных ссылок | **подтверждена** | пометки в подписях кадров 18/19/20 листа 12-13 и трёх кадров 17; ссылки исправлены (1302; 72, 82; entry-cluster.md:261; 06:33; 60, 61, 64; 20:47) |
| Согласованность: 14H «ответ прочитан · дата» против `chat_RU.md:2824` | **подтверждена** | подпись «ответ» без даты, ссылка в подписи кадра |
| Нильсен 12: «поговорить» контуром, «понятно» подсказки залита | **не применено** | Н7 и три старших кадра подсказок держат «понятно» единственной залитой; менять образец подсказок — решение шага 9 |

Ворота после правок: `check-design-text.sh` — 178 кадров, 3147 подписей, чисто (один новый наезд на 17 пойман воротами и укорочен); `check-design-sheets.sh` — ни одна мера не выросла.

## 3. Нарезка на задачи

| # | Задача | Уровень | Цена |
|---|---|---|---|
| 1 | Владельцу: перевыпуск бумажного кода после восстановления (12:89 «только предъявлением текущего» против погашенного кода) — решение в описании 12 и на кадре 20 листа 12-13 | продукт | решение |
| 2 | ~~Реестр `limits.tsv`: четыре константы офферов~~ — закрыто 18.09.2026 18:58: `offer.link.ttl`, `offer.autohide.complaints`, `offer.complaint.examination`, `offer.suspend.window` (сверок 384 → 395); EN-спека писала «Fifteen minutes» словом и «24» без единицы — обе половины теперь называют число с единицей. Осталось: «сколько неверных кодов гасят конверт» — открытый параметр | реестр | 0.1 ч |
| 3 | Разнобой рук: шапка беседы на 20-21-22-24 по образцу 08; ~~«остаток N» везде~~ (закрыто: три строки на 20-21-22-24); одно сердце (путь листа 25) на 23 и 20-21-22-24; высота ряда действий 44 против 56 на 17/23; крестик и пейджер на 17 получают зоны как на 23; причина неактивной кнопки в одном месте | согласованность | 1.5 ч |
| 4 | Доступность: подписи `.cap` внутри телефона на 17 — под кадр; консоль 21 — глиф среднему уровню; `data-hit` на 02/12-13/19 (ноль зон). ~~третий план `#6b5f4c` на 17 → `--muted`; зоны у крестика, «следующая», «перейти на сайт», «показать ещё», вкладок «Предложения»/«Беседы»~~ — закрыто 18.09.2026 18:58: 38 строк и 12 зон | доступность | 1 ч |
| 5 | Ворота: `check-design-sheets.sh` считает невидимые `rect fill="none"` без `data-hit` и текстовые действия без зоны (по классу кнопки) — по находке линзы доступности; XML well-formed внутри ворот | инструмент | 1 ч |
| 6 | Мелочи Нильсена 16–25: «назад к форме»/«к форме», формат дат, статусы жалоб одной подачей, «4 часа 20 минут» в кабинете, счётчик попыток кода, подтверждение «это не мы», дрейф фикстуры стола, шапка листа 25, грамматика системных строк | мелочи | 1 ч |
| 7 | «Заявка на следующую игру» на 19 против «сыграть ещё»/«начать игру» в 19:41, 19:49 — унаследованный разнобой словаря, решить кворумом | словарь | 0.3 ч |
