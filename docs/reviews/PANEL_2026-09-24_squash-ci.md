# Панель ревью 24.09.2026 — склейка миграций и наборы с базой в CI

## Шапка

- **Дата:** 24.09.2026.
- **Что ревьюили:** ветка `day57`, диапазон `b74b60a..bc385fc` — четыре коммита:
  `1bc0507` (`--allow-run` для `database.test.ts` в CI), `f142f18` (шаг `database suites`:
  список из `--ignore` в `relay/node/deno.json`), `a54b2d1` (склейка черновых миграций
  `relay/node/db`: 035→030, 036+038→031, 037→032, 040→039), `bc385fc` (срез
  `docs/roadmap_*.md` на 24.09 и строка `docs/facts/decisions.tsv`).
- **Воспроизвести диапазон:** `git log --oneline b74b60a..bc385fc`, `git diff b74b60a..bc385fc --stat`.
- **Линзы:** Безопасность (обязательна), Данные и СУБД, Эксплуатация, Согласованность.
  Протоколы и стандарты — не по предмету, не собиралась.
- **До панели прогнано:** полный `scripts/run-relay-database-tests.sh` — 10 наборов,
  276 пройдено, 0 упало; `pg_dump --schema-only` свежей базы из склеенных файлов против
  локального стенда на старых 16 — совпало; CI на `bc385fc` (run 35959906714) — test
  222/222 с lint и type check, database 276/276; `scripts/check-all.sh` — красный только
  `ontology` (сироты памяти, до диапазона).

## Сырые находки линз

Дословно, как вернули агенты. Не редактировалось.

### Линза «Безопасность»

Линза «Безопасность», диапазон b74b60a..bc385fc. Итог: ни критичных находок, ни уязвимостей я не нашёл. Есть две мелочи в CI и одна оговорка про сквош. Сквош миграций ни одного свойства безопасности не меняет.

Что проверено (только чтение: `git show`, `grep`, `sed`; ничего не запускал):

**1. Сквош миграций (a54b2d1).** Сравнил перенесённый DDL с удалёнными файлами построчно по `git show a54b2d1`.
- `ephemeral_signature text` в `match_participants` (`relay/node/db/030_likes_matches_blocks.sql:49`): столбец по-прежнему допускает NULL, как было в 035.
- В `chat_participants` (`relay/node/db/031_chats.sql:26-33`): `match_id uuid`, `ephemeral_public_key text`, `ephemeral_signature text`, `key_epoch integer NOT NULL DEFAULT 0`. Типы, NOT NULL и DEFAULT совпадают с 036 и 038. Внешних ключей не было и не появилось.
- Индексы `pending_deliveries_by_age` (`032_pending_deliveries.sql:17`) и `support_by_brand_created` (`039_support_requests.sql:25`) такие же, как были.
- `brand text` без внешнего ключа и без NOT NULL, как в 040.
- Не пропало ни одного CHECK, внешнего ключа, значения по умолчанию или NOT NULL. Скрытности на уровне строк (RLS) не было ни до, ни после.

Может ли база, где применены старые имена, остаться без столбца? `relay/node/tools/migrate_db.ts:5-8,46-48` учитывает только имя файла, контрольных сумм нет. Значит, база, где прошли старые 030–032/039, а 035–038/040 ещё не прошли, после сквоша пропустит 030/031/032/039 и столбцов не получит.
- Это не тихая деградация. Код обращается к столбцам напрямую: `relay/node/src/routes/matches.ts:149,202-203`, `inbox.ts:75-76`, `chats.ts:323-353`. Запрос упадёт с ошибкой SQL, а не примет половину ключа без подписи.
- Окно риска: 030 добавлен 21.09 (0e0b1f3), 035 и следующие — 22.09. Опасна только база, которую мигрировали между этими датами.

**2. CI: `--allow-run` (1bc0507, `.github/workflows/relay.yml:132`).** Новой возможности у атакующего не появляется.
- Тот, кто может изменить файл теста, в PR может изменить и сам workflow.
- С `--allow-read`/`--allow-write`/`--allow-net` произвольный код теста и раньше мог прочитать `.git/config` с токеном от checkout и отправить его наружу.
- Для PR из форка GitHub выдаёт токен только на чтение и не передаёт секреты. `pull_request_target` не используется: триггеры по строкам 5-49 — `push`, `pull_request`, `workflow_call`.

**3. CI: динамический список наборов тестов (f142f18, `.github/workflows/relay.yml:141-150`).** Внедрения shell-команд нет. Раскрытие `$suites` без кавычек делает только разбиение на слова и подстановку шаблонов, `$(...)` и `;` из значения не исполняются. Кроме того, `grep -o -- '--ignore=[^ "]*'` отсекает пробелы и кавычки.

**Вижу дефект:**

- **Мелочь.** `.github/workflows/relay.yml:147`, `deno test --allow-env --allow-net --allow-read --allow-write "$suite"`: перед `"$suite"` нет `--`.
  - Сценарий: запись в `--ignore=` файла `relay/node/deno.json:10` вида `test/a.ts,--allow-all` передаст `--allow-all` как флаг, и набор получит все права.
  - Граница доверия та же: кто правит `deno.json`, тот правит и workflow. Поэтому это мелочь, а не дыра.
  - Исправление: `deno test ... -- "$suite"`, или отбрасывать элементы, которые не совпадают с `^test/[A-Za-z0-9_]+\.test\.ts$`.

- **Мелочь.** Там же, `relay.yml:143,145`: цикл `for suite in $suites` без кавычек подставляет шаблоны. Элемент со `*` или `?` раскроется в набор файлов, и прогонится больше, чем в списке. Безопасность это не задевает, это предсказуемость.
  - Исправление: `set -f` перед циклом или `while IFS= read -r suite`.

**Не нашёл описанным** (это было до диапазона и в диапазоне не менялось):

- У job `database` (`relay.yml:99`) нет своего блока `permissions:`. Он есть только у `build-push` (`relay.yml:195-197`).
- У `actions/checkout@v4` (`relay.yml:119`) не указано `persist-credentials: false`.
- Итог: при push-событии токен с правами репозитория по умолчанию лежит в `.git/config`, и его читает любой набор тестов с `--allow-read`. Если в настройках репозитория права по умолчанию на запись, это лишняя поверхность атаки. Эти настройки я не видел.
- Исправление: `permissions: contents: read` на уровне workflow или job, плюс `persist-credentials: false`.

**Оговорка** к сообщению коммита a54b2d1: `migrate_db.ts:46-48`. Утверждение «nothing is applied twice» верно, но опасность обратная: может не примениться ничего.
- Сценарий: чей-то стенд, мигрированный 21.09 без 035–038, после сквоша молча пропустит 030/031. Консент и инбокс будут падать с 500, пока это не заметят.
- Исправление, если такие стенды вообще существуют: проверка столбцов после миграций или ручной `ALTER ... ADD COLUMN IF NOT EXISTS` на них.
- НЕ ПРОВЕРЕНО: утверждение «none of 025–045 applied on any live box». Доступа к боксам у меня нет.

Коммит bc385fc содержит только документы, к безопасности отношения не имеет.

### Линза «Данные и СУБД»

## Линза DATA & DBMS: склейка миграций a54b2d1 и CI-изменения 1bc0507/f142f18

```diff
+ Схема после склейки совпадает со схемой старых 16 файлов: колонки, индексы и ограничения одинаковы, pg_dump отличается только токенами \restrict (одноразовый PG16)
+ Среды «030–034 применены, 035 нет» не нашлось: ни одна ветка, ни один тег и ни один образ не несут 030 без 035
! Такое частичное состояние, если бы нашлось, молча потеряло бы 5 колонок и 1 индекс: воспроизведено
@@ CI на bc385fc (в ней склейка) зелёный, все шаги джоба database success. Число 276 я не проверял
```

Критичных находок нет. Реально увиден один дефект дизайна и две мелочи, всё ниже.

### Чем проверял (всё ПРОВЕРЕНО)

**Эксперимент.** Скрипт `scratchpad/mig/run.sh`, одноразовый `postgres:16-alpine`, файлы миграций из `a54b2d1^` (42 файла) и из `a54b2d1` (37), каждый файл через `psql -1`:
- **Старый набор против нового.** Сравнил `information_schema.columns` (тип, NULL, DEFAULT), `pg_indexes` и `pg_get_constraintdef`: одинаково. pg_dump без сортировки отличается 4 строками, и все четыре — токены `\restrict`/`\unrestrict`.
  - Оговорка коммита «column order aside» лишняя: порядок колонок тоже совпал. Старые `ALTER ... ADD COLUMN` дописывали колонки в конец таблицы, а новые файлы ставят их в конец `CREATE TABLE` в том же порядке. На работу это не влияет.
- **Откат.** Сначала новый набор, потом старые 035, 036, 037, 038 и 040 поверх. Все пятеро идут без ошибок, только NOTICE «already exists, skipping». Откат на образ со старыми файлами безопасен.
- **Частичное состояние.** Старые 001–034, затем новые файлы после 035. Не хватает `match_participants.ephemeral_signature`, `chat_participants.{match_id, ephemeral_public_key, ephemeral_signature, key_epoch}` и индекса `pending_deliveries_by_age`. Сценарий реален, но только если такая база существует. Какие базы могли в нём оказаться, разобрано ниже.

**Раннер** (`relay/node/tools/migrate_db.ts:46-78`). Файл и запись о нём в `schema_migrations` идут одной транзакцией под `pg_advisory_xact_lock`. Поэтому база не может остановиться посреди файла, и «частичность» возможна только по границам имён. Раннер не сверяет записанные имена с файлами на диске, так что пять имён без файла ему не мешают. Другого места, которое читает `schema_migrations`, нет: проверил `git grep`.

### Какие среды могли применить 030–034 без 035 (ПРОВЕРЕНО, итог: ни одна)

| Среда | Что увидел | Итог |
|---|---|---|
| **dev (n1)** | `relay/wizard/environments.toml:16` = `sha-622a848`; `git merge-base` показывает, что этот коммит не содержит даже 025 | чисто |
| **staging (n1)** | `environments.toml:25` = `v2026.8.14-gc3d10fb`; `git tag --contains 0e0b1f3` (030) и `6cd3b1a` (025) пусты, ни один релизный тег новых миграций не несёт | чисто |
| **prod (p1)** | `environments.toml:34` = `v2026.9.11-g8f89e7a` | чисто |
| **Образы с day-веток** | `.github/workflows/relay.yml:155`: build-push для day-веток не запускается. В последнем прогоне на day57 шаг build-push в статусе skipped (GitHub API). По reflog `origin/day56` пушилась 21.09 в 14:48, то есть до 030 (18:48), и 24.09 уже `b74b60a` со всеми 16 файлами | образов с частичным набором нет |
| **`origin/dev`, `origin/main`** | `cd21688` от 19.09 и `684a103` от 11.09; 030 содержат только `origin/day56` и `origin/day57` | чисто |
| **Локальный стенд** (`relay/local/docker-compose.yml`) | Образ `edge-node-local-migrate` собран 23.09 в 16:09, внутри все 42 старых файла (смотрел `ls` в новом контейнере `--rm` из этого образа). Контейнер migrate завершился с кодом 0 шестнадцать часов назад | 🟠 значит, у стенда полный набор старых имён. `schema_migrations` стенда я не опрашивал: существующих контейнеров не трогал |
| **CI, `run-relay-database-tests.sh`, `run-depth-tests.sh`** | каждый прогон заводит новую базу | чисто |
| **`relay-test-db-924384`** | висит в статусе Exited(255) 3 дня, это остаток старого прогона, порт наружу не открыт | не стенд, мусор |

⚪ Живые `schema_migrations` на n1 и p1 не опрашивались. Вывод выше сделан по закреплённым образам и тегам, а не по базе.

### Находки: дефект увиден

1. **Дефект дизайна: `relay/node/tools/migrate_db.ts:46-47` плюс дыры в нумерации `relay/node/db/` (теперь 011, 018, 019, 035–038, 040).**
   - **Сценарий.** Кто-то займёт свободный номер, например `036_x.sql`. На свежей базе этот файл применится между 034 и 039. На любой базе, где уже применены 041–045 (локальный стенд, а после выката и dev), он применится последним. Порядок разойдётся без единого сообщения.
   - Дыры были и до коммита, но склейка добавила их пять. Защиты нет: `git grep` не нашёл ни теста, ни проверки.
   - **Исправление.** Раннер отказывается применять ожидающий файл, если его имя сортируется раньше максимального уже записанного. Либо тест: имя каждого нового файла больше имени последнего в `HEAD` основной ветки.

2. **Мелочь: `relay/node/deno.json:6`.** Комментарий `"//test"` говорит о «three suites that need Postgres», а в `--ignore` на строке 7 их десять. После f142f18 этот список стал источником правды для CI (`.github/workflows/relay.yml:139-151`), поэтому врущий комментарий рядом с ним вредит больше, чем раньше. **Исправление:** переписать комментарий под 10 наборов и написать, что CI читает этот список.

3. **Мелочь: `.github/workflows/relay.yml:141` против `scripts/run-relay-database-tests.sh`.**
   - CI идёт по наборам в порядке `--ignore`: identity_routes, session_freeze, identity_sweeper… feed_publish. Локальный скрипт — в своём порядке: feed_publish, transfer, queue_metrics… identity_routes.
   - База у всех наборов одна и между наборами не чистится. Зелёными оказались оба порядка (CI — ПРОВЕРЕНО по GitHub API на bc385fc; локально — со слов коммита, НЕ ПРОВЕРЕНО). Значит, сейчас наборы друг другу не мешают.
   - Но зависимость от порядка никто не зафиксировал. Например, `test/job_rearm.test.ts:48` удаляет все задачи `sweep_chats`, а не только свои.
   - **Исправление:** один порядок в обоих местах. Проще всего, чтобы локальный скрипт тоже читал список из `deno.json`.

### Смотрел и ничего не нашёл

- **`ALTER TABLE matches ADD CONSTRAINT matches_chat_id_fkey`** (`relay/node/db/031_chats.sql:52-53`) не идемпотентен. Повтор файла воспроизведён: `ERROR: constraint ... already exists`. Но строка в 031 была и до склейки, а раннер повторить записанный файл не даст. Коммит тут ничего не изменил.
- **`SELECT *` и `INSERT` без списка колонок** по четырём затронутым таблицам: в `src`, `tools` и `test` нет. Все вставки со списком колонок: `src/routes/matches.ts:202`, `support.ts:122`, `chats.ts:85`, `likes.ts:216`, в тестах `feed_publish.test.ts:1717/3047/3088` и `database.test.ts:2296`.
- **Ссылки на удалённые номера 035–038 и 040** в живом коде, документах и `docs/facts/*.tsv` остались только в `docs/reviews`, это задумано. `docs/facts/schema.tsv` указывает на оставшиеся файлы.
- **Счёт в roadmap.** «37 файлов, последняя 045» сходится с `ls`, EN/RU-пара roadmap согласована.
- **DEFAULT, NULL, CHECK, FK.** `key_epoch integer NOT NULL DEFAULT 0` совпадает. `chat_participants.match_id` без внешнего ключа — и раньше, и сейчас. `brand` без внешнего ключа, индекс `(brand, created_at)` тот же.

### Что осталось непроверенным

- 🟠 Число 276 тестов: логи CI без авторизации не читаются, а полный локальный прогон я не запускал.
- 🟠 Содержимое `schema_migrations` на n1, p1 и на локальном стенде.

Хвостов в `docker ps` после эксперимента нет: контейнер удалён через `trap`, существующие контейнеры я не трогал.

### Линза «Эксплуатация»

Линза «Эксплуатация». Проверял только чтением, `git` и одним опытом с bash в scratchpad. Боксы, реестр и `schema_migrations` на живых базах не опрашивал.

```diff
- Критичных находок нет. Схема при выкате и откате в безопасности: ни один построенный образ не содержал удалённых имён.
! Противоречие в плане: deploy-dev.yml узел на n1 не катит, катит только панель.
! В CI охрана от пустого списка не срабатывает, при сбое шаг падает без текста. Опыт в bash это подтвердил.
@@ У CI нет timeout-minutes: зависший набор держит job до 6 ч и блокирует build-push.
```

## Замеченные дефекты

**1. Противоречие. Узел на n1 после мержа в dev сам не обновится.**
- `.github/workflows/deploy-dev.yml:3-11` вызывает `_deploy.yml`, а тот, по `_deploy.yml:3`, собирает и выкладывает на CDN только панель.
- `relay/RELEASE_EN.md:29-31,75` говорит прямо: «CI does not deploy the node», «The node never rolls by itself».
- Путь для узла — `bash scripts/deploy-relay-dev.sh` вручную, со своей машины. Он закрепляет тег в `relay/wizard/environments.toml`, затем `wizard --node n1 deploy`, миграции идут из нового образа, потом `up -d`.
- Сценарий: мерж в dev. Панель `dev.xor.panov.id` сразу выкачена с кодом day56/57. Узел dev остаётся на `sha-622a848` (`environments.toml:16`), пока скрипт не запущен руками. Сломается ли что-то у панели против старого узла — НЕ ПРОВЕРЕНО.
- Что поправить: в плане — сразу после зелёного `build-push` на dev запускать `scripts/deploy-relay-dev.sh`, до того как кто-то откроет панель.

**2. Противоречие. На dev не хватает не 025–045, а 022–045.**
- `docs/roadmap_RU.md:51` пишет «Миграции 025–045 на контурах не применялись».
- `git ls-tree 622a848 relay/node/db/`: последняя миграция в образе dev — `021_idempotency_age.sql`.
- Файлы 022 и 024 появились 20–21.09 (`git log --diff-filter=A`), это позже `origin/dev` (`cd21688`, 19.09).
- Значит, первый выкат на dev применит 19 файлов: 022–034, 039, 041–045, а не 16.
- Staging закреплён на `v2026.8.14-gc3d10fb` (`environments.toml:25`), в том образе последняя миграция 010.
- Прод закреплён на `v2026.9.11-g8f89e7a` (`environments.toml:34`), последняя 021. Это та же дата, что у dev, так что слова «прод ещё старше» не подтверждаются. Оговорка: `inventory.toml` может переопределять теги, его я не видел.
- Всё это выведено из закреплений и кода. Сам `schema_migrations` на боксах не опрашивался, roadmap говорит об этом честно.
- Что поправить: в §1 написать `022`–`045`, а перед выкатом снять `SELECT name FROM schema_migrations` на n1, dev и staging.

**3. Дефект дизайна. Раннер молча проходит мимо имён, которых больше нет на диске, и не видит «половинчатых» баз.**
- `relay/node/tools/migrate_db.ts:41-47,88`: множество `applied` берётся целиком из таблицы, а цикл идёт по файлам.
- Имя из таблицы, которого нет на диске, не даёт ни предупреждения, ни ошибки. Оно только попадает в `applied.size` итоговой строки, и там легко увидеть «37 on disk, 42 already applied» без объяснения.
- Для отката это правильно: старый образ не падает на базе с более новыми именами.
- Но сквош безопасен только при одном условии: нет базы, где записан целевой файл (030, 031, 032 или 039), а вливаемый (035, 036, 037, 038, 040) — нет. На такой базе новый 030 будет пропущен как применённый, 035 исчез, и колонки `ephemeral_signature` не будет. Раннер об этом промолчит.
- Сейчас такой базы, по доступным данным, нет. Файлы 030–040 есть только в `day56`/`day57`: `git branch -a --contains` и `git tag --contains` пусты для dev, main и тегов. `build-push` строит образы только на dev, тегах и вызове релиза (`relay.yml:163-166`). Итог: ни один образ с этими файлами не собирался и не мог попасть на бокс. Проверено по локальным ссылкам на remote, свежий fetch не делал.
- Постоянная база с этими именами только одна — локальный стенд `relay/local` (том postgres). По сообщению коммита a54b2d1, он прогнал все 16 файлов, так что схема там полная, а 5 имён остались «призраками». Эту часть сообщения я не перепроверял. Остальные локальные прогоны (`run-relay-database-tests.sh`, `run-depth-*.sh`) поднимают одноразовые базы.
- Что поправить (одна строка, не ломает откат): вывести `console.warn` со списком имён из `schema_migrations`, которых нет в `files`.

**4. Дефект (проверено опытом). Охрана пустого списка в CI — мёртвый код.**
- `.github/workflows/relay.yml:142-143`. GitHub запускает `run:` как `bash -eo pipefail`.
- Если последний `grep -v` ничего не выдал, или первый `grep` не нашёл `--ignore=` (например, deno.json перешёл на `"test": {"exclude": [...]}`), то подстановка возвращает 1. Шаг умирает на строке 142 с `exit 1` и без единого слова, до строки 143 не доходит.
- Опыт: `bash --noprofile --norc -eo pipefail` в scratchpad. Оба случая — `exit=1`, сообщения «no suites found» нет.
- Ошибка в безопасную сторону (красный), но читать её нечего.
- Что поправить: `suites=$(... || true)` на 142, тогда проверка на 143 действительно сработает.

**5. Дефект дизайна. У CI нет лимита времени.**
- В `relay.yml` нет ни одного `timeout-minutes` (`grep` пуст). Job `database` (`relay.yml:99`) наследует 360 минут по умолчанию.
- Набор, который повиснет — незакрытый пул postgres.js (было 21.09, `migrate_db.ts:90-96`) или LISTEN в `session_freeze` — продержит job до 6 ч. `build-push` ждёт через `needs: [test, database]`, и образ dev не соберётся.
- Цикл `|| failed=` от зависания не защищает.
- Что поправить: `timeout-minutes: 20` на job `database`, и/или `timeout 600 deno test ...` в цикле на `relay.yml:147`, чтобы зависший набор считался упавшим и попадал в `failed:`.

**6. Мелочь. Упавший набор трудно увидеть в интерфейсе.**
- `relay.yml:146-150`. Имена упавших печатаются только в конце, в stderr, строкой `failed: a b`. Группы `::group::` свёрнуты, и та, что упала, внешне не отличается от прошедших.
- Что поправить: в ветке `||` добавить `echo "::error title=database suite::$suite failed"`, тогда имя появится аннотацией в сводке прогона.

**7. Мелочь. Разбор deno.json держится на `grep`.**
- `relay.yml:142`: `grep -o -- '--ignore=[^ "]*'` берёт любое вхождение во всём файле.
- Если `--ignore=` появится во второй задаче (например `test:watch`), наборы прогонятся дважды. Если в комментарии `"//test"` появится буквальное `--ignore=x`, оно попадёт в список. Сейчас в комментарии `deno.json:6` стоит `--ignore that`, без `=`, и не совпадает.
- Переход на глобы или `exclude` дает сбой, описанный в п. 4, то есть в безопасную сторону.
- Что поправить: читать через `jq -r '.tasks.test'`, а не по всему файлу.

**8. Мелочь. Два списка наборов могут разойтись.**
- `scripts/run-relay-database-tests.sh:71-155` перечисляет 10 файлов руками, CI берёт список из `deno.json:7`. Сейчас они совпадают: 10 и 10, сверил поимённо.
- Опасное направление закрыто: набор, забытый в `--ignore`, валит обычный unit-прогон, потому что сам бросает ошибку без `DATABASE_URL` (`test/feed_publish.test.ts:10-11`, `test/queue_metrics.test.ts:11-13`). Такой набор тихо не потеряется.
- Остаётся обратное: файл добавили в `--ignore`, CI его гоняет, локальный скрипт — нет. Будет «локально зелено, в CI красно», но не тихий пропуск.
- Второе различие: локальный скрипт с `set -e` (строка 17) останавливается на первом падении, CI доходит до конца.
- Что поправить: в скрипте брать тот же список из deno.json в цикле. Особые случаи, `--allow-run` для database и монтирование `/repo:ro` для `identity_sweeper`, оставить отдельными ветками.

## Проверил, дефекта не нашёл

- **Откат.** Раннер старого образа, по коду `622a848:relay/node/tools/migrate_db.ts`, лишние имена в `schema_migrations` пропускает и не падает.
  - Миграции 022–045 для уже существующих таблиц только добавляют. Это `ADD COLUMN IF NOT EXISTS` в `026:16`, `041:5-7`, `042:6,9`, `043:6-9`. Единственная `NOT NULL` — `arrival_attempts` с `DEFAULT 0` в `043:8`. `DROP` и `RENAME` нет. Значит, старый код на новой схеме пишет без ошибок.
  - Полного прогона старого образа против новой схемы не было — НЕ ПРОВЕРЕНО.
  - Если откатиться на образ, где файлы 035–040 ещё есть, все пять идемпотентны (`IF NOT EXISTS`) и безопасно доприменятся. Но такого образа, по п. 3, не существует.
- **Сбой посреди миграций на выкате.** Каждый файл идёт в своей транзакции (`migrate_db.ts:63-79`), а визард падает до `up -d` (`relay/wizard/wizard.py:807-815`, без `check=False`). Старый контейнер продолжит работать на частично обновлённой схеме, которая только добавляет. Приемлемо.
- **`set -e` и цикл.** `deno test ... || failed=` на `relay.yml:147` из-под `-e` выведен, и цикл доходит до конца, как задумано.
- **`identity_sweeper` читает `docs/facts/limits.tsv`.** В CI весь checkout доступен, `--allow-read` без ограничений, так что работает.

### Линза «Согласованность»

```diff
+ Удалённые миграции 035–038 и 040 вне docs/reviews/ больше нигде не упоминаются; docs/facts/schema.tsv указывает только на существующие файлы
! Два склеенных .sql ссылаются на DDL спеки, но расходятся с ней по индексам: 032 и 039
! Число 195 коммитов в дорожной карте устарело внутри самого среза: сейчас 197
@@ Проверено скриптами: 37 миграций, 276 тестов, 90/48 операций, 30 тестов экранов, 11 страниц; RU и EN совпадают
```

Линза «Согласованность». Всё ниже я проверил сам, чем именно — указано в каждом пункте. Работал только на чтение. Соседние репозитории `sosed.place` и `neighbro.place` (ветка day57) проверил grep'ом на `db/035…040`: совпадений нет.

## Найденные дефекты

1. **Противоречие.** `relay/node/db/032_pending_deliveries.sql:4`
   - В шапке файла написано «The DDL is the spec's own», но после склейки в файле есть индекс `pending_deliveries_by_age` (строки 15–17).
   - В DDL спеки его нет: `docs/chat_RU.md:2313–2321` и `docs/chat_EN.md:2338–2346`. Там только `(recipient_session, created_at)`.
   - По grep, `pending_deliveries_by_age` не упоминается нигде в `docs/`.
   - Как исправить: добавить `CREATE INDEX pending_deliveries_by_age ON pending_deliveries (created_at);  -- уборщик по возрасту (панель шага 5)` в оба DDL-блока. Другой вариант — дописать в шапку 032 «plus the by-age index below».

2. **Противоречие.** `relay/node/db/039_support_requests.sql:2`
   - Там сказано «The spec's DDL, plus the storefront's brand below». Но колонка `brand` в спеке уже есть: `docs/chat_RU.md:2860`, `docs/chat_EN.md:2902`.
   - Настоящее отличие другое: индекса `support_by_brand_created` (039:25) в спеке нет. Там только `support_by_identity` и `support_by_created` (`chat_RU.md:2863–2864`, `chat_EN.md:2905–2906`).
   - Как исправить: добавить индекс в оба блока спеки с комментарием «суточная сводка по витрине (db/039)», а шапку 039 вернуть к «Exactly the spec's DDL». Другой вариант — написать в шапке «plus the brand index».

3. **Противоречие.** `docs/roadmap_RU.md:51`, `docs/roadmap_EN.md:54`
   - Там написано «195 коммитов впереди `origin/dev`». `git rev-list --count origin/dev..X` даёт: для b74b60a — 193, для f142f18 — 195, для a54b2d1 — 196, для HEAD (bc385fc) — 197.
   - Число снято до коммита склейки, хотя тот же абзац (строки 53–55 и 56–57) эту склейку описывает.
   - Как исправить: поставить 197 и уточнить «вместе с этим коммитом», либо 196 «до среза».

4. **Мелочь, адрес уехал.** `docs/facts/decisions.tsv:171`
   - EN-адрес `roadmap_EN.md:138` сейчас указывает на заголовок `## 3. Product: build order`, а строка шага 7 стоит на `roadmap_EN.md:152`.
   - Коммит bc385fc поправил только RU-половину (133→143). EN была сбита уже до диапазона: 138 против фактических 140.
   - `readdress-facts.py` этого не видит: для decisions он сверяет только RU-адрес (`decisions_locate` берёт `subject_ru`).
   - Как исправить: EN:152.

5. **Мелочь.** `docs/facts/decisions.tsv:172`
   - У новой строки половины говорят о разном: `subject_ru` = «10 наборов, 276 тестов», `subject_en` = «9 of the 10 ran only locally».
   - EN-адрес `:60` при этом указывает на строку с «10 suites, 276 tests», а фраза из `subject_en` стоит на строке 61.
   - Как исправить: `subject_en` = «10 suites, 276 tests».

6. **Противоречие, вне диапазона, но задето им.** `relay/node/deno.json:6`
   - Комментарий `"//test"` говорит о «three suites that need Postgres». В `--ignore` на строке 7 наборов десять.
   - С f142f18 CI берёт список наборов из этого `--ignore`, а дорожная карта (`roadmap_RU.md:56–58`) ссылается на него как на источник.
   - Как исправить: «the ten suites that need Postgres (scripts/run-relay-database-tests.sh and CI run them)».

7. **Мелочь.** `docs/roadmap_RU.md:53–55`, `docs/roadmap_EN.md:56–57`
   - Фраза «16 файлов в 11» читается так, будто склеили 16 файлов. На деле 9 файлов слиты в 4, а 16→11 — это счёт по диапазону 030–045.
   - Сами числа верны: 16 в дереве b74b60a (`git ls-tree`) и 11 сейчас (`ls`).
   - Как исправить: «9 файлов в 4; в диапазоне 030–045 — 16 в 11».

8. **Мелочь.** `docs/roadmap_RU.md:7`, `docs/roadmap_EN.md:8`
   - В перечне панелей 23.09 названо 7 тем, а в §5 (`roadmap_RU.md:166`, EN «eight reports») — восемь протоколов. Протоколов действительно 8 (`ls`), в перечне пропущен `PANEL_2026-09-23_loop.md`.
   - Как исправить: добавить «цикл» / «loop».

## Что проверил и где чисто

- **Ссылки на удалённые миграции.** Искал grep'ом по всему репозиторию, кроме `.git`, `node_modules` и `docs/reviews/`: `db/035…040`, `0NN_name.sql`, диапазоны, номера в обратных кавычках, имена `ephemeral_signature.sql`, `chat_keys`, `key_epoch.sql`, `pending_deliveries_by_age.sql`, `support_brand`. Остались только намеренные упоминания в описании склейки (`roadmap_*:53–57`). В `docs/api/*`, `openapi.yaml`, `depth/`, `panel/`, `relay/wizard`, `relay/RELEASE_*` и тестах ничего нет.
- **Уцелевшие ссылки ведут туда, куда надо.** `depth/ink/rooms.ts:235` (db/031 — default `idle_ttl_minutes`), `openapi.yaml:52` (db/039), `inbox.ts:74`, `support_sweeper.ts:11`.
- **Реестр схемы.** `docs/facts/schema.tsv` указывает только на существующие файлы. `check-facts-schema.sh`: «в базе 31, обещано базе 31 — сходится».
- **Колонки против спеки.** `match_participants`, `chat_participants`, `pending_deliveries` и `support_requests` в `relay/node/db` совпадают по колонкам с DDL в `chat_RU.md` и `chat_EN.md`. Расходятся только индексы из пунктов 1 и 2.
- **Комментарии в склеенных файлах.** Историю в духе «030 kept the half…» после склейки переписали. В 031 осталось повествование в прошедшем времени («took the halves with it») как обоснование — это не ошибка.
- **Числа.**
  - 37 `.sql` (`ls | wc -l`).
  - 276 = сумма по 10 наборам из `--ignore` (тот же образец, что в `count-tests.sh`).
  - 90 / 48 (`check-openapi.sh`: built 90, spec 48).
  - 30 тестов экранов (`depth/ink/screens.node-test.ts`).
  - 11 страниц `panel/src/pages`.
  - Панель: юнит-тестов 32, e2e 5 (`count-tests.sh --check`: «карта называет те же числа»).
  - «не запушенных нет»: `origin/day57` = `bc385fc`, `origin/day56` = `b74b60a`.
- **RU и EN.** Пословный дифф по `chat_*` и `roadmap_*` в диапазоне симметричен, числа и даты в парах совпадают.
- **Проверочные скрипты.** Все зелёные: `check-facts-coverage`, `-decisions`, `-limits`, `-open`, `-schema`, `check-docs-pairing-all.sh` (72/37/37 пар), `readdress-facts.py` («переадресовывать нечего»). Пункты 4 и 5 скрипты не ловят: EN-половину decisions никто не сверяет.
- **Не описывал и не проверял.** Корректность самого CI-шага (права `--allow-run` у остальных наборов) — это вопрос не согласованности, а другой линзы.

## Опровержения

Опровергающий агент получил 15 существенных находок с заданием опровергнуть; по умолчанию —
«опровергнута». Его вердикты, затем мои проверки поверх.

| ID | Источник | Вердикт | Доказательство опровергающего | Проверка ведущего |
|---|---|---|---|---|
| O1 | Эксплуатация 1 | подтверждена | `_deploy.yml`: только «Build panel» и «Deploy panel to CDN»; `RELEASE_EN.md`: «CI does not deploy the node» | ПРОВЕРЕНО: `_deploy.yml:3`, `relay/RELEASE_EN.md:29,75` |
| O2 | Эксплуатация 2 | подтверждена | `environments.toml:16` `sha-622a848`; `git ls-tree 622a848 relay/node/db/` кончается на 021; прод тоже 021, staging 010; `inventory.toml` без `image_tag` | ПРОВЕРЕНО: `git ls-tree` → `021_idempotency_age.sql` |
| O4 | Эксплуатация 4 | частично | `bash -e`: без `--ignore` и при одном database.test.ts — exit 1 без сообщения; при пустом `--ignore=` охрана срабатывает | ПРОВЕРЕНО: `bash -eo pipefail` на `{}` → exit 1, строки «no suites found» нет |
| O5 | Эксплуатация 5 | подтверждена | `grep -c timeout-minutes` = 0; `needs: [test, database]` у build-push | ПРОВЕРЕНО: `grep -c` → 0 |
| O3/D1 | Эксплуатация 3, Данные 1 | подтверждена (гипотетика) | `migrate_db.ts:29-86`: только `sort()` и `applied.has`; охраны порядка нет; дыры 011, 018, 019, 035–038, 040 | НЕ ПРОВЕРЕНО опытом, только чтением агента |
| C1 | Согласованность 1 | подтверждена | `032:4` «spec's own», `032:17` индекс; в `chat_RU.md:2321` один индекс | НЕ ПРОВЕРЕНО мной построчно |
| C2 | Согласованность 2 | подтверждена | `brand` в `chat_RU.md:2860`; `support_by_brand_created` только в `039:25` | НЕ ПРОВЕРЕНО мной построчно |
| C3 | Согласованность 3 | подтверждена | `rev-list --count`: 195 / 196 / 197 | ПРОВЕРЕНО: `git rev-list --count origin/dev..bc385fc` → 197 |
| C4 | Согласованность 4 | подтверждена, влияние малое | `decisions.tsv:171` EN → `roadmap_EN.md:138`, нужна 152; `readdress-facts.py:154` ведёт только RU | НЕ ПРОВЕРЕНО мной |
| C5 | Согласованность 5 | **опровергнута** | якоря — фрагменты для grep каждой половины, не перевод; проверка зелёная | — |
| C6 | Согласованность 6, Данные 2 | подтверждена (до диапазона) | `deno.json` «three suites» при 10 в `--ignore` | — |
| C8 | Согласованность 8 | подтверждена (мелочь) | 8 файлов `PANEL_2026-09-23_*`, в перечне 7 тем, нет `loop` | — |
| S1 | Безопасность, мелочи 1–2 | **опровергнута** | список из того же репозитория, что и workflow; граница доверия не пересекается; масок в `--ignore` нет | — |
| S3 | Безопасность, «не нашёл описанным» | частично (до диапазона) | `database` без `permissions:`, checkout без `persist-credentials: false`; читают только тесты самого репозитория | — |
| D3 | Данные 3 | **опровергнута** | `sweep_chats` только в `job_rearm.test.ts`; прочие `DELETE FROM jobs` — по своему `kind` | — |
| — | новая заявка опровергающего: «сверено 162 при 172 строках» | **опровергнута ведущим** | — | ПРОВЕРЕНО: 162 строки данных + 9 комментариев + 1 шапка = 172 |

Не отданы на опровержение как мелочи без последствий: Эксплуатация 6 (аннотация `::error`),
7 (разбор `deno.json` через `jq`), 8 (два списка наборов), Согласованность 7 (формулировка
«16 файлов в 11»). Все — НЕ ПРОВЕРЕНО.

## Сводка ведущего

- **Критичного нет.** Склейка не меняет ни одного свойства схемы: три линзы независимо и
  опыт линзы «Данные» (`information_schema`, `pg_indexes`, `pg_get_constraintdef`) — совпало;
  среды с частичным набором старых имён нет (dev, staging, prod — по закреплённым образам,
  не по базе).
- **Дефект плана (O1):** мерж в `dev` узел не катит — `deploy-dev.yml` выкладывает только
  панель; узел n1 — руками, `scripts/deploy-relay-dev.sh`. План «мерж → deploy-dev.yml
  катит n1» был неверен.
- **Противоречие (O2):** на dev не хватает `022`–`045` (19 файлов), а не `025`–`045`.
- **Дефекты дизайна:** O3/D1 — раннер не охраняет порядок и молчит о призраках; O5 —
  нет таймаутов в CI; O4 — охрана пустого списка мертва в двух главных сценариях.
- **Противоречия документов:** C1, C2 (индексы против DDL спеки), C3 (195 → 197),
  C6 (комментарий `deno.json`), C8 (нет `loop`), C4 (EN-адрес решения).
- **До диапазона:** S3 (права токена в CI), C6.
- **Отброшено:** C5, S1, D3, заявка «162 при 172».
- Тем же заходом ничего не закрывалось: панель запускалась на ревью, правки — отдельным словом.

## Нарезка на задачи

1. **Документы разом** — C1, C2 (индексы в DDL `chat_RU/EN.md` или шапки 032/039), C3
   (197), O2 (`022`–`045`, 19 файлов), C4 (EN:152), C6, C7, C8. Уровень: противоречие.
   Цена: ~20 мин, плюс `check-all`.
2. **CI крепче** — O4 (`|| true` в подстановке), O5 (`timeout-minutes` на job и `timeout`
   на набор), Э6 (`::error` на упавший набор), S3 (`permissions: contents: read`,
   `persist-credentials: false`). Уровень: дефект дизайна. Цена: ~30 мин, контрольная
   поломка на одноразовой ветке.
3. **Охрана порядка в раннере** — O3/D1: отказ применять файл ниже максимального
   записанного, предупреждение о призраках; тест, увиденный красным. Уровень: дефект
   дизайна. Цена: ~40 мин.
4. **Один список наборов** — Э8: `scripts/run-relay-database-tests.sh` читает `--ignore`
   из `deno.json`, особые случаи отдельно. Уровень: мелочь. Цена: ~20 мин.
5. **План выката на dev** — O1: мерж в dev, зелёный build-push, затем
   `scripts/deploy-relay-dev.sh`; перед этим `SELECT name FROM schema_migrations` на n1.
   Уровень: дефект плана. Цена: ~1 ч, решение владельца.
