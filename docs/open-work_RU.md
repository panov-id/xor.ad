# Открытые работы: сводный чеклист

Собрано 27.07.2026 из трёх параллельных линий, обновлено 29.07.2026. Охватывает
`xor.ad` (релей, панель, инфраструктура), `sosed.place` и `neighbro.place`
(лендинги). Живой документ — отмечать по мере закрытия.

Источники: `status-tenancy_RU.md`, `tenancy-review_RU.md`,
`review-checklist_RU.md`, `sosed.place/docs/SEO_AND_ANALYTICS_RU.md`,
`sosed.place/docs/PENDING_FROM_NEIGHBRO_RU.md`,
`neighbro.place/docs/SEO_AND_ANALYTICS_RU.md`.

---

## A. Тенантность: довести выкат

Порядок внутри раздела обязателен: узел всегда раньше лендинга, миграция всегда
раньше флага.

- [x] **A1. Миграция данных на dev** — сделано 27.07.2026 через новый
      `scripts/migrate-tenants-remote.sh` (тот же инструмент, но против реального
      хранилища; prod закрыт за `CONFIRM_PROD=yes`). Три прохода: план — 7 лидов
      (2 sosed, 5 neighbro), `--apply` — скопированы без ошибок, `--delete` —
      оригиналы удалены после проверки копий. Итог: корень `waitlist/dev/` пуст,
      арендаторы видят 4 и 6, платформа 10 вместо 17, то есть удвоение ушло.
      Проверялось панельным API, не глазами в панели.
- [x] **A2. Узел staging** — сделано 27.07.2026. По регламенту staging берёт
      релизный образ, а не sha, поэтому сначала нарезан и опубликован релиз
      **v0.4.0** от `e090c2f` — того самого коммита, чей образ уже крутился на
      dev, так что на staging уехал байт-в-байт проверенный билд. Затем
      `image_tag` среды `staging` = `v0.4.0` и `wizard --node n1 deploy`.
      Смоук: `/health` ок, preflight отдаёт `x-api-key` в allow-headers, ключ
      перебивает тело, ключ с чужого Origin → 403.
- [x] **A3. Миграция данных на staging** — сделано 27.07.2026. План — 4 лида
      (1 sosed, 3 neighbro), `--apply` без ошибок, `--delete` после проверки
      копий. Итог: корень `waitlist/staging/` пуст, sosed видит 2, neighbro 4,
      платформа 6 вместо 10.
- [x] **A4. Лендинги на uat** — оказалось, закрыто автоматически: у лендингов
      `deploy-uat.yml` висит на push в `main`, а не на ручном запуске, поэтому uat
      уехал вместе с мержем — 26.07 с ключами, 27.07 со счётчиком. Проверено по
      `config.js` обоих uat-доменов. Порядок «узел раньше лендинга» при этом не
      нарушился: staging уже был на `v0.4.0`.
- [x] **A5. Узел prod** — сделано 27.07.2026: тот же релизный `v0.5.0`, что
      обкатан на staging, промоутнут на бокс p1 с `--confirm-prod`. Смоук:
      health, preflight с домена `sosed.place`, бесключевой вызов (как делают
      живые лендинги) и ключевой — оба 200.
- [x] **A6. Миграция данных на prod** — 27 боевых лидов (25 neighbro, 2 sosed).
      Перед необратимым проходом сверены **все 27** копий с оригиналами по
      данным, а не побайтово: оригиналы писала прошлая миграция из Supabase с
      пробелами в JSON, копии пишет узел без них — побайтовая сверка дала бы
      ложную тревогу. Расхождений и пропаж нет. Итог: корень пуст, платформа
      29 вместо 56.
- [x] **A7. Лендинги на prod** — 27.07.2026, теми же тегами, что крутились на
      uat (`v2026.07.27-d8cbb02` и `-4e17a76`). Живые сайты отдают ключ и GA4 ID
      в `config.js`, `robots.txt` с `Allow`, sitemap на 19 и 12 адресов,
      языковые страницы с `hreflang` и без `noindex`, счётчик и разметку FAQ.
- [x] **A8. `REQUIRE_API_KEY=true` включён во всех средах** — 28.07.2026.
      Флаг живёт в `inventory.toml` per env (`require_api_key`), потому что это
      свойство того, насколько выкачены лендинги этой среды, а не образа.
      Проверено: с ключом 200, без ключа 401, счётчик просмотров по-прежнему
      отвечает 200 и просто ничего не пишет.
      **Перед включением** пришлось закрыть дыру, которой не было в плане:
      чинёный edge rule укорачивает кэш только для копий, скачанных после него,
      а у зашедшего раньше лежал `config.js` без ключа со сроком до 30 дней —
      его форма начала бы получать 401. Теперь адрес версионируется
      (`config.js?v=<build>`), страница живёт 5 минут, поэтому старая копия
      просто перестаёт запрашиваться.
- [x] **A9. Тег образа вне истории** — 29.07.2026, разделением файла.
      Снять `inventory.toml` с игнора целиком было нельзя: репозиторий
      публичный, и коммит `8002c46` («remove concrete server/egress IP addresses
      from the public repo») убирал оттуда адреса намеренно, а в инвентаре лежат
      `ssh_host` четырёх боксов и белые списки адресов трёх сред.

      Поэтому файл разделён. `relay/wizard/environments.toml` — **в git**:
      состав среды, и прежде всего `image_tag`, так что «какой билд на dev»
      теперь отвечает история, а не машина, с которой катали.
      `inventory.toml` остаётся игнорируемым и держит то, чему в публичном
      репозитории не место: адреса боксов и белые списки. Визард читает оба и
      склеивает, локальный перекрывает общий — местный эксперимент не требует
      править отслеживаемый файл.
- [x] **A10. Панель на UAT** — закрылось само: деплой от мержа в `main` всё-таки
      прошёл, просто позже, чем я посмотрел. Проверено 27.07.2026 по бандлу
      `uat.xor.panov.id` — в нём есть страница серверных логов, роль
      `tenant_admin` и скоуп `unattributed`.

- [x] **A11. Хвост day16 дошёл до dev, но не до main — закрыто 09.08.2026** мержем `dev` → `main`; обе ветки на `e4ce220`, прод накатан на релиз из неё.** 29.07.2026 ветка `dev`
      перемотана на `6785bd1`, так что квоты (`6bf6f9e`), лимит ключа в панели
      (`945d246`), typecheck релея в CI (`52a8582`) и работа day17 уехали на
      **dev-окружение**. `origin/main` по-прежнему стоит на `4734392` —
      середине day16, — а uat и prod едут от него. То есть **B5 сделан и
      обкатывается на dev, но в бою его всё ещё нет**. Закрывается мержем
      `dev` → `main`, не кодом.
- [x] **A12. Ветка `dev` вернулась в тракт** — 29.07.2026. Она стояла на
      `7d1c2cc` (эпоха day13), отстав на 32 коммита, а `main` всё это время
      перематывался прямо на коммиты day-веток — merge-коммитов в нём нет
      вовсе. `dev` перемотана вперёд на `6785bd1` (вершину day17): истории не
      расходились, поэтому обошлось без мержа и без потерь. Дальше — по канону
      `dayN` → `dev` → `main` из `deployment_RU.md`.

## B. Тенантность: незавершённая функциональность

- [x] **B1. Страницы API-ключей** — 28.07.2026. `GET/POST /admin/api-keys` и
      `POST /admin/api-keys/:id/revoke`; страница показывает ключ целиком (он
      публичен по устройству), список разрешённых Origin и кнопку отзыва.
      Арендатор видит и выпускает только свои — бренд берётся из сессии, а не
      из тела. Отзыв **штампует** ключ, а не удаляет: исчезнувший ключ унёс бы
      с собой ответ на вопрос «что это было и когда мы перестали ему верить».
      Пустой список Origin отклоняется там, где среда требует ключи.
      Выпуск живёт в `lib/api_key.ts`, и CLI-инструмент теперь зовёт ту же
      функцию — ключ, выпущенный двумя путями, рано или поздно получил бы две
      формы.
- [x] **B2. Страница брендов** — 28.07.2026. `POST /admin/brands` пишет в
      реестр, узел подхватывает сразу, остальные — в пределах TTL кэша: именно
      это делает подключение арендатора записью, а не редеплоем. Бренды из env
      показаны с пометкой `environment` и не редактируются — запись поверх
      затенила бы сид копией, которая от него разъедется.
- [x] **B3. Подключение арендатора — приглашением, а не регистрацией.**
      Отклонено 29.07.2026: самостоятельной регистрации не будет. Бренд — это
      граница данных, а не строка в анкете, и открытая форма означала бы, что
      незнакомый человек одним нажатием заводит себе пространство имён, ключи и
      вход в панель, а платформа узнаёт постфактум.

      Вместо неё: админ платформы создаёт бренд, затем оператора внутри бренда,
      и узел шлёт приглашение на почту. Приглашение — тот же одноразовый токен,
      что и обычная ссылка входа, но живёт **семь дней** вместо пятнадцати
      минут: письмо читают из почты на следующий день. Повторная отправка —
      `POST /admin/panel-users/:email/invite` и кнопка в строке оператора, в тех
      же границах видимости.

      Попутно закрыта дыра: отправитель писем панели не проверял
      `MAIL_TRANSPORT=none` и в этом случае уходил в Resend с пустым ключом.
      И убран мёртвый код — панель хранила состояние под ссылку-приглашение,
      которую никогда не показывала.
- [x] **B4. Живая проверка входа арендатора** — 29.07.2026, на стенде.
      `scripts/verify-tenant-login-local.sh` был написан, но ни разу не
      запускался. Оператор проходит весь путь: письмо → ссылка → сессия, панель
      видит его как `tenant_admin` бренда `sosed`. Границы держатся: свои
      заявки, ключи и просмотры — 200; серверные логи, реестр брендов и чужой
      бренд — 403; попытка выдать себе `admin` — 403; ключ, выпущенный с
      просьбой «для neighbro», выпускается для `sosed`.

      **Нашлась ошибка, которую тесты поймать не могли.** Выпуск ключа падал с
      500 на внешнем ключе `api_keys.brand → brands.key`. Причина — противоречие
      внутри `001_control_state.sql`: комментарий за двадцать строк до
      ограничения говорит, что бренд может жить только в переменной `BRANDS`, а
      ограничение требует строку в таблице. Оба утверждения верными быть не
      могут. Снято миграцией `003_api_keys_brand_fk.sql`; проверка бренда в коде
      уже была и знает оба источника (`brandByKey`).

      Тесты этого не видели и не могли: они гоняются без базы, на файловом
      хранилище, где ветка с `INSERT` не выполняется вовсе. Такое ловится только
      на стенде — ради чего пункт и существовал.
- [x] **B5. Квоты и лимиты на ключ** — 28.07.2026, после E1. Дневной лимит
      живёт на ключе, счётчики — в `quota_counters` (`db/002_quotas.sql`).
      Считает узел у себя и сбрасывает в базу таймером раз в 10 секунд:
      UPDATE на каждый публичный запрос поставил бы базу на путь каждой заявки
      и каждого просмотра — ровно ту зависимость, которой избегает хранилище
      как источник правды. Цена честная: за интервал сброса ключ может
      перебрать лимит на объём одного интервала, поэтому минимальная разумная
      квота здесь дневная, а не посекундная. Инкременты **складываются**, а не
      присваиваются — два узла, сбросившие одновременно, суммируются.
      Проверка считает базу плюс несброшенное на этом узле, так что всплеск
      ловится сразу. Недоступная база **не** отклоняет запрос: квота — предел
      коммерческий, а не защитный. Панель правит лимит и показывает расход за
      сегодня. Проверено `scripts/verify-quota-local.sh`.
- [x] **B7. Не задело — проверено 17.08.2026.** `GET /admin/brands` на всех
      трёх средах: и `sosed`, и `neighbro` помечены `registry`, то есть строка
      в таблице `brands` есть везде — внешний ключ удовлетворим, выпуск через
      панель не падает. Подкрепление: на проде лежат три публичных ключа, у
      обоих брендов, от 26 и 28 июля. Проверено чтением, а не выпуском: боевой
      ключ, выпущенный ради проверки, остался бы навсегда.

      Было записано так — «Проверить, задел ли внешний ключ прод»:

      Ошибка из `B4` касается
      брендов, у которых нет строки в таблице `brands`, — а это ровно
      «собственные витрины платформы», про которые схема и говорит, что они
      остаются на env-посеве. Если в боевой таблице нет строк для `sosed` и
      `neighbro`, то выпуск ключа через панель там падал так же, просто никто не
      пробовал: боевые ключи витрин выпущены до переезда состояния в Postgres.
      Проверяется одним `GET /admin/brands` на проде — если бренд помечен
      `environment` и строки нет, значит задело. Лечится тем же `003`, который
      приедет со следующим образом.
- [x] **B6. Секретные ключи (server-to-server)** — второй тип ключей из
      раздела 1 `api-platform_*`, вместе с публичным `/v1`. Код приехал
      коммитом `d87a721`, но пункт остался неотмеченным; 30.07.2026 проверено на
      стенде и вычеркнуто.

      Ключ выпускается со скоупами, секрет **показывается один раз** и обратно не
      читается — список отдаёт ключ без него. `/v1` узнаёт вызывающего по
      секрету, повтор того же запроса отвечает тем же и ничего не пишет второй
      раз (`idempotent-replay: true`, лид у арендатора один). Скоуп — это предел:
      чужой секрет и запрос без ключа получают 401, арендатор, просящий
      `logs.server.read`, — 403, отозванный ключ — 401.
      Проверено `scripts/verify-secret-keys-local.sh`.

## C. Тенантность: хвосты ревью

Пункты 9 и 10 из `tenancy-review_RU.md` кода не потребовали, но остаются
доводами.

- [x] **C1. Веер `/admin/waitlist` у платформы** — 29.07.2026. Маршрут читал
      **каждый** объект заявки по всем брендам на каждый заход в панель: 29
      лидов незаметны, 29 000 — это 29 000 чтений хранилища на одно открытие
      страницы.

      Теперь он следует той же дисциплине, что и журналы: перечислить
      метаданные (один вызов на бренд, без тел), выбрать окно, прочитать только
      показываемое. Появились `limit` (200 по умолчанию, 500 максимум) и курсор
      `before`, заголовки отдают полное число и число в окне.

      Форма ответа не менялась — массив плюс `x-total-count`, — поэтому панель и
      её тесты не тронуты.

      **Размен:** порядок теперь по времени объекта в хранилище, а не по
      `created_at` внутри записи: узнать второе, не прочитав объект, нельзя, а
      читать всё ради сортировки значит не чинить ничего. Для лидов, которые
      писал узел, это одно и то же; перенесённые миграцией тенантности несут
      время миграции и потому встают одним блоком. Тот же размен уже принят для
      журналов и там же записан.
- [x] **C2. Фолбэк `resolveBrand`** больше не достижим на публичных роутах:
      с `REQUIRE_API_KEY=true` запрос без ключа отклоняется до него.

## D. Поиск и индексация

Код сделан на обоих лендингах: языковые страницы, взаимные `hreflang`,
генерируемый `sitemap.xml`, `robots.txt` с непродовой подменой, JSON-LD
(`Organization`/`WebSite`/`FAQPage`), FAQ, IndexNow, баннер согласия, GA4 на
флаге, edge rules (www→apex, короткий TTL для HTML). Осталось:

- [x] **D1. `ANALYTICS_ID` (GA4)** — 27.07.2026 записан в окружение
      `production` обоих репозиториев: `G-WWHXHZ5QWQ` для sosed,
      `G-K7EP39DDK9` для neighbro. dev и uat остались пустыми, то есть без
      счётчика и без баннера. `SEARCH_CONSOLE_TOKEN` **не нужен**: домены
      верифицируются TXT-записью в DNS, а не meta-тегом, поэтому в секреты
      его не клали — иначе верификация дублировалась бы двумя способами.
- [x] **D2. Локальные проверки F1–F3** — закрыто 27.07.2026. Появился
      `landing/verify-seo.mjs` (одинаковый файл в обеих витринах): читает
      сгенерированный сайт и отвечает на все три вопроса до выката. sosed —
      17 языков, 18 альтернатив на страницу, sitemap из 19 адресов; neighbro —
      10 языков, 11 альтернатив, 12 адресов. Мёртвых ссылок нет.
- [x] **D3. Живые проверки — закрыто 18.08.2026, обе половины измерены.**

      **До согласия ни одного запроса к доменам Google.** Прогнаны браузером обе
      боевые витрины, по пять страниц каждая: `/`, `/ru/`, `/legal.html`,
      `/rules.html`, `/report.html`. Записывался **весь** сетевой трафик, не
      консоль. Из 5–16 запросов на страницу к Google — **ноль** везде.

      **Половина, без которой первая ничего не значит.** «Ноль запросов к Google»
      верно и на сайте без идентификатора аналитики, и на странице, которая не
      загрузилась, и у пробника, который смотрит не туда. Поэтому в том же прогоне
      стоит положительный контроль: нажать «принять» и убедиться, что запросы
      **появляются**. Появились на обеих —
      `googletagmanager.com/gtag/js?id=G-K7EP39DDK9` и `…G-WWHXHZ5QWQ`. То есть
      проверка способна покраснеть, и её зелёный цвет что-то значит.

      Идентификаторы на проде заданы (`config.js` обеих витрин), поэтому проверять
      имело смысл именно там: на dev и uat `analyticsId` пуст, и `gtag.js` не
      загрузился бы независимо от согласия.

      **Попутно уточнено.** На `legal.html`, `rules.html` и `report.html` баннера
      согласия нет — и не должно быть: кода аналитики на этих страницах нет вовсе
      (`consentAccept`, `loadAnalytics`, `googletagmanager` — ноль совпадений).
      Там нечему сработать ни до согласия, ни после, и это не пробел, а свойство.

      **Первая половина была закрыта 14.08.2026** после прод-выката: шесть страниц,
      нарушений CSP ноль.

      Пробник положен в репозиторий — `scripts/check-consent-gate.sh` и
      `testing/consent-gate.mjs`, запускать после каждого прод-выката витрин.
      Проверен падением: копии подложили уже данное согласие, аналитика
      загрузилась до всякого нажатия — два провала, код возврата 1, в выводе
      названы и `gtag.js`, и запрос `google-analytics.com/g/collect`.
- [x] **D4. Sitemap отправлен в Search Console** — 27.07.2026, оба домена
      (`https://sosed.place/sitemap.xml` и `https://neighbro.place/sitemap.xml`).
      Свойства типа Domain, поэтому адрес указывается полным. www не
      отправляли: на зонах стоит редирект на апекс, это был бы дубль.
- [ ] **D5. Регистрация в Bing Webmaster Tools** — ручной шаг; для IndexNow не
      требуется, но даёт отчёты. Проверено 17.08.2026: подтверждения нет —
      `/BingSiteAuth.xml` отдаёт 404 на обеих витринах, метки `msvalidate` в
      разметке нет. IndexNow при этом работает: ключ прошит в выкат и
      пингуется на каждом. Завести кабинет может только человек.
- [x] **D6. Своя страница 404 — принято как есть.** Решено 29.07.2026:
      won't-fix. `ErrorPageCustomCode` в Bunny относится к ошибкам origin, а не
      к 404 от storage-зоны. Страница написана, отдаётся по `/404.html`,
      статус-код верный; теряется только вид чужой заглушки при опечатке в
      адресе. Свой origin вместо storage решил бы это, но добавил бы движущуюся
      часть там, где сейчас просто файлы, — цена выше пользы.
- [x] **D7. Картинка для соцсетей на язык — закрыто 19.08.2026. По дороге нашлось,
      что дело было не в картинке.** Пункт говорил: «пайплайн рисует одну на сайт,
      смысл появится только с переведённой типографикой». Оказалось, что
      типографики для половины языков у нас просто не было.

      **Слоган уже был переведён** — он лежит в словаре витрины ключом `eyebrow`,
      на все 10 языков у neighbro и 17 у sosed. Генератор принимает подпись
      параметром, так что картинки делались сразу.

      **А вот шрифтов не хватало.** У обеих витрин три своих шрифта, и ни один не
      покрывает грузинский; у sosed вдобавок армянский. Значит грузинская страница
      всё это время рисовалась тем, что найдётся на устройстве читателя, а картинка
      запекала в себя шрифт из контейнера сборки — невоспроизводимо. У sosed было
      хуже: подпись в шаблоне набиралась **системным** моноширинным, без единого
      нашего шрифта, то есть боевая картинка не повторялась в принципе. В моём
      контейнере она нарисовалась китайским WenQuanYi.

      **Сделано:**
      - `Noto Sans Georgian` (41 КБ) в обе витрины, `Noto Sans Armenian` (26 КБ) в
        sosed — по одному файлу, только свой юникод-диапазон, запасными в стеках:
        39 стеков в neighbro, 21 + 21 в sosed;
      - шаблон картинки sosed переведён с системного моноширинного на `JetBrains
        Mono` с теми же двумя запасными;
      - `og/render-all.mjs` + `og/docker/run-all.sh` в обеих витринах: одна картинка
        на язык, подписи читаются **из того же словаря**, что и страницы, поэтому
        правка перевода не оставляет устаревшую картинку;
      - `build-pages.mjs` ставит каждой языковой странице `og:image`,
        `og:image:secure_url` и `twitter:image` своего языка.

      **Проверено не шириной строки, а вопросом к браузеру.** `CSS.getPlatformFontsForNode`
      называет шрифт, которым отрисован узел. По нему же работает сторож внутри
      генератора: чужой шрифт в подписи — отказ с ненулевым кодом, «чинить шрифты,
      а не рендер».

      Итог: **10 картинок** у neighbro и **17** у sosed, все своими шрифтами.
      Грузинская страница проверена на живых элементах: `h1`, абзац и моноширинная
      строка — Noto Sans Georgian на грузинских буквах, фирменные на латинских.

      **Три раза ошибся в методе и каждый раз поймал следующей проверкой:** мерил
      до загрузки юникод-подмножества (шесть ложных «не покрыто»), мерил не тот
      шрифт (подпись набрана моно, а проверял заголовочный), и объявил грузинский
      под именами фирменных семейств — приём выглядит аккуратнее и **не работает**:
      Chromium выбирает начертание по весу внутри семейства раньше, чем смотрит на
      покрытие символа. Работает только запасное семейство в стеке.

## D-bis. Свой счётчик просмотров

Сделан 27.07.2026 и выкачен во все среды, включая прод: `POST /pageview` на релее, страница
«Page views» в панели, отправка с обоих лендингов. Считает всех, потому что
согласия не требует — в записи нет ни адреса, ни user-agent, ни идентификатора.

- [x] **Db1. Ретенция.** `tools/prune_pageviews.ts` + `scripts/prune-pageviews-remote.sh`:
      окно по умолчанию 90 дней, отбор по метке листинга (объекты не читаются —
      уборка не должна стоить как трафик, за которым убирает), план по умолчанию,
      удаление только с `--apply`, окно короче 7 дней отвергается, prod закрыт
      `CONFIRM_PROD=yes`. Проверено на стенде: состаренная запись удалена, свежая
      осталась. Запускать руками раз в месяц, пока не решён E1.
- [x] **Db2. Отдаём** — решено 29.07.2026, сделано в тот же день.
      `POST /v1/pageview` под скоупом `pageviews.write`. Отдаём именно потому,
      что счётчик отказывается записывать: ни адреса, ни user-agent, ни
      идентификатора, переживающего запрос, — арендатору не нужен баннер
      согласия, и это всё предложение целиком.

      Идемпотентности здесь нет, в отличие от заявки: повторный просмотр — это
      просмотр, и склейка двух одинаковых отчётов исказила бы счётчик в ту
      сторону, ради которой он и заведён.
- [x] **Db3. Свёртка в дневные агрегаты** — 29.07.2026, вместе с `Eb4`. Объект
      на просмотр остался, но перестал быть источником цифр: счёт живёт в
      `pageview_daily`, объект — только носитель детализации на 14 дней.

## E. Открытые решения

- [x] **E1. Куда переезжает состояние** — решено 28.07.2026: свой Postgres
      рядом с узлом, только под управляющее состояние; данные остаются в
      объектном хранилище. Схема, слой доступа, перенос ключей и брендов —
      сделаны, квоты следом (B5); очередь и агрегаты идут дальше. В тот же день
      доведено до прода вместе с бэкапом — что именно построено, см. **E-bis**.
      `state-decision_RU.md`.
- [x] **E2. Остаётся ли узел взаимозаменяемым** — да, пока на среду приходится
      один бокс: узел по-прежнему не хранит состояния сам, база стоит рядом.
      Второй бокс той же среды расщепил бы состояние молча, поэтому визард такую
      конфигурацию отклоняет с объяснением. Вернуться к вопросу — когда пул
      реально вырастет.

## E-bis. Управляющее состояние: что построено

Решение E1 доведено до прода 28.07.2026. Раздел описывает построенное; открытым
из него остаётся только Eb4.

- [x] **Eb1. База рядом с узлом.** Postgres на том же боксе, только под
      управляющее состояние — ключи и бренды переехали туда первыми. Узел
      по-прежнему не хранит состояния сам (E2), поэтому остаётся
      взаимозаменяемым.
- [x] **Eb2. Миграции едут в образе** и применяются до старта узла: схема и код,
      который её ждёт, не могут разъехаться, потому что приезжают одним
      артефактом. Визард ждёт готовности базы перед миграцией — иначе первый
      деплой падал на гонке между поднявшимся контейнером и принимающим
      соединения Postgres.
- [x] **Eb3. Ночные дампы и учение на восстановление.** `backup-postgres.sh`
      живёт на боксе и запускается systemd-таймером — бэкап, который случается,
      только когда кто-то вспомнил, бэкапом не является. Дамп с
      `--clean --if-exists` (чтобы восстанавливаться поверх непустой базы),
      gzip, загрузка в Bunny Storage, хранение две недели. Дамп меньше 512 байт
      не загружается — это сообщение об ошибке, а не база. Уборка старых копий
      идёт **после** успешной загрузки, никогда до. Восстановление проверяется
      `scripts/verify-backup-restore.sh`, а не предполагается.
- [x] **Eb4. Очередь и дневные агрегаты** — 29.07.2026, вместе с `Db3`.

      **Счёт.** `POST /pageview` пишет не только объект, но и инкремент в
      `pageview_daily` (бренд · день · путь · язык). Батчами по 10 секунд, как
      квоты: просмотр — запрос, которого ждёт посетитель, и база не может быть
      на его пути. Инкременты складываются, а не присваиваются, поэтому два
      узла, сбросившие одновременно, суммируются.

      **Ретенция.** Объекты живут 14 дней вместо 90. Срок перестал определять
      сохранность цифр — он теперь равен тому, сколько времени интересна
      детализация, которой в агрегате нет: реферер, размер экрана, время суток.

      **Очередь.** `lib/jobs.ts` — воркер в узле: захват через
      `FOR UPDATE SKIP LOCKED`, лизинг `locked_until` (умерший узел не уносит
      задачу с собой), ретраи с ростом паузы, исчерпанные попытки оставляют
      строку с `last_error`, а не удаляют её. Задача уборки переназначает себя
      на завтра сама, поэтому расписание живёт в той же таблице, что и работа.
      Без `DATABASE_URL` всё это молча не запускается.

      **Что пришлось добавить сверх плана.** `total` в ответе панели считает
      объекты, а не просмотры, — после первой же уборки он показал бы падение
      трафика втрое. Поэтому в ответе появилось `lifetime` из агрегата, и панель
      пишет обе цифры: «36 stored · 36 all time». А чтобы они сходились на
      окружении, которое уже работает, написан `tools/backfill_pageview_daily.ts`
      — сворачивает существующие объекты в строки. Пересобирает дни целиком, а
      не добавляет к ним, поэтому повторный запуск сходится, а не удваивает.
      **Запустить его на dev до того, как объекты начнут удаляться.**

      Проверено `scripts/verify-pageview-aggregate-local.sh` против настоящего
      Postgres: сложение инкрементов, независимость счёта от уборки, выдача
      задачи ровно одному из двух работников, отсрочка упавшей. Юнит-тестами это
      не покрыть — они гоняются на файловом хранилище, где этих веток нет.

## F. Перенос neighbro → sosed — закрыт 28.07.2026

Сверено с кодом, а не с документом: шрифты, CSP, service worker, санитайзер
legal и контраст приглушённых цветов оказались давно перенесены; push и
контурный заголовок на sosed отсутствуют, переносить нечего. Сделано:
озвучивание статусов формы, заставка держит экран только при первом показе за
вкладку, удалён мёртвый ключ `m10` (на neighbro, переведён на 10 языков впустую).

**Сверх списка:** контраст акцентных кнопок — акцент случаен при загрузке, и
4 темы из 6 на sosed (и 3 из 5 на neighbro, в проде) давали 3.41–3.91:1 при
пороге 4.5. Цвета затемнены на ~12%, оттенок и светлый текст сохранены. Обе
витрины получили `check-contrast.mjs` и `find-dead-keys.mjs`.

Подробности — `sosed.place/docs/PENDING_FROM_NEIGHBRO_RU.md`.

## F. Перенос neighbro → sosed (исходный список) — сверен 29.07.2026

Список из `sosed.place/docs/PENDING_FROM_NEIGHBRO_RU.md`, девять пунктов. Все
закрыты, но отмечены не были: секция выше объявляла перенос завершённым, а этот
перечень продолжал стоять неотмеченным — трекер девять раз показывал работу,
которой нет. Сверено с кодом, а не с документом:

| Пункт | Где |
|---|---|
| F1 шрифты self-host, Google CDN убран | `landing/fonts.css`, `landing/fonts/*.woff2` |
| F2 CSP | `index.html`, `legal.html` |
| F3 `:focus-visible`, `role="status"`, `prefers-reduced-motion` | `index.html` |
| F4 service worker + `controllerchange` | `landing/sw.js` |
| F5 санитайзер `safeUrl` | `legal.html` |
| F6 контраст `--muted-2` | палитра `index.html` |
| F7 заставка держит экран один раз | ключ `ss-splash` |
| F9 мёртвые ключи перевода | `find-dead-keys.mjs` |

**F8 (`subscribePush`) неприменим:** push на sosed нет как фичи — переносить
нечего, что и сказано в секции выше.

## H. Наследие Supabase убрано — 29.07.2026

Проект Supabase выведен из эксплуатации 22.07.2026, но обвязка вокруг него
осталась и продолжала выглядеть рабочей. Убрано:

- [x] **H1. Мёртвые файлы.** `docker-compose.functions.yml`,
      `scripts/setup-supabase.sh`, `reload-functions.sh`, `apply-migrations.sh`,
      `bootstrap-admin.sh`, `scaffold-panel.sh` (генерировал панель с пресетом
      `refine-supabase`), `test-last-admin-guard.sh` (проверял SQL-триггер в
      контейнере `supabase-db`; защита живёт в релее и покрыта его тестами),
      одноразовые скрипты переезда `migrate_waitlist.py` и `seed_panel_users.py`
      (читали через `api.supabase.com` — запустить их уже невозможно), каталог
      `supabase/` на диске.
- [x] **H2. E2E лендинга переведены на релей.** Проверка заявки шла через
      клиент Supabase в таблицу `waitlist`, которой нет: тест подтверждал бы
      пустоту. Теперь лид читается через `GET /admin/waitlist`. Заодно
      оказалось, что половина набора написана под разметку neighbro: у sosed нет
      ни `form.waitlist-form`, ни `data-status-for`. Селекторы разнесены по
      витринам, учтена механика «помним состоявшуюся запись» (`ss-wl-done` /
      `nb-wl-done`). 14 из 14.
- [x] **H3. Шлюз воскрешён.** `nginx.conf` проксировал в `kong:8000` —
      контейнер Supabase. Теперь проксирует публичные маршруты на стенд релея;
      `docker-compose.gateway.yml` больше не зависит от чужого стека.
- [x] **H4. E2E панели переведены на релей.** Три спеки про RLS удалены — слоя
      нет. Оставшиеся стояли на тех же хелперах: вход шёл через Supabase Auth,
      посев — через сервисный ключ. Теперь вход кладёт в `localStorage` токен,
      подписанный как это делает `/auth/callback`, посев идёт через
      `POST /admin/panel-users` и публичный `POST /waitlist`. 21 из 21.
- [x] **H5. Хвосты.** `@supabase/supabase-js` убран из обоих наборов тестов;
      `github-secrets.example.json` переписан под ключи релея; из
      `setup-panov-id-email.sh` убран шаг настройки Custom SMTP в Supabase Auth;
      dev-фолбэк панели вёл на `localhost:8080` — шлюз, который `/admin/*` не
      отдаёт, — исправлен на стенд `62080`.
- [x] **H6. Стек Supabase убран с машины** — 29.07.2026. Одиннадцать
      контейнеров и сеть `xorad_default` удалены, каталоги `supabase/` и
      `functions/` стёрты. Бэкап от 22.07.2026 цел:
      `~/Projects/panov-id/supabase-backup-2026-07-22`. Попутно выяснилось,
      **почему** стек делил сеть со шлюзом: его поднимали командой
      `docker compose -f supabase/docker-compose.yml -f docker-compose.gateway.yml`
      из корня репозитория, а compose берёт имя проекта из каталога первого
      файла — то есть `xorad`.
- [x] **H7. Статус формы sosed при сбое** — 29.07.2026. Витрина меняла текст, но
      не класс, поэтому единственный момент, когда форма обязана быть
      однозначной, выглядел как любой другой. Добавлен токен `--err` в четыре
      палитры и правило `.status.err`; цвет не акцентный — акцент случаен при
      загрузке и на части тем сам терракотовый. Контраст посчитан: 7.17 и 6.74
      на тёмных фонах, 5.52 и 6.08 на светлых при пороге 4.5. Теперь E2E
      проверяет обе витрины одинаково.

## J. Юридическое и почта

Из разбора приватности 02.08.2026. Политики обеих витрин уже называют адрес, по
которому пишут о данных, — значит адрес обязан существовать.

- [x] **J1. Заведены — 05.08.2026.** `privacy@sosed.place` и `privacy@neighbro.place`
      созданы скриптом `deploy/add-privacy-aliases.sh`, пересылка на `ev.panov@gmail.com`.
      DNS, MX и Resend не трогали — только псевдонимы. Исходный текст задачи: `privacy@sosed.place` и
      `privacy@neighbro.place` → пересылка на `ev.panov@gmail.com`. Оба домена
      уже на ImprovMX (`mx1/mx2.improvmx.com`), так что это псевдоним в панели
      сервиса, а не правка DNS.

      **Почему срочно:** политика конфиденциальности обеих витрин уже опубликована
      с этим адресом и обещает ответ в течение месяца. Пока ящика нет, документ
      обещает канал, которого не существует, — а срок ответа по GDPR идёт с
      момента обращения, не с момента, когда мы его прочитали.

- [x] **J3. Пересылка проверена — 05.08.2026.** Исходная задача: написать на
      каждый из трёх адресов и убедиться, что письмо пришло. Псевдоним, который
      не проверяли, — это ящик, о котором узнают из жалобы.

      **Проверено 05.08.2026, все три адреса — `delivered`.** Оба `privacy@`
      проверены письмом с `no-reply@panov.id`, `eugene@panov.id` — письмом с
      `hey@sosed.place` (в обоих случаях с чужого домена, чтобы не поймать петлю
      внутри своего). `delivered` означает, что письмо принял **входящий MX
      ImprovMX**; последний участок до личного ящика подтверждается глазами.

      J2 снята: `eugene@panov.id` уже существовал и пересылается на
      `eugene.panov.id@gmail.com` — это верный адрес, менять не нужно.

Из юридического прохода 05.08.2026. Оплата убрана из сервиса, офферы описаны в
условиях, разобран DSA. Ниже — то, что этот проход открыл и не закрыл.

- [x] **J4. Механизм уведомлений по ст. 16 DSA — закрыт 10.08.2026.** Оба
      оставшихся подпункта были сделаны 09.08.2026 и записаны в
      `dsa/CHECKLIST_RU.md`; здесь об этом не узнали.

      **Прежняя формулировка**, «построен, осталось проверить и закрыть ст. 14(6)»: Формулировка «интерфейса нет» устарела: проверено
      09.08.2026 по живым сайтам и по коду.

      **Есть и работает:** таблицы, публичный `POST /report` (отвечает и через CDN
      на проде, и на staging; неполное уведомление отвергается до записи), слепок
      с тремя состояниями, подтверждение получения, удаление через год, форма
      `report.html` на обеих витринах (200), ссылка в футере обеих, экран разбора
      `panel/src/pages/dsa-notices`, маршруты `routes/dsa.ts`, оба шаблона писем в
      `lib/mailer.ts`, права `dsa_notices.read` и `dsa_notices.decide`.

      **Осталось:**
      - [x] **сквозная проверка на staging** — пройдена 09.08.2026, тестовая
        запись удалена (`dsa/CHECKLIST_RU.md`, раздел «Проверка»). Прежний текст: — настоящее уведомление, письмо
        подтверждения, очередь в панели, решение, оба письма (ст. 16(5) и 17(3)),
        затем удалить тестовую запись. Цепочку целиком не проходили ни разу;
      - [x] **уведомление о новой редакции документов, ст. 14(6)** — сделано
        09.08.2026: полоса на витринах, редакция выводится из `Last updated`
        самих документов, выкат гейтится проверкой. Прежний текст: — единственная
        по-настоящему недостроенная часть. Условия и политика сами обещают
        «signposted in the Service». Решено 09.08.2026: показываем полосу на
        витринах и экран при входе в панель, **уведомляем, не требуя принятия** —
        ст. 14(6) требует информировать, а собирать согласия не на кого: на
        витринах нет личности, есть браузер. Версия выводится из самих документов
        (`Last updated` в `terms_EN.md` и `privacy_EN.md`, поздняя из двух), а не
        заводится руками четвёртым местом — иначе разъедется в первый же раз.

      **Это не теория: изменение уже произошло.** 07.08.2026 из политики убраны
      обещания про пуш-уведомления, дата редакции поднята — и сообщить об этом
      было нечем.

      Подробно и с именами файлов — [`dsa/CHECKLIST_RU.md`](./dsa/CHECKLIST_RU.md).

- [x] **J5. Языки точки контакта названы — 05.08.2026.** В условиях обеих витрин:
      английский и греческий, ответ на языке обращения. Греческий взят потому,
      что ст. 11(3) требует официальный язык страны учреждения, а сайт
      координатора ведётся по-гречески. Носителя у нас нет — входящее переводим;
      это принятый риск, записан в `dsa/README_RU.md`, §2.

- [x] **J6. Уведомление о новой редакции — 05.08.2026.** Почты и аккаунтов нет,
      поэтому единственный честный способ — сказать при входе. Обе витрины
      сравнивают сохранённую редакцию с текущей и показывают полосу с кнопкой
      «принять»; первый визит запоминает редакцию молча (пользование и есть
      принятие, `terms §3`). Переведено на все локали, поведение проверено
      Playwright: новый посетитель полосы не видит, владелец старой редакции
      видит на своём языке, после принятия она не возвращается.

- [x] **J7. Вторая итерация переводов — 05.08.2026.** Пройдены все строки, которые
      писал ассистент: слово доната, полоса о новой редакции, фраза про офферы в
      правилах — на 17 языках sosed и 10 neighbro.

      Исправлено три места, и это были ошибки, а не вкусовщина: `uz` «ianot» →
      **`xayriya`** (книжное слово заменено обычным), `be` «данат» →
      **`падтрымаць`** (калька с русского, в белорусском так не говорят), `el`
      «διαβάστε» → **`διάβασε`** (вежливое множественное рядом с неформальным
      «Θες» — разнобой регистра).

      **Остаточный риск остаётся и снят не будет.** Грамматику и регистр вычитать
      можно, естественность звучания без носителя — нет. Если однажды появится
      человек на `kk`, `ka`, `hy`, `az`, `uz`, `ky`, `tg` — показать ему футер и
      правила.

- [ ] **J9. Перепроверить статус микропредприятия до 05.08.2027.** На нём держится
      снятие Раздела 3 DSA. Запись о текущей проверке — `dsa/README_RU.md`, §3.

- [x] **J10. Два новых субпроцессора Bunny — не возражаем. Решено 07.08.2026.**
      Уведомление пришло 05.08.2026, изменение действует с 19.08.2026, окно
      возражения по п. 3.2 — 5 дней, молчание считается согласием. Окно
      пропускаем **сознательно**.

      Возражать не на что: изменение касается «personal account data» — данных
      учётной записи оператора, а не посетителей витрин. Субъект здесь оператор,
      и блокировать изменение о собственных данных смысла нет. Общая авторизация
      субпроцессоров с окном возражения — штатный механизм ст. 28(2), не
      возразить не нарушает ничего.

      Принято как остаточный риск: имена так и не названы, «account data» не
      подтверждено письменно, страна неизвестна. Разбор и полный список того, что
      мы приняли — [`legal-archive/bunny-dpa_RU.md`](./legal-archive/bunny-dpa_RU.md).
      В `vendors-dpa_RU.md` двое помечены как неназванные.
- [x] **J12. Субпроцессоры Bunny — проверено 19.08.2026, имён так и нет.**
      Изменение, о котором уведомили 05.08.2026, вступило в силу сегодня. На
      `bunny.net/gdpr/sub-processors/` — **прежние шесть** (Zendesk, Slack, Google
      Workspace, MailChannels, OpenAI, Atlassian), ни одного нового имени, ни даты
      обновления, ни отметки о новых записях. Страница `bunny.net/gdpr/` тоже
      ничего не датирует. Вносить в реестр нечего — не потому, что не посмотрели.

      **Решение: считаем закрытым.** Имён не запрашиваем и даты следующей проверки
      не назначаем — вернёмся при следующем уведомлении. Возражать поздно: окно по
      п. 3.2 закрылось около 10.08.2026 и было пропущено сознательно; договор не
      обязывает Bunny называть субпроцессоров по запросу, а публичный список они
      ведут как считают нужным. Запрос вернул бы в лучшем случае имена, которые мы
      уже приняли, и не поменял бы ни одного решения.

      **Цена названа и остаётся записанной:** мы согласились на двоих, которых нам
      не назвали, и обоснование трансграничной передачи держится на формулировке
      письма про «данные учётной записи», проверить которую нечем. Это не вопрос,
      решённый по существу, — это вопрос, на который решено не тратить ход.

      Записи: `vendors-dpa_{RU,EN}.md` (строка Bunny) и
      `legal-archive/bunny-dpa_{RU,EN}.md` (раздел «Проверка в день вступления в
      силу»).

- [x] **J11. Автотег UAT не собирает образ — закрыто 09.08.2026.** Проверено в реестре: под `v2026.8.9-ge4ce220` есть оба образа, `relay-node` и `relay-caddy`.** Слияние `dev → main` помечает
      коммит тегом вида `v2026.08.05-<sha>`, но образа под ним нет: `type=semver`
      в `.github/workflows/relay.yml` строит тег только из корректной
      семантической версии, а `2026.08.05-…` с ведущим нулём ею не является.
      Проверено 05.08.2026 в реестре: `v2026.08.05-1480743` → 404,
      `sha-1480743` → 200.

      **Исправлено 05.08.2026.** Формат автотега в `deploy-uat.yml` всех трёх
      репозиториев стал `v$(date +%Y.%-m.%-d)-g${GITHUB_SHA::7}` — без ведущих
      нулей и с буквенным префиксом у хеша. Префикс не косметика: sha из одних
      цифр с ведущим нулём был бы невалидным числовым идентификатором предрелиза
      и ломал бы сборку так же, раз в тысячу выкатов. Проверено официальным
      регулярным выражением semver: старый формат — нет, новый — да, включая
      крайний случай.

**Причина оказалась глубже формата — 05.08.2026.** Сборки от тега не было
      ни одной: тег пушится внутри workflow штатным `GITHUB_TOKEN`, а GitHub
      намеренно не запускает workflow на события, созданные этим токеном. То есть
      автотег **никогда** не собирал образ, при любом формате. `v0.9.0` в реестре
      есть потому, что его пушил человек руками.

      **Починено вызовом, а не токеном.** `relay.yml` принимает `workflow_call` с
      параметром `release_tag` и помечает им образ (`type=raw`, потому что
      `type=semver` читает ref, а при вызове ref — ветка, не тег);
      `deploy-uat.yml` вызывает его сразу после создания тега. Сборка перестала
      зависеть от того, чьим токеном создан тег. Долгоживущий PAT в секретах не
      заводили намеренно: он протухает молча.

      Остаётся хвост: **уже проставленные теги не перевыпускаются**, поэтому
      `v2026.08.05-*` так и останутся без образов, а staging стоит на
      `sha-1480743`. Следующее слияние `dev → main` даст тег, под которым образ
      соберётся, и тогда staging и prod можно закреплять за релизом, как задумано.

## K. Решения по чату — 07.08.2026

Приняты в один день и связаны между собой: каждое следующее стало возможным
из-за предыдущего. Записано здесь, чтобы через полгода не выяснять заново.

1. **Чат не модерируется.** Спека это делала, а политика конфиденциальности,
   реестр по ст. 30 и описание экрана чата — отрицали. Правы были документы:
   лента публична, а разговор двоих по взаимному согласию не публикация.
   Спека была отставшей и приведена к остальным.
2. **Сквозное шифрование.** Стало возможно ровно потому, что чат перестали
   читать: видеть текст и не видеть его одновременно нельзя. Ключ **на
   разговор**, из эфемерных пар обоих, умирает вместе с чатом.
3. **Отпечаток браузера убран целиком.** Он собирался из соединения, а
   соединение теперь приходит от сети доставки: два входа из четырёх стали
   одинаковыми у всех. И он бил не по тому — по соседу за одним роутером,
   — обходясь очисткой данных за десять секунд.
4. **Сессии вместо отпечатка.** Одна личность — несколько устройств,
   подключение по QR, ключ идёт во фрагменте ссылки мимо сервера. Отзыв
   настоящий: отозванная сессия не получает ключей будущих чатов.
5. **Персональное скрытие фразы.** Правила сообщества обещали это публично,
   в проекте механизма не было. Добавлен в §8.9.
6. **Откровенный контент запрещён полностью** (решение параллельной сессии).
   Экран 11 и согласие с почтой отменены, спецификация экрана удалена.

**Что из этого следует по срокам.** Политика конфиденциальности стала новой
редакцией `2026-08-07`: убраны отпечаток, согласие на расширенный отпечаток и
согласие на откровенное — три позиции, все в пользу человека. Баннер принятия
сработает у всех, кто уже принимал предыдущую.

**Что осталось незакрытым:** сам чат не построен (ни одной таблицы из §8),
восемь пунктов в §8.14 требуют замеров, заголовков безопасности на витринах
нет ни одного, план ротации секретов — см. `secret-rotation_RU.md`.

## L. Резервные копии и боевые данные — 07.08.2026

Открылось на учении по восстановлению 07.08.2026: боевой дамп развернулся в
одноразовый `postgres:16-alpine` **без единой ошибки**, все девять таблиц и пять
миграций на месте. Само копирование работает — ночной таймер, две недели
хранения, чистка после успешной загрузки. Но учение показало две вещи, которых
до него никто не видел.

### L1. Лист ожидания в резервную копию не входит — открыто

Таблицы `waitlist` не существует: заявки пишутся **в хранилище Bunny**
(`relay/node/src/routes/waitlist.ts`, зона из `BUNNY_STORAGE_ZONE`), а
`backup-postgres.sh` копирует только Postgres. То есть копия покрывает
управляющее состояние — ключи, бренды, квоты, уведомления по ст. 16 — и **не
покрывает единственные накопленные пользовательские данные**, какие у нас
сегодня есть.

Что с этим делать:

- копировать и хранилище тоже, отдельным шагом того же ночного таймера;
- либо перенести лист ожидания в Postgres, и тогда он попадёт под уже
  работающую копию;
- в любом случае — **проверить восстановлением**, а не считать сделанным.

Рядом лежит вторая половина той же проблемы: копии складываются в зону
`sosed-waitlist-dev` вместе с 269 файлами витрины. Pull-зоны к ней сейчас не
привязано (проверено 07.08.2026), но защита держится на том, что никто её не
привяжет. Отдельная зона `relay-backups` — в том же пункте.

### L2. Тестовые уведомления в боевом реестре — закрыто 07.08.2026

В `dsa_notices` на проде лежали **пять записей**, созданных 05.08.2026 при
проверке выноса за CDN, замка Caddy и Shield: «Lock check: a real report still
goes through the front door» и подобные. Ни одна не решена, у всех
`target_kind = other`. Удалены по конкретным идентификаторам в одной
транзакции; реестр пуст.

**Правило на будущее:** проверять сквозные пути на **staging**, а не на проде.
Реестр по ст. 16 — документ, который однажды предъявят надзорному органу, и
тестовые записи в нём придётся объяснять. Если проверка на проде всё же
неизбежна, запись удаляется в тот же час, а не через двое суток.

**Остаточный след:** ночные копии за 5–7 августа эти пять записей содержат и
будут содержать до истечения двух недель, то есть примерно до 21.08.2026. Это
нормально и соответствует заявленному сроку хранения.

## G. Осознанно отложенное

Из `review-checklist_RU.md`. Не забыто, но и не в работе.

- [x] **G1. Юнит-тесты панели** — 29.07.2026. Появился vitest (`npm test`,
      `scripts/run-panel-unit-tests.sh`, всё в Docker), 10 проверок на чистую
      логику: карта прав страниц, отказ по умолчанию для неописанных действий,
      заголовок сессии у фетч-клиента, отображение имени ресурса в маршрут.

      **Дыра оказалась крупнее пункта: панель не проверялась в CI вообще.**
      Типы и сборка запускались впервые внутри `_deploy.yml`, то есть во время
      выката. Заведён `.github/workflows/panel.yml`: `npm ci`, юниты, `tsc
      --noEmit` — на push и PR по путям `panel/**` и по правам релея.

      **И нашлось расхождение, ради которого всё это и нужно:** сегодня в релее
      появилось право `waitlist.write`, а в списке панели его не было — страница
      выпуска секретных ключей не смогла бы предложить единственный скоуп, ради
      которого право заводилось. Добавлено, и написан тест, который читает файл
      релея и сверяет оба списка строка в строку.
- [x] **G2. Шрифты панели — закрыто 17.08.2026, но не той работой, что была
      записана.** Формулировка «свести к двум variable-файлам» опиралась на
      неверную посылку: файлы **уже вариативные**, все 15. Они нарезаны по
      юникод-подмножествам, и сливать их нельзя — тогда латинская страница стала
      бы возить кириллицу. Число «27 файлов» тоже было завышено: 27 получается,
      только если считать вместе с каталогом сборки.

      **Настоящий дефект нашёлся при измерении.** В `fonts.css` Inter был
      объявлен только на 400 и 500, оба объявления — на один и тот же файл.
      Вёрстка же просит 600 и 700, и браузер, не найдя начертания, **подделывал
      жирность**: размазывал пятисотку. Измерено в браузере: 500, 600, 700 и 900
      давали одну ширину, 627px, — то есть полужирный и жирный в панели были
      неотличимы друг от друга. У Unbounded тем же образом 600 подменялся
      семисоткой.

      Чтением это поймать нельзя: объявления выглядели осмысленными.

      **Сделано:**
      - `panel/src/fonts.css` — одно объявление на семейство и подмножество,
        29 → 12, вес диапазоном. Диапазоны измерены в браузере (Inter различим
        на каждой ступени от 100, у Unbounded 100 и 200 совпадают) и **сошлись с
        тем, что объявляет сам Google** — независимое подтверждение.
      - файл больше не рукописный: пересоздаётся `scripts/fetch-fonts.sh`, и
        команда пересоздания записана в его шапке. Прежде точный адрес запроса не
        хранился нигде, поэтому воспроизвести файл было нельзя.
      - `scripts/fetch-fonts.sh` — отказ, если Google вернул несколько объявлений
        на один файл (значит в адресе перечислены отдельные веса вариативного
        шрифта). Проверка стоит **до** скачивания. Отказ проверен: на адресе с
        `wght@400;500` скрипт останавливается, не скачав ни одного файла и не
        записав CSS.
      - `panel/tests/check-font-weights.mjs` — сторож: рисует в браузере веса,
        которые запрашивает вёрстка, и падает, если два из них совпали по ширине.
        Список весов читается из `App.css`, а не задан в скрипте. Проверен
        падением на прежнем файле: 6 совпадений, код 1, замеры и указания на
        дефект в одном потоке, чтобы не перемешивались в журнале CI.
      - подключён в `panel.yml`: ставится только chromium и только на этом шаге,
        остальная работа там по-прежнему без браузера. Локальная дверь к тому же
        файлу — `scripts/check-panel-font-weights.sh` (Docker, на хост ничего).
        Путь CI отрепетирован в контейнере: `npm install` в `panel/tests`, запуск
        оттуда без заданного пути к панели — зелено. Строка установки браузера
        локально не воспроизводилась: её проверит первый прогон CI.

      Сборка панели проходит, в собранном CSS 12 объявлений: 7 × `100 900`
      (Inter) и 5 × `200 900` (Unbounded). **На средах не выкачено.**

      Витрины проверены заодно: там 39 объявлений на 15 файлов, но объявлены
      ровно те веса, что запрашивает вёрстка (Golos 400/500/600, Unbounded
      600/800/900) — поддельной жирности нет, только избыточность. Не трогали.
- [x] **G3. Мокапы убраны из сборки — закрыто 18.08.2026. Вопрос был не тот.**
      Пункт спрашивал, переводить ли пять декоративных SVG в `panel/public`. При
      разборе выяснилось, что переводить в них нечего, потому что публиковать их
      не следовало вовсе.

      **Что это на самом деле.** Не декорация интерфейса, а рабочие дизайн-документы:
      `app-screens.svg` — **одиннадцать экранов непостроенного чата**, нарисованных
      по `docs/chat_RU.md`, со ссылками на §8.2 и §8.3; `kit-full.svg` — инвентарь
      компонентов с отметками `● built` / `○ drawn`; `reference-behaviour.svg` —
      разбор чужого кода. На них **не ссылается ни один файл кода**.

      **И все они лежали на боевой панели.** Vite копирует `public/` в `dist/`
      целиком, поэтому пять SVG и обёртка `app-screens.html` отдавались с
      `https://xor.panov.id/<имя>.svg` — проверено `curl`, все `200`, 236 КБ.
      Ни ключей, ни секретов там нет, но публикация проекта неготового продукта —
      не решение, которое кто-то принимал.

      **Сделано:**
      - файлы перенесены `git mv` в `panel/design/` — вне `public/`, значит вне
        сборки. Заодно туда уехали три файла шрифтов (Golos Text, JetBrains Mono),
        которые нужны только `app-screens.svg` и которые `fonts.css` не объявляет;
      - `scripts/render-design-mockup.sh` + `scripts/render-design-mockup.mjs` —
        рендер сохранён: браузер в Docker, каталог `panel/design` отдаётся с корня,
        `/fonts/` берётся сначала из его собственных шрифтов, потом из панельных.
        Виден отказ: если шрифт не нашёлся, скрипт помечает мокап `✗` и падает;
      - подписи внутри мокапов и ссылки в `design-system_{RU,EN}.md` переписаны на
        новый путь.

      **Проверено:** все пять рисуются в своей истинной геометрии, шрифты
      резолвятся (снимок `button-states` просмотрен целиком — Unbounded в
      заголовках, Inter в тексте, моноширинный в примечаниях). Сборка панели
      пересобрана с нуля: SVG в `dist` **ни одного**, шрифтов 12 вместо 15,
      вес 1000 КБ.

      **Не сделано и это надо знать:** на проде мокапы **всё ещё лежат** — уедут
      со следующим выкатом панели, вместе с очисткой кэша зоны.
- [x] **G4. Защита публичного `/waitlist` — закрыто 17.08.2026.** Цепочка
      пройдена целиком: лимит по адресу в узле, honeypot, замок `X-Origin-Token`
      в Caddy, переключение имени на зону и, последним, Shield в режиме
      наблюдения. Проверено сегодня со стороны сети: `api.relay.panov.id` отдаёт
      `server: BunnyCDN-CY1-1191` и `via: 1.1 Caddy`, то есть боевой трафик
      действительно идёт через край, а прод-витрина смотрит именно на это имя.

      **Первый абзац ниже с тех пор устарел** — он утверждал, что Shield перед
      узлом не стоит и CDN здесь не поможет. Теперь стоит. Остальное оставлено
      как есть: это история решения, и в ней две ошибки, которые полезно помнить.

      Что решать дальше — в **G11**, там же срок.

      Проверено 05.08.2026:
      у `/waitlist` **нет никакой защиты**, а прежняя формулировка «либо квоты,
      либо Bunny Shield перед узлом» неверна — Shield перед узлом не стоит.
      Все девять зон Bunny раздают статику из хранилища (`origin` пуст), лендинг
      бьёт `fetch(API_URL + "/waitlist")` прямо в узел за Caddy на Hetzner, и в
      Caddy рейтлимита тоже нет. CDN здесь не поможет ни на каком тарифе.

      Сегодняшняя суточная квота `lib/quota.ts` считается **по ключу**, а ключ у
      всех посетителей общий. Это не защита, а рычаг отказа в обслуживании: бот,
      выжегший квоту за минуту, закрывает запись всем на сутки.

      **Сделано 05.08.2026:**
      - `lib/rate_limit.ts` — окно по адресу в памяти узла, отдельно от квоты
        ключа. `/waitlist` — 5 в час, `/report` — 10 в час (выше: отказ в приёме
        уведомления о незаконном контенте это отказ в исполнении обязанности).
        Отказ — `429` с `retry-after`, 4 юнит-теста в `test/rate_limit.test.ts`.
      - `lib/client_ip.ts` — адрес берётся из `X-Real-IP` **только** при валидном
        `X-Origin-Token`, иначе из самого соединения. Без этого заголовок
        подделывается и лимит обходится подстановкой чужого адреса.
      - honeypot в обеих формах лендингов и в форме уведомления; заполненное поле
        выглядит успехом и не отправляет ничего. Проверено в браузере: запросов
        на `/waitlist` — ноль.
      - `ORIGIN_TOKEN` в конфиге узла, пустой по умолчанию: узел без него никогда
        не верит заголовку, что и нужно на dev.

      **Сделано 05.08.2026 на боевом узле:**
      - [x] `ORIGIN_TOKEN` выдан p1-prod (`wizard configure --confirm-prod`),
        `/health` после перезапуска отвечает. Секрет в `wizard/secrets.env`,
        вне репозитория; в `wizard.py` он попадает в env узла.
      - [x] edge-правило в зоне `xorad-api-prod` добавляет `X-Origin-Token` к
        каждому проксируемому запросу (`deploy/bunny-api-origin-token.sh`,
        перечитано с сервера). Условие «любой адрес» — Bunny требует хотя бы одно.

      **Порядок был записан неверно, и это важно.** Проверку токена в Caddy
      нельзя включать до переключения DNS: сейчас трафик идёт к узлу напрямую и
      токена не несёт — принуждение уронило бы боевой API. Сначала возможность,
      потом переключение, и только потом принуждение.

**Проверка 05.08.2026 показала, что заголовок с адресом не доходил.** Токен
      доезжает (проверено на выброшенной зоне с эхо-источником), а `X-Real-IP` —
      нет: **Bunny считает это имя своим служебным и правило по нему молча
      игнорирует**. Подстановка `%{User.IP}` при этом работает — тот же адрес под
      именем `X-Client-IP` доходит всегда. В боевую зону добавлено правило
      `X-Client-IP`, узел научен читать его первым.

      Обнаружено экспериментом, а не чтением: лимит заполнялся прямым путём до
      `429`, а тот же запрос через CDN проходил — значит вёдра разные и узел
      видит адрес edge, а не клиента. **Именно поэтому проверку токена в Caddy
      нельзя было включать вслепую.**

**И тот же эксперимент вскрыл ошибку в самом лимите — 05.08.2026.** Узел
      стоит за Caddy, поэтому «адрес соединения» для него всегда один и тот же:
      адрес прокси. Запасной путь считал по нему, то есть **все посетители
      попадали в одно ведро** — двадцать запросов в час на весь сайт, и один
      скрипт закрывал запись всем. Ровно то, ради предотвращения чего лимит и
      писался.

      Исправлено: без токена адрес берётся из `X-Forwarded-For`, который
      проставляет сам Caddy. Подделать его снаружи нельзя — порт узла не
      опубликован, и достучаться до него может только прокси рядом.

      **Замок включён 05.08.2026.** Caddy на боевом узле пропускает `api.relay…`
      только с валидным `X-Origin-Token`, который добавляет edge-правило; без него
      — `403`, и запрос не доходит до приложения. Проверено: через CDN `200`, тем
      же именем в обход — `403`, боевой `POST /report` — `202`.

      Прямой вход `p1-prod.relay.panov.id` **оставлен открытым намеренно**: это
      способ посмотреть на узел, когда подозрение падает на CDN, а лимит по адресу
      там работает так же. Цена — обход CDN возможен по другому имени, но
      выигрывает от этого только тот, кому нужна не мощность, а обход Shield.

      **Осталось, в этом порядке:**
      - [x] **выкатить на prod образ с этим кодом — сделано 09.08.2026.** Узел
        накатан на релиз `v2026.8.9-ge4ce220`, тот же образ, что перед этим обкатан
        на staging. `/health` отвечает по обоим именам, лимит проверен на живом
        узле: `429` с двадцатого запроса.

        **Формулировка выше была неверна, и это важнее самого выката.** Она
        утверждала, что лимита на узле нет. Измерение показало обратное: защита
        работала и до выката. Ошибка была в методе — порог взяли «5 в час» из
        старого текста, тогда как в коде `max: 20` плюс суточные 60, и проба из
        шести запросов физически не могла до него дотянуться.

        Выкат всё равно был нужен, но по другой причине: `environments.toml`
        обещал `v2026.8.5-gfd6587a`, а что крутилось на боксе, никто не знал —
        визард по этому тегу не запускали. Теперь пин и бокс совпадают.

        Попутно выяснилось: публикация GitHub Release — обязательное условие
        визарда для публичной среды и единственное одобрение боевого выката. И:
        пересоздание контейнера **обнуляет счётчики лимита**, потому что окно
        живёт в памяти узла;
      - [x] **перевесить `api.relay.panov.id` на зону — оказалось уже сделано,
        проверено 09.08.2026.** Имя резолвится в тот же адрес, что и
        `xorad-api-prod.b-cdn.net`, то есть трафик идёт через CDN и несёт
        заголовок. Пункт стоял открытым по инерции.

        Замок проверен всеми тремя путями сразу: через CDN — `200`; тем же именем
        принудительно мимо CDN (`--resolve` на адрес бокса) — **`403`**; прямое имя
        `p1-prod.relay.panov.id`, оставленное открытым намеренно, — `200`. Это и
        есть доказательство, что защита отбивает обход, а не просто присутствует;
      - [x] проверка `X-Origin-Token` в Caddy — включена 05.08.2026;
      - [x] **файрвол по edge-адресам Bunny — решено НЕ делать, 05.08.2026.**
        В списке 582 адреса IPv4 и 320 IPv6: почти тысяча правил, которым нужен
        `ipset` и демон обновления. Замок в Caddy уже отвергает всё, что пришло
        мимо CDN, **до приложения**; файрвол добавляет только обрыв на уровне TCP,
        что от настоящего флуда не спасает — канал узла зальют раньше, ради чего
        перед ним и стоит Bunny. Взамен получаем риск: список у Bunny меняется, и
        устаревший означает отказ боевого API без единой ошибки в коде.
        Пересматривать, если появится флуд, доходящий до узла;
      - [x] **включить Shield Basic — сделано 16.08.2026.** Shield привязан к
        `xorad-api-prod` и заодно к трём витринным зонам (`neighbro-prod`,
        `sosed-prod`, `panel-prod`), везде в режиме наблюдения:
        `wafExecutionMode = 0`, то есть правила только пишут в журнал и ничего не
        режут. Learning Mode продлён до 21.08.2026. Состояние читается скриптом
        `deploy/shield_state.py` — он и вскрыл, что ответ Bunny `202` с текстом,
        похожим на отказ, на самом деле означал применённое изменение.

        Записано было так:
      - [x] **было: подтверждено 09.08.2026, что он ВЫКЛЮЧЕН.**
        В конфигурации зоны `xorad-api-prod` (id 6278420) `ShieldDDosProtectionEnabled
        = False`. Это последний незакрытый шаг цепочки: всё остальное — образ,
        переключение на зону, замок в Caddy — сделано и проверено.

        Включать не стал: это изменение тарифного плана и поведения боевой зоны,
        такое не делается попутно. Делается в кабинете, вкладка Bunny Shield у зоны
        `xorad-api-prod`. Профиль брать общий, не «WordPress»: за зоной API с
        JSON-телом, и профиль под сайт режет `POST` как подозрительный. Из
        Learning Mode не выводить, пока через зону не пройдёт настоящий
        `POST /report`.

      Внешней капчи не будет — решение записано в `relay/HARDENING_RU.md`.
      Цена ошибки конкретная: `/waitlist` шлёт письмо через Resend, то есть
      злоупотребление превращает нас в источник спама и жжёт репутацию домена.

      **Состояние на 05.08.2026 — история; всё перечисленное ниже с тех пор
      сделано, включая переключение DNS.** Pull-зона `xorad-api-prod` создана и проверена
      (`deploy/bunny-api-zone.sh`, `/health` через CDN отвечает 200), **DNS не
      переключён намеренно**. Порядок дальше строгий: сначала замок на origin
      (`X-Origin-Token` в Caddy + файрвол по edge-адресам Bunny), затем доверие
      `X-Real-IP` только при валидном токене, затем лимит по IP и honeypot, и
      только потом перевесить `api.relay.panov.id` на зону и включить Shield
      Basic. Переключить раньше — значит оставить узел открытым в обход CDN.
- [ ] **G11. Решить режим Shield до 21.08.2026.** Четыре боевые зоны
      (`xorad-api-prod`, `neighbro-prod`, `sosed-prod`, `panel-prod`) стоят в
      наблюдении, Learning Mode кончается 21.08.2026. Решение принимается по
      журналу WAF, а он читается только в кабинете Bunny — это шаг человека.
      Смотреть на две вещи: попал ли под правила настоящий `POST /report`
      (JSON-тело у профиля под сайт выглядит подозрительно) и попал ли вход в
      панель. Пока журнал не прочитан, **включать блокировку нельзя** — иначе
      первым, что отвалится, будет приём уведомлений о незаконном контенте.
      Состояние проверяется `deploy/shield_state.py`; после 21.08.2026 он покраснеет.
- [x] **G5. `manifest lang` — won't-fix, закрыто 10.08.2026.** Решение было
      принято давно, галочка осталась открытой и три месяца числилась работой.
      Метаданные установленного PWA несут брендовое имя, язык у них не меняется.
- [ ] **G6. Предел длины сообщения — проверка на узле.** Решено 07.08.2026 и
      записано в спеку чата (§4, §8.6): `max_message_length` отдаётся при
      открытии чата, по умолчанию **256 символов**, и проверяется **на узле** —
      сообщение длиннее отвергается с `error`. Лента остаётся на **128**, это
      другой предел и сводить их не нужно.
- [x] **G7. Политики конфиденциальности витрин — закрыто 10.08.2026.** Заведено
      07.08.2026 как два пробела; на проверке оказалось, что первого уже нет, а
      второй закрыт решением, а не работой.

      **Про «два источника правды разошлись» запись устарела.** `legal/*.md` в
      обеих витринах — уже не копии, а указатели: «действующий текст живёт в
      `landing/legal/`, двух источников у юридического текста быть не должно».
      Источником назначен `landing/legal/`, и там же лежат настоящие тексты
      (privacy 10 КБ, terms 15 КБ). Пункт описывал состояние, которого не было
      на момент, когда его перечитали.

      **Про «опубликована только английская версия» — решение: так и остаётся.**
      Витрина говорит на шести языках, правила сообщества переведены больше чем на
      десять, поэтому вопрос был задан прямо. Ответ: переведённый юридический
      текст это вторая редакция, и в день, когда правят одну из них, они начинают
      говорить разное — молча и там, где ошибка дорога. Кому нужно на своём языке,
      тот переведёт. Решение записано в самих указателях, чтобы через полгода его
      не приняли за забытый перевод.
- [x] **G8. Пуши отменены полностью — 07.08.2026.** Решение: уведомления о новых
      сообщениях и мэтчах есть, но **пушей нет нигде** — ни Web Push в браузере,
      ни системных уведомлений в терминале, ни `BEL`.

      **Причина — метаданные.** Пуш невозможен без посредника: service worker плюс
      чужая служба доставки (Google, Mozilla, Apple), в терминале — системная шина.
      Даже при непрозрачной нагрузке посреднику достаётся устойчивый идентификатор
      подписки и **ритм** — когда именно с человеком заговорили и как часто.
      Продукт, в котором переписку не хранит даже собственный сервер, не может
      раздавать её метаданные третьей стороне ради удобства.

      **Цена названа и принята:** позвать человека, которого нет в приложении,
      нечем. Сгоревший мэтч он не увидит. Система работает для того, кто заходит
      сам и регулярно — так её и надо описывать.

      **Обработки не было ни дня:** `vapidPublicKey` был пуст на обеих витринах,
      кнопка скрыта, эндпоинта приёма подписки на узле не появилось, таблицы в
      схеме нет — ни одного endpoint мы не получили.

      **Сделано:** спека чата §8.12 переписана (плюс §8.14 и §12); `pwa-push_*`
      разделён — PWA-оболочка остаётся, Web Push отменён; из витрин удалён код
      (`index.html`, оба `sw.js`, оба `config.js`) и подстановка `VAPID_PUBLIC_KEY`
      из обоих `deploy-landing.sh`; из реестра по ст. 30 убран процесс и
      субпроцессор «Push-служба браузера», процессы перенумерованы 1–12; из политик
      обеих витрин сняты три обещания; вычищены роадмап, `api-platform`,
      `ARCHITECTURE`, `PROJECT_OVERVIEW`, `00-mechanics`, `PENDING_FROM_NEIGHBRO` и
      незакрытые пункты `backend-*`.

      **Не тронуто намеренно:** `review-checklist_*` и `relay/MIGRATION_PLAN_*` —
      они фиксируют прошлое (проведённый аудит, состояние на момент миграции), и
      переписывать их значит терять след.

      **Зов на запуске** идёт почтой вейтлиста через Resend: она уже работает,
      описана в реестре, и человек оставляет её осознанно.

      **В коде этого нет вообще.** Сегодня в узле нет ни проверки длины, ни
      самого параметра: `lib/quota.ts` считает суточную квоту по ключу,
      `lib/rate_limit.ts` — окно по адресу, длину не смотрит никто. Пока роутов
      чата нет, пункт не горит, но завести его надо вместе с ними, а не после:
      клиент у нас открытый — образ `depth` пересобирается кем угодно, веб-скрипт
      правится в отладчике, — и проверка, живущая только на клиенте, защитой не
      является.

      За пределом стоит транспорт: 256 символов UTF-8 → шифротекст ~1.4 КБ с
      nonce, тегом и base64, и это должно с запасом влезать в 8 КБ payload
      `NOTIFY` (§8.1, §8.13). Поднимать значение можно, но не вслепую.
- [ ] **G9. Ключ для нативного клиента — два подпункта из пяти сделаны
      12.08.2026, три ждут своего повода.**

      **Сделано:**
      - [x] **`client_type` (`browser` | `native`)** — миграция `008`, поле в
        интерфейсе ключа, в выпуске, в списке, в форме панели и в утилите
        (`--native`). Решение записано в данных: раньше «нативность» выводилась
        из пустого списка Origin, а браузерный ключ, сохранённый без источников,
        выглядел ровно так же. Нативный ключ со списком источников теперь
        отвергается — и в API, и в утилите, — потому что это противоречие, а не
        мелочь;
      - [x] **суточная квота по ключу к `native` не применяется и не тратится.**
        Ключ общий у всех копий клиента: счётчик на ключ означал бы, что один
        скрипт закрывает клиент всем остальным за минуту.

      **Проверено на настоящей базе** (`test/database.test.ts`): нативный ключ
      выпускается без источников и отвергается со списком; браузерный ключ с
      лимитом 1 упирается в 429 со второго запроса, нативный с тем же лимитом
      проходит четыре подряд. Сломал `isNative` — тест покраснел, восстановил.

      **Ждёт своего повода, и это не отговорка:**
      - [ ] **пределы по личности** — в коде нет ни `identities`, ни сессий
        (J19). Предел «на личность» сегодня не к чему привязать; он появится
        вместе с личностью и не раньше;
      - [ ] **бренд `depth` и первый `native`-ключ** — образа `depth` ещё нет,
        публиковать нечего. Ключ выпускается одной командой в день публикации;
      - [ ] **ротация перекрытием** — механизм уже есть: у бренда может жить
        сколько угодно ключей одновременно, и отзыв стамповый, а не удаляющий.
        Недостаёт не кода, а объявленного окна и процедуры — их писать в день,
        когда появится первый образ, иначе окно будет выдумано из воздуха.

      **Чего делать не надо:** колонки `brand` в `identities` и `feed_messages`.
      Лента общая для всех лиц — §8 спеки чата, второй принцип.
- [x] **G10. Условия для авторов клиентов — написаны 10.08.2026.** Лежат в
      `CLIENT_TERMS.md` в корне репозитория, рядом с README: там их найдёт тот, кто
      пришёл за образом.

      Что вошло: что узел гарантирует (права, пределы, модерация до публикации,
      возрастные полосы) и чего не гарантирует (счётчики, показ условий, стирание
      локальной истории — это на авторе клиента); обязанности автора — показать
      условия и путь по ст. 16, не собирать данные наших людей, **не держать чужие
      ключи личности централизованно**, не выдавать свой клиент за наш; ключ
      клиента как единственный механизм принуждения — мы его выдаём и можем
      отозвать.

      **DPA не готовим и хостинг чужих сетей не предлагаем** — записано там же с
      причиной: услуги нет, а хостинг чужого мира вернул бы границу по бренду в
      ленту и сделал бы нас процессором чужого оператора.

      **Язык — английский**, как и у остальных публикуемых юридических текстов
      (решение в G7).

      **Зависимость:** документ описывает ключ клиента, которого в коде узла нет —
      см. G9. До его появления отзывать нечего.

      Не сделано: ссылки на `CLIENT_TERMS.md` из README обеих витрин и из описания
      образа в реестре — их вешать в день публикации образа.
## M. Найденное в августе — не отложено, а в работе

Пункты J13–J21 и D8 физически лежали в разделе «G. Осознанно отложенное» и из-за
этого числились сознательным откладыванием. Раздел заведён 10.08.2026, чтобы
свежие находки не выглядели решением их не делать.

**J8 отсутствует.** Номер пропущен, следа в документе нет; J2 снята осознанно и
объяснена внутри J3, а про J8 не сказано нигде. Не восстанавливаю — записываю,
чтобы пропуск не приняли за потерю.

- [x] **M3. Выкат никогда ничего не удалял, а витрины отдавали наш инструментарий —
      закрыто 18.08.2026.** Две тихие дыры, найденные из-за третьей задачи: мокапы
      убрали из сборки, выкат прошёл зелёным, а мокапы остались на сайте.

      **Дыра первая: заливка без удаления.** Скрипты выката загружали каждый файл
      и не удаляли ни одного, поэтому опубликованное однажды оставалось
      опубликованным навсегда. В зоне `panel-dev` лежало 44 файла при 16 в
      сборке: шестнадцать старых бандлов, шесть мокапов и три шрифта, **7.86 МБ**.
      Очистка кэша тут ни при чём — объекты в хранилище, а не в кэше. Следствие
      шире мокапов: файл, убранный по любой причине, включая юридическую,
      продолжал бы отдаваться.

      **Дыра вторая: витрины публиковали собственные проверки.** На боевых
      `neighbro.place` и `sosed.place` отдавались `200`
      `check-contrast.mjs`, `check-legal-consistency.mjs`, `find-dead-keys.mjs`,
      `verify-seo.mjs` и `test-security-headers.sh`. Не секреты, но публиковать
      список ожидаемых заголовков и внутренние правила никто не решал. Причина —
      исключения жили **в двух местах**, и та половина, что была списком из пяти
      имён, отстала, пока каталог рос до десяти файлов.

      **Сделано:**
      - `deploy/prune-storage-zone.py` — уборка с тремя защитами: без `--apply`
        ничего не происходит; план больше половины зоны останавливается; и **ничего
        из того, на что ссылается `index.html` самой зоны, не удаляется никогда**;
      - встроена в выкат панели и обеих витрин — сразу после заливки, в
        единственном месте, где сравниваемый каталог и есть залитый, и только если
        залились все файлы;
      - у витрин исключения сведены в один блок, а список служебных файлов заменён
        правилом «никаких `.mjs` и `.sh`» — правило не устаревает при добавлении
        файла, как устаревает список;
      - цикл заливки витрин научился замечать коды ответа: раньше зона, отвергшая
        каждый байт, давала зелёный выкат. У панели это было починено давно, у
        витрин — нет.

      **Две собственные ошибки, обе стоили времени и обе записаны в код:**

      1. **Сломал панель на dev.** Запустил уборку, сравнивая зону с локальной
         сборкой, тогда как в зоне лежала сборка из CI — у них разные хэши в
         именах, и живой бандл был удалён как лишний. Панель стояла пустой до
         перезапуска выката. Отсюда третья защита; проверена повторением ровно той
         команды.
      2. **Уронил выкат uat.** Сделал отказ уборки фатальным, а она идёт **после**
         заливки — предохранитель честно отказался чистить 60% зоны, и выкат
         остановился, не пересчитав заголовки и не очистив кэш. Самый осторожный
         исход превратился в самый плохой. Теперь отказ печатается предупреждением,
         выкат продолжается: лишние файлы — это неопрятно, а политика, не
         совпадающая с бандлом, — пустой экран.

      **Проверено на всех трёх средах.** Зоны панели: 44 → 16, 40 → 16, 38 → 16.
      Витрины: служебные файлы `404` везде, страницы `200` и отрисовываются.
      Боевая панель открыта браузером — ноль ошибок, ноль нарушений политики,
      `Inter 500/600/700 → 627/633/640px`. Перед чисткой боевой зоны сборка была
      **воспроизведена локально до имени бандла** (`index-DgdjqIRF.js`), чтобы
      доказать её идентичность выкаченной.

      Счётчик `FilesStored` в API Bunny обновляется с задержкой и показывал
      прежние числа, когда файлов уже не было; считать надо обходом содержимого.

- [x] **J13. Один коммит — два разных образа. Найдено и исправлено 07.08.2026.**
      Вскрылось при проверке J11: под тегом `v2026.8.7-g3fbd1e1` и под
      `sha-3fbd1e1` у `relay-caddy` оказались **разные дайджесты**
      (`2af974ef…` и `599c5d9d…`). У `relay-node` совпали случайно — его сборка
      оказалась воспроизводимой, поэтому дефект и не замечали раньше.

      **Причина — лишний триггер.** `relay.yml` ловил push в любую ветку, включая
      `main`, а `deploy-uat.yml` дополнительно вызывал его же через
      `workflow_call`, чтобы навесить релизный тег. На пуш в `main` запускались
      два независимых прогона, каждый со своим `type=sha`: второй перезаписывал
      тег `sha-…`, который написал первый, а релизный тег указывал на вторую
      сборку — не на ту, что прошла тесты в первом прогоне.

      Ломались сразу два записанных правила: **неизменяемые теги** (sha
      перезаписан) и **собрать один раз, промоутить тот же образ** (утверждение
      A2 про «байт-в-байт проверенный билд» переставало быть гарантией).

      **Исправлено** в `relay.yml`: `branches: ["**"]` → `branches-ignore: [main]`.
      На `main` сборкой владеет релизный путь, и прогон остаётся один — sha и
      релизный тег ложатся на один дайджест. `deploy-uat.yml` не менялся.

      **Заодно сужен фильтр путей:** `!relay/**.md`. Одна правка
      `relay/ARCHITECTURE_RU.md` запускала две мультиархитектурные сборки с
      эмуляцией arm64 — именно так этот прогон сегодня и появился. Отрицание в
      `paths` на живом прогоне **не проверено**: убедиться можно следующей
      правкой документа внутри `relay/` — если сборка не стартует, работает.

      **Остаётся нерешённым:** ручной релизный тег `vX.Y.Z`, поставленный
      человеком на уже собранный коммит, по-прежнему запускает пересборку через
      `tags: ["v*"]`. Тот же дефект, только реже. Лечится промоушеном по
      дайджесту (`docker buildx imagetools create --tag`) либо принимается
      осознанно — не решено.

      **Проверка 07.08.2026 показала, что правка выше неполная.** На коммите
      `e3de4ec`: `relay-node` — релизный `8a561fe8…` против sha `472661b7…`,
      **разные**; `relay-caddy` совпал. Наоборот относительно первого случая, где
      разошёлся caddy, — то есть совпадения **случайны**, сборки нерепродуцируемы,
      и правило держалось на жребии.

      Причина осталась: коммит в штатном потоке попадает в **три** ссылки — `dayN`,
      `dev`, `main`, — и триггер по ветке запускал сборку на каждой. Убрав `main`,
      я убрал одну из трёх, а не восстановил правило. Формулировка коммита
      `e3de4ec` («Build the commit once») была сильнее сделанного.

      **Полное исправление — внутри `relay.yml`, а не в триггерах**, потому что
      только там оно покрывает все пути разом, включая ручной тег `vX.Y.Z`:

      - `concurrency: relay-build-<sha>-<image>` — прогоны одного коммита встают
        в очередь, а не гонятся. Без этого два одновременных прогона оба увидели
        бы «образа нет» и оба собрали;
      - шаг **«already built?»** — если `sha-<short>` уже в реестре, коммит
        собирали раньше;
      - если собирали — вместо сборки на существующий манифест вешаются **все**
        теги, которые дал бы `metadata-action` (релизный, ветка, sha), через
        `docker buildx imagetools create`;
      - подпись cosign и скан Trivy выполняются только при настоящей сборке: тому,
        кто лишь навесил теги, подписывать нечего — дайджест подписан при первой.

      Это закрывает и ручной релизный тег, который выше записан как нерешённый.

      **Третья итерация, 07.08.2026: очередь сама создала риск.** Первый же прогон
      с `concurrency` показал `relay · day25` в состоянии **cancelled**. Причина в
      поведении, которого я не учёл: `cancel-in-progress: false` защищает
      **выполняющийся** прогон, но не ждущий. GitHub держит в группе ровно один
      ожидающий и отменяет его, когда приходит третий.

      Здесь это было безобидно — образ собирал `dev`, а `day25` добавил бы только
      тег ветки. Но сценарий переворачивается: **окажись в очереди прогон из
      `Deploy UAT`, отменили бы его**, релизный тег не навесился бы, а `_deploy.yml`
      всё равно поехал бы катать UAT по тегу без образа. Ровно та поломка, от
      которой лечили J11, только с другой стороны.

      **Лечится потолком в два прогона на коммит**, а не защитой очереди: образ
      нужен только тем ссылкам, которые куда-то выкатываются. Условие переехало с
      триггера на задачу `build-push` — важное различие, потому что `branches: [dev]`
      в триггере отключил бы workflow на день-ветках **вместе с тестами**:

      ```
      push day25   → test ✓  build-push ✗     тесты идут, образ не нужен
      push dev     → test ✓  build-push ✓     собирает
      push main    → workflow не стартует     (branches-ignore)
      Deploy UAT   → вызов:  build-push ✓     вешает теги на образ dev
      тег vX.Y.Z   → test ✓  build-push ✓
      ```

      Вызов распознаётся по `inputs.release_tag`, а **не** по имени события:
      вызванный workflow видит контекст **вызывающего**, поэтому при вызове из
      `deploy-uat` `github.event_name` равно `push`, а `github.ref` — `main`.
      Входные параметры существуют только при вызове, и это единственный честный
      признак.
- [x] **D8. Search Console: три причины неиндексирования — разобрано 09.08.2026.**
      Уведомление по `neighbro.place`: «страница с переадресацией» — 2,
      «вариант страницы с тегом canonical» — 1, «обнаружена, не проиндексирована»
      — 5. Проверено по живому сайту, а не по догадке.

      **Первые две — признак того, что настройка работает, а не поломки.**
      Переадресация это `https://www.neighbro.place/` и `http://neighbro.place/`,
      оба отдают 301 на канонический адрес; ни один не перечислен в sitemap —
      Google нашёл их сам. Canonical-вариант это `/index.html`, который отдавал
      200 и объявлял каноническим корень.

      Все 12 адресов из sitemap отвечают 200, каждый объявляет каноническим сам
      себя и несёт 11 hreflang-альтернатив. Расхождения «в карте одно, на
      странице другое» нет ни одного — только оно было бы настоящей ошибкой.

      **Сделано:** `deploy/bunny-seo-index-redirect.sh` — edge-правило на обеих
      боевых зонах, `/index.html` → 301 → `/`. Проверено по живым сайтам.
      Действие `ActionType=1` взято из работающего правила «seo: www to apex» в
      той же зоне, а не из предположения о константах API.

      **Осталось намеренно:** `/xx/index.html` у каждого языка тоже отдаёт 200.
      Одним правилом это не чинится — цель редиректа литеральная, `{{path}}`
      подставляет путь целиком, отрезать суффикс нечем. Двадцать два правила
      ради адресов, на которые никто не ссылается и которых нет в sitemap, — плохой
      размен; их canonical и так разрешает верно.

      **«Обнаружена, не проиндексирована», 5 страниц** — не наша ошибка: источник
      в консоли «Системы Google». Адреса известны, обход отложен. Обычное для
      молодого домена, где страницы различаются в основном языком. Правится
      временем и различимостью, а не кодом.
- [x] **J15. `acknowledged` стал фактом, а не намерением — 11.08.2026.**
      Поле возвращало `Boolean(email)` — «адрес указан» — под именем, которое
      читается как «подтверждение по ст. 16(4) отправлено». В базе было хуже:
      `acknowledged_at` проставлялся **при вставке безусловно**, то есть каждое
      уведомление утверждало исполненную обязанность, включая те, у которых не
      было адреса.

      Теперь оба места говорят факт, а `sendNoticeReceipt` возвращает, ушло ли
      письмо, вместо `void` — узел с выключенной почтой молча выходил и дал бы ту
      же неправду другим путём. Пустое `acknowledged_at` при непустом адресе
      читается как невыполненная обязанность: повторной отправки нет, и такие
      записи должны быть видимы. Проверено на настоящей базе, тест краснел на
      прежней вставке.
- [x] **J16. Мотивировка по ст. 17(3) — закрыт 11.08.2026 как уже сделанный.**
      Все три пробела были закрыты 09–10.08.2026, когда письма приводились к
      общему виду, а этот список об этом не узнал. Проверено в коде, а не по
      памяти:

      - **охват и срок** — `lib/mailer.ts`: «It applies everywhere the Service is
        available, and it is not time-limited», с оговоркой в комментарии, что
        это станет полем формы в день, когда появится временное или региональное
        ограничение;
      - **что именно ограничено** — `whatWasRestricted()` вставляет цитату из
        слепка, а при его отсутствии различает «истекло» и «мы не смотрели», не
        выдавая одно за другое;
      - **две формулировки про обжалование** — сведены. Сравнил их не на глаз, а
        разбором файла: обе строки совпадают **дословно**, включая «We do not
        operate a formal internal appeals body», которой в письме автору не было.

      Ничего не менял: пункт описывал состояние, которого уже нет.
- [x] **J17. Форма говорит про чат отдельно — 11.08.2026.** При выборе «в чате»
      появляется предупреждение: переписка не хранится нигде, включая нас,
      поэтому цитату надо вставить со своего устройства, и она будет единственной
      копией.

      Постоянного текста здесь быть не должно: предупреждение, показанное всем про
      случай, который касается одного вида из четырёх, читать перестают. Проверено
      в браузере на обеих витринах — скрыта, появляется, снова скрывается; ломал
      нарочно, чтобы убедиться, что проверка это ловит.
- [x] **J24. Простой вход — решено 18.08.2026: бумажный код ушёл с регистрации.**
      Вход стал таким: имя и возраст → ПИН → лента. Код спрашивается, когда
      **открылся первый чат**, тем же экраном «переписать и подтвердить вводом
      двух групп», и пропустить его нельзя: до подтверждения писать в чат нельзя.

      **Почему сдвинули.** Он выписывал страховку раньше, чем появлялось
      страхуемое: на первой минуте нет ни чатов, ни переписки, имя и возраст
      набираются заново за десять секунд, и терялись только безымянные счётчики.
      За это просили единственное во всём входе действие в физическом мире, и
      стояло оно до того, как человек увидел продукт.

      **Почему после открытия чата, а не перед.** Гейт «перед чатом» лёг бы на
      экран согласия, где уже дисклеймер и выбор срока, а сверху таймер мэтча —
      `least()` от обеих фраз, в худшем случае считанные минуты. Первый мэтч с
      тремя минутами на часах и просьбой переписать шестнадцать символов кончился
      бы «потом» или потерянным мэтчем.

      **ПИН остался при регистрации, и решил это терминал.** В вебе ключи лежат
      неизвлекаемыми `CryptoKey`, ключ хранилища нужен только истории, которой ещё
      нет, — то есть в браузере ПИН можно было бы отложить. Но `depth` пишет файл
      ключей сразу и шифрует его тем же ключом хранилища: отложить значит
      подержать ключи на диске открытыми. Разводить лица нельзя — §13 ставит
      терминал первым.

      **Цена названа и записана в UI:** между регистрацией и первым чатом человек
      живёт без страховки, и потеря устройства в это время означает потерю
      личности вместе со счётчиками и опубликованными фразами. Это одна строка на
      экране регистрации, а не сноска.

      Схема №1 в `chat-flows_RU.md` перерисована; выдуманный порядок «ПИН → код»
      исчез вместе с самим кодом на том экране.

- [ ] **J25. Открытые вопросы спеки чата — список, чтобы не терялся.** Живут в
      самой спеке, но из чеклиста не видны, а решать их придётся до кода:
      какая модель модерации — **замер, а не спор** (§8.14) — и пропускная
      способность очереди модерации (§8.3).

      **Один пункт отсюда закрыт 19.08.2026 — алгоритм подписи.** Замерено в трёх
      движках, не по документации: сегодняшние Chromium 151, Firefox 153 и
      WebKit 26.5 умеют Ed25519 целиком, включая `wrapKey`. Но **Chromium 136 и
      старше не умеет его вовсе** (граница между 136 и 138, примерно май 2025), а
      человек на таком устройстве не подписал бы ни одного запроса — это запертая
      дверь, а не ухудшение. Выбран **ECDSA P-256**: прошёл везде, включая движки
      без Ed25519. Спека и схемы переписаны, пробник — `scripts/check-webcrypto-support.sh`
      и `testing/webcrypto-support.mjs`, чтобы вернуться к вопросу замером, когда
      хвост старых Chromium вымрет.

      **Ещё один закрыт 20.08.2026 — последний кадр истечения.** Надгробие видит
      только тот, кто в этот момент смотрел: открытый на экране чат показывает
      «чат истёк» и кнопку «закрыть», строка в списке исчезает молча. Содержимое
      стирается сразу в обоих случаях, и надгробие в список не возвращается —
      иначе след переживает то, что должно было исчезнуть. Записано в `chat_RU.md`
      §5 вместе с причиной различия: молча забрать экран у набирающего сообщение
      неотличимо от сбоя или бана.

      **И третий, тем же днём — под-разделы табов.** Их не будет: табов ровно два,
      `Chats` и `Matches`, а «угасающий» остаётся состоянием строки. Раздел
      «Угасающие» наполнялся бы переездом чата по таймеру — собеседник исчезал бы
      из вида в ту самую минуту, когда времени меньше всего, — и потребовал бы
      раскладывать счётчики инбокса на три. Записано в `chat_RU.md` §3.

      **И четвёртый, тем же днём — окна с непроверенным именем больше нет.**
      Закрыто переносом момента проверки, а не порядка шагов: имя уходит в очередь
      **в момент первой публикации**, когда очередь уже существует, и дальше
      проверяется при каждой установке. До первого поста проверять нечего — имя не
      видно никому: в ленте автор не светится никогда (§8.11), чужому глазу имя
      достаётся только с первого мэтча. Отклонённое имя не отменяет пост, автору
      предлагается его исправить; **пока имя отклонено, мэтч не открывается** — это
      следствие §8.11, последняя точка, где отклонённое имя ещё можно не показать.

      **Оттуда же — имя заморожено, пока живы посты или чаты.** Менять его можно
      только на чистом счету. Следствие записано в §4 и §8.2: системного сообщения
      «собеседник теперь называет себя иначе» больше не существует, менять нечего
      при открытом чате. Возраст меняется по-прежнему и по-прежнему только вверх.
      Отменённая формулировка занесена в `docs/retired-terms.txt`, чтобы не
      всплыла заново; проверка на ней сломана и починена — правило ловит.

      **И пятый, тем же днём — набор сроков чата.** Остаются три: 20 минут, 1 час,
      4:20; четвёртого не будет ни снизу, ни сверху. Короче 20 минут ломает
      собственный счётчик тишины — при `min(20 минут, срок / 3)` пятиминутный чат
      начал бы отсчёт через 1 минуту 40 секунд, а действует меньший из двух, так
      что один осторожный закрывал бы разговор обоим. Длиннее 4:20 переживает
      собственную причину: фраза в ленте живёт ровно столько. Записано в §5.

      **Осталось в J25 два вопроса, и оба ждут не решения, а очереди:** какая
      модель модерации (§8.14 — «замер, а не спор», и сама спека велит гонять его
      в день, когда очередь появится) и пропускная способность этой очереди (§8.3).
      Стенд для замера готов — `relay/moderation-bench`.

- [ ] **J19. Модель личности переписана в спеке и не существует в коде.**
      Решено 10.08.2026: одна живая сессия на личность, обязательный бумажный код
      восстановления, обязательный ПИН из шести цифр, половина ключа хранилища у
      узла (`chat_RU.md` §8.2).

      Ни одной строки под это ещё нет — `identities` и `sessions` в БД тоже нет,
      чат пока целиком в спеке. Пункт заведён не как задача на завтра, а чтобы
      расхождение «спека описывает, кода нет» было записано явно.

      **Что придётся построить, когда дойдёт очередь:**

      - `identities` с `recovery_auth_hash` и `recovery_wrapped_key` — **без
        счётчика попыток**: колонка убрана 18.08.2026, потому что почти не могла
        сработать (ошибка в символе рушит первую половину, узел не находит
        личность, уменьшать нечего). Считает ручка восстановления: по адресу плюс
        общий счётчик промахов;
      - `sessions` с `frozen_at` и **частичным уникальным индексом** — именно он, а
        не проверка в коде, держит правило одной сессии;
      - место под эфемерные половины при **перевыпуске ключа чата**: при согласии
        их держит `match_participants.ephemeral_public_key`, у перевыпуска такого
        места нет (`chat_RU.md` §8.13, решено 18.08.2026);
      - `vault_shares`: проверка ПИНа **на узле** до выдачи доли и счётчик попыток
        там же. Если долю отдавать без проверки, весь замысел рассыпается —
        злоумышленник забирает её один раз и перебирает шесть цифр офлайн;
      - экран подтверждения переноса с контекстом: как устройство назвалось, та же
        сеть или другая, когда. Сравнение адресов, без геобазы;
      - чистка долей у сессий, не появлявшихся год, и срок в политике хранения.

      **Дополнено 18.08.2026, когда механику восстановления разобрали до конца.**
      Записаны: энтропия бумажного кода (16 символов Crockford base32, соль
      `xor.ad/recovery/v1`, 80 бит) — раньше стоял только пример строки, и алфавит
      выбрал бы тот, кто первым сядет писать код; выпуск **нового** кода при
      восстановлении с умерщвлением прежнего; и **перевыпуск ключа чата** после
      смены устройства — один механизм для переноса и восстановления, с подписью
      долгим ключом и согласием второй стороны. До этого оба случая оставляли чат
      числящимся живым и молчащим.

      **Что проверить, когда построится** (§14 спеки): десять неверных ПИНов
      действительно сжигают долю и база после этого не открывается **ничем**;
      бумажный код поднимает личность на чистом устройстве; без «это я» переноса
      не происходит.

- [x] **J20. Спека ст. 16 приведена к тому, как работает форма — 11.08.2026.**
      Таблица полей помечала имя и почту обязательными «кроме §5.1», а §5.1
      обещал отдельный путь в форме. Ни того, ни другого нет и быть не должно:
      поля необязательны у всех, рядом стоит строка про сообщения о насилии над
      детьми, и узел не требует их ни при каких условиях.

      Записано вместе с причиной: отдельный путь заставлял бы человека самому
      вешать на уведомление ярлык до того, как он написал хоть слово, а отказ
      из-за пустого имени — ровно то, чего не допускает ст. 16(2)(c). Цена названа:
      часть уведомлений придёт без обратного адреса, и отвечать будет некому.
- [x] **J21. Оговорка о преимущественной силе есть во всех переводах правил —
      сделано 11.08.2026.** 27 файлов: 10 у neighbro, 17 у sosed.

      Оговорка стоит **на языке документа**, а рядом — английская строка. Писать
      её только по-английски внутри казахского файла бессмысленно: её не прочтёт
      тот, для кого она написана. Английская рядом страхует обратное — если
      местная формулировка окажется корявой, действующая версия всё равно названа
      однозначно.

      **Проверено отрисовкой, а не наличием строки в файле.** Страница правил
      подтягивает markdown запросом, поэтому строка в файле сама по себе ничего
      не доказывает. Поднял статические серверы обеих витрин в контейнере и
      посмотрел через Playwright четыре языка — en, ru, kk, el: везде обе строки
      на экране.

      **Чего подтвердить не могу:** качество семнадцати переводов. За русский,
      английский, немецкий, французский, испанский, польский и украинский
      отвечаю; за казахский, киргизский, узбекский, азербайджанский, армянский,
      грузинский и таджикский — нет. Формулировка короткая и однотипная, но
      носителю их стоит просмотреть.
- [x] **J22. Уведомление по ст. 16 отклонялось при исчерпании квоты ключа —
      найдено и исправлено 11.08.2026.** Нашёл независимый обход
      «спека против кода»; чинилось в тот же день.

      **Что было.** `resolveTenant` отказывал 401 или 429 — неизвестный ключ,
      отозванный, чужой origin, исчерпанная дневная квота, — и отказ стоял в
      `routes/report.ts` **до** снимка контента и **до** `INSERT`. Значит
      уведомления не существовало: подтверждение по ст. 16(4) не уходило, в
      очередь модератора ничего не попадало, следом оставался один счётчик
      Prometheus. При этом ту же дневную квоту тратят `/pageview` и `/waitlist`:
      витрины, разошедшейся по чатам, хватило бы, чтобы до конца суток перестать
      принимать сообщения о незаконном содержании.

      Собственный предел маршрута (10 в час на адрес) стоял **раньше** по коду и
      честного заявителя пропускал — он упирался в чужую квоту.

      **Чем починено.** `lib/tenant.ts` получил `resolveTenantSoft`: ключ
      отвечает только «через какое лицо», всё остальное понижает запрос до
      неатрибутированного. Квота там не проверяется и не тратится — обязанность
      не тарифицируется. Миграция `007` сняла `NOT NULL` с `dsa_notices.brand`,
      иначе потеря просто переехала бы на шаг позже. Образец поведения был уже
      написан рядом, в `routes/client_error.ts`.

      **Проверено:** `test/report_never_refused.test.ts` — на прежнем коде обе
      проверки краснели с `401 {"error":"invalid api key"}` и
      `401 {"error":"missing x-api-key"}`, после правки зелёные; весь набор 67/67.

      **Заодно:** `client_error.ts` переведён на ту же функцию и перестал тратить
      квоту ключа — страница, сообщающая, что она сломана, не должна
      конкурировать за бюджет с тем, что она не смогла отправить.

- [x] **M1. Снимок контента выбирал колонки, которых нет ни в одной схеме —
      найдено и исправлено 11.08.2026.**

      `dsa_snapshot.ts` брал у ленты `body`, `zone`, `identity_id`, у офферов
      `business_profile_id` и `created_at`. Ни одного из этих имён нет: в спеке
      ленты `text` и `author_identity`, у оффера `venue_id` и `published_at`.
      `mailer.ts` читал оттуда `row.body` — то есть письмо автору открывалось бы
      «что-то, что вы опубликовали» и **никогда не говорило, что именно**.

      **Почему это провалилось бы молча.** `query()` отдаёт `null` и когда базы
      нет, и когда запрос упал, а проверка таблицы выше первый случай уже
      отсекла. Значит сломанный `SELECT` возвращал `received` с пустым снимком —
      уведомление ложилось в очередь как «копия не требовалась» и разбиралось бы
      против ничего. Тот же класс дефекта, ради которого писалась миграция `006`.
      Теперь это `not_accessible` с ошибкой в логе.

      **Чем закрыто на будущее.** `test/dsa_snapshot_columns.test.ts` читает
      `docs/chat_EN.md` и `docs/offers/SPEC_EN.md` и сверяет с ними перечень
      колонок — как тест прав панели читает `App.tsx`. На прежнем коде он краснел
      с «dsa_snapshot copies feed_messages.body, which the chat spec does not
      define» и перечислял, что в спеке есть.

      **Заодно решено:** область (`lat`, `lon`, `area_radius`) в снимок **не
      попадает**. Уведомление спрашивает, незаконен ли текст, и отвечает на это
      текст; снимок живёт год, и копировать координаты значило бы хранить год
      чужих мест ради ничего.

      **И попутный пропуск:** `visible_at`, введённый вчера вместе с очередью
      модерации, был описан в прозе, но отсутствовал в `CREATE TABLE
      feed_messages`. Дописан, вместе с отсчётом `expires_at` от него.

- [x] **M2. Очередь уведомлений по ст. 16 не была ограничена брендом — найдено и
      исправлено 11.08.2026.**

      `routes/dsa.ts` делал голый `SELECT ... FROM dsa_notices`, а решение
      выбирало запись по одному идентификатору. Права проверялись, принадлежность
      — нет. Через нынешние роли это не достаётся: у `tenant_admin` нет
      `dsa_notices.read`. Достаётся **в один шаг**: тенант-админ вправе выдать
      роль `moderator` кому-то у себя (`panel/src/pages/panel-users`), а она
      несёт и чтение, и решение. То есть имена и почты заявителей **всех**
      брендов плюс право решать чужие уведомления.

      **Починено:** выборка получила условие по бренду читающего, решение —
      проверку принадлежности с ответом `404` (не `403`: есть ли у другого бренда
      такая запись, не дело этого бренда — то же правило, что у списка
      операторов). Неатрибутированные записи (`brand IS NULL`, миграция `007`)
      остаются у платформы: уведомление пришло без пригодного ключа, и какого
      лица оно касается — ровно то, чего никто не знает.

      **Проверено на настоящей базе** (`test/database.test.ts`, прогон через
      `scripts/run-relay-database-tests.sh`): до правки тест краснел с «a tenant
      is reading another tenant's notice». После — 26/26 в базовом наборе и 69/69
      в обычном.

- [x] **J14. Первого администратора новой среды теперь заводит визард — сделано
      11.08.2026.**

      `wizard seed-admin --env <среда> <адрес>`, рядом с `deploy`. Пишет **не
      визард, а узел** (`tools/seed_admin.ts`, запускается через
      `docker compose run --rm`): он уже знает транспорт хранилища, имя среды и
      то, как ключуется объект оператора. Учить этому визард значило бы
      переложить проблему — ровно то, чем был обход руками.

      **Заводит только в пустой среде.** Есть хоть один оператор — отказ с кодом
      `2`. Это и делает команду безопасной внутри образа: она не способ добавить
      администратора, а способ иметь первого; второго добавляет панель, где
      действие авторизовано и попадает в аудит.

      Продовая среда требует `--confirm-prod`, но **не** опубликованного релиза,
      в отличие от `deploy`: заведение оператора образ не меняет, и гейт по
      релизу здесь ничего бы не значил.

      **Проверено запуском**, не чтением: на файловой среде в контейнере команда
      записала `panel/probe/users/<sha256>.json` с телом
      `{email, role: "admin", brand: null, created_at}` — побайтово та же форма,
      что пишет панель. Повторный запуск отказал с кодом `2`, и второй объект не
      появился. На живом узле по SSH не гонял: это следующий выкат.

- [x] **J23. Визард отказывал в проде не по той причине, а `pool` не спрашивал
      вовсе — исправлено 11.08.2026.**

      **Первое.** `github.is_published_release` читает `GITHUB_TOKEN`, а
      `secrets.env` визарда его не содержит (он лежит в `deploy/.env.deploy` —
      другой файл, другой инструмент). Репозиторий приватный, поэтому
      неавторизованный запрос получает **404**, функция возвращала `False`, и
      визард говорил: «tag is not a published GitHub Release — publish the
      release first». То есть отправлял оператора публиковать уже
      опубликованное, ровно в момент выката прода.

      Починено разделением: «релиза нет» и «проверить не смог» — разные исходы.
      Нет токена, токен отвергнут (401/403), GitHub недоступен — всё это
      `CannotCheck` с причиной, и прод останавливается честным сообщением.
      Перечни секретов в справке визарда и в `inventory.example.toml` дополнены
      до полных: неполный перечень и есть то, из-за чего токен потерялся.

      **Второе.** `pool` — переключение боевого DNS — была единственной продовой
      операцией **без** `--confirm-prod`, при том что `deploy`, меняющий меньше,
      его требовал. Теперь требует. Гейт по релизу к ней не применяется: образ не
      участвует.

      **Проверено запуском:** без токена — «GITHUB_TOKEN is not set…»; с заведомо
      негодным токеном (живой запрос к api.github.com) — «GitHub rejected the
      token (401)»; `pool` на минимальном инвентаре с боевым узлом — «pool steers
      LIVE traffic to … — pass --confirm-prod». С `--confirm-prod` не запускал:
      это настоящее переключение DNS.

      **Токен добавлен человеком 11.08.2026**, и проверка прогнана против живого
      API: репозиторий виден (5 опубликованных релизов), настоящие теги
      `v2026.8.9-g4c8229c` и `v2026.8.5-gfd6587a` дают `True`, выдуманный
      `v9999.1.1-gdeadbee` — `False`, а не ошибку. То есть все три исхода теперь
      различаются: есть, нет, не смог посмотреть.

      Заодно заведён `relay/wizard/secrets.env.example` — образец со всеми
      именами и без единого значения. Его отсутствие и было причиной: перечень
      жил в докстроке, называл половину, и в пропущенной половине оказалось то,
      без чего прод отказывает не по той причине.

- [x] **J18. Образ собирается по содержимому, а не по коммиту — сделано
      11.08.2026.**

      Ключ «уже собрано?» был `sha-<коммит>`: у нового коммита такого тега нет,
      значит сборка шла всегда — даже когда в контексте не изменился ни байт.
      `relay/caddy` прошёл так 78 коммитов, каждый раз собираясь под две
      архитектуры, причём arm64 через эмуляцию.

      **Ключом стал хеш дерева каталога** — `git rev-parse HEAD:relay/<образ>`.
      Изобретать нечего: git и так адресует каталог по содержимому, и хеш
      меняется тогда и только тогда, когда меняется байт в контексте. Проверено
      на восьми последних коммитах: у `caddy` хеш **не менялся ни разу**, у
      `node` менялся ровно при правках кода и дважды совпал у соседних коммитов.
      В клоне глубиной 1 — а именно такой делает `actions/checkout` — считается и
      даёт те же значения.

      Проверка по содержимому **включает в себя** прежнюю: тот же коммит — то же
      содержимое. Тег `sha-<коммит>` при этом продолжает навешиваться на каждый
      коммит, поэтому ничто, что на него ссылается, не ломается.

      **Что меняется в чтении образа:** переиспользованный образ сохраняет метку
      сборки, то есть `org.opencontainers.image.revision` называет коммит,
      который эти байты **создал**. Теги говорят, каким коммитам образ служит;
      метка — каким он собран. Записано в workflow, чтобы не выглядело ошибкой.

      **Остаточный зазор назван там же.** `concurrency` вычисляется до шагов и не
      видит их вывод, поэтому группа осталась по коммиту. Значит два **разных**
      коммита с одинаковым контекстом теоретически могут пересечься и оба
      собрать. Это уже, чем было (один коммит — два дайджеста стало невозможно),
      и требует двух пушей в dev внутри одного окна сборки.

      **Чего не проверял:** живого прогона в Actions не было — правка уходит в
      ветку, и первый настоящий замер будет на ближайшем выкате. Проверены части,
      которые можно проверить локально: хеш дерева, его поведение на истории, его
      доступность в мелком клоне и разбор YAML.
