# Панель ревью · 22.09.2026 · PATCH /identities/me

- **Что ревьюили:** `relay/node/src/routes/profile.ts` (коммит `cd64cdd`), связки с
  `lib/feed_verdict.ts`, `lib/feed_limits.ts`, `lib/identity_guard.ts`, `lib/rate_limit.ts`;
  контракт `docs/api/openapi.yaml`, протоколы RU/EN, `docs/chat_RU.md` §8.2.
- **Линзы:** Безопасность (чтением), Данные и контракт (чтением).
- **Перепроверка:** ведущий — тест, увиденный красным; дедлок воспроизведён вживую.

## Находки по существу

### Безопасность
1. Дефект дизайна — инверсия порядка блокировок: маршрут брал `identities`, потом
   `identity_stats`; вердикт — наоборот. Дедлок на одном человеке.
2. Дефект дизайна — заморозка проверялась только для `accepted`; `pending` обходил.
3. Дефект дизайна — `likes.ts:169` пропускает `pending`, в `name` может лежать отклонённое;
   недостижимо, пока отказ имени никто не пишет.
4. Противоречие — спека велит зажимать фильтр при смене возраста и слать системную строку
   в чаты; маршрут отказывал и ничего не слал.
5. Мелочь — возраст без потолка: переполнение integer → 500 при истраченном токене.
6. Мелочь — `min > high` / `max < low` проходили: пустая лента себе.
7. Мелочь — невидимые символы в имени (ZWSP, RTL override, управляющие) проходят;
   регистрация — так же.
8. Мелочь — предел в памяти процесса; токен тратится до разбора тела (сознательно).
   Без дефекта: оракул `name_frozen` (владелец видит своё), порядок подписи и предела,
   отлучка, шаг 20/21, null-границы, TOCTOU имени, пауза, закрытая личность, дедуп.

### Данные и контракт
1. Тот же дедлок, тот же вывод.
2. Противоречие — отказ за фильтр, которого не присылали (то же, что Без. 4).
3. Противоречие — протоколы называют `filter.age.step` и `filter.age.min_width`, которых
   нет в реестре (сняты 17.09).
4. Противоречие — `paused` нигде не описан как статус 429.
5. Противоречие — 400 в контракте только BadVersion; восемь причин `invalid_body` не названы.
6. Мелочь — ни GET, ни PATCH не отдают `name_state`; отклонённое неотличимо от принятого.
7. Без дефекта — потерянных обновлений нет; `[]` отличим от «не менять» (тестом не покрыт).
8–9. Мелочи — невидимые символы; языки `zz`, дубли.
10. Мелочь — не покрыто тестами: пауза, trim, 400 байт, `[]`, `null`-сброс, 503.
11–12. Без дефекта.

## Проверка ведущим и что закрыто

| Находка | Чем |
|---|---|
| Дедлок (Без. 1, Данн. 1) | **ПРОВЕРЕНО вживую**: 12 гонок вердикт×маршрут — `deadlock detected` на старом порядке; после правки зелёно; обратная поломка — снова красно |
| Кламп при смене возраста (Без. 4, Данн. 2) | тест красный до правки (`filter_out_of_band` на `{age: 21}`); первая версия клампа сама была с дефектом (`b.high ?? min`) — поймана тем же тестом |
| Заморозка всех состояний кроме rejected (Без. 2) | чтением; правка |
| Потолок возраста 150, `min > high` (Без. 5, 6) | чтением; правка |
| Невидимые символы, NFC, схлопывание пробелов (Без. 7, Данн. 8) | тест красный до правки |
| Контракт: 400, 409, 429 с `paused` (Данн. 4, 5); протоколы без снятых пределов (Данн. 3) | ворота контракта, парности, пределов |

Отброшено: Без. 8 (сознательно), Данн. 7, 11, 12.

## Нарезка — открыто

| # | Задача | Цена |
|---|---|---|
| ~~1~~ | ~~`likes.ts:169` — требовать `= 'accepted'`~~ — закрыто 22.09.2026: отказ имени построен (`refuseName`, `POST …/refuse-name`), лайк требует `accepted`, тест «the moderator refuses a name…» | — |
| 2 | Системная строка «сменил возраст» в открытые чаты (Без. 4, вторая половина) | час; шаг 5 сокета |
| ~~3~~ | ~~`name_state` в `Profile`~~ — закрыто 22.09.2026 тем же куском | — |
| 6 | Подписанный вызывающий всегда `brand: null` (`identity_guard.ts:176`), и каждая фраза с настоящего пути ложится `unattributed` — модератор тенанта её не видит; замечено на стенде по скриншоту очереди 22.09.2026 | час: бренд из ключа регистрации или из сессии |
| 4 | Та же чистка невидимых символов при регистрации `identity.ts:153` (Без. 7) | полчаса |
| 5 | Тесты: пауза, `[]`, `null`-сброс, 400 байт (Данн. 10); языки — допустимый набор (решение) | час |
