# Панель ревью · 23.09.2026 · «не сейчас», «я», свой срок в чате

- **Что:** ветка `day56`, коммиты `b89edbb` («не сейчас» и «вернуть» во входящих `depth`),
  `0bf96cf` (малый экран «я»), `3e95554` (свой срок беседы в `depth`, поле `span` в `GET /inbox`).
- **Воспроизвести диапазон:** `git diff 1302219..3e95554`.
- **Линзы:** безопасность и согласованность. Данные не звались: из схемы и запросов изменилось
  одно поле выборки; эксплуатация — нет новых задач и выката.
- **Опровержение:** не понадобилось — обе линзы не принесли ничего выше мелочи, и каждую мелочь
  ведущий сверил сам по коду перед правкой.

## 1. Сырьё линз (без правки)

### Безопасность

Линза «Безопасность», диапазон `git diff 1302219..3e95554`. Критичных находок нет. Всё ниже я проверил чтением кода; тесты не запускал, код не правил.

**Утечки нет (ПРОВЕРЕНО, `relay/node/src/routes/inbox.ts:71,79-82`).**
- `span` берётся из `p.idle_ttl_minutes`. Алиас `p` жёстко привязан к вызывающему условием `WHERE p.identity = $1`, а `o` — вторая сторона (`o.identity <> $1`).
- `ends` на строке 70 тоже считается из `p`.
- Срок второй стороны не выбирается совсем.
- Тест разводит спаны двух сторон и проверяет, что каждая получает свой: `relay/node/test/feed_publish.test.ts:2069-2085`. Этот прогон я не запускал.

**«Не сейчас» и «вернуть» трогают только своё (ПРОВЕРЕНО).**
- Оба запроса `UPDATE` в `relay/node/src/routes/matches.ts:102-113` ограничены условием `identity = $2`, где `$2` — вызывающий. Перед этим узел берёт матч под блокировкой, требует участия вызывающего и отсутствия блокировки между сторонами (`:85-97`).
- В клиенте id проходит через `encodeURIComponent`: `depth/core/client.ts` (decline, undoDecline, setChatSpan).
- PATCH спана тоже ограничен своей строкой (`relay/node/src/routes/chats.ts:262-266`) и проверяет UUID и допустимые значения.

**Экранирование (ПРОВЕРЕНО).**
- Имя и возраст в строках входящих, в том числе в отклонённых, проходят через `plain()`: `depth/ink/rooms.ts:126`.
- Шапка чата и экран «я» — тоже через `plain()`.
- Спан перед подстановкой в ключ локали сверяется с белым списком: `rooms.ts:219`.

**Таймер (ПРОВЕРЕНО).** `setInterval` снимается при размонтировании: `rooms.ts:226`.

**Экран «я» (ПРОВЕРЕНО).** `profile()`, `hidden()` и `blocks()` вызываются без параметров, то есть по своей сессии. Данных другой личности на экране нет.

**Мелочи — это дефекты:**
1. `rooms.ts:222`: значение `endsAt`, пришедшее с узла, не проверяется на число. Прямо рядом, в `rooms.ts:542`, такая проверка (`Number.isFinite`) есть.
   - Если узел пришлёт строку, `left` станет NaN (`:228`). Счётчик тогда не включится, и таймер молча не покажет конец чата.
   - Если узел пришлёт `0`, оператор `??` не заменит его на значение по умолчанию. Шапка сразу станет красной с `0:00`.
   - Правка точечная: `Number.isFinite(startEnds) && startEnds > 0 ? startEnds : default`.
2. `rooms.ts:348`: клиент ставит `next`, а не `answer.body.span`, который вернул узел. Сейчас узел возвращает ровно то, что получил (`chats.ts:271`), так что расхождения нет. Но экран верит себе, а не узлу.

**Не нашёл, где обработано (не дефект, а пробел):** проверка, что `chosen.match_id` принадлежит строке типа «матч», есть только на узле. Пункт меню скрыт для чатов (`rooms.ts:~146`), а подменённый id отсечёт узел с ответом 404 (`matches.ts:98`). Этого достаточно.

### Согласованность

Линза СОГЛАСОВАННОСТЬ, диапазон `1302219..3e95554`. Всё проверено чтением файлов и `git diff`; ничего не запускалось. Нарушений уровня «критично» нет, противоречий уровня «дефект дизайна» тоже нет.

**Противоречия (вижу расхождение)**

1. Мелочь. `docs/chat_RU.md:54` и `docs/chat_EN.md:54`: «ручка стоит в шапке беседы». То же в §8.6: `docs/chat_RU.md:2205`. В коде ручки в шапке нет: в шапке только строка `chat.fades` (`depth/ink/rooms.ts:286`), а срок меняется пунктом меню `chat.span` (`depth/ink/rooms.ts:334`). Для терминала это оговорено в `docs/depth-client_RU.md:778-783` и `docs/depth-client_EN.md:810-816`. Сама спека чата оговорки не содержит. Правка: одна скобка в §5 «в терминале — пункт меню (depth-client §4.7)».
2. Мелочь. `docs/depth-client_RU.md:950` («`m` откуда угодно») и соответствующая строка EN расходятся с кодом (`depth/ink/screens.ts:148`, пункт `me` в меню ленты) и с абзацем «построено» (`docs/depth-client_RU.md:998-999`). Абзац этот текст отменяет, но исходная строка не помечена `[retired]`/«построено иначе», хотя в §4.4 так сделано (`:573`).
3. Мелочь, перевод. `depth/ink/locales/ro.json`, `chat.fades`: «după {span} din TĂCEREA ta». Прописными выделена «тишина», а не «ваша», хотя в ru/en выделено слово «ВАШЕГО/YOUR». Нужно «din tăcerea TA».

**Не нашёл описания**

4. `docs/protocol_RU.md:176` и `docs/protocol_EN.md:186`, строки `GET /inbox`: поля `span` нет. Строки `PATCH /chats/:id` (`docs/protocol_RU.md:181`, `docs/protocol_EN.md:191`) тоже не говорят, что выбранное значение возвращается в `GET /inbox`. В коде поле есть (`relay/node/src/routes/inbox.ts:71,116`), в `docs/api/openapi.yaml:407` тоже. Правка: дописать в строку `GET /inbox`: «у чата — свой `span` (23.09.2026), чужой не отдаётся».
5. Для экрана `Hidden` нет абзаца «построено». Он открывается строкой «скрытое · N» с экрана `me` (`depth/ink/app.ts:129`, `depth/ink/rooms.ts:718`). В `docs/depth-client_RU.md` §4.11 он лишь упомянут в перечне строк (`:1001`). Как он устроен и что на нём можно сделать, не описано.

**Сходится**

- «В меню ленты» по отношению к ограничениям, лайкнутому, скрытому и заблокированным больше нигде не встречается. Остались только слова про вход через пункт «я» (`docs/depth-client_RU.md:999`, `docs/depth-client_EN.md:1045`), и это совпадает с `screens.ts:141-148`.
- Правило четверти совпадает: «последняя четверть своего срока» (`docs/chat_RU.md:55`, `:2238`) и `rooms.ts:230`. Отсчёт от своего сообщения: `rooms.ts:371`. Сдвиг конца при смене срока: `rooms.ts:347`.
- Макеты совпадают с локалями: EN «fades after 1h of YOUR silence» (`docs/depth-client_EN.md:782`) и `en.json` `chat.fades` плюс `spanShort60`. RU «гаснет после 1ч ВАШЕГО молчания» (`docs/depth-client_RU.md:750`) и `ru.json`.
- Пункт «свой срок: час» и порядок 10 → 30 → час → «пока говорим» совпадают с `rooms.ts:192` и `spanLong*`.
- `inbox.notNow`/`declined`/`undo` совпадают с §4.6 (`docs/depth-client_RU.md:734-740`) и `rooms.ts:128-145`.
- Во всех 17 локалях есть все 16 новых ключей, `{span}` стоит в `chat.fades` и `chat.span`, смысл сохранён. Грузинский без прописных, это нормально.
- Пары RU/EN у абзацев «построено» совпадают по смыслу. EN вставил скобку §4.6 посреди фразы (`docs/depth-client_EN.md:767`), RU — после неё (`:736`). На смысл это не влияет.

## 2. Сводка ведущего

| Заявка | Итог | Проверка |
|---|---|---|
| Б1: `endsAt` с узла не проверен на число, `0` красит шапку сразу | **исправлено**: число, конечное и больше нуля, иначе час от открытия | `rooms.ts`, код Chat — **ПРОВЕРЕНО** мной |
| Б2: после смены срока экран верит себе, а не узлу | **исправлено**: берётся ответ `PATCH` | `rooms.ts` |
| С1: «ручка в шапке беседы» в спеке чата при пункте меню в терминале | **исправлено** оговоркой в §5 RU/EN | `chat_RU.md:54`, `chat_EN.md:54` |
| С2: «`m` откуда угодно» не помечено | **исправлено**: «построено иначе» RU/EN | `depth-client` §4.11 |
| С3: `ro` выделяет не то слово | **исправлено**: «din tăcerea TA» | `ro.json` |
| С4: `span` нет в строке `GET /inbox` протокола | **исправлено** RU/EN | `protocol` §4 |
| С5: у экрана «скрытое» нет абзаца | **исправлено**; дата сверена по `git log` — экран 23.09, маршруты 21.09 | коммит `38f5116` |

Ничего не отложено.
