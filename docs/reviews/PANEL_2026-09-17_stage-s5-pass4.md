# Панель ревью: Э5 маршрута до отрисовки, проход 4 — 17.09.2026

**Что ревьюили.** Короткий проход только по диффу прохода 3: `xor.ad` `972a9ec..deebbe0`, `sosed.place` `4b7d84b..2668fe0`, `neighbro.place` `723edd9..b23f035`. Ветка `day54`.

**Линзы.** Право; безопасность; данные и СУБД (DDL канона в `postgres:16`, все опыты прохода 3 повторены); автономность и эксплуатация; согласованность. **Опровергающий не собирался** — см. сводку.

## Сырьё линз (дословно, из транскриптов агентов)

### Право

## Линза «Право» · Э5, проход 4 (17.09.2026) · короткий проход по диффам прохода 3

Предмет: `xor.ad` 972a9ec..deebbe0; репозиторий `sosed.place` 4b7d84b..2668fe0 (репозиторий `neighbro.place` b23f035 несёт те же три строки политики и условий — проверено grep по `landing/legal/privacy_EN.md:21,:25`, `terms_EN.md:93`).

### Закрытие LAW-1…8 прохода 3 — по существу

- LAW-1 (`GET /statements`) — **закрыт частично.** `openapi.yaml:461–467` `Statement: {id, restriction, facts, ground_kind, ground_text, automated_used, created_at, appeal}`; `protocol_RU.md:185` то же плюс «первая выдача ставит `delivered_at`». Остаток — LAW-1 ниже.
- LAW-2 (`Notice.receipt_hash`) — закрыт в контракте: `openapi.yaml:176`, паттерн `^[0-9a-f]{64}$`, честно помечено «report.ts его ещё не принимает»; незастроенность держит `open.tsv` `dsa.receipt.unbuilt`. Закрыт.
- LAW-3 (возраст) — `sosed.place/landing/legal/privacy_EN.md:21` «shown, with its changes, to the people you match with, as your name is». Получатель и факт объявления смены названы. Закрыт.
- LAW-4 (реестр §7) — `article-30-register_RU.md:164`, `_EN.md` та же строка: адресат, что сделано и срок, факты, основание, автоматика, год; хэш квитанции без личности заявителя. Закрыт; срок «год» совпадает с `dsa/SPEC_RU.md:496` и `privacy_EN.md:57`.
- LAW-5 (где живут счётчики) — `protocol_RU.md:295` «счётчики по личности живут в памяти узла и в журнал не пишутся». Закрыт.
- LAW-6 (цена блокировки по фразе) — `privacy_EN.md:25` полфразы «Blocking somebody by a phrase also removes their other phrases… shows you which ones were theirs». Закрыт.
- LAW-7 (записка №16) — `legal-review-brief_RU.md:182–184` и `_EN.md`: вопрос и цена переписаны, факт и вопрос больше не спорят. Закрыт.
- LAW-8 (реплики стола скрытые; имя правится) — `privacy_EN.md:25` «phrases and table lines… until they fade», `terms_EN.md:93` «The message — or the name — can be edited». Закрыт.
- Ревизии: `sosed.place/deploy/legal-revisions.json` пересчитан; `check-legal-revisions.py` прогнан — «дата и текст сходятся у всех» (3 документа, 16 переводов). ПРОВЕРЕНО запуском.

### Новые находки (только в изменённых строках)

**LAW-1 · дефект дизайна · в `Statement` нет срока ограничения — ст. 17(3)(a) DSA закрыта на две трети.**
- `openapi.yaml:461–467`: `restriction` — строка без срока; `dsa/SPEC_RU.md:440` требует «что сделано… **срок действия**», сущность `:483` `restriction` — enum `removed | hidden | offer_taken_down | access_restricted` тоже без срока. Ст. 17(3)(a): «where applicable, the territorial scope of the decision and its duration».
- **Я вижу дефект:** у `access_restricted` срок есть по смыслу, а поля для него нет ни в сущности, ни в ответе автору; для `removed/hidden` бессрочность нигде не проговорена.
- Предложение: `Statement.until: {type: string, format: date-time, nullable}` (null — бессрочно) и та же колонка в `statement_of_reasons` (`SPEC_RU.md:483`); в описании — «территориальная область не применяется: ограничение действует в сервисе целиком».

**LAW-2 · противоречие · экран 19 запирает высаженного, канон — нет.**
- `sosed.place/docs/19-table_RU.md:46` / `_EN.md:47` (добавлено проходом 3, CON-26): «Высаженный видит стол как несуществующий: сокет закрывается, **стол не открывается**».
- Против: `xor.ad/docs/chat_RU.md:645–662` «Высадка выпроваживает, но не запирает — решено 08.09.2026… **пока никто не заблокировал, высаженный возвращается тем же движением, и так сколько угодно раз**»; `protocol_RU.md:199` «вернуться — тем же `POST`».
- **Я вижу дефект:** по контракту (`:159`) 404 получает лишь `GET /tables/:id` и билет при потерянном месте, а `POST /tables/:id/seat` открыт; фраза экрана обещает пользователю замок, которого канон сознательно не заводит (§1 «следа не остаётся»). Юридический хвост: обещание на витрине сильнее механики.
- Предложение: на экране 19 → «стол исчезает с экрана и сокет закрывается; сесть снова можно, пока никто из сидящих не заблокировал (канон §6.1, 08.09.2026)».

**LAW-3 · мелочь · имена оснований расходятся между спекой DSA и контрактом.**
- `openapi.yaml:464` `ground_kind: enum [law, terms]`; `dsa/SPEC_RU.md:485` `ground_kind # legal | contractual`; реестр `:164` «закон или условия». Один список, три написания. Предложение: в контракте — `[legal, contractual]`, как в сущности, из которой поле берётся.

**LAW-4 · мелочь · `delivered_at` обещан протоколом, но не контрактом.**
- `protocol_RU.md:185` «первая выдача ставит `delivered_at`»; `openapi.yaml:2008–2009` описание `GET /statements` этого не несёт. Доказательство вручения по ст. 17 (у `SPEC_RU.md:489` «null, пока не доставлена») должно стоять там, откуда генерируется узел. Предложение: одну фразу в `description`/`x-description-ru` операции.

### Проверил и находкой не счёл

- `nonces` в базе с первым ответом на 10 минут (`protocol_RU.md:52–58`, `limits.tsv:125`): ответы `{until}`, `{public_no}`, 204 — ничего сверх уже выданного клиенту; технический признак, категорией данных по ст. 30 не становится. Оговорка для линзы данных: DDL таблицы `nonces` в `chat_RU.md` нет (grep — 0), только слово в протоколе.
- Счётчики по личности в памяти, обнуляются перезапуском (`protocol_RU.md:295`): ни один не держит юридический срок; `support.requests.day` — квота вежливости, не обязанность. Нарушения нет.
- `POST /identities/close` теперь работает и в отлучке (`protocol_RU.md:269`) — право на удаление (ст. 17 GDPR) не блокируется до четырёх часов; `GET /statements` в отлучке по-прежнему 409, срок ≤4 ч, права ст. 15 идут почтой — приемлемо, как и в проходе 3.
- Реестр §7: «личность адресата» в мотивировке на год при удалении личности через 30 дней после закрытия — `privacy_EN.md:51` и «не счёл» прохода 3 это уже покрывают.
- Большинство при высадке — из играющих (SEC-24): голоса в памяти, следа нет, решение пользователей, а не провайдера — ст. 17 DSA не задевает.
- `GET /hidden` держит `text` реплики до `tables.closed_at` — реплика публична по построению, `privacy_EN.md:26` называет её.
- Стикеры 6/мин на экране 19 — предел, не данные.
- `feed_cursor`, `feed_live_geo` (`chat_RU.md:1434–1435`) — индексы по уже описанным колонкам, новой обработки нет.
- Записка №16 «Блокирует публикацию: да» оставлена — верно: порог возраста стоит в опубликованном тексте.
- Сторож С7 через `GET /metrics` `relay_backup_age_seconds` — эксплуатация, не право.

### Безопасность

## Линза «Безопасность» · Э5, проход 4 · 17.09.2026 · короткий проход по `972a9ec..deebbe0`

```diff
! 5 находок: 1 дефект дизайна (нет DDL nonces — ключ, размер, чистка), 2 дефекта контракта (Nonced съел ПИН-доказательство; счётчики по личности не общие на пул), 2 мелочи
+ SEC-16…24 прохода 3 закрыты по существу — проверил каждое по изменённым строкам
@@ Всё — чтение документов; кода за этими маршрутами нет (x-status: proposed), живого ничего не гонял
```

### Я вижу дефект

**SEC-25 · дефект дизайна · таблица `nonces` объявлена, но нигде не описана** — `protocol_EN.md:57-61` («в таблице `nonces` базы, общей на пул, чистка — `nonce.ttl`»), `openapi.yaml:271`, `:381`, `:387`, `:474` (то же слово в слово); в каноне `chat_EN.md` ни одного `CREATE TABLE nonces` (grep пуст), в `docs/facts/schema.tsv:40-73` строки нет, в `watchdogs_EN.md` слов `nonce`/`janitor` нет, хотя `limits.tsv:111` вешает `nonce.ttl` на сторожа `chat.janitor.missing`. Что не решено: (а) **ключ** — «пара (session, nonce)», без маршрута: клиент, отдавший nonce `N` в `POST /tables`, через минуту шлёт тот же `N` в `POST /away` — по букве это «повтор», и узел отдаёт ответ стола вместо ухода; (б) **что хранится** — «первый ответ» целиком: у `POST /tables` это вид стола с `board`, у `/support` — `{public_no}`, у `/recovery/reissue` — подтверждение перевыпуска; размер строки не ограничен, а десятиминутное окно × семь маршрутов × лимиты по личности даёт потолок, который надо назвать; (в) **кто чистит** — сторож не назначен. Сверх того `chat_EN.md:967-969` по-прежнему говорит «nonce потребовал бы общей памяти», а `protocol_EN.md:60` этот довод уже отозвал — противоречие между каноном и протоколом. Предложение: DDL `nonces (session uuid, nonce bytea, route text, status smallint, body bytea, expires_at) PRIMARY KEY (session, nonce)` с проверкой `route` при повторе (чужой маршрут — 409 `invalid_body`), строка в `schema.tsv`, строка сторожа рядом с `idempotency` (`chat_EN.md:449` уже ссылается на тот же sweep в `scheduled.ts`), правка `:967-969`.

**SEC-26 · дефект контракта · `Nonced` у `/vault/pin` и `/identities/close` вытеснил доказательство ПИНа** — `openapi.yaml:1086-1088` («старый ПИН, новый `auth_hash`, перевыпуск доли» — а тело `Nonced` `:428-434` несёт только `nonce`), `:1102-1104` («с доказательством ПИНа» — тело то же). У личности одна сессия на устройство (`chat_EN.md:1022`, `:1038`), подпись — это устройство; ПИН — единственный второй фактор у смены ПИНа и закрытия личности. Исполнитель, собирающий по схеме, соберёт смену ПИНа без старого ПИНа и закрытие без доказательства: уведённый разблокированный телефон закрывает личность одной подписью. Проход 3 закрыл SEC-20 («у них нет тела») телом-заглушкой. Предложение: две схемы `PinChange {nonce, current_auth_hash, next_auth_hash, next_share}` и `IdentityClose {nonce, auth_hash}` с `required` по всем полям; `Nonced` оставить только там, где кроме nonce правда ничего нет.

**SEC-27 · дефект контракта · счётчики по личности «в памяти узла», а узлов в пуле несколько** — `protocol_EN.md:313` (новая фраза OPS-2: «живут в памяти узла, не журналируются»), при том что `:60` в том же документе вводит общую базу «на пул» именно потому, что память узла не общая. Ни в §2, ни в §5, ни в §6 не сказано, что личность прибита к одному узлу. Сценарий: пул из трёх узлов — `chat.messages.minute` 60 становится 180, `blocks.hour` 20 — 60, `hidden.hour` — втрое; клиент даже не выбирает узел — достаточно, что балансировщик крутит по кругу. Оговорка «ни один не охраняет секрет» верна (проверил `limits.tsv:38-43`: попытки ПИНа — отдельный механизм с заморозкой в базе), но лимит-на-пул ≠ лимит-на-узел. Предложение: одна фраза — либо «личность прибита к узлу по `session` в балансировщике», либо счётчики в `quota_counters` (`schema.tsv:60`, уже есть) с признанием стоимости записи.

**SEC-28 · мелочь · `POST /tables/:id/proposals` — не сказано, что предлагать может только игрок** — `protocol_EN.md:213` («одно открытое предложение на стол, второе — 409 `pending_exists`»), `openapi.yaml:1500-1510` (только 201/400/429). У `refusal` явно «принимается только от игрока» (`:211`), у высадки после SEC-24 — «зрители не решают»; у предложений ограничения нет. Сценарий: зритель садится (потолка зрителей по-прежнему нет — остаток SEC-24) и кладёт `undo`/`draw` в единственный слот `pending`; игроки не могут предложить ничью, пока его предложение не отклонят или не истечёт. Не оракул (`pending` и так отдаёт `GET /tables/:id` всем сидящим, `:185`), а занятие слота. Предложение: «от игрока (`playing_from` set); зритель — 409 `refused`» в `:213` и 409 в openapi.

**SEC-29 · мелочь · порядок ответов у `/chats/:id/messages` не назван** — `protocol_EN.md:133`: 404 не члену и 409 `stepped_away` описаны, но не сказано, что́ идёт первым. Если 409 отвечается по состоянию отправителя до проверки членства — это только про самого себя, безвредно; если после — тоже. Единственный вредный порядок — 429 `chat.messages.minute` *после* проверки членства с 404: тогда счётчик не расходуется на чужие чаты, а расходуется на свои — это как раз правильно. Записать: «сначала 409, потом 404, потом 429» — три слова, и тестовая карта §23 получит порядок.

### Проверил и находкой не счёл

- **SEC-16 → `chat_EN.md:323-330`, `:462-471`.** `id uuid PK`, `UNIQUE (table_id, seat_no)` без `WHERE left_at IS NULL`, `max+1` под `FOR UPDATE` — номер невозвратен, цель `{table, seat}` не перепривязывается. `seat_id` наружу не выходит: `:462` «счёт по местам», `seat_no ↔ seat_id` 1:1 внутри стола, uuid места и так ни к чему не сводится. Закрыто.
- **SEC-17/OPS-1 → `protocol_EN.md:57-61`.** Хранилище названо, срок в `limits.tsv:111`. Закрыто по существу; остаток — SEC-25 (DDL и ключ).
- **SEC-18 → `:133`, `limits.tsv:110`, §5 `:305`.** Предел есть, ключ по личности. Остаток — SEC-27 (пул).
- **SEC-19 → `:134`.** «Только строки со своей `recipient_session` в этом чате, чужие `ids` молча пропускаются»; PK `pending_deliveries (chat, recipient_session, local_id)` (`chat_EN.md:2261-2268`) делает это естественным; одна сессия на устройство — второй сессии той же личности, чьи строки можно стереть, нет. Закрыто.
- **SEC-20 → `:57`, `openapi.yaml:1088`, `:1104`, `BlockTarget.nonce` `:366`.** Список — семь маршрутов, тела есть. Закрыто как было поставлено; что тела пустые — SEC-26.
- **SEC-21 → `:243`, `openapi.yaml` `/hidden` 200 `{id}`.** Один код. Закрыто.
- **SEC-22 → `:133`, `openapi.yaml:1978-1996`.** 404 ApiError, 409 `oneOf [LegalReacceptance, ApiError]`, `stepped_away` в enum `:250`. Закрыто; порядок — SEC-29.
- **SEC-23 → `:262`.** 204 на чужой номер. Закрыто.
- **SEC-24 → `:217`, `chat_EN.md:672`.** Большинство из играющих. Закрыто; потолка зрителей по-прежнему нет — упомянуто в SEC-28, отдельной находкой не дублирую.
- **`pending_exists` как оракул** — нет: `GET /tables/:id` (`:185`, `:206`) отдаёт `pending` всем с живым местом, 404 без места стоит раньше.
- **404 у билета `:209`** — той же формы, что у несуществующего стола; стол и так виден в ленте, утечки нет.
- **`is_playing`** — `openapi.yaml:284` переименовано, `protocol_EN.md:206` по-прежнему `playing: false` рядом с `{playing, watching}` — рассогласование имён, не безопасность; для линзы согласованности.
- **`x-protocol-sunset` из `PROTOCOL_SUNSET_AT`** (`:82`) — не подписан, ничего не открывает; подмена на границе только «ускоряет» обновление клиента, отдельного риска нет.
- **`check-openapi.sh:49-64`** — обход дерева ловит дубли и порванные flow-описания; эвристика по суффиксам `).`/`true.`/`false.` неполна (описание, кончающееся на `»` или цифру, пройдёт), но это ворота, не поверхность.
- **Голоса `kick` в памяти узла** (`:217`) — та же пуловая слабость, что SEC-27, но строка не менялась проходом 3 — не в предмете.

Живого ничего не гонял: все маршруты `x-status: proposed`, узел их не имеет — **НЕ ПРОВЕРЕНО** ничего сверх текста.

### Данные и СУБД

Линза «Данные и СУБД», Э5 проход 4 (17.09.2026), короткий проход. Предмет — diff 972a9ec..deebbe0 по docs/chat_*.md, protocol_RU.md, openapi.yaml, docs/facts. Все опыты — в Docker postgres:16, скрипт `scratchpad/run.sh` (вытаскивает все 30 ```sql-блоков chat_RU.md, гонит DDL-блоки кругами до сходимости, затем checks.sql / checks2.sql).

```diff
+ готово: весь DDL канона сходится за 2 круга — 24 таблицы; 2 «красных» блока — фрагменты условий (b07, b14), не DDL и не тронуты проходом 3
+ готово: table_seats с id/UNIQUE(table_id, seat_no)/one_at_a_time, возврат новой строкой, каскад table_scores(seat_id), chat_games.seq/last_move_hash/pending, индексы feed_cursor и feed_live_geo, PREPARE лайка :1907 — всё ПРОВЕРЕНО в контейнере
! внимание: таблица `nonces` объявлена в протоколе, но DDL в каноне и строки в schema.tsv нет; `leave_table` названа процедурой без определения
! дефект: table_scores принимает строку счёта на место чужого стола — ПРОВЕРЕНО, PK (table_id, seat_id) этого не держит
```

## Находки

**DATA-1 🟡 `nonces` — таблица без DDL и без реестра.**
`docs/protocol_RU.md:57` (и `protocol_EN.md:59`): пара (сессия, nonce) «держится в таблице `nonces` базы — общей для пула», `docs/facts/limits.tsv:111` ссылается на неё же (`nonce.ttl`). ПРОВЕРЕНО grep: в `docs/chat_RU.md`, `chat_EN.md` и `docs/facts/schema.tsv` слова `nonces` нет; в контейнере после полного прогона таблицы `nonces` нет (список 24 таблиц). Одновременно `docs/chat_RU.md:956` «Окно вместо nonce, и это размен … общая — это запись в базу на каждый запрос» всё ещё стоит без пометки, что для семи маршрутов довод снят (протокол говорит «снят», канон — нет).
Предложение: в §8.13 канона рядом с `sessions` добавить блок
```sql
CREATE TABLE nonces (
  session_id  uuid NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  nonce       bytea NOT NULL CHECK (octet_length(nonce) = 16),
  response    jsonb NOT NULL,           -- первый ответ, отдаётся на повтор
  created_at  timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (session_id, nonce)
);
CREATE INDEX nonces_expiry ON nonces (created_at);   -- уборка nonce.ttl
```
строку в `schema.tsv`, и абзац :956 дополнить одной фразой «для одноразовых действий (§2 протокола, OPS-1) память всё же общая — таблица `nonces`».

**DATA-2 🟡 `table_scores`: счёт может указать на место другого стола.**
`docs/chat_RU.md:473–479`: `PRIMARY KEY (table_id, seat_id)`, два независимых FK. ПРОВЕРЕНО в контейнере (checks2.sql): `INSERT INTO table_scores (table_id=стол-2, seat_id=место стола-1)` принят, join показывает `score_table ≠ seat_table`. После DATA-1 прохода 3 `table_id` в счёте избыточен и ничем не связан с местом.
Предложение (точечно, одна из двух): либо убрать `table_id` и сделать `seat_id` PRIMARY KEY (каскад от стола уже идёт через `table_seats`), либо оставить и связать составным ключом: в `table_seats` — `UNIQUE (table_id, id)`, в `table_scores` — `FOREIGN KEY (table_id, seat_id) REFERENCES table_seats (table_id, id) ON DELETE CASCADE`. Первое короче. Отразить в chat_EN.md (`:461` области table_scores) и в schema.tsv.

**DATA-3 🟠 `leave_table(identity)` — имя процедуры без тела.**
`docs/chat_RU.md:330` (и EN:312) называет «одну процедуру `leave_table(identity)`», которую зовут пять путей, — но ни SQL, ни перечня шагов (`left_at = now()` живого ряда, снятие голосов kick, `closed_at`, когда живых рядов не осталось, судьба `table_games.pending`, если ушёл автор предложения) нигде нет; grep по всем четырём документам даёт только эти две строки-упоминания. Имя в DDL-комментарии читается как объект схемы, которого в схеме нет.
Предложение: под `table_seats_one_at_a_time` добавить абзац «`leave_table` — транзакция узла, не объект базы:» с четырьмя шагами в одну строку каждый, либо переименовать в тексте в «одна транзакция `leave_table` (`relay/node`)».

**DATA-4 🟠 `POST /tables/:id/proposals` в openapi без 409 `pending_exists`.**
`docs/protocol_RU.md:204` и `protocol_EN.md:213` обещают 409 `pending_exists`; код добавлен в enum `ApiError` (`openapi.yaml:250`), но у операции `openapi.yaml:1494–1510` ответов только 400/429/201 — ПРОВЕРЕНО чтением блока. Это ровно тот тип расхождения, который ворота openapi прохода 3 не ловят (они смотрят ключи, а не полноту ответов).
Предложение: добавить `"409": {$ref: "#/components/responses/Conflict"}` (или как названа общая 409-ссылка) с `x-description-ru: pending_exists — открытое предложение уже есть`.

**DATA-5 ⚪ `GET /hidden`: «реплика — пока стол не закрыт» — точнее «пока стол не убран».**
`docs/protocol_RU.md` (строка `GET /hidden`, DATA-8): срок жизни скрытой реплики назван через `tables.closed_at IS NULL`. По DDL `hidden_messages.table_line_id` каскадит от `table_lines`, та — от `tables`; `closed_at` сам «ничего не чистит — это пометка» (`chat_RU.md:548`), удаляет оператор `DELETE FROM tables WHERE closed_at IS NOT NULL OR …`. Практически то же самое (оператор идёт следом), но условие названо не то. Уровень «отложено»: править одну фразу на «до уборки стола (`DELETE FROM tables`, §8.x)».

## Проверил и находкой не счёл
- `table_seats_seat_taken` — после замены ни одного упоминания в RU/EN/протоколе/openapi/реестрах (grep пуст). `UNIQUE (table_id, seat_no)` держит номер, `table_seats_one_at_a_time` — один живой ряд; оба ловят дубли с ожидаемыми 23505 (ПРОВЕРЕНО).
- Возврат новой строкой с новым `seat_no` проходит при `left_at` у старой; удаление старого ряда сносит его `table_scores` (ПРОВЕРЕНО: `scores_left = 0`).
- `feed_cursor`: план `(visible_at, id) < (:va, :id) ORDER BY … DESC LIMIT` — Index Only Scan по `feed_cursor` (ПРОВЕРЕНО, `enable_seqscan=off`). `feed_live_geo` btree по `(lat, lon)` для прямоугольного отсева достаточен, второй столбец работает только внутри равного `lat` — для «отсева», а не точного гео, приемлемо.
- Запрос лайка `chat_RU.md:1907–1919` с `b.blocker_identity/b.blocked_identity` — PREPARE проходит без ошибки колонок (ПРОВЕРЕНО).
- `chat_games.seq/last_move_hash/pending` — совпадают с `table_games` по типам и умолчаниям, EN `:434–436` синхронен.
- `seat_no smallint` при «номера не переиспользуются»: потолок 32767 посадок за жизнь стола, стол живёт часами — не риск.
- `pending_deliveries.recipient_session` (DATA-8 из протокола) существует в DDL `:2240`, PK его включает.
- `table_autopass` — задача планировщика без строки в реестрах; реестра задач планировщика нет ни у `prune_chat_games`, ни у уборки столов, поэтому это не новая дыра прохода 3.
- b07 (`other.age BETWEEN …`) и b14 (`EXISTS (…) OR …`) падают как самостоятельные операторы — это фрагменты условий, проходом 3 не менялись.

Скрипты: `/tmp/claude-1000/-home-eugene-panov-Projects-panov-id-xor-ad/4ddfcd0b-6525-4b13-8e81-8860638e6717/scratchpad/{run.sh,extract.py,checks.sql,checks2.sql}`.

### Автономность и эксплуатация

Линза «Автономность и эксплуатация», Э5, проход 4 (17.09.2026). Только чтение. Репозиторий xor.ad, day54, HEAD deebbe0; предмет — `git diff 972a9ec..deebbe0`.

```diff
+ Ворота `scripts/check-facts-limits.sh` зелёные: 360 сверок, 26 строк таблицы §5, обе новые строки limits.tsv проходят
+ С7: строка в таблице «Как проверить» (`watchdogs_RU.md:108`, `_EN.md:112`) и в «Открыто» (`:113`, `:117`) есть, RU↔EN парны
! Исполнитель `nonce.ttl` — пункт, который про nonces не знает; задача `table_autopass` нигде не записана, кроме комментария DDL
! `PROTOCOL_SUNSET_AT` живёт только в протоколе: ни в `config.ts`, ни в визарде, ни в примерах env, ни в `deployment_*`
```

## Находки

**OPS-1 · дефект · я вижу дефект.** `docs/facts/limits.tsv:111` — `nonce.ttl 10 минут`, исполнитель `chat.janitor.missing`. Ворота проходят по формальному признаку (id есть в `open.tsv:155`), но сам пункт говорит «ни одной задачи про `gone_at` и удаление `chats`» — про таблицу `nonces` ни слова; `grep nonce docs/facts/open.tsv` пуст. Смысл С4 («открытый пункт, который об этом помнит») не выполнен: закроют уборщика бесед — nonces останутся расти без срока, и ни один реестр этого не заметит. Предложение: в описание `chat.janitor.missing` добавить «и строк `nonces` по `expires_at` (nonce.ttl, OPS-1 прохода 3)», либо отдельный пункт `nonces.sweeper.missing` и его в колонку исполнителя.

**OPS-2 · дефект · я вижу дефект.** Таблица `nonces` объявлена абзацем `protocol_RU.md:57` / `EN:59` («таблица `nonces` базы — общей для пула»), но DDL нет ни в каноне `chat_RU.md` (grep пуст), ни в `docs/facts/schema.tsv` (77 строк, `nonces` нет). Проход 3 предлагал `nonces(session_id, nonce, response jsonb, expires_at)` с PK — в текст вошло только имя. Реестр «таблицы: где объявлены и существуют ли» её не видит, счёт `product.tables.unmigrated` (28) её не включает. Предложение: DDL в канон рядом с `sessions`, строка в `schema.tsv`, `+1` в `product.tables.unmigrated`.

**OPS-3 · дефект · я вижу дефект.** Задача планировщика `table_autopass` названа только в комментарии DDL `docs/chat_RU.md:421` (и через него в протоколе). В `open.tsv` слово `autopass` отсутствует; `table.timers.unbuilt` (`open.tsv`, исполнитель `table.move.window`, `limits.tsv:84`) говорит «никто не исполняет — стола в коде ещё нет» и имени задачи не несёт; `scheduled.ts` его не знает (ожидаемо). Когда стол появится и `table.timers.unbuilt` закроют, ворота С4 будут требовать `handle + enqueueOnce` для исполнителя, но исполнителем останется id пункта, а не имя задачи. Предложение: в описание `table.timers.unbuilt` вписать «задача `table_autopass` раз в 30 с + та же функция на любом `/tables/:id/*`», чтобы при закрытии пункта исполнитель менялся на `table_autopass`.

**OPS-4 · мелочь · я вижу дефект.** `PROTOCOL_SUNSET_AT` есть в `protocol_RU.md:75` / `EN:82` и больше нигде: `relay/node/src/config.ts` (grep пуст), список переменных визарда `relay/wizard/wizard.py:24`, `relay/wizard/secrets.env.example`, `relay/wizard/inventory.example.toml:19`, `docs/deployment_RU.md` — ни одного упоминания. Маршрут «предложено», код не обязан; но перечень переменных узла — единственное место, откуда оператор узнаёт, что заголовок включается через env, и оператора он не достигает. Предложение: строка в `inventory.example.toml:19` и `wizard.py:24` группой «protocol: PROTOCOL_SUNSET_AT? (пусто — заголовка нет)»; в openapi общий `headers:` компонент, обещанный OPS-6, не проверял — вне моего предмета.

**OPS-5 · мелочь · я вижу дефект.** `watchdogs_RU.md:108` / `EN:112`: «поставить порог через env в 0» — имя переменной не названо; в `limits.tsv:109` `backup.age.alert 26 часов` тоже без env. Проверка, которую нельзя набрать, не воспроизводится. Предложение: назвать переменную (например `BACKUP_AGE_ALERT_HOURS`) в датчике С7 и в строке проверки; та же переменная — в список визарда рядом с OPS-4.

**OPS-6 · мелочь · я вижу дефект.** `watchdogs_RU.md:113` / `EN:117`: «С1–С3, С5, С6 и С7 не построены — пункты `watchdogs.unbuilt` (С1–С2) и `watchdogs.jobs.unbuilt` (С3)». С7 дописан в перечисление, а его собственный пункт `backup.silent.failure` (`open.tsv:167`) в этой строке не назван — читатель «Открыто» ищет С7 в двух чужих пунктах. Предложение: «… и `backup.silent.failure` (С7, эксплуатация)».

**OPS-7 · внимание · не нашёл.** §5 `protocol_RU.md` (абзац «Семь строк “по личности”»): «счётчики по личности живут в памяти узла … обнуляются перезапуском». Проход 3 предлагал ещё «умножаются на число узлов»; в тексте этого нет (grep «число узлов» — 0 совпадений, «на узел» только у промахов кода переноса `:285`). При этом §2 в том же диффе говорит о nonces «общей для пула». Сегодня узел на среду один (память `relay-pool-state`), поэтому дефектом не считаю, но принятие цены неполно: при втором узле `reissue.day 3` становится 6. Предложение: три слова «и на каждом узле свои».

## Проверил и находкой не счёл

- `check-facts-limits.sh` прогнан: 360 сверок, 26 строк, exit 0. `chat.messages.minute` — скорость («в минуту»), сторона С4 её осознанно пропускает (`normalise-unit.py`), пустой исполнитель здесь по правилам ворот.
- `ticket.lifetime` с исполнителем «запрос», `backup.age.alert` с `backup.silent.failure` — оба находятся воротами; строки не изменены проходом 3.
- Датчик С7 как задача планировщика раз в час: узлу нужен ключ к хранилищу — он у узла есть (`relay/node/src/lib/storage.ts`, `BUNNY_STORAGE_HOST` в `inventory.example.toml:19`); «бокс лежит целиком ловит С5» — оговорка записана.
- `relay_backup_age_seconds` в коде нет (`relay/node/src/lib/metrics.ts` grep пуст) — С7 объявлен непостроенным, это честно.
- Автопас на «любой запрос `/tables/:id/*`» и «просроченные сроки по порядку, каждый отдельным `seq`» — обе половины OPS-3/OPS-4 прохода 3 записаны в `chat_RU.md:421` и протоколе §4.6, `pending` снимается той же дорогой.
- `nonce.ttl 10` совпадает с «десять минут» в `protocol_RU.md:56-57` и EN:58-59.
- RU↔EN у С7 (датчик, строка проверки, «Открыто») парны построчно.
- Сдвиги номеров строк в `open.tsv` (chat_RU.md:852→859 и др.) — только перенумерация, содержание пунктов не менялось.

### Согласованность

# Линза «Согласованность» · Э5, проход 4 (17.09.2026) · короткий проход по изменённому проходом 3

```diff
+ ворота зелёные: check-openapi.sh — 102 операции (39/7/56), страницы API совпадают; check-docs-pairing-all.sh — 67/35/35 пар, числа сходятся; check-retired-terms.sh — 324 формулировки, 240 документов; check-legal-revisions.py витрин sosed.place и neighbro.place — 3 документа, дата и текст сходятся
- я вижу дефект: 5 (CON-1…5) — все в стыках протокол ↔ openapi / спека DSA / карта тестов / канон EN
! мелочь: 3 (CON-6…8) — запись изменений витрин, RU-описание ответа в openapi, retired-строка без EN-пары
```

Репозитории: основной `xor.ad` (deebbe0); чужие — `sosed.place` (2668fe0) и `neighbro.place` (b23f035), оба на `day54`.

## Я вижу дефект

**CON-1 · противоречие · протокол ↔ openapi.** `protocol_RU.md:204` / `protocol_EN.md:213` (§4.6, `POST /tables/:id/proposals`): «второе отвечает 409 `pending_exists` (SEC-5, DATA-7)». `openapi.yaml:1494–1512` `/tables/{id}/proposals`: ответы 400/429/201, **409 нет**; код `pending_exists` в `ApiError.enum:250` добавлен, но ни один маршрут его не отдаёт. Предложение: добавить `"409": {$ref: ApiError}` с описанием `pending_exists` в `/tables/{id}/proposals`.

**CON-2 · противоречие · openapi ↔ спека DSA.** `openapi.yaml:463` `Statement.ground_kind: enum [law, terms]`; `docs/dsa/SPEC_RU.md:485` / `SPEC_EN.md:506` `statement_of_reasons.ground_kind # legal | contractual`. Протокол §4.5 (`protocol_RU.md:187`) обещает «поля по `statement_of_reasons` спеки DSA». Предложение: enum в openapi → `[legal, contractual]` (или править спеку — но она первичный источник, LAW-1).

**CON-3 · противоречие · протокол ↔ карта тестов.** Проход 3 (CON-28) добавил третье исключение отлучки — `POST /identities/close` (`protocol_RU.md:245` / `EN:254`), а тест 23.5 остался с двумя: `test-map_RU.md:413` / `test-map_EN.md:424` «кроме `DELETE /away` и `GET /identities/me`». Предложение: дописать `POST /identities/close` в 23.5 обеих карт.

**CON-4 · противоречие · канон RU ↔ EN.** `chat_RU.md:647` возврат за стол переписан на «новой строкой `table_seats` с новым `seat_no` (DATA-1; здесь стояло `UPDATE … SET left_at = NULL…` [retired])», а `chat_EN.md:648` по-прежнему: «back is `UPDATE ... SET left_at = NULL, joined_at = now(), playing_from = NULL`» — противоречит собственному DDL-комментарию `chat_EN.md:330` («Coming back is a new row, not `UPDATE left_at = NULL`»). `retired-terms.txt:507` заведена только RU-строка, поэтому ворота этого не ловят. Предложение: переписать `chat_EN.md:648` зеркально RU и добавить EN-строку в retired-terms.

**CON-5 · противоречие · протокол ↔ openapi.** Протокол §2 (`protocol_RU.md:54–57`) и §4.8 (`:229`): `POST /blocks` «с `nonce`», семь маршрутов. `openapi.yaml:360–369` `BlockTarget`: `nonce` описан как «Required (protocol §2)» словами, но списка `required:` у схемы нет вовсе — единственный из семи nonce-маршрутов, где обязательность не машинная (`TableCreate:270`, `AwayCreate:379`, `SupportCreate:384`, `RecoveryReissue:472`, `Nonced:431` — везде `required: [nonce]`). Предложение: `required: [nonce]` в `BlockTarget` (feed/chat/table остаются «ровно одно» прозой, как сейчас).

## Мелочь

**CON-6 · витрины ↔ их запись изменений.** Проход 3 правил `privacy_EN.md:21` (возраст показывается мэтчам с изменениями), `:25` (скрытые реплики стола; блокировка по фразе раскрывает остальные фразы автора) и `terms_EN.md:93` («— or the name —») в `sosed.place` и `neighbro.place`; хэши в `deploy/legal-revisions.json` пересчитаны. Но `landing/legal/changes_EN.md` редакции 16.09 (строки 6, 12, 33) этих трёх правок не называет: «the phrases you hid» без реплик стола, ни слова о возрасте у мэтчей и о раскрытии фраз при блокировке. Оба репозитория одинаковы. Предложение: дописать три пункта в редакцию 16.09 обоих `changes_EN.md`.

**CON-7 · openapi RU ↔ EN.** `openapi.yaml:499` `responses.ApiError.description` получил вторую фразу про `x-protocol-sunset`, а `x-description-ru:500` — нет. Предложение: дописать «На любом ответе узла может быть `x-protocol-sunset` (…)».

**CON-8 · openapi: описание `Nonced` ↔ его применение.** `Nonced:428–434` — «тело, несущее только nonce», а подставлен он в `/vault/pin:1088` («старый ПИН, новый auth_hash, перевыпуск доли») и `/identities/close:1104` («с доказательством ПИНа»). Тело не «только nonce». Предложение: либо описать «nonce плюс поля маршрута» (`additionalProperties: true` явно), либо снять слово «только».

## Проверил и находкой не счёл

- §2 семь маршрутов и таблица `nonces`: RU:54–58 ↔ EN:56–61 парны; `nonce.ttl` есть в `limits.tsv:111`; описания `nonce` в пяти схемах openapi одинаковы; тест 23.8 обеих карт перечисляет те же семь.
- §3 `PROTOCOL_SUNSET_AT` — RU:75 ↔ EN:80 парны; `ProtocolSunset` в openapi подключён к `ApiError`.
- §4.3 сообщения: 404/409/429 и `chat.messages.minute` — RU:126–127 ↔ EN:134–135; openapi `/chats/{id}/messages` 404 + oneOf `LegalReacceptance|ApiError`; лайк `:1251` тот же oneOf (CON-22 закрыт).
- §4.5 `GET /statements`: набор из 8 полей совпадает RU ↔ EN ↔ `Statement` в openapi (кроме enum — CON-2); `receipt_hash` у `Notice` ↔ `dsa/SPEC_EN.md:62,494`.
- §4.6: `is_playing` (CON-21), 404 у `/tables/{id}/ticket` (CON-25), большинство играющих — RU:208 ↔ EN:217 ↔ канон `chat_RU.md:670`/`chat_EN.md:675` ↔ витрины `19-table_*:46`; автопас по планировщику раз в 30 с — протокол ↔ DDL `turn_due` в обоих канонах.
- §4.8 `POST /hidden` 200 `{id}` (CON-20) — openapi `/hidden` 200; `GET /hidden` с `tables.closed_at` — парно; `hidden.hour` есть в §5 и в `limits.tsv:103`.
- §4.10 `/support/:no/seen` 204 на чужой номер — парно, openapi 204.
- §5: 26 строк в обоих (28 с шапкой), 8 строк «по личности» = 7 по OPS-13 + сообщения по SEC-18 — фраза «семь строк и срок билета» точна; CON-23 закрыт («столов» убрано).
- §6 курсор инбокса `(created_at, id)` — RU:346 ↔ EN:355; индекс `feed_cursor` в обоих канонах.
- Канон DDL: `table_seats.id` + `UNIQUE (table_id, seat_no)`, `table_scores.seat_id`, `chat_games.seq/last_move_hash/pending`, `feed_cursor`/`feed_live_geo`, `blocker_identity` — RU ↔ EN построчно; `table_seats_one_at_a_time` существует (`chat_RU.md:362`).
- Сторожа С7/W7: датчик, строка испытания и «Открыто» парны; `backup.age.alert` в реестре.
- Retired-terms: 4 новые строки — ворота зелёные, `states its version is open` и `The lowest free number` в текстах не найдены; `SET left_at = NULL` в `chat_RU.md` только в кавычках `[retired]` (в EN — живой, это CON-4).
- Реестр ст. 30 и записка юристу (п. 16) — RU ↔ EN парны.
- Числа: 102 операции (скрипт и свой пересчёт), 324 формулировки (скрипт и `grep -cv`), sha256 витрин — скрипт `deploy/check-legal-revisions.py` обоих репозиториев зелёный (прямой `sha256sum` файла с json не совпадает — это ожидаемо: скрипт хэширует нормализованный текст, не файл).

Всё выше проверено мною чтением диффов и запуском скриптов; вживую ничего не исполнялось — код узла не в предмете прохода.

## Опровержения

В этом проходе состязательной перепроверки агентом не было. Вместо неё ведущий проверил сам: пять
находок по существу — по строкам до правки (**ПРОВЕРЕНО**: `openapi.yaml` тело `Nonced` у `/vault/pin`;
`chat_RU.md:478` `PRIMARY KEY (table_id, seat_id)`; `grep 'CREATE TABLE nonces'` — 0; `sosed.place/docs/19-table_RU.md:46`
против `chat_RU.md:645–648`; `chat_EN.md:648` живой `UPDATE … left_at = NULL`); остальные 24 — механические стыки
протокол ↔ openapi ↔ карта тестов ↔ канон EN ↔ витрины, и каждая подтвердилась тем, что якорь линзы нашёлся в
тексте ровно один раз при наложении пачки (скрипт отказывается писать при нуле или двух совпадениях). Опровергнутых
нет; «мелочь» линзы данных DATA-5 принята как уточнение формулировки.

## Сводка ведущего

**Счёт.** 29 находок: право 4, безопасность 5, данные 5, эксплуатация 7, согласованность 8. Критичных нет;
дефектов дизайна 4 (LAW-1 срок ограничения, SEC-25 `nonces` без DDL, SEC-26 `Nonced` вытеснил доказательство ПИНа,
DATA-2 счёт на место чужого стола); остальное — противоречия стыков и мелочи. Дубли: SEC-25 = DATA-1 = OPS-2
(`nonces`), LAW-3 = CON-2 (enum оснований), DATA-4 = CON-1 (409 у предложений), SEC-27 = OPS-7 (узлы пула).
Тренд по проходам: 65 → 43 → 29 находок; критичных 1 → 1 → 0; в проходе 4 всё найденное — следствия правок прохода 3.

**Закрыто пачкой** (`scratchpad/batch-s5p4.py`, коммит следует): DDL `nonces` с привязкой к маршруту и уборкой
по `nonce.ttl`, строка в `schema.tsv`, 29 таблиц продукта; `table_scores` только по `seat_id`; шаги `leave_table`;
тела `PinChange` и `IdentityClose` вместо `Nonced`; `required: [nonce]` у `BlockTarget`; 409 у предложений
(`pending_exists`, `refused`, только от играющего); `Statement.until`, enum `legal|contractual`, `delivered_at`
в описании, колонка `until` в спеке DSA RU/EN; порядок отказов у сообщений; §5 — «на каждом узле пула свои»;
`GET /hidden` — до уборки стола; тест 23.5 с `POST /identities/close`; EN-строка возврата и её retired-пара;
С7 — `BACKUP_AGE_ALERT_HOURS`, `backup.silent.failure` в «Открыто»; пункты `chat.janitor.missing` и
`table.timers.unbuilt` знают про `nonces` и `table_autopass`; экран 19 обеих витрин — высадка не запирает, как в
каноне; страница изменений витрин — три пункта; редакции пересчитаны (тексты политики и соглашения не менялись —
только страница изменений и экраны).

**Оставлено:** `PROTOCOL_SUNSET_AT` и `BACKUP_AGE_ALERT_HOURS` названы в протоколе и сторожах, в список переменных
визарда (`relay/wizard`) не внесены — это код узла, вне разрешения процесса; потолок числа зрителей за столом не
задан (остаток SEC-24/SEC-28) — цена названа: зритель может занять слот предложения только через посадку, а
предложения теперь только от играющих.

## Что осталось открытым

- Открытые пункты без изменений: `hetzner.dpa` (действие владельца), `dsa.receipt.unbuilt`, `backup.silent.failure`,
  `table.timers.unbuilt`, `chat.janitor.missing`.
- Мелочи без правки: DATA-6 прохода 3 (окно повтора хода), потолок зрителей.

**Вывод для ворот 3 маршрута.** Выше мелочи в проходе 4 — четыре дефекта дизайна, все закрыты той же пачкой;
опровергающий не собирался. Ноль по правилу маршрута требует прохода, где линзы не найдут ничего выше мелочи, —
проход 5 по диффу этой пачки, короче четвёртого.
